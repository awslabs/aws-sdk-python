{
  "changes": [
    {
      "type": "dependency",
      "description": "Bump `aws-sdk-sts` from `~=0.9.0` to `~=0.10.0`."
    },
    {
      "type": "dependency",
      "description": "Bump `smithy-aws-core` from `~=0.9.0` to `~=0.10.0`."
    }
  ]
}

# Code generated by smithy-python-codegen DO NOT EDIT.

from dataclasses import dataclass
from datetime import datetime, timezone
from enum import StrEnum
import logging
from typing import Any, Literal, Self, Union

from smithy_core.deserializers import ShapeDeserializer
from smithy_core.documents import TypeRegistry
from smithy_core.exceptions import ModeledError, SerializationError
from smithy_core.schemas import APIOperation, Schema
from smithy_core.serializers import ShapeSerializer
from smithy_core.shapes import ShapeID
from smithy_core.types import UnknownEnumMixin

from ._private.schemas import (
    ARCHIVAL_SUMMARY as _SCHEMA_ARCHIVAL_SUMMARY,
    ATTRIBUTE_DEFINITION as _SCHEMA_ATTRIBUTE_DEFINITION,
    ATTRIBUTE_VALUE as _SCHEMA_ATTRIBUTE_VALUE,
    ATTRIBUTE_VALUE_UPDATE as _SCHEMA_ATTRIBUTE_VALUE_UPDATE,
    AUTO_SCALING_POLICY_DESCRIPTION as _SCHEMA_AUTO_SCALING_POLICY_DESCRIPTION,
    AUTO_SCALING_POLICY_UPDATE as _SCHEMA_AUTO_SCALING_POLICY_UPDATE,
    AUTO_SCALING_SETTINGS_DESCRIPTION as _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION,
    AUTO_SCALING_SETTINGS_UPDATE as _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE,
    AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION as _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION,
    AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE as _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE,
    BACKUP_DESCRIPTION as _SCHEMA_BACKUP_DESCRIPTION,
    BACKUP_DETAILS as _SCHEMA_BACKUP_DETAILS,
    BACKUP_IN_USE_EXCEPTION as _SCHEMA_BACKUP_IN_USE_EXCEPTION,
    BACKUP_NOT_FOUND_EXCEPTION as _SCHEMA_BACKUP_NOT_FOUND_EXCEPTION,
    BACKUP_SUMMARY as _SCHEMA_BACKUP_SUMMARY,
    BATCH_EXECUTE_STATEMENT as _SCHEMA_BATCH_EXECUTE_STATEMENT,
    BATCH_EXECUTE_STATEMENT_INPUT as _SCHEMA_BATCH_EXECUTE_STATEMENT_INPUT,
    BATCH_EXECUTE_STATEMENT_OUTPUT as _SCHEMA_BATCH_EXECUTE_STATEMENT_OUTPUT,
    BATCH_GET_ITEM as _SCHEMA_BATCH_GET_ITEM,
    BATCH_GET_ITEM_INPUT as _SCHEMA_BATCH_GET_ITEM_INPUT,
    BATCH_GET_ITEM_OUTPUT as _SCHEMA_BATCH_GET_ITEM_OUTPUT,
    BATCH_STATEMENT_ERROR as _SCHEMA_BATCH_STATEMENT_ERROR,
    BATCH_STATEMENT_REQUEST as _SCHEMA_BATCH_STATEMENT_REQUEST,
    BATCH_STATEMENT_RESPONSE as _SCHEMA_BATCH_STATEMENT_RESPONSE,
    BATCH_WRITE_ITEM as _SCHEMA_BATCH_WRITE_ITEM,
    BATCH_WRITE_ITEM_INPUT as _SCHEMA_BATCH_WRITE_ITEM_INPUT,
    BATCH_WRITE_ITEM_OUTPUT as _SCHEMA_BATCH_WRITE_ITEM_OUTPUT,
    BILLING_MODE_SUMMARY as _SCHEMA_BILLING_MODE_SUMMARY,
    CANCELLATION_REASON as _SCHEMA_CANCELLATION_REASON,
    CAPACITY as _SCHEMA_CAPACITY,
    CONDITION as _SCHEMA_CONDITION,
    CONDITIONAL_CHECK_FAILED_EXCEPTION as _SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION,
    CONDITION_CHECK as _SCHEMA_CONDITION_CHECK,
    CONSUMED_CAPACITY as _SCHEMA_CONSUMED_CAPACITY,
    CONTINUOUS_BACKUPS_DESCRIPTION as _SCHEMA_CONTINUOUS_BACKUPS_DESCRIPTION,
    CONTINUOUS_BACKUPS_UNAVAILABLE_EXCEPTION as _SCHEMA_CONTINUOUS_BACKUPS_UNAVAILABLE_EXCEPTION,
    CONTRIBUTOR_INSIGHTS_SUMMARY as _SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY,
    CREATE_BACKUP as _SCHEMA_CREATE_BACKUP,
    CREATE_BACKUP_INPUT as _SCHEMA_CREATE_BACKUP_INPUT,
    CREATE_BACKUP_OUTPUT as _SCHEMA_CREATE_BACKUP_OUTPUT,
    CREATE_GLOBAL_SECONDARY_INDEX_ACTION as _SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION,
    CREATE_GLOBAL_TABLE as _SCHEMA_CREATE_GLOBAL_TABLE,
    CREATE_GLOBAL_TABLE_INPUT as _SCHEMA_CREATE_GLOBAL_TABLE_INPUT,
    CREATE_GLOBAL_TABLE_OUTPUT as _SCHEMA_CREATE_GLOBAL_TABLE_OUTPUT,
    CREATE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION as _SCHEMA_CREATE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION,
    CREATE_REPLICATION_GROUP_MEMBER_ACTION as _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION,
    CREATE_REPLICA_ACTION as _SCHEMA_CREATE_REPLICA_ACTION,
    CREATE_TABLE as _SCHEMA_CREATE_TABLE,
    CREATE_TABLE_INPUT as _SCHEMA_CREATE_TABLE_INPUT,
    CREATE_TABLE_OUTPUT as _SCHEMA_CREATE_TABLE_OUTPUT,
    CREATE_VECTOR_INDEX_ACTION as _SCHEMA_CREATE_VECTOR_INDEX_ACTION,
    CSV_OPTIONS as _SCHEMA_CSV_OPTIONS,
    DELETE as _SCHEMA_DELETE,
    DELETE_BACKUP as _SCHEMA_DELETE_BACKUP,
    DELETE_BACKUP_INPUT as _SCHEMA_DELETE_BACKUP_INPUT,
    DELETE_BACKUP_OUTPUT as _SCHEMA_DELETE_BACKUP_OUTPUT,
    DELETE_GLOBAL_SECONDARY_INDEX_ACTION as _SCHEMA_DELETE_GLOBAL_SECONDARY_INDEX_ACTION,
    DELETE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION as _SCHEMA_DELETE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION,
    DELETE_ITEM as _SCHEMA_DELETE_ITEM,
    DELETE_ITEM_INPUT as _SCHEMA_DELETE_ITEM_INPUT,
    DELETE_ITEM_OUTPUT as _SCHEMA_DELETE_ITEM_OUTPUT,
    DELETE_REPLICATION_GROUP_MEMBER_ACTION as _SCHEMA_DELETE_REPLICATION_GROUP_MEMBER_ACTION,
    DELETE_REPLICA_ACTION as _SCHEMA_DELETE_REPLICA_ACTION,
    DELETE_REQUEST as _SCHEMA_DELETE_REQUEST,
    DELETE_RESOURCE_POLICY as _SCHEMA_DELETE_RESOURCE_POLICY,
    DELETE_RESOURCE_POLICY_INPUT as _SCHEMA_DELETE_RESOURCE_POLICY_INPUT,
    DELETE_RESOURCE_POLICY_OUTPUT as _SCHEMA_DELETE_RESOURCE_POLICY_OUTPUT,
    DELETE_TABLE as _SCHEMA_DELETE_TABLE,
    DELETE_TABLE_INPUT as _SCHEMA_DELETE_TABLE_INPUT,
    DELETE_TABLE_OUTPUT as _SCHEMA_DELETE_TABLE_OUTPUT,
    DELETE_VECTOR_INDEX_ACTION as _SCHEMA_DELETE_VECTOR_INDEX_ACTION,
    DESCRIBE_BACKUP as _SCHEMA_DESCRIBE_BACKUP,
    DESCRIBE_BACKUP_INPUT as _SCHEMA_DESCRIBE_BACKUP_INPUT,
    DESCRIBE_BACKUP_OUTPUT as _SCHEMA_DESCRIBE_BACKUP_OUTPUT,
    DESCRIBE_CONTINUOUS_BACKUPS as _SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS,
    DESCRIBE_CONTINUOUS_BACKUPS_INPUT as _SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_INPUT,
    DESCRIBE_CONTINUOUS_BACKUPS_OUTPUT as _SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_OUTPUT,
    DESCRIBE_CONTRIBUTOR_INSIGHTS as _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS,
    DESCRIBE_CONTRIBUTOR_INSIGHTS_INPUT as _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_INPUT,
    DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT as _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT,
    DESCRIBE_ENDPOINTS as _SCHEMA_DESCRIBE_ENDPOINTS,
    DESCRIBE_ENDPOINTS_INPUT as _SCHEMA_DESCRIBE_ENDPOINTS_INPUT,
    DESCRIBE_ENDPOINTS_OUTPUT as _SCHEMA_DESCRIBE_ENDPOINTS_OUTPUT,
    DESCRIBE_EXPORT as _SCHEMA_DESCRIBE_EXPORT,
    DESCRIBE_EXPORT_INPUT as _SCHEMA_DESCRIBE_EXPORT_INPUT,
    DESCRIBE_EXPORT_OUTPUT as _SCHEMA_DESCRIBE_EXPORT_OUTPUT,
    DESCRIBE_GLOBAL_TABLE as _SCHEMA_DESCRIBE_GLOBAL_TABLE,
    DESCRIBE_GLOBAL_TABLE_INPUT as _SCHEMA_DESCRIBE_GLOBAL_TABLE_INPUT,
    DESCRIBE_GLOBAL_TABLE_OUTPUT as _SCHEMA_DESCRIBE_GLOBAL_TABLE_OUTPUT,
    DESCRIBE_GLOBAL_TABLE_SETTINGS as _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS,
    DESCRIBE_GLOBAL_TABLE_SETTINGS_INPUT as _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_INPUT,
    DESCRIBE_GLOBAL_TABLE_SETTINGS_OUTPUT as _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_OUTPUT,
    DESCRIBE_IMPORT as _SCHEMA_DESCRIBE_IMPORT,
    DESCRIBE_IMPORT_INPUT as _SCHEMA_DESCRIBE_IMPORT_INPUT,
    DESCRIBE_IMPORT_OUTPUT as _SCHEMA_DESCRIBE_IMPORT_OUTPUT,
    DESCRIBE_KINESIS_STREAMING_DESTINATION as _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION,
    DESCRIBE_KINESIS_STREAMING_DESTINATION_INPUT as _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_INPUT,
    DESCRIBE_KINESIS_STREAMING_DESTINATION_OUTPUT as _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_OUTPUT,
    DESCRIBE_LIMITS as _SCHEMA_DESCRIBE_LIMITS,
    DESCRIBE_LIMITS_INPUT as _SCHEMA_DESCRIBE_LIMITS_INPUT,
    DESCRIBE_LIMITS_OUTPUT as _SCHEMA_DESCRIBE_LIMITS_OUTPUT,
    DESCRIBE_TABLE as _SCHEMA_DESCRIBE_TABLE,
    DESCRIBE_TABLE_INPUT as _SCHEMA_DESCRIBE_TABLE_INPUT,
    DESCRIBE_TABLE_OUTPUT as _SCHEMA_DESCRIBE_TABLE_OUTPUT,
    DESCRIBE_TABLE_REPLICA_AUTO_SCALING as _SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING,
    DESCRIBE_TABLE_REPLICA_AUTO_SCALING_INPUT as _SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_INPUT,
    DESCRIBE_TABLE_REPLICA_AUTO_SCALING_OUTPUT as _SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_OUTPUT,
    DESCRIBE_TIME_TO_LIVE as _SCHEMA_DESCRIBE_TIME_TO_LIVE,
    DESCRIBE_TIME_TO_LIVE_INPUT as _SCHEMA_DESCRIBE_TIME_TO_LIVE_INPUT,
    DESCRIBE_TIME_TO_LIVE_OUTPUT as _SCHEMA_DESCRIBE_TIME_TO_LIVE_OUTPUT,
    DISABLE_KINESIS_STREAMING_DESTINATION as _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION,
    DISABLE_KINESIS_STREAMING_DESTINATION_INPUT as _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_INPUT,
    DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT as _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT,
    DUPLICATE_ITEM_EXCEPTION as _SCHEMA_DUPLICATE_ITEM_EXCEPTION,
    ENABLE_KINESIS_STREAMING_CONFIGURATION as _SCHEMA_ENABLE_KINESIS_STREAMING_CONFIGURATION,
    ENABLE_KINESIS_STREAMING_DESTINATION as _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION,
    ENABLE_KINESIS_STREAMING_DESTINATION_INPUT as _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_INPUT,
    ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT as _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT,
    ENDPOINT as _SCHEMA_ENDPOINT,
    EXECUTE_STATEMENT as _SCHEMA_EXECUTE_STATEMENT,
    EXECUTE_STATEMENT_INPUT as _SCHEMA_EXECUTE_STATEMENT_INPUT,
    EXECUTE_STATEMENT_OUTPUT as _SCHEMA_EXECUTE_STATEMENT_OUTPUT,
    EXECUTE_TRANSACTION as _SCHEMA_EXECUTE_TRANSACTION,
    EXECUTE_TRANSACTION_INPUT as _SCHEMA_EXECUTE_TRANSACTION_INPUT,
    EXECUTE_TRANSACTION_OUTPUT as _SCHEMA_EXECUTE_TRANSACTION_OUTPUT,
    EXPECTED_ATTRIBUTE_VALUE as _SCHEMA_EXPECTED_ATTRIBUTE_VALUE,
    EXPORT_CONFLICT_EXCEPTION as _SCHEMA_EXPORT_CONFLICT_EXCEPTION,
    EXPORT_DESCRIPTION as _SCHEMA_EXPORT_DESCRIPTION,
    EXPORT_NOT_FOUND_EXCEPTION as _SCHEMA_EXPORT_NOT_FOUND_EXCEPTION,
    EXPORT_SUMMARY as _SCHEMA_EXPORT_SUMMARY,
    EXPORT_TABLE_TO_POINT_IN_TIME as _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME,
    EXPORT_TABLE_TO_POINT_IN_TIME_INPUT as _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT,
    EXPORT_TABLE_TO_POINT_IN_TIME_OUTPUT as _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_OUTPUT,
    FAILURE_EXCEPTION as _SCHEMA_FAILURE_EXCEPTION,
    GET as _SCHEMA_GET,
    GET_ITEM as _SCHEMA_GET_ITEM,
    GET_ITEM_INPUT as _SCHEMA_GET_ITEM_INPUT,
    GET_ITEM_OUTPUT as _SCHEMA_GET_ITEM_OUTPUT,
    GET_RESOURCE_POLICY as _SCHEMA_GET_RESOURCE_POLICY,
    GET_RESOURCE_POLICY_INPUT as _SCHEMA_GET_RESOURCE_POLICY_INPUT,
    GET_RESOURCE_POLICY_OUTPUT as _SCHEMA_GET_RESOURCE_POLICY_OUTPUT,
    GLOBAL_SECONDARY_INDEX as _SCHEMA_GLOBAL_SECONDARY_INDEX,
    GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE as _SCHEMA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE,
    GLOBAL_SECONDARY_INDEX_DESCRIPTION as _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION,
    GLOBAL_SECONDARY_INDEX_INFO as _SCHEMA_GLOBAL_SECONDARY_INDEX_INFO,
    GLOBAL_SECONDARY_INDEX_UPDATE as _SCHEMA_GLOBAL_SECONDARY_INDEX_UPDATE,
    GLOBAL_SECONDARY_INDEX_WARM_THROUGHPUT_DESCRIPTION as _SCHEMA_GLOBAL_SECONDARY_INDEX_WARM_THROUGHPUT_DESCRIPTION,
    GLOBAL_TABLE as _SCHEMA_GLOBAL_TABLE,
    GLOBAL_TABLE_ALREADY_EXISTS_EXCEPTION as _SCHEMA_GLOBAL_TABLE_ALREADY_EXISTS_EXCEPTION,
    GLOBAL_TABLE_DESCRIPTION as _SCHEMA_GLOBAL_TABLE_DESCRIPTION,
    GLOBAL_TABLE_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE as _SCHEMA_GLOBAL_TABLE_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE,
    GLOBAL_TABLE_NOT_FOUND_EXCEPTION as _SCHEMA_GLOBAL_TABLE_NOT_FOUND_EXCEPTION,
    GLOBAL_TABLE_WITNESS_DESCRIPTION as _SCHEMA_GLOBAL_TABLE_WITNESS_DESCRIPTION,
    GLOBAL_TABLE_WITNESS_GROUP_UPDATE as _SCHEMA_GLOBAL_TABLE_WITNESS_GROUP_UPDATE,
    IDEMPOTENT_PARAMETER_MISMATCH_EXCEPTION as _SCHEMA_IDEMPOTENT_PARAMETER_MISMATCH_EXCEPTION,
    IMPORT_CONFLICT_EXCEPTION as _SCHEMA_IMPORT_CONFLICT_EXCEPTION,
    IMPORT_NOT_FOUND_EXCEPTION as _SCHEMA_IMPORT_NOT_FOUND_EXCEPTION,
    IMPORT_SUMMARY as _SCHEMA_IMPORT_SUMMARY,
    IMPORT_TABLE as _SCHEMA_IMPORT_TABLE,
    IMPORT_TABLE_DESCRIPTION as _SCHEMA_IMPORT_TABLE_DESCRIPTION,
    IMPORT_TABLE_INPUT as _SCHEMA_IMPORT_TABLE_INPUT,
    IMPORT_TABLE_OUTPUT as _SCHEMA_IMPORT_TABLE_OUTPUT,
    INCREMENTAL_EXPORT_SPECIFICATION as _SCHEMA_INCREMENTAL_EXPORT_SPECIFICATION,
    INDEX_NOT_FOUND_EXCEPTION as _SCHEMA_INDEX_NOT_FOUND_EXCEPTION,
    INPUT_FORMAT_OPTIONS as _SCHEMA_INPUT_FORMAT_OPTIONS,
    INTERNAL_SERVER_ERROR as _SCHEMA_INTERNAL_SERVER_ERROR,
    INVALID_ENDPOINT_EXCEPTION as _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
    INVALID_EXPORT_TIME_EXCEPTION as _SCHEMA_INVALID_EXPORT_TIME_EXCEPTION,
    INVALID_RESTORE_TIME_EXCEPTION as _SCHEMA_INVALID_RESTORE_TIME_EXCEPTION,
    ITEM_COLLECTION_METRICS as _SCHEMA_ITEM_COLLECTION_METRICS,
    ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION as _SCHEMA_ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION,
    ITEM_RESPONSE as _SCHEMA_ITEM_RESPONSE,
    KEYS_AND_ATTRIBUTES as _SCHEMA_KEYS_AND_ATTRIBUTES,
    KEY_SCHEMA_ELEMENT as _SCHEMA_KEY_SCHEMA_ELEMENT,
    KINESIS_DATA_STREAM_DESTINATION as _SCHEMA_KINESIS_DATA_STREAM_DESTINATION,
    LIMIT_EXCEEDED_EXCEPTION as _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
    LIST_BACKUPS as _SCHEMA_LIST_BACKUPS,
    LIST_BACKUPS_INPUT as _SCHEMA_LIST_BACKUPS_INPUT,
    LIST_BACKUPS_OUTPUT as _SCHEMA_LIST_BACKUPS_OUTPUT,
    LIST_CONTRIBUTOR_INSIGHTS as _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS,
    LIST_CONTRIBUTOR_INSIGHTS_INPUT as _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_INPUT,
    LIST_CONTRIBUTOR_INSIGHTS_OUTPUT as _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_OUTPUT,
    LIST_EXPORTS as _SCHEMA_LIST_EXPORTS,
    LIST_EXPORTS_INPUT as _SCHEMA_LIST_EXPORTS_INPUT,
    LIST_EXPORTS_OUTPUT as _SCHEMA_LIST_EXPORTS_OUTPUT,
    LIST_GLOBAL_TABLES as _SCHEMA_LIST_GLOBAL_TABLES,
    LIST_GLOBAL_TABLES_INPUT as _SCHEMA_LIST_GLOBAL_TABLES_INPUT,
    LIST_GLOBAL_TABLES_OUTPUT as _SCHEMA_LIST_GLOBAL_TABLES_OUTPUT,
    LIST_IMPORTS as _SCHEMA_LIST_IMPORTS,
    LIST_IMPORTS_INPUT as _SCHEMA_LIST_IMPORTS_INPUT,
    LIST_IMPORTS_OUTPUT as _SCHEMA_LIST_IMPORTS_OUTPUT,
    LIST_TABLES as _SCHEMA_LIST_TABLES,
    LIST_TABLES_INPUT as _SCHEMA_LIST_TABLES_INPUT,
    LIST_TABLES_OUTPUT as _SCHEMA_LIST_TABLES_OUTPUT,
    LIST_TAGS_OF_RESOURCE as _SCHEMA_LIST_TAGS_OF_RESOURCE,
    LIST_TAGS_OF_RESOURCE_INPUT as _SCHEMA_LIST_TAGS_OF_RESOURCE_INPUT,
    LIST_TAGS_OF_RESOURCE_OUTPUT as _SCHEMA_LIST_TAGS_OF_RESOURCE_OUTPUT,
    LOCAL_SECONDARY_INDEX as _SCHEMA_LOCAL_SECONDARY_INDEX,
    LOCAL_SECONDARY_INDEX_DESCRIPTION as _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION,
    LOCAL_SECONDARY_INDEX_INFO as _SCHEMA_LOCAL_SECONDARY_INDEX_INFO,
    ON_DEMAND_THROUGHPUT as _SCHEMA_ON_DEMAND_THROUGHPUT,
    ON_DEMAND_THROUGHPUT_OVERRIDE as _SCHEMA_ON_DEMAND_THROUGHPUT_OVERRIDE,
    PARAMETERIZED_STATEMENT as _SCHEMA_PARAMETERIZED_STATEMENT,
    POINT_IN_TIME_RECOVERY_DESCRIPTION as _SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION,
    POINT_IN_TIME_RECOVERY_SPECIFICATION as _SCHEMA_POINT_IN_TIME_RECOVERY_SPECIFICATION,
    POINT_IN_TIME_RECOVERY_UNAVAILABLE_EXCEPTION as _SCHEMA_POINT_IN_TIME_RECOVERY_UNAVAILABLE_EXCEPTION,
    POLICY_NOT_FOUND_EXCEPTION as _SCHEMA_POLICY_NOT_FOUND_EXCEPTION,
    PROJECTION as _SCHEMA_PROJECTION,
    PROVISIONED_THROUGHPUT as _SCHEMA_PROVISIONED_THROUGHPUT,
    PROVISIONED_THROUGHPUT_DESCRIPTION as _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION,
    PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION as _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
    PROVISIONED_THROUGHPUT_OVERRIDE as _SCHEMA_PROVISIONED_THROUGHPUT_OVERRIDE,
    PUT as _SCHEMA_PUT,
    PUT_ITEM as _SCHEMA_PUT_ITEM,
    PUT_ITEM_INPUT as _SCHEMA_PUT_ITEM_INPUT,
    PUT_ITEM_OUTPUT as _SCHEMA_PUT_ITEM_OUTPUT,
    PUT_REQUEST as _SCHEMA_PUT_REQUEST,
    PUT_RESOURCE_POLICY as _SCHEMA_PUT_RESOURCE_POLICY,
    PUT_RESOURCE_POLICY_INPUT as _SCHEMA_PUT_RESOURCE_POLICY_INPUT,
    PUT_RESOURCE_POLICY_OUTPUT as _SCHEMA_PUT_RESOURCE_POLICY_OUTPUT,
    QUERY as _SCHEMA_QUERY,
    QUERY_INPUT as _SCHEMA_QUERY_INPUT,
    QUERY_OUTPUT as _SCHEMA_QUERY_OUTPUT,
    REPLICA as _SCHEMA_REPLICA,
    REPLICATED_WRITE_CONFLICT_EXCEPTION as _SCHEMA_REPLICATED_WRITE_CONFLICT_EXCEPTION,
    REPLICATION_GROUP_UPDATE as _SCHEMA_REPLICATION_GROUP_UPDATE,
    REPLICA_ALREADY_EXISTS_EXCEPTION as _SCHEMA_REPLICA_ALREADY_EXISTS_EXCEPTION,
    REPLICA_AUTO_SCALING_DESCRIPTION as _SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION,
    REPLICA_AUTO_SCALING_UPDATE as _SCHEMA_REPLICA_AUTO_SCALING_UPDATE,
    REPLICA_DESCRIPTION as _SCHEMA_REPLICA_DESCRIPTION,
    REPLICA_GLOBAL_SECONDARY_INDEX as _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX,
    REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_DESCRIPTION as _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_DESCRIPTION,
    REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE as _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE,
    REPLICA_GLOBAL_SECONDARY_INDEX_DESCRIPTION as _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_DESCRIPTION,
    REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION as _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION,
    REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE as _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE,
    REPLICA_NOT_FOUND_EXCEPTION as _SCHEMA_REPLICA_NOT_FOUND_EXCEPTION,
    REPLICA_SETTINGS_DESCRIPTION as _SCHEMA_REPLICA_SETTINGS_DESCRIPTION,
    REPLICA_SETTINGS_UPDATE as _SCHEMA_REPLICA_SETTINGS_UPDATE,
    REPLICA_UPDATE as _SCHEMA_REPLICA_UPDATE,
    REQUEST_LIMIT_EXCEEDED as _SCHEMA_REQUEST_LIMIT_EXCEEDED,
    RESOURCE_IN_USE_EXCEPTION as _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
    RESOURCE_NOT_FOUND_EXCEPTION as _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    RESTORE_SUMMARY as _SCHEMA_RESTORE_SUMMARY,
    RESTORE_TABLE_FROM_BACKUP as _SCHEMA_RESTORE_TABLE_FROM_BACKUP,
    RESTORE_TABLE_FROM_BACKUP_INPUT as _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT,
    RESTORE_TABLE_FROM_BACKUP_OUTPUT as _SCHEMA_RESTORE_TABLE_FROM_BACKUP_OUTPUT,
    RESTORE_TABLE_TO_POINT_IN_TIME as _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME,
    RESTORE_TABLE_TO_POINT_IN_TIME_INPUT as _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT,
    RESTORE_TABLE_TO_POINT_IN_TIME_OUTPUT as _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_OUTPUT,
    S3_BUCKET_SOURCE as _SCHEMA_S3_BUCKET_SOURCE,
    SCAN as _SCHEMA_SCAN,
    SCAN_INPUT as _SCHEMA_SCAN_INPUT,
    SCAN_OUTPUT as _SCHEMA_SCAN_OUTPUT,
    SEARCH_RESULT_ITEM as _SCHEMA_SEARCH_RESULT_ITEM,
    SEARCH_SCHEMA_ELEMENT as _SCHEMA_SEARCH_SCHEMA_ELEMENT,
    SEARCH_VECTORS as _SCHEMA_SEARCH_VECTORS,
    SEARCH_VECTORS_INPUT as _SCHEMA_SEARCH_VECTORS_INPUT,
    SEARCH_VECTORS_OUTPUT as _SCHEMA_SEARCH_VECTORS_OUTPUT,
    SOURCE_TABLE_DETAILS as _SCHEMA_SOURCE_TABLE_DETAILS,
    SOURCE_TABLE_FEATURE_DETAILS as _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS,
    SSE_DESCRIPTION as _SCHEMA_SSE_DESCRIPTION,
    SSE_SPECIFICATION as _SCHEMA_SSE_SPECIFICATION,
    STREAM_SPECIFICATION as _SCHEMA_STREAM_SPECIFICATION,
    TABLE_ALREADY_EXISTS_EXCEPTION as _SCHEMA_TABLE_ALREADY_EXISTS_EXCEPTION,
    TABLE_AUTO_SCALING_DESCRIPTION as _SCHEMA_TABLE_AUTO_SCALING_DESCRIPTION,
    TABLE_CLASS_SUMMARY as _SCHEMA_TABLE_CLASS_SUMMARY,
    TABLE_CREATION_PARAMETERS as _SCHEMA_TABLE_CREATION_PARAMETERS,
    TABLE_DESCRIPTION as _SCHEMA_TABLE_DESCRIPTION,
    TABLE_IN_USE_EXCEPTION as _SCHEMA_TABLE_IN_USE_EXCEPTION,
    TABLE_NOT_FOUND_EXCEPTION as _SCHEMA_TABLE_NOT_FOUND_EXCEPTION,
    TABLE_WARM_THROUGHPUT_DESCRIPTION as _SCHEMA_TABLE_WARM_THROUGHPUT_DESCRIPTION,
    TAG as _SCHEMA_TAG,
    TAG_RESOURCE as _SCHEMA_TAG_RESOURCE,
    TAG_RESOURCE_INPUT as _SCHEMA_TAG_RESOURCE_INPUT,
    TAG_RESOURCE_OUTPUT as _SCHEMA_TAG_RESOURCE_OUTPUT,
    THROTTLING_EXCEPTION as _SCHEMA_THROTTLING_EXCEPTION,
    THROTTLING_REASON as _SCHEMA_THROTTLING_REASON,
    TIME_TO_LIVE_DESCRIPTION as _SCHEMA_TIME_TO_LIVE_DESCRIPTION,
    TIME_TO_LIVE_SPECIFICATION as _SCHEMA_TIME_TO_LIVE_SPECIFICATION,
    TRANSACTION_CANCELED_EXCEPTION as _SCHEMA_TRANSACTION_CANCELED_EXCEPTION,
    TRANSACTION_CONFLICT_EXCEPTION as _SCHEMA_TRANSACTION_CONFLICT_EXCEPTION,
    TRANSACTION_IN_PROGRESS_EXCEPTION as _SCHEMA_TRANSACTION_IN_PROGRESS_EXCEPTION,
    TRANSACT_GET_ITEM as _SCHEMA_TRANSACT_GET_ITEM,
    TRANSACT_GET_ITEMS as _SCHEMA_TRANSACT_GET_ITEMS,
    TRANSACT_GET_ITEMS_INPUT as _SCHEMA_TRANSACT_GET_ITEMS_INPUT,
    TRANSACT_GET_ITEMS_OUTPUT as _SCHEMA_TRANSACT_GET_ITEMS_OUTPUT,
    TRANSACT_WRITE_ITEM as _SCHEMA_TRANSACT_WRITE_ITEM,
    TRANSACT_WRITE_ITEMS as _SCHEMA_TRANSACT_WRITE_ITEMS,
    TRANSACT_WRITE_ITEMS_INPUT as _SCHEMA_TRANSACT_WRITE_ITEMS_INPUT,
    TRANSACT_WRITE_ITEMS_OUTPUT as _SCHEMA_TRANSACT_WRITE_ITEMS_OUTPUT,
    UNTAG_RESOURCE as _SCHEMA_UNTAG_RESOURCE,
    UNTAG_RESOURCE_INPUT as _SCHEMA_UNTAG_RESOURCE_INPUT,
    UNTAG_RESOURCE_OUTPUT as _SCHEMA_UNTAG_RESOURCE_OUTPUT,
    UPDATE as _SCHEMA_UPDATE,
    UPDATE_CONTINUOUS_BACKUPS as _SCHEMA_UPDATE_CONTINUOUS_BACKUPS,
    UPDATE_CONTINUOUS_BACKUPS_INPUT as _SCHEMA_UPDATE_CONTINUOUS_BACKUPS_INPUT,
    UPDATE_CONTINUOUS_BACKUPS_OUTPUT as _SCHEMA_UPDATE_CONTINUOUS_BACKUPS_OUTPUT,
    UPDATE_CONTRIBUTOR_INSIGHTS as _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS,
    UPDATE_CONTRIBUTOR_INSIGHTS_INPUT as _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT,
    UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT as _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT,
    UPDATE_GLOBAL_SECONDARY_INDEX_ACTION as _SCHEMA_UPDATE_GLOBAL_SECONDARY_INDEX_ACTION,
    UPDATE_GLOBAL_TABLE as _SCHEMA_UPDATE_GLOBAL_TABLE,
    UPDATE_GLOBAL_TABLE_INPUT as _SCHEMA_UPDATE_GLOBAL_TABLE_INPUT,
    UPDATE_GLOBAL_TABLE_OUTPUT as _SCHEMA_UPDATE_GLOBAL_TABLE_OUTPUT,
    UPDATE_GLOBAL_TABLE_SETTINGS as _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS,
    UPDATE_GLOBAL_TABLE_SETTINGS_INPUT as _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT,
    UPDATE_GLOBAL_TABLE_SETTINGS_OUTPUT as _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_OUTPUT,
    UPDATE_ITEM as _SCHEMA_UPDATE_ITEM,
    UPDATE_ITEM_INPUT as _SCHEMA_UPDATE_ITEM_INPUT,
    UPDATE_ITEM_OUTPUT as _SCHEMA_UPDATE_ITEM_OUTPUT,
    UPDATE_KINESIS_STREAMING_CONFIGURATION as _SCHEMA_UPDATE_KINESIS_STREAMING_CONFIGURATION,
    UPDATE_KINESIS_STREAMING_DESTINATION as _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION,
    UPDATE_KINESIS_STREAMING_DESTINATION_INPUT as _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_INPUT,
    UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT as _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT,
    UPDATE_REPLICATION_GROUP_MEMBER_ACTION as _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION,
    UPDATE_TABLE as _SCHEMA_UPDATE_TABLE,
    UPDATE_TABLE_INPUT as _SCHEMA_UPDATE_TABLE_INPUT,
    UPDATE_TABLE_OUTPUT as _SCHEMA_UPDATE_TABLE_OUTPUT,
    UPDATE_TABLE_REPLICA_AUTO_SCALING as _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING,
    UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT as _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT,
    UPDATE_TABLE_REPLICA_AUTO_SCALING_OUTPUT as _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_OUTPUT,
    UPDATE_TIME_TO_LIVE as _SCHEMA_UPDATE_TIME_TO_LIVE,
    UPDATE_TIME_TO_LIVE_INPUT as _SCHEMA_UPDATE_TIME_TO_LIVE_INPUT,
    UPDATE_TIME_TO_LIVE_OUTPUT as _SCHEMA_UPDATE_TIME_TO_LIVE_OUTPUT,
    VECTOR_ATTRIBUTE_DEFINITION as _SCHEMA_VECTOR_ATTRIBUTE_DEFINITION,
    VECTOR_CAPACITY as _SCHEMA_VECTOR_CAPACITY,
    VECTOR_INDEX as _SCHEMA_VECTOR_INDEX,
    VECTOR_INDEX_DESCRIPTION as _SCHEMA_VECTOR_INDEX_DESCRIPTION,
    VECTOR_INDEX_INFO as _SCHEMA_VECTOR_INDEX_INFO,
    VECTOR_INDEX_UPDATE as _SCHEMA_VECTOR_INDEX_UPDATE,
    WARM_THROUGHPUT as _SCHEMA_WARM_THROUGHPUT,
    WRITE_REQUEST as _SCHEMA_WRITE_REQUEST,
)


logger = logging.getLogger(__name__)


class ServiceError(ModeledError):
    """
    Base error for all errors in the service.

    Some exceptions do not extend from this class, including
    synthetic, implicit, and shared exception types.
    """


class ApproximateCreationDateTimePrecision(UnknownEnumMixin, StrEnum):
    MILLISECOND = "MILLISECOND"
    MICROSECOND = "MICROSECOND"


@dataclass(kw_only=True)
class ArchivalSummary:
    """Contains details of a table archival operation."""

    archival_date_time: datetime | None = None
    """
    The date and time when table archival was initiated by DynamoDB, in UNIX
    epoch time format.
    """

    archival_reason: str | None = None
    """
    The reason DynamoDB archived the table. Currently, the only possible
    value is:

    - `INACCESSIBLE_ENCRYPTION_CREDENTIALS` - The table was archived due to
      the table's KMS key being inaccessible for more than seven days. An
      On-Demand backup was created at the archival time.
    """

    archival_backup_arn: str | None = None
    """
    The Amazon Resource Name (ARN) of the backup the table was archived to,
    when applicable in the archival reason. If you wish to restore this
    backup to the same table name, you will need to delete the original
    table.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ARCHIVAL_SUMMARY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.archival_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_ARCHIVAL_SUMMARY.members["ArchivalDateTime"],
                self.archival_date_time,
            )

        if self.archival_reason is not None:
            serializer.write_string(
                _SCHEMA_ARCHIVAL_SUMMARY.members["ArchivalReason"], self.archival_reason
            )

        if self.archival_backup_arn is not None:
            serializer.write_string(
                _SCHEMA_ARCHIVAL_SUMMARY.members["ArchivalBackupArn"],
                self.archival_backup_arn,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["archival_date_time"] = de.read_timestamp(
                        _SCHEMA_ARCHIVAL_SUMMARY.members["ArchivalDateTime"]
                    )

                case 1:
                    kwargs["archival_reason"] = de.read_string(
                        _SCHEMA_ARCHIVAL_SUMMARY.members["ArchivalReason"]
                    )

                case 2:
                    kwargs["archival_backup_arn"] = de.read_string(
                        _SCHEMA_ARCHIVAL_SUMMARY.members["ArchivalBackupArn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ARCHIVAL_SUMMARY, consumer=_consumer)
        return kwargs


class AttributeAction(UnknownEnumMixin, StrEnum):
    ADD = "ADD"
    PUT = "PUT"
    DELETE = "DELETE"


class ScalarAttributeType(UnknownEnumMixin, StrEnum):
    S = "S"
    N = "N"
    B = "B"


@dataclass(kw_only=True)
class AttributeDefinition:
    """
    Represents an attribute for describing the schema for the table and
    indexes.
    """

    attribute_name: str
    """A name for the attribute."""

    attribute_type: ScalarAttributeType
    """
    The data type for the attribute, where:

    - `S` - the attribute is of type String

    - `N` - the attribute is of type Number

    - `B` - the attribute is of type Binary
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_DEFINITION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_ATTRIBUTE_DEFINITION.members["AttributeName"], self.attribute_name
        )
        serializer.write_string(
            _SCHEMA_ATTRIBUTE_DEFINITION.members["AttributeType"], self.attribute_type
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attribute_name"] = de.read_string(
                        _SCHEMA_ATTRIBUTE_DEFINITION.members["AttributeName"]
                    )

                case 1:
                    kwargs["attribute_type"] = ScalarAttributeType(
                        de.read_string(
                            _SCHEMA_ATTRIBUTE_DEFINITION.members["AttributeType"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ATTRIBUTE_DEFINITION, consumer=_consumer)
        if "attribute_name" not in kwargs:
            kwargs["attribute_name"] = ""
        if "attribute_type" not in kwargs:
            kwargs["attribute_type"] = ScalarAttributeType._corrected("")
        return kwargs


def _serialize_attribute_definitions(
    serializer: ShapeSerializer, schema: Schema, value: list[AttributeDefinition]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_attribute_definitions(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[AttributeDefinition]:
    result: list[AttributeDefinition] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(AttributeDefinition.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_binary_set_attribute_value(
    serializer: ShapeSerializer, schema: Schema, value: list[bytes]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_blob(member_schema, e)


def _deserialize_binary_set_attribute_value(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[bytes]:
    result: list[bytes] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_blob(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_number_set_attribute_value(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_number_set_attribute_value(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_string_set_attribute_value(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_string_set_attribute_value(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_attribute_name_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_attribute_name_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class AutoScalingTargetTrackingScalingPolicyConfigurationDescription:
    """Represents the properties of a target tracking scaling policy."""

    target_value: float
    """
    The target value for the metric. The range is 8.515920e-109 to
    1.174271e+108 (Base 10) or 2e-360 to 2e360 (Base 2).
    """

    disable_scale_in: bool | None = None
    """
    Indicates whether scale in by the target tracking policy is disabled. If
    the value is true, scale in is disabled and the target tracking policy
    won't remove capacity from the scalable resource. Otherwise, scale in
    is enabled and the target tracking policy can remove capacity from the
    scalable resource. The default value is false.
    """

    scale_in_cooldown: int | None = None
    """
    The amount of time, in seconds, after a scale in activity completes
    before another scale in activity can start. The cooldown period is used
    to block subsequent scale in requests until it has expired. You should
    scale in conservatively to protect your application's availability.
    However, if another alarm triggers a scale out policy during the
    cooldown period after a scale-in, application auto scaling scales out
    your scalable target immediately.
    """

    scale_out_cooldown: int | None = None
    """
    The amount of time, in seconds, after a scale out activity completes
    before another scale out activity can start. While the cooldown period
    is in effect, the capacity that has been added by the previous scale out
    event that initiated the cooldown is calculated as part of the desired
    capacity for the next scale out. You should continuously (but not
    excessively) scale out.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION,
            self,
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.disable_scale_in is not None:
            serializer.write_boolean(
                _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION.members[
                    "DisableScaleIn"
                ],
                self.disable_scale_in,
            )

        if self.scale_in_cooldown is not None:
            serializer.write_integer(
                _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION.members[
                    "ScaleInCooldown"
                ],
                self.scale_in_cooldown,
            )

        if self.scale_out_cooldown is not None:
            serializer.write_integer(
                _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION.members[
                    "ScaleOutCooldown"
                ],
                self.scale_out_cooldown,
            )

        serializer.write_double(
            _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION.members[
                "TargetValue"
            ],
            self.target_value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["disable_scale_in"] = de.read_boolean(
                        _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION.members[
                            "DisableScaleIn"
                        ]
                    )

                case 1:
                    kwargs["scale_in_cooldown"] = de.read_integer(
                        _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION.members[
                            "ScaleInCooldown"
                        ]
                    )

                case 2:
                    kwargs["scale_out_cooldown"] = de.read_integer(
                        _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION.members[
                            "ScaleOutCooldown"
                        ]
                    )

                case 3:
                    kwargs["target_value"] = de.read_double(
                        _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION.members[
                            "TargetValue"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_DESCRIPTION,
            consumer=_consumer,
        )
        if "target_value" not in kwargs:
            kwargs["target_value"] = 0.0
        return kwargs


@dataclass(kw_only=True)
class AutoScalingPolicyDescription:
    """Represents the properties of the scaling policy."""

    policy_name: str | None = None
    """The name of the scaling policy."""

    target_tracking_scaling_policy_configuration: (
        AutoScalingTargetTrackingScalingPolicyConfigurationDescription | None
    ) = None
    """Represents a target tracking scaling policy configuration."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_AUTO_SCALING_POLICY_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.policy_name is not None:
            serializer.write_string(
                _SCHEMA_AUTO_SCALING_POLICY_DESCRIPTION.members["PolicyName"],
                self.policy_name,
            )

        if self.target_tracking_scaling_policy_configuration is not None:
            serializer.write_struct(
                _SCHEMA_AUTO_SCALING_POLICY_DESCRIPTION.members[
                    "TargetTrackingScalingPolicyConfiguration"
                ],
                self.target_tracking_scaling_policy_configuration,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["policy_name"] = de.read_string(
                        _SCHEMA_AUTO_SCALING_POLICY_DESCRIPTION.members["PolicyName"]
                    )

                case 1:
                    kwargs["target_tracking_scaling_policy_configuration"] = (
                        AutoScalingTargetTrackingScalingPolicyConfigurationDescription.deserialize(
                            de
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_AUTO_SCALING_POLICY_DESCRIPTION, consumer=_consumer
        )
        return kwargs


def _serialize_auto_scaling_policy_description_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[AutoScalingPolicyDescription],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_auto_scaling_policy_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[AutoScalingPolicyDescription]:
    result: list[AutoScalingPolicyDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(AutoScalingPolicyDescription.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class AutoScalingTargetTrackingScalingPolicyConfigurationUpdate:
    """
    Represents the settings of a target tracking scaling policy that will be
    modified.
    """

    target_value: float
    """
    The target value for the metric. The range is 8.515920e-109 to
    1.174271e+108 (Base 10) or 2e-360 to 2e360 (Base 2).
    """

    disable_scale_in: bool | None = None
    """
    Indicates whether scale in by the target tracking policy is disabled. If
    the value is true, scale in is disabled and the target tracking policy
    won't remove capacity from the scalable resource. Otherwise, scale in
    is enabled and the target tracking policy can remove capacity from the
    scalable resource. The default value is false.
    """

    scale_in_cooldown: int | None = None
    """
    The amount of time, in seconds, after a scale in activity completes
    before another scale in activity can start. The cooldown period is used
    to block subsequent scale in requests until it has expired. You should
    scale in conservatively to protect your application's availability.
    However, if another alarm triggers a scale out policy during the
    cooldown period after a scale-in, application auto scaling scales out
    your scalable target immediately.
    """

    scale_out_cooldown: int | None = None
    """
    The amount of time, in seconds, after a scale out activity completes
    before another scale out activity can start. While the cooldown period
    is in effect, the capacity that has been added by the previous scale out
    event that initiated the cooldown is calculated as part of the desired
    capacity for the next scale out. You should continuously (but not
    excessively) scale out.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE,
            self,
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.disable_scale_in is not None:
            serializer.write_boolean(
                _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE.members[
                    "DisableScaleIn"
                ],
                self.disable_scale_in,
            )

        if self.scale_in_cooldown is not None:
            serializer.write_integer(
                _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE.members[
                    "ScaleInCooldown"
                ],
                self.scale_in_cooldown,
            )

        if self.scale_out_cooldown is not None:
            serializer.write_integer(
                _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE.members[
                    "ScaleOutCooldown"
                ],
                self.scale_out_cooldown,
            )

        serializer.write_double(
            _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE.members[
                "TargetValue"
            ],
            self.target_value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["disable_scale_in"] = de.read_boolean(
                        _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE.members[
                            "DisableScaleIn"
                        ]
                    )

                case 1:
                    kwargs["scale_in_cooldown"] = de.read_integer(
                        _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE.members[
                            "ScaleInCooldown"
                        ]
                    )

                case 2:
                    kwargs["scale_out_cooldown"] = de.read_integer(
                        _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE.members[
                            "ScaleOutCooldown"
                        ]
                    )

                case 3:
                    kwargs["target_value"] = de.read_double(
                        _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE.members[
                            "TargetValue"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_AUTO_SCALING_TARGET_TRACKING_SCALING_POLICY_CONFIGURATION_UPDATE,
            consumer=_consumer,
        )
        if "target_value" not in kwargs:
            kwargs["target_value"] = 0.0
        return kwargs

    @classmethod
    def _smithy_default(cls) -> Self:
        return cls(target_value=0.0)


@dataclass(kw_only=True)
class AutoScalingPolicyUpdate:
    """Represents the auto scaling policy to be modified."""

    target_tracking_scaling_policy_configuration: (
        AutoScalingTargetTrackingScalingPolicyConfigurationUpdate
    )
    """Represents a target tracking scaling policy configuration."""

    policy_name: str | None = None
    """The name of the scaling policy."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_AUTO_SCALING_POLICY_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.policy_name is not None:
            serializer.write_string(
                _SCHEMA_AUTO_SCALING_POLICY_UPDATE.members["PolicyName"],
                self.policy_name,
            )

        serializer.write_struct(
            _SCHEMA_AUTO_SCALING_POLICY_UPDATE.members[
                "TargetTrackingScalingPolicyConfiguration"
            ],
            self.target_tracking_scaling_policy_configuration,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["policy_name"] = de.read_string(
                        _SCHEMA_AUTO_SCALING_POLICY_UPDATE.members["PolicyName"]
                    )

                case 1:
                    kwargs["target_tracking_scaling_policy_configuration"] = (
                        AutoScalingTargetTrackingScalingPolicyConfigurationUpdate.deserialize(
                            de
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_AUTO_SCALING_POLICY_UPDATE, consumer=_consumer)
        if "target_tracking_scaling_policy_configuration" not in kwargs:
            kwargs["target_tracking_scaling_policy_configuration"] = (
                AutoScalingTargetTrackingScalingPolicyConfigurationUpdate._smithy_default()
            )
        return kwargs


@dataclass(kw_only=True)
class AutoScalingSettingsDescription:
    """
    Represents the auto scaling settings for a global table or global
    secondary index.
    """

    minimum_units: int | None = None
    """
    The minimum capacity units that a global table or global secondary index
    should be scaled down to.
    """

    maximum_units: int | None = None
    """
    The maximum capacity units that a global table or global secondary index
    should be scaled up to.
    """

    auto_scaling_disabled: bool | None = None
    """Disabled auto scaling for this global table or global secondary index."""

    auto_scaling_role_arn: str | None = None
    """Role ARN used for configuring the auto scaling policy."""

    scaling_policies: list[AutoScalingPolicyDescription] | None = None
    """Information about the scaling policies."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.minimum_units is not None:
            serializer.write_long(
                _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION.members["MinimumUnits"],
                self.minimum_units,
            )

        if self.maximum_units is not None:
            serializer.write_long(
                _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION.members["MaximumUnits"],
                self.maximum_units,
            )

        if self.auto_scaling_disabled is not None:
            serializer.write_boolean(
                _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION.members[
                    "AutoScalingDisabled"
                ],
                self.auto_scaling_disabled,
            )

        if self.auto_scaling_role_arn is not None:
            serializer.write_string(
                _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION.members["AutoScalingRoleArn"],
                self.auto_scaling_role_arn,
            )

        if self.scaling_policies is not None:
            _serialize_auto_scaling_policy_description_list(
                serializer,
                _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION.members["ScalingPolicies"],
                self.scaling_policies,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["minimum_units"] = de.read_long(
                        _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION.members[
                            "MinimumUnits"
                        ]
                    )

                case 1:
                    kwargs["maximum_units"] = de.read_long(
                        _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION.members[
                            "MaximumUnits"
                        ]
                    )

                case 2:
                    kwargs["auto_scaling_disabled"] = de.read_boolean(
                        _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION.members[
                            "AutoScalingDisabled"
                        ]
                    )

                case 3:
                    kwargs["auto_scaling_role_arn"] = de.read_string(
                        _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION.members[
                            "AutoScalingRoleArn"
                        ]
                    )

                case 4:
                    kwargs["scaling_policies"] = (
                        _deserialize_auto_scaling_policy_description_list(
                            de,
                            _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION.members[
                                "ScalingPolicies"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_AUTO_SCALING_SETTINGS_DESCRIPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class AutoScalingSettingsUpdate:
    """
    Represents the auto scaling settings to be modified for a global table
    or global secondary index.
    """

    minimum_units: int | None = None
    """
    The minimum capacity units that a global table or global secondary index
    should be scaled down to.
    """

    maximum_units: int | None = None
    """
    The maximum capacity units that a global table or global secondary index
    should be scaled up to.
    """

    auto_scaling_disabled: bool | None = None
    """Disabled auto scaling for this global table or global secondary index."""

    auto_scaling_role_arn: str | None = None
    """Role ARN used for configuring auto scaling policy."""

    scaling_policy_update: AutoScalingPolicyUpdate | None = None
    """
    The scaling policy to apply for scaling target global table or global
    secondary index capacity units.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_AUTO_SCALING_SETTINGS_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.minimum_units is not None:
            serializer.write_long(
                _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE.members["MinimumUnits"],
                self.minimum_units,
            )

        if self.maximum_units is not None:
            serializer.write_long(
                _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE.members["MaximumUnits"],
                self.maximum_units,
            )

        if self.auto_scaling_disabled is not None:
            serializer.write_boolean(
                _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE.members["AutoScalingDisabled"],
                self.auto_scaling_disabled,
            )

        if self.auto_scaling_role_arn is not None:
            serializer.write_string(
                _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE.members["AutoScalingRoleArn"],
                self.auto_scaling_role_arn,
            )

        if self.scaling_policy_update is not None:
            serializer.write_struct(
                _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE.members["ScalingPolicyUpdate"],
                self.scaling_policy_update,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["minimum_units"] = de.read_long(
                        _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE.members["MinimumUnits"]
                    )

                case 1:
                    kwargs["maximum_units"] = de.read_long(
                        _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE.members["MaximumUnits"]
                    )

                case 2:
                    kwargs["auto_scaling_disabled"] = de.read_boolean(
                        _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE.members[
                            "AutoScalingDisabled"
                        ]
                    )

                case 3:
                    kwargs["auto_scaling_role_arn"] = de.read_string(
                        _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE.members[
                            "AutoScalingRoleArn"
                        ]
                    )

                case 4:
                    kwargs["scaling_policy_update"] = (
                        AutoScalingPolicyUpdate.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_AUTO_SCALING_SETTINGS_UPDATE, consumer=_consumer
        )
        return kwargs


class BackupStatus(UnknownEnumMixin, StrEnum):
    CREATING = "CREATING"
    DELETED = "DELETED"
    AVAILABLE = "AVAILABLE"


class BackupType(UnknownEnumMixin, StrEnum):
    USER = "USER"
    SYSTEM = "SYSTEM"
    AWS_BACKUP = "AWS_BACKUP"


@dataclass(kw_only=True)
class BackupDetails:
    """Contains the details of the backup created for the table."""

    backup_arn: str
    """ARN associated with the backup."""

    backup_name: str
    """Name of the requested backup."""

    backup_status: BackupStatus
    """Backup can be in one of the following states: CREATING, ACTIVE, DELETED."""

    backup_type: BackupType
    """
    BackupType:

    - `USER` - You create and manage these using the on-demand backup
      feature.

    - `SYSTEM` - If you delete a table with point-in-time recovery enabled,
      a `SYSTEM` backup is automatically created and is retained for 35 days
      (at no additional cost). System backups allow you to restore the
      deleted table to the state it was in just before the point of
      deletion.

    - `AWS_BACKUP` - On-demand backup created by you from Backup service.
    """

    backup_creation_date_time: datetime
    """
    Time at which the backup was created. This is the request time of the
    backup.
    """

    backup_size_bytes: int | None = None
    """
    Size of the backup in bytes. DynamoDB updates this value approximately
    every six hours. Recent changes might not be reflected in this value.
    """

    backup_expiry_date_time: datetime | None = None
    """
    Time at which the automatic on-demand backup created by DynamoDB will
    expire. This `SYSTEM` on-demand backup expires automatically 35 days
    after its creation.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BACKUP_DETAILS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_BACKUP_DETAILS.members["BackupArn"], self.backup_arn
        )
        serializer.write_string(
            _SCHEMA_BACKUP_DETAILS.members["BackupName"], self.backup_name
        )
        if self.backup_size_bytes is not None:
            serializer.write_long(
                _SCHEMA_BACKUP_DETAILS.members["BackupSizeBytes"],
                self.backup_size_bytes,
            )

        serializer.write_string(
            _SCHEMA_BACKUP_DETAILS.members["BackupStatus"], self.backup_status
        )
        serializer.write_string(
            _SCHEMA_BACKUP_DETAILS.members["BackupType"], self.backup_type
        )
        serializer.write_timestamp(
            _SCHEMA_BACKUP_DETAILS.members["BackupCreationDateTime"],
            self.backup_creation_date_time,
        )
        if self.backup_expiry_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_BACKUP_DETAILS.members["BackupExpiryDateTime"],
                self.backup_expiry_date_time,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["backup_arn"] = de.read_string(
                        _SCHEMA_BACKUP_DETAILS.members["BackupArn"]
                    )

                case 1:
                    kwargs["backup_name"] = de.read_string(
                        _SCHEMA_BACKUP_DETAILS.members["BackupName"]
                    )

                case 2:
                    kwargs["backup_size_bytes"] = de.read_long(
                        _SCHEMA_BACKUP_DETAILS.members["BackupSizeBytes"]
                    )

                case 3:
                    kwargs["backup_status"] = BackupStatus(
                        de.read_string(_SCHEMA_BACKUP_DETAILS.members["BackupStatus"])
                    )

                case 4:
                    kwargs["backup_type"] = BackupType(
                        de.read_string(_SCHEMA_BACKUP_DETAILS.members["BackupType"])
                    )

                case 5:
                    kwargs["backup_creation_date_time"] = de.read_timestamp(
                        _SCHEMA_BACKUP_DETAILS.members["BackupCreationDateTime"]
                    )

                case 6:
                    kwargs["backup_expiry_date_time"] = de.read_timestamp(
                        _SCHEMA_BACKUP_DETAILS.members["BackupExpiryDateTime"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BACKUP_DETAILS, consumer=_consumer)
        if "backup_arn" not in kwargs:
            kwargs["backup_arn"] = ""
        if "backup_name" not in kwargs:
            kwargs["backup_name"] = ""
        if "backup_status" not in kwargs:
            kwargs["backup_status"] = BackupStatus._corrected("")
        if "backup_type" not in kwargs:
            kwargs["backup_type"] = BackupType._corrected("")
        if "backup_creation_date_time" not in kwargs:
            kwargs["backup_creation_date_time"] = datetime.fromtimestamp(
                0, tz=timezone.utc
            )
        return kwargs


class BillingMode(UnknownEnumMixin, StrEnum):
    PROVISIONED = "PROVISIONED"
    PAY_PER_REQUEST = "PAY_PER_REQUEST"


class KeyType(UnknownEnumMixin, StrEnum):
    HASH = "HASH"
    RANGE = "RANGE"


@dataclass(kw_only=True)
class KeySchemaElement:
    """
    Represents *a single element* of a key schema. A key schema specifies
    the attributes that make up the primary key of a table, or the key
    attributes of an index.

    A `KeySchemaElement` represents exactly one attribute of the primary
    key. For example, a simple primary key would be represented by one
    `KeySchemaElement` (for the partition key). A composite primary key
    would require one `KeySchemaElement` for the partition key, and another
    `KeySchemaElement` for the sort key.

    A `KeySchemaElement` must be a scalar, top-level attribute (not a nested
    attribute). The data type must be one of String, Number, or Binary. The
    attribute cannot be nested within a List or a Map.
    """

    attribute_name: str
    """The name of a key attribute."""

    key_type: KeyType
    """
    The role that this key attribute will assume:

    - `HASH` - partition key

    - `RANGE` - sort key

    Note:
        The partition key of an item is also known as its *hash attribute*. The
        term \"hash attribute\" derives from DynamoDB's usage of an internal
        hash function to evenly distribute data items across partitions, based
        on their partition key values.

        The sort key of an item is also known as its *range attribute*. The term
        \"range attribute\" derives from the way DynamoDB stores items with the
        same partition key physically close together, in sorted order by the
        sort key value.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_KEY_SCHEMA_ELEMENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_KEY_SCHEMA_ELEMENT.members["AttributeName"], self.attribute_name
        )
        serializer.write_string(
            _SCHEMA_KEY_SCHEMA_ELEMENT.members["KeyType"], self.key_type
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attribute_name"] = de.read_string(
                        _SCHEMA_KEY_SCHEMA_ELEMENT.members["AttributeName"]
                    )

                case 1:
                    kwargs["key_type"] = KeyType(
                        de.read_string(_SCHEMA_KEY_SCHEMA_ELEMENT.members["KeyType"])
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_KEY_SCHEMA_ELEMENT, consumer=_consumer)
        if "attribute_name" not in kwargs:
            kwargs["attribute_name"] = ""
        if "key_type" not in kwargs:
            kwargs["key_type"] = KeyType._corrected("")
        return kwargs


def _serialize_key_schema(
    serializer: ShapeSerializer, schema: Schema, value: list[KeySchemaElement]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_key_schema(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[KeySchemaElement]:
    result: list[KeySchemaElement] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(KeySchemaElement.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class OnDemandThroughput:
    """
    Sets the maximum number of read and write units for the specified
    on-demand table. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    max_read_request_units: int | None = None
    """
    Maximum number of read request units for the specified table.

    To specify a maximum `OnDemandThroughput` on your table, set the value
    of `MaxReadRequestUnits` as greater than or equal to 1. To remove the
    maximum `OnDemandThroughput` that is currently set on your table, set
    the value of `MaxReadRequestUnits` to -1.
    """

    max_write_request_units: int | None = None
    """
    Maximum number of write request units for the specified table.

    To specify a maximum `OnDemandThroughput` on your table, set the value
    of `MaxWriteRequestUnits` as greater than or equal to 1. To remove the
    maximum `OnDemandThroughput` that is currently set on your table, set
    the value of `MaxWriteRequestUnits` to -1.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ON_DEMAND_THROUGHPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.max_read_request_units is not None:
            serializer.write_long(
                _SCHEMA_ON_DEMAND_THROUGHPUT.members["MaxReadRequestUnits"],
                self.max_read_request_units,
            )

        if self.max_write_request_units is not None:
            serializer.write_long(
                _SCHEMA_ON_DEMAND_THROUGHPUT.members["MaxWriteRequestUnits"],
                self.max_write_request_units,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["max_read_request_units"] = de.read_long(
                        _SCHEMA_ON_DEMAND_THROUGHPUT.members["MaxReadRequestUnits"]
                    )

                case 1:
                    kwargs["max_write_request_units"] = de.read_long(
                        _SCHEMA_ON_DEMAND_THROUGHPUT.members["MaxWriteRequestUnits"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ON_DEMAND_THROUGHPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ProvisionedThroughput:
    """
    Represents the provisioned throughput settings for the specified global
    secondary index. You must use `ProvisionedThroughput` or
    `OnDemandThroughput` based on your table's capacity mode.

    For current minimum and maximum provisioned throughput values, see
    [Service, Account, and Table
    Quotas](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Limits.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    read_capacity_units: int
    """
    The maximum number of strongly consistent reads consumed per second
    before DynamoDB returns a `ThrottlingException`. For more information,
    see [Specifying Read and Write
    Requirements](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ProvisionedThroughput.html)
    in the *Amazon DynamoDB Developer Guide*.

    If read/write capacity mode is `PAY_PER_REQUEST` the value is set to 0.
    """

    write_capacity_units: int
    """
    The maximum number of writes consumed per second before DynamoDB returns
    a `ThrottlingException`. For more information, see [Specifying Read and
    Write
    Requirements](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ProvisionedThroughput.html)
    in the *Amazon DynamoDB Developer Guide*.

    If read/write capacity mode is `PAY_PER_REQUEST` the value is set to 0.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PROVISIONED_THROUGHPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_long(
            _SCHEMA_PROVISIONED_THROUGHPUT.members["ReadCapacityUnits"],
            self.read_capacity_units,
        )
        serializer.write_long(
            _SCHEMA_PROVISIONED_THROUGHPUT.members["WriteCapacityUnits"],
            self.write_capacity_units,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["read_capacity_units"] = de.read_long(
                        _SCHEMA_PROVISIONED_THROUGHPUT.members["ReadCapacityUnits"]
                    )

                case 1:
                    kwargs["write_capacity_units"] = de.read_long(
                        _SCHEMA_PROVISIONED_THROUGHPUT.members["WriteCapacityUnits"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PROVISIONED_THROUGHPUT, consumer=_consumer)
        if "read_capacity_units" not in kwargs:
            kwargs["read_capacity_units"] = 0
        if "write_capacity_units" not in kwargs:
            kwargs["write_capacity_units"] = 0
        return kwargs

    @classmethod
    def _smithy_default(cls) -> Self:
        return cls(read_capacity_units=0, write_capacity_units=0)


@dataclass(kw_only=True)
class SourceTableDetails:
    """Contains the details of the table when the backup was created."""

    table_name: str
    """The name of the table for which the backup was created."""

    table_id: str
    """Unique identifier for the table for which the backup was created."""

    key_schema: list[KeySchemaElement]
    """Schema of the table."""

    table_creation_date_time: datetime
    """Time when the source table was created."""

    provisioned_throughput: ProvisionedThroughput
    """Read IOPs and Write IOPS on the table when the backup was created."""

    table_arn: str | None = None
    """ARN of the table for which backup was created."""

    table_size_bytes: int | None = None
    """Size of the table in bytes. Note that this is an approximate value."""

    on_demand_throughput: OnDemandThroughput | None = None
    """
    Sets the maximum number of read and write units for the specified
    on-demand table. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    item_count: int | None = None
    """Number of items in the table. Note that this is an approximate value."""

    billing_mode: BillingMode | None = None
    """
    Controls how you are charged for read and write throughput and how you
    manage capacity. This setting can be changed later.

    - `PROVISIONED` - Sets the read/write capacity mode to `PROVISIONED`. We
      recommend using `PROVISIONED` for predictable workloads.

    - `PAY_PER_REQUEST` - Sets the read/write capacity mode to
      `PAY_PER_REQUEST`. We recommend using `PAY_PER_REQUEST` for
      unpredictable workloads.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SOURCE_TABLE_DETAILS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_SOURCE_TABLE_DETAILS.members["TableName"], self.table_name
        )
        serializer.write_string(
            _SCHEMA_SOURCE_TABLE_DETAILS.members["TableId"], self.table_id
        )
        if self.table_arn is not None:
            serializer.write_string(
                _SCHEMA_SOURCE_TABLE_DETAILS.members["TableArn"], self.table_arn
            )

        if self.table_size_bytes is not None:
            serializer.write_long(
                _SCHEMA_SOURCE_TABLE_DETAILS.members["TableSizeBytes"],
                self.table_size_bytes,
            )

        _serialize_key_schema(
            serializer,
            _SCHEMA_SOURCE_TABLE_DETAILS.members["KeySchema"],
            self.key_schema,
        )
        serializer.write_timestamp(
            _SCHEMA_SOURCE_TABLE_DETAILS.members["TableCreationDateTime"],
            self.table_creation_date_time,
        )
        serializer.write_struct(
            _SCHEMA_SOURCE_TABLE_DETAILS.members["ProvisionedThroughput"],
            self.provisioned_throughput,
        )
        if self.on_demand_throughput is not None:
            serializer.write_struct(
                _SCHEMA_SOURCE_TABLE_DETAILS.members["OnDemandThroughput"],
                self.on_demand_throughput,
            )

        if self.item_count is not None:
            serializer.write_long(
                _SCHEMA_SOURCE_TABLE_DETAILS.members["ItemCount"], self.item_count
            )

        if self.billing_mode is not None:
            serializer.write_string(
                _SCHEMA_SOURCE_TABLE_DETAILS.members["BillingMode"], self.billing_mode
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_SOURCE_TABLE_DETAILS.members["TableName"]
                    )

                case 1:
                    kwargs["table_id"] = de.read_string(
                        _SCHEMA_SOURCE_TABLE_DETAILS.members["TableId"]
                    )

                case 2:
                    kwargs["table_arn"] = de.read_string(
                        _SCHEMA_SOURCE_TABLE_DETAILS.members["TableArn"]
                    )

                case 3:
                    kwargs["table_size_bytes"] = de.read_long(
                        _SCHEMA_SOURCE_TABLE_DETAILS.members["TableSizeBytes"]
                    )

                case 4:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de, _SCHEMA_SOURCE_TABLE_DETAILS.members["KeySchema"]
                    )

                case 5:
                    kwargs["table_creation_date_time"] = de.read_timestamp(
                        _SCHEMA_SOURCE_TABLE_DETAILS.members["TableCreationDateTime"]
                    )

                case 6:
                    kwargs["provisioned_throughput"] = (
                        ProvisionedThroughput.deserialize(de)
                    )

                case 7:
                    kwargs["on_demand_throughput"] = OnDemandThroughput.deserialize(de)

                case 8:
                    kwargs["item_count"] = de.read_long(
                        _SCHEMA_SOURCE_TABLE_DETAILS.members["ItemCount"]
                    )

                case 9:
                    kwargs["billing_mode"] = BillingMode(
                        de.read_string(
                            _SCHEMA_SOURCE_TABLE_DETAILS.members["BillingMode"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SOURCE_TABLE_DETAILS, consumer=_consumer)
        if "table_name" not in kwargs:
            kwargs["table_name"] = ""
        if "table_id" not in kwargs:
            kwargs["table_id"] = ""
        if "key_schema" not in kwargs:
            kwargs["key_schema"] = []
        if "table_creation_date_time" not in kwargs:
            kwargs["table_creation_date_time"] = datetime.fromtimestamp(
                0, tz=timezone.utc
            )
        if "provisioned_throughput" not in kwargs:
            kwargs["provisioned_throughput"] = ProvisionedThroughput._smithy_default()
        return kwargs


def _serialize_non_key_attribute_name_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_non_key_attribute_name_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


class ProjectionType(UnknownEnumMixin, StrEnum):
    ALL = "ALL"
    KEYS_ONLY = "KEYS_ONLY"
    INCLUDE = "INCLUDE"


@dataclass(kw_only=True)
class Projection:
    """
    Represents attributes that are copied (projected) from the table into an
    index. These are in addition to the primary key attributes and index key
    attributes, which are automatically projected.
    """

    projection_type: ProjectionType | None = None
    """
    The set of attributes that are projected into the index:

    - `KEYS_ONLY` - Only the index and primary keys are projected into the
      index.

    - `INCLUDE` - In addition to the attributes described in `KEYS_ONLY`,
      the secondary index will include other non-key attributes that you
      specify.

    - `ALL` - All of the table attributes are projected into the index.

    When using the DynamoDB console, `ALL` is selected by default.
    """

    non_key_attributes: list[str] | None = None
    """
    Represents the non-key attribute names which will be projected into the
    index.

    For global and local secondary indexes, the total count of
    `NonKeyAttributes` summed across all of the secondary indexes, must not
    exceed 100. If you project the same attribute into two different
    indexes, this counts as two distinct attributes when determining the
    total. This limit only applies when you specify the ProjectionType of
    `INCLUDE`. You still can specify the ProjectionType of `ALL` to project
    all attributes from the source table, even if the table has more than
    100 attributes.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PROJECTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.projection_type is not None:
            serializer.write_string(
                _SCHEMA_PROJECTION.members["ProjectionType"], self.projection_type
            )

        if self.non_key_attributes is not None:
            _serialize_non_key_attribute_name_list(
                serializer,
                _SCHEMA_PROJECTION.members["NonKeyAttributes"],
                self.non_key_attributes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["projection_type"] = ProjectionType(
                        de.read_string(_SCHEMA_PROJECTION.members["ProjectionType"])
                    )

                case 1:
                    kwargs["non_key_attributes"] = (
                        _deserialize_non_key_attribute_name_list(
                            de, _SCHEMA_PROJECTION.members["NonKeyAttributes"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PROJECTION, consumer=_consumer)
        return kwargs

    @classmethod
    def _smithy_default(cls) -> Self:
        return cls()


@dataclass(kw_only=True)
class GlobalSecondaryIndexInfo:
    """
    Represents the properties of a global secondary index for the table when
    the backup was created.
    """

    index_name: str | None = None
    """The name of the global secondary index."""

    key_schema: list[KeySchemaElement] | None = None
    """
    The complete key schema for a global secondary index, which consists of
    one or more pairs of attribute names and key types:

    - `HASH` - partition key

    - `RANGE` - sort key

    Note:
        The partition key of an item is also known as its *hash attribute*. The
        term \"hash attribute\" derives from DynamoDB's usage of an internal
        hash function to evenly distribute data items across partitions, based
        on their partition key values.

        The sort key of an item is also known as its *range attribute*. The term
        \"range attribute\" derives from the way DynamoDB stores items with the
        same partition key physically close together, in sorted order by the
        sort key value.
    """

    projection: Projection | None = None
    """
    Represents attributes that are copied (projected) from the table into
    the global secondary index. These are in addition to the primary key
    attributes and index key attributes, which are automatically projected.
    """

    provisioned_throughput: ProvisionedThroughput | None = None
    """
    Represents the provisioned throughput settings for the specified global
    secondary index.
    """

    on_demand_throughput: OnDemandThroughput | None = None
    """
    Sets the maximum number of read and write units for the specified
    on-demand table. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GLOBAL_SECONDARY_INDEX_INFO, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_INFO.members["IndexName"],
                self.index_name,
            )

        if self.key_schema is not None:
            _serialize_key_schema(
                serializer,
                _SCHEMA_GLOBAL_SECONDARY_INDEX_INFO.members["KeySchema"],
                self.key_schema,
            )

        if self.projection is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_INFO.members["Projection"],
                self.projection,
            )

        if self.provisioned_throughput is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_INFO.members["ProvisionedThroughput"],
                self.provisioned_throughput,
            )

        if self.on_demand_throughput is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_INFO.members["OnDemandThroughput"],
                self.on_demand_throughput,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_GLOBAL_SECONDARY_INDEX_INFO.members["IndexName"]
                    )

                case 1:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de, _SCHEMA_GLOBAL_SECONDARY_INDEX_INFO.members["KeySchema"]
                    )

                case 2:
                    kwargs["projection"] = Projection.deserialize(de)

                case 3:
                    kwargs["provisioned_throughput"] = (
                        ProvisionedThroughput.deserialize(de)
                    )

                case 4:
                    kwargs["on_demand_throughput"] = OnDemandThroughput.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GLOBAL_SECONDARY_INDEX_INFO, consumer=_consumer
        )
        return kwargs


def _serialize_global_secondary_indexes(
    serializer: ShapeSerializer, schema: Schema, value: list[GlobalSecondaryIndexInfo]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_global_secondary_indexes(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[GlobalSecondaryIndexInfo]:
    result: list[GlobalSecondaryIndexInfo] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(GlobalSecondaryIndexInfo.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class LocalSecondaryIndexInfo:
    """
    Represents the properties of a local secondary index for the table when
    the backup was created.
    """

    index_name: str | None = None
    """Represents the name of the local secondary index."""

    key_schema: list[KeySchemaElement] | None = None
    """
    The complete key schema for a local secondary index, which consists of
    one or more pairs of attribute names and key types:

    - `HASH` - partition key

    - `RANGE` - sort key

    Note:
        The partition key of an item is also known as its *hash attribute*. The
        term \"hash attribute\" derives from DynamoDB's usage of an internal
        hash function to evenly distribute data items across partitions, based
        on their partition key values.

        The sort key of an item is also known as its *range attribute*. The term
        \"range attribute\" derives from the way DynamoDB stores items with the
        same partition key physically close together, in sorted order by the
        sort key value.
    """

    projection: Projection | None = None
    """
    Represents attributes that are copied (projected) from the table into
    the global secondary index. These are in addition to the primary key
    attributes and index key attributes, which are automatically projected.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LOCAL_SECONDARY_INDEX_INFO, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_LOCAL_SECONDARY_INDEX_INFO.members["IndexName"], self.index_name
            )

        if self.key_schema is not None:
            _serialize_key_schema(
                serializer,
                _SCHEMA_LOCAL_SECONDARY_INDEX_INFO.members["KeySchema"],
                self.key_schema,
            )

        if self.projection is not None:
            serializer.write_struct(
                _SCHEMA_LOCAL_SECONDARY_INDEX_INFO.members["Projection"],
                self.projection,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_LOCAL_SECONDARY_INDEX_INFO.members["IndexName"]
                    )

                case 1:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de, _SCHEMA_LOCAL_SECONDARY_INDEX_INFO.members["KeySchema"]
                    )

                case 2:
                    kwargs["projection"] = Projection.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LOCAL_SECONDARY_INDEX_INFO, consumer=_consumer)
        return kwargs


def _serialize_local_secondary_indexes(
    serializer: ShapeSerializer, schema: Schema, value: list[LocalSecondaryIndexInfo]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_local_secondary_indexes(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[LocalSecondaryIndexInfo]:
    result: list[LocalSecondaryIndexInfo] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(LocalSecondaryIndexInfo.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


class SSEType(UnknownEnumMixin, StrEnum):
    AES256 = "AES256"
    KMS = "KMS"


class SSEStatus(UnknownEnumMixin, StrEnum):
    ENABLING = "ENABLING"
    ENABLED = "ENABLED"
    DISABLING = "DISABLING"
    DISABLED = "DISABLED"
    UPDATING = "UPDATING"


@dataclass(kw_only=True)
class SSEDescription:
    """
    The description of the server-side encryption status on the specified
    table.
    """

    status: SSEStatus | None = None
    """
    Represents the current state of server-side encryption. The only
    supported values are:

    - `ENABLED` - Server-side encryption is enabled.

    - `UPDATING` - Server-side encryption is being updated.
    """

    sse_type: SSEType | None = None
    """
    Server-side encryption type. The only supported value is:

    - `KMS` - Server-side encryption that uses Key Management Service. The
      key is stored in your account and is managed by KMS (KMS charges
      apply).
    """

    kms_master_key_arn: str | None = None
    """The KMS key ARN used for the KMS encryption."""

    inaccessible_encryption_date_time: datetime | None = None
    """
    Indicates the time, in UNIX epoch date format, when DynamoDB detected
    that the table's KMS key was inaccessible. This attribute will
    automatically be cleared when DynamoDB detects that the table's KMS key
    is accessible again. DynamoDB will initiate the table archival process
    when table's KMS key remains inaccessible for more than seven days from
    this date.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SSE_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.status is not None:
            serializer.write_string(
                _SCHEMA_SSE_DESCRIPTION.members["Status"], self.status
            )

        if self.sse_type is not None:
            serializer.write_string(
                _SCHEMA_SSE_DESCRIPTION.members["SSEType"], self.sse_type
            )

        if self.kms_master_key_arn is not None:
            serializer.write_string(
                _SCHEMA_SSE_DESCRIPTION.members["KMSMasterKeyArn"],
                self.kms_master_key_arn,
            )

        if self.inaccessible_encryption_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_SSE_DESCRIPTION.members["InaccessibleEncryptionDateTime"],
                self.inaccessible_encryption_date_time,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["status"] = SSEStatus(
                        de.read_string(_SCHEMA_SSE_DESCRIPTION.members["Status"])
                    )

                case 1:
                    kwargs["sse_type"] = SSEType(
                        de.read_string(_SCHEMA_SSE_DESCRIPTION.members["SSEType"])
                    )

                case 2:
                    kwargs["kms_master_key_arn"] = de.read_string(
                        _SCHEMA_SSE_DESCRIPTION.members["KMSMasterKeyArn"]
                    )

                case 3:
                    kwargs["inaccessible_encryption_date_time"] = de.read_timestamp(
                        _SCHEMA_SSE_DESCRIPTION.members[
                            "InaccessibleEncryptionDateTime"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SSE_DESCRIPTION, consumer=_consumer)
        return kwargs


class StreamViewType(UnknownEnumMixin, StrEnum):
    NEW_IMAGE = "NEW_IMAGE"
    OLD_IMAGE = "OLD_IMAGE"
    NEW_AND_OLD_IMAGES = "NEW_AND_OLD_IMAGES"
    KEYS_ONLY = "KEYS_ONLY"


@dataclass(kw_only=True)
class StreamSpecification:
    """Represents the DynamoDB Streams configuration for a table in DynamoDB."""

    stream_enabled: bool
    """
    Indicates whether DynamoDB Streams is enabled (true) or disabled (false)
    on the table.
    """

    stream_view_type: StreamViewType | None = None
    """
    When an item in the table is modified, `StreamViewType` determines what
    information is written to the stream for this table. Valid values for
    `StreamViewType` are:

    - `KEYS_ONLY` - Only the key attributes of the modified item are written
      to the stream.

    - `NEW_IMAGE` - The entire item, as it appears after it was modified, is
      written to the stream.

    - `OLD_IMAGE` - The entire item, as it appeared before it was modified,
      is written to the stream.

    - `NEW_AND_OLD_IMAGES` - Both the new and the old item images of the
      item are written to the stream.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_STREAM_SPECIFICATION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_boolean(
            _SCHEMA_STREAM_SPECIFICATION.members["StreamEnabled"], self.stream_enabled
        )
        if self.stream_view_type is not None:
            serializer.write_string(
                _SCHEMA_STREAM_SPECIFICATION.members["StreamViewType"],
                self.stream_view_type,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["stream_enabled"] = de.read_boolean(
                        _SCHEMA_STREAM_SPECIFICATION.members["StreamEnabled"]
                    )

                case 1:
                    kwargs["stream_view_type"] = StreamViewType(
                        de.read_string(
                            _SCHEMA_STREAM_SPECIFICATION.members["StreamViewType"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_STREAM_SPECIFICATION, consumer=_consumer)
        if "stream_enabled" not in kwargs:
            kwargs["stream_enabled"] = False
        return kwargs


class TimeToLiveStatus(UnknownEnumMixin, StrEnum):
    ENABLING = "ENABLING"
    DISABLING = "DISABLING"
    ENABLED = "ENABLED"
    DISABLED = "DISABLED"


@dataclass(kw_only=True)
class TimeToLiveDescription:
    """The description of the Time to Live (TTL) status on the specified table."""

    time_to_live_status: TimeToLiveStatus | None = None
    """The TTL status for the table."""

    attribute_name: str | None = None
    """The name of the TTL attribute for items in the table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TIME_TO_LIVE_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.time_to_live_status is not None:
            serializer.write_string(
                _SCHEMA_TIME_TO_LIVE_DESCRIPTION.members["TimeToLiveStatus"],
                self.time_to_live_status,
            )

        if self.attribute_name is not None:
            serializer.write_string(
                _SCHEMA_TIME_TO_LIVE_DESCRIPTION.members["AttributeName"],
                self.attribute_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["time_to_live_status"] = TimeToLiveStatus(
                        de.read_string(
                            _SCHEMA_TIME_TO_LIVE_DESCRIPTION.members["TimeToLiveStatus"]
                        )
                    )

                case 1:
                    kwargs["attribute_name"] = de.read_string(
                        _SCHEMA_TIME_TO_LIVE_DESCRIPTION.members["AttributeName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TIME_TO_LIVE_DESCRIPTION, consumer=_consumer)
        return kwargs


class VectorDistanceFunction(UnknownEnumMixin, StrEnum):
    COSINE = "COSINE"
    DOT_PRODUCT = "DOT_PRODUCT"
    EUCLIDEAN = "EUCLIDEAN"


class SearchSchemaElementType(UnknownEnumMixin, StrEnum):
    HASH = "HASH"
    INLINE_FILTER = "INLINE_FILTER"


@dataclass(kw_only=True)
class SearchSchemaElement:
    """An element in the search schema of a vector index."""

    attribute_name: str
    """The name of the attribute."""

    search_schema_element_type: SearchSchemaElementType
    """
    The role of the attribute in the search schema. Valid values:

    - `HASH` - A partition key that partitions the vector index for
      independent scaling. When specified, you must provide this
      attribute's value in the `SearchConditionExpression`.

    - `INLINE_FILTER` - An attribute projected into the vector index for
      filtering at the storage layer during search. Inline filters are
      optional in the `SearchConditionExpression`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SEARCH_SCHEMA_ELEMENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_SEARCH_SCHEMA_ELEMENT.members["AttributeName"], self.attribute_name
        )
        serializer.write_string(
            _SCHEMA_SEARCH_SCHEMA_ELEMENT.members["SearchSchemaElementType"],
            self.search_schema_element_type,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attribute_name"] = de.read_string(
                        _SCHEMA_SEARCH_SCHEMA_ELEMENT.members["AttributeName"]
                    )

                case 1:
                    kwargs["search_schema_element_type"] = SearchSchemaElementType(
                        de.read_string(
                            _SCHEMA_SEARCH_SCHEMA_ELEMENT.members[
                                "SearchSchemaElementType"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SEARCH_SCHEMA_ELEMENT, consumer=_consumer)
        if "attribute_name" not in kwargs:
            kwargs["attribute_name"] = ""
        if "search_schema_element_type" not in kwargs:
            kwargs["search_schema_element_type"] = SearchSchemaElementType._corrected(
                ""
            )
        return kwargs


def _serialize_search_schema(
    serializer: ShapeSerializer, schema: Schema, value: list[SearchSchemaElement]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_search_schema(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[SearchSchemaElement]:
    result: list[SearchSchemaElement] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(SearchSchemaElement.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class VectorAttributeDefinition:
    """The definition of a vector attribute for a vector index."""

    attribute_name: str
    """The name of the vector attribute."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_VECTOR_ATTRIBUTE_DEFINITION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_VECTOR_ATTRIBUTE_DEFINITION.members["AttributeName"],
            self.attribute_name,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attribute_name"] = de.read_string(
                        _SCHEMA_VECTOR_ATTRIBUTE_DEFINITION.members["AttributeName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_VECTOR_ATTRIBUTE_DEFINITION, consumer=_consumer
        )
        if "attribute_name" not in kwargs:
            kwargs["attribute_name"] = ""
        return kwargs

    @classmethod
    def _smithy_default(cls) -> Self:
        return cls(attribute_name="")


@dataclass(kw_only=True)
class VectorIndexInfo:
    """
    Contains the configuration of a vector index as it existed at the time a
    backup was created.
    """

    index_name: str | None = None
    """The name of the vector index."""

    vector_attribute: VectorAttributeDefinition | None = None
    """The vector attribute configuration for the index."""

    search_schema: list[SearchSchemaElement] | None = None
    """
    The search schema that defines partition key and inline filter
    attributes for the vector index.
    """

    projection: Projection | None = None
    """
    Specifies attributes that are copied (projected) from the table into the
    vector index.
    """

    dimensions: int | None = None
    """The number of dimensions in each vector."""

    distance_function: VectorDistanceFunction | None = None
    """The distance function used to calculate similarity between vectors."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_VECTOR_INDEX_INFO, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_VECTOR_INDEX_INFO.members["IndexName"], self.index_name
            )

        if self.vector_attribute is not None:
            serializer.write_struct(
                _SCHEMA_VECTOR_INDEX_INFO.members["VectorAttribute"],
                self.vector_attribute,
            )

        if self.search_schema is not None:
            _serialize_search_schema(
                serializer,
                _SCHEMA_VECTOR_INDEX_INFO.members["SearchSchema"],
                self.search_schema,
            )

        if self.projection is not None:
            serializer.write_struct(
                _SCHEMA_VECTOR_INDEX_INFO.members["Projection"], self.projection
            )

        if self.dimensions is not None:
            serializer.write_long(
                _SCHEMA_VECTOR_INDEX_INFO.members["Dimensions"], self.dimensions
            )

        if self.distance_function is not None:
            serializer.write_string(
                _SCHEMA_VECTOR_INDEX_INFO.members["DistanceFunction"],
                self.distance_function,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_VECTOR_INDEX_INFO.members["IndexName"]
                    )

                case 1:
                    kwargs["vector_attribute"] = VectorAttributeDefinition.deserialize(
                        de
                    )

                case 2:
                    kwargs["search_schema"] = _deserialize_search_schema(
                        de, _SCHEMA_VECTOR_INDEX_INFO.members["SearchSchema"]
                    )

                case 3:
                    kwargs["projection"] = Projection.deserialize(de)

                case 4:
                    kwargs["dimensions"] = de.read_long(
                        _SCHEMA_VECTOR_INDEX_INFO.members["Dimensions"]
                    )

                case 5:
                    kwargs["distance_function"] = VectorDistanceFunction(
                        de.read_string(
                            _SCHEMA_VECTOR_INDEX_INFO.members["DistanceFunction"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_VECTOR_INDEX_INFO, consumer=_consumer)
        return kwargs


def _serialize_vector_indexes(
    serializer: ShapeSerializer, schema: Schema, value: list[VectorIndexInfo]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_vector_indexes(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[VectorIndexInfo]:
    result: list[VectorIndexInfo] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(VectorIndexInfo.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class SourceTableFeatureDetails:
    """
    Contains the details of the features enabled on the table when the
    backup was created. For example, LSIs, GSIs, streams, TTL.
    """

    local_secondary_indexes: list[LocalSecondaryIndexInfo] | None = None
    """
    Represents the LSI properties for the table when the backup was created.
    It includes the IndexName, KeySchema and Projection for the LSIs on the
    table at the time of backup.
    """

    global_secondary_indexes: list[GlobalSecondaryIndexInfo] | None = None
    """
    Represents the GSI properties for the table when the backup was created.
    It includes the IndexName, KeySchema, Projection, and
    ProvisionedThroughput for the GSIs on the table at the time of backup.
    """

    stream_description: StreamSpecification | None = None
    """Stream settings on the table when the backup was created."""

    time_to_live_description: TimeToLiveDescription | None = None
    """Time to Live settings on the table when the backup was created."""

    sse_description: SSEDescription | None = None
    """
    The description of the server-side encryption status on the table when
    the backup was created.
    """

    vector_indexes: list[VectorIndexInfo] | None = None
    """
    The vector index properties for the table at the time the backup was
    created, including the index name, vector attribute, dimensions,
    distance function, search schema, and projection.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SOURCE_TABLE_FEATURE_DETAILS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.local_secondary_indexes is not None:
            _serialize_local_secondary_indexes(
                serializer,
                _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS.members["LocalSecondaryIndexes"],
                self.local_secondary_indexes,
            )

        if self.global_secondary_indexes is not None:
            _serialize_global_secondary_indexes(
                serializer,
                _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS.members["GlobalSecondaryIndexes"],
                self.global_secondary_indexes,
            )

        if self.stream_description is not None:
            serializer.write_struct(
                _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS.members["StreamDescription"],
                self.stream_description,
            )

        if self.time_to_live_description is not None:
            serializer.write_struct(
                _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS.members["TimeToLiveDescription"],
                self.time_to_live_description,
            )

        if self.sse_description is not None:
            serializer.write_struct(
                _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS.members["SSEDescription"],
                self.sse_description,
            )

        if self.vector_indexes is not None:
            _serialize_vector_indexes(
                serializer,
                _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS.members["VectorIndexes"],
                self.vector_indexes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["local_secondary_indexes"] = (
                        _deserialize_local_secondary_indexes(
                            de,
                            _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS.members[
                                "LocalSecondaryIndexes"
                            ],
                        )
                    )

                case 1:
                    kwargs["global_secondary_indexes"] = (
                        _deserialize_global_secondary_indexes(
                            de,
                            _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS.members[
                                "GlobalSecondaryIndexes"
                            ],
                        )
                    )

                case 2:
                    kwargs["stream_description"] = StreamSpecification.deserialize(de)

                case 3:
                    kwargs["time_to_live_description"] = (
                        TimeToLiveDescription.deserialize(de)
                    )

                case 4:
                    kwargs["sse_description"] = SSEDescription.deserialize(de)

                case 5:
                    kwargs["vector_indexes"] = _deserialize_vector_indexes(
                        de,
                        _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS.members["VectorIndexes"],
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_SOURCE_TABLE_FEATURE_DETAILS, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class BackupDescription:
    """Contains the description of the backup created for the table."""

    backup_details: BackupDetails | None = None
    """Contains the details of the backup created for the table."""

    source_table_details: SourceTableDetails | None = None
    """Contains the details of the table when the backup was created."""

    source_table_feature_details: SourceTableFeatureDetails | None = None
    """
    Contains the details of the features enabled on the table when the
    backup was created. For example, LSIs, GSIs, streams, TTL.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BACKUP_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.backup_details is not None:
            serializer.write_struct(
                _SCHEMA_BACKUP_DESCRIPTION.members["BackupDetails"], self.backup_details
            )

        if self.source_table_details is not None:
            serializer.write_struct(
                _SCHEMA_BACKUP_DESCRIPTION.members["SourceTableDetails"],
                self.source_table_details,
            )

        if self.source_table_feature_details is not None:
            serializer.write_struct(
                _SCHEMA_BACKUP_DESCRIPTION.members["SourceTableFeatureDetails"],
                self.source_table_feature_details,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["backup_details"] = BackupDetails.deserialize(de)

                case 1:
                    kwargs["source_table_details"] = SourceTableDetails.deserialize(de)

                case 2:
                    kwargs["source_table_feature_details"] = (
                        SourceTableFeatureDetails.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BACKUP_DESCRIPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class BackupInUseException(ServiceError):
    """
    There is another ongoing conflicting backup control plane operation on
    the table. The backup is either being created, deleted or restored to a
    table.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BACKUP_IN_USE_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_BACKUP_IN_USE_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_BACKUP_IN_USE_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BACKUP_IN_USE_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class BackupNotFoundException(ServiceError):
    """Backup not found for the given BackupARN."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BACKUP_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_BACKUP_NOT_FOUND_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_BACKUP_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BACKUP_NOT_FOUND_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class BackupSummary:
    """Contains details for the backup."""

    table_name: str | None = None
    """Name of the table."""

    table_id: str | None = None
    """Unique identifier for the table."""

    table_arn: str | None = None
    """ARN associated with the table."""

    backup_arn: str | None = None
    """ARN associated with the backup."""

    backup_name: str | None = None
    """Name of the specified backup."""

    backup_creation_date_time: datetime | None = None
    """Time at which the backup was created."""

    backup_expiry_date_time: datetime | None = None
    """
    Time at which the automatic on-demand backup created by DynamoDB will
    expire. This `SYSTEM` on-demand backup expires automatically 35 days
    after its creation.
    """

    backup_status: BackupStatus | None = None
    """Backup can be in one of the following states: CREATING, ACTIVE, DELETED."""

    backup_type: BackupType | None = None
    """
    BackupType:

    - `USER` - You create and manage these using the on-demand backup
      feature.

    - `SYSTEM` - If you delete a table with point-in-time recovery enabled,
      a `SYSTEM` backup is automatically created and is retained for 35 days
      (at no additional cost). System backups allow you to restore the
      deleted table to the state it was in just before the point of
      deletion.

    - `AWS_BACKUP` - On-demand backup created by you from Backup service.
    """

    backup_size_bytes: int | None = None
    """Size of the backup in bytes."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BACKUP_SUMMARY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_BACKUP_SUMMARY.members["TableName"], self.table_name
            )

        if self.table_id is not None:
            serializer.write_string(
                _SCHEMA_BACKUP_SUMMARY.members["TableId"], self.table_id
            )

        if self.table_arn is not None:
            serializer.write_string(
                _SCHEMA_BACKUP_SUMMARY.members["TableArn"], self.table_arn
            )

        if self.backup_arn is not None:
            serializer.write_string(
                _SCHEMA_BACKUP_SUMMARY.members["BackupArn"], self.backup_arn
            )

        if self.backup_name is not None:
            serializer.write_string(
                _SCHEMA_BACKUP_SUMMARY.members["BackupName"], self.backup_name
            )

        if self.backup_creation_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_BACKUP_SUMMARY.members["BackupCreationDateTime"],
                self.backup_creation_date_time,
            )

        if self.backup_expiry_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_BACKUP_SUMMARY.members["BackupExpiryDateTime"],
                self.backup_expiry_date_time,
            )

        if self.backup_status is not None:
            serializer.write_string(
                _SCHEMA_BACKUP_SUMMARY.members["BackupStatus"], self.backup_status
            )

        if self.backup_type is not None:
            serializer.write_string(
                _SCHEMA_BACKUP_SUMMARY.members["BackupType"], self.backup_type
            )

        if self.backup_size_bytes is not None:
            serializer.write_long(
                _SCHEMA_BACKUP_SUMMARY.members["BackupSizeBytes"],
                self.backup_size_bytes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_BACKUP_SUMMARY.members["TableName"]
                    )

                case 1:
                    kwargs["table_id"] = de.read_string(
                        _SCHEMA_BACKUP_SUMMARY.members["TableId"]
                    )

                case 2:
                    kwargs["table_arn"] = de.read_string(
                        _SCHEMA_BACKUP_SUMMARY.members["TableArn"]
                    )

                case 3:
                    kwargs["backup_arn"] = de.read_string(
                        _SCHEMA_BACKUP_SUMMARY.members["BackupArn"]
                    )

                case 4:
                    kwargs["backup_name"] = de.read_string(
                        _SCHEMA_BACKUP_SUMMARY.members["BackupName"]
                    )

                case 5:
                    kwargs["backup_creation_date_time"] = de.read_timestamp(
                        _SCHEMA_BACKUP_SUMMARY.members["BackupCreationDateTime"]
                    )

                case 6:
                    kwargs["backup_expiry_date_time"] = de.read_timestamp(
                        _SCHEMA_BACKUP_SUMMARY.members["BackupExpiryDateTime"]
                    )

                case 7:
                    kwargs["backup_status"] = BackupStatus(
                        de.read_string(_SCHEMA_BACKUP_SUMMARY.members["BackupStatus"])
                    )

                case 8:
                    kwargs["backup_type"] = BackupType(
                        de.read_string(_SCHEMA_BACKUP_SUMMARY.members["BackupType"])
                    )

                case 9:
                    kwargs["backup_size_bytes"] = de.read_long(
                        _SCHEMA_BACKUP_SUMMARY.members["BackupSizeBytes"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BACKUP_SUMMARY, consumer=_consumer)
        return kwargs


def _serialize_backup_summaries(
    serializer: ShapeSerializer, schema: Schema, value: list[BackupSummary]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_backup_summaries(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[BackupSummary]:
    result: list[BackupSummary] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(BackupSummary.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


class BackupTypeFilter(UnknownEnumMixin, StrEnum):
    USER = "USER"
    SYSTEM = "SYSTEM"
    AWS_BACKUP = "AWS_BACKUP"
    ALL = "ALL"


class ReturnConsumedCapacity(UnknownEnumMixin, StrEnum):
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    INDEXES = "INDEXES"
    TOTAL = "TOTAL"
    NONE = "NONE"


class ReturnValuesOnConditionCheckFailure(UnknownEnumMixin, StrEnum):
    ALL_OLD = "ALL_OLD"
    NONE = "NONE"


@dataclass(kw_only=True)
class Capacity:
    """
    Represents the amount of provisioned throughput capacity consumed on a
    table or an index.
    """

    read_capacity_units: float | None = None
    """The total number of read capacity units consumed on a table or an index."""

    write_capacity_units: float | None = None
    """
    The total number of write capacity units consumed on a table or an
    index.
    """

    capacity_units: float | None = None
    """The total number of capacity units consumed on a table or an index."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CAPACITY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.read_capacity_units is not None:
            serializer.write_double(
                _SCHEMA_CAPACITY.members["ReadCapacityUnits"], self.read_capacity_units
            )

        if self.write_capacity_units is not None:
            serializer.write_double(
                _SCHEMA_CAPACITY.members["WriteCapacityUnits"],
                self.write_capacity_units,
            )

        if self.capacity_units is not None:
            serializer.write_double(
                _SCHEMA_CAPACITY.members["CapacityUnits"], self.capacity_units
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["read_capacity_units"] = de.read_double(
                        _SCHEMA_CAPACITY.members["ReadCapacityUnits"]
                    )

                case 1:
                    kwargs["write_capacity_units"] = de.read_double(
                        _SCHEMA_CAPACITY.members["WriteCapacityUnits"]
                    )

                case 2:
                    kwargs["capacity_units"] = de.read_double(
                        _SCHEMA_CAPACITY.members["CapacityUnits"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CAPACITY, consumer=_consumer)
        return kwargs


def _serialize_secondary_indexes_capacity_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, Capacity]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_secondary_indexes_capacity_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, Capacity]:
    result: dict[str, Capacity] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = Capacity.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class VectorCapacity:
    """
    The consumed capacity for vector index operations, including vector
    search request bytes and vector write request bytes.
    """

    vector_search_request_bytes: float | None = None
    """
    The number of vector search request bytes consumed by a `SearchVectors`
    operation.
    """

    vector_write_request_bytes: float | None = None
    """
    The number of vector write request bytes consumed when writing to a
    vector index. Reported for write operations that modify attributes
    indexed by a vector index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_VECTOR_CAPACITY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.vector_search_request_bytes is not None:
            serializer.write_double(
                _SCHEMA_VECTOR_CAPACITY.members["VectorSearchRequestBytes"],
                self.vector_search_request_bytes,
            )

        if self.vector_write_request_bytes is not None:
            serializer.write_double(
                _SCHEMA_VECTOR_CAPACITY.members["VectorWriteRequestBytes"],
                self.vector_write_request_bytes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["vector_search_request_bytes"] = de.read_double(
                        _SCHEMA_VECTOR_CAPACITY.members["VectorSearchRequestBytes"]
                    )

                case 1:
                    kwargs["vector_write_request_bytes"] = de.read_double(
                        _SCHEMA_VECTOR_CAPACITY.members["VectorWriteRequestBytes"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_VECTOR_CAPACITY, consumer=_consumer)
        return kwargs


def _serialize_vector_indexes_capacity_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, VectorCapacity]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_vector_indexes_capacity_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, VectorCapacity]:
    result: dict[str, VectorCapacity] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = VectorCapacity.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ConsumedCapacity:
    """
    The capacity units consumed by an operation. The data returned includes
    the total provisioned throughput consumed, along with statistics for the
    table and any indexes involved in the operation. `ConsumedCapacity` is
    only returned if the request asked for it. For more information, see
    [Provisioned capacity
    mode](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/provisioned-capacity-mode.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    table_name: str | None = None
    """
    The name of the table that was affected by the operation. If you had
    specified the Amazon Resource Name (ARN) of a table in the input,
    you'll see the table ARN in the response.
    """

    capacity_units: float | None = None
    """The total number of capacity units consumed by the operation."""

    read_capacity_units: float | None = None
    """The total number of read capacity units consumed by the operation."""

    write_capacity_units: float | None = None
    """The total number of write capacity units consumed by the operation."""

    table: Capacity | None = None
    """
    The amount of throughput consumed on the table affected by the
    operation.
    """

    local_secondary_indexes: dict[str, Capacity] | None = None
    """
    The amount of throughput consumed on each local index affected by the
    operation.
    """

    global_secondary_indexes: dict[str, Capacity] | None = None
    """
    The amount of throughput consumed on each global index affected by the
    operation.
    """

    vector_indexes: dict[str, VectorCapacity] | None = None
    """
    The amount of throughput consumed on each vector index affected by the
    operation. Each entry contains `VectorWriteRequestBytes` (for write
    operations) or `VectorSearchRequestBytes` (for search operations).
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CONSUMED_CAPACITY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_CONSUMED_CAPACITY.members["TableName"], self.table_name
            )

        if self.capacity_units is not None:
            serializer.write_double(
                _SCHEMA_CONSUMED_CAPACITY.members["CapacityUnits"], self.capacity_units
            )

        if self.read_capacity_units is not None:
            serializer.write_double(
                _SCHEMA_CONSUMED_CAPACITY.members["ReadCapacityUnits"],
                self.read_capacity_units,
            )

        if self.write_capacity_units is not None:
            serializer.write_double(
                _SCHEMA_CONSUMED_CAPACITY.members["WriteCapacityUnits"],
                self.write_capacity_units,
            )

        if self.table is not None:
            serializer.write_struct(
                _SCHEMA_CONSUMED_CAPACITY.members["Table"], self.table
            )

        if self.local_secondary_indexes is not None:
            _serialize_secondary_indexes_capacity_map(
                serializer,
                _SCHEMA_CONSUMED_CAPACITY.members["LocalSecondaryIndexes"],
                self.local_secondary_indexes,
            )

        if self.global_secondary_indexes is not None:
            _serialize_secondary_indexes_capacity_map(
                serializer,
                _SCHEMA_CONSUMED_CAPACITY.members["GlobalSecondaryIndexes"],
                self.global_secondary_indexes,
            )

        if self.vector_indexes is not None:
            _serialize_vector_indexes_capacity_map(
                serializer,
                _SCHEMA_CONSUMED_CAPACITY.members["VectorIndexes"],
                self.vector_indexes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_CONSUMED_CAPACITY.members["TableName"]
                    )

                case 1:
                    kwargs["capacity_units"] = de.read_double(
                        _SCHEMA_CONSUMED_CAPACITY.members["CapacityUnits"]
                    )

                case 2:
                    kwargs["read_capacity_units"] = de.read_double(
                        _SCHEMA_CONSUMED_CAPACITY.members["ReadCapacityUnits"]
                    )

                case 3:
                    kwargs["write_capacity_units"] = de.read_double(
                        _SCHEMA_CONSUMED_CAPACITY.members["WriteCapacityUnits"]
                    )

                case 4:
                    kwargs["table"] = Capacity.deserialize(de)

                case 5:
                    kwargs["local_secondary_indexes"] = (
                        _deserialize_secondary_indexes_capacity_map(
                            de,
                            _SCHEMA_CONSUMED_CAPACITY.members["LocalSecondaryIndexes"],
                        )
                    )

                case 6:
                    kwargs["global_secondary_indexes"] = (
                        _deserialize_secondary_indexes_capacity_map(
                            de,
                            _SCHEMA_CONSUMED_CAPACITY.members["GlobalSecondaryIndexes"],
                        )
                    )

                case 7:
                    kwargs["vector_indexes"] = _deserialize_vector_indexes_capacity_map(
                        de, _SCHEMA_CONSUMED_CAPACITY.members["VectorIndexes"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CONSUMED_CAPACITY, consumer=_consumer)
        return kwargs


def _serialize_consumed_capacity_multiple(
    serializer: ShapeSerializer, schema: Schema, value: list[ConsumedCapacity]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_consumed_capacity_multiple(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ConsumedCapacity]:
    result: list[ConsumedCapacity] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ConsumedCapacity.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


class BatchStatementErrorCodeEnum(UnknownEnumMixin, StrEnum):
    CONDITIONAL_CHECK_FAILED = "ConditionalCheckFailed"
    ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED = "ItemCollectionSizeLimitExceeded"
    REQUEST_LIMIT_EXCEEDED = "RequestLimitExceeded"
    VALIDATION_ERROR = "ValidationError"
    PROVISIONED_THROUGHPUT_EXCEEDED = "ProvisionedThroughputExceeded"
    TRANSACTION_CONFLICT = "TransactionConflict"
    THROTTLING_ERROR = "ThrottlingError"
    INTERNAL_SERVER_ERROR = "InternalServerError"
    RESOURCE_NOT_FOUND = "ResourceNotFound"
    ACCESS_DENIED = "AccessDenied"
    DUPLICATE_ITEM = "DuplicateItem"


@dataclass(kw_only=True)
class InternalServerError(ServiceError):
    """An error occurred on the server side."""

    fault: Literal["client", "server"] | None = "server"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INTERNAL_SERVER_ERROR, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INTERNAL_SERVER_ERROR.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INTERNAL_SERVER_ERROR.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INTERNAL_SERVER_ERROR, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ThrottlingReason:
    """
    Represents the specific reason why a DynamoDB request was throttled and
    the ARN of the impacted resource. This helps identify exactly what
    resource is being throttled, what type of operation caused it, and why
    the throttling occurred.
    """

    reason: str | None = None
    """
    The reason for throttling. The throttling reason follows a specific
    format: `ResourceType+OperationType+LimitType`:

    - Resource Type (What is being throttled): Table or Index

    - Operation Type (What kind of operation): Read or Write

    - Limit Type (Why the throttling occurred):

      - `ProvisionedThroughputExceeded`: The request rate is exceeding the
        [provisioned throughput
        capacity](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/provisioned-capacity-mode.html)
        (read or write capacity units) configured for a table or a global
        secondary index (GSI) in provisioned capacity mode.

      - `AccountLimitExceeded`: The request rate has caused a table or
        global secondary index (GSI) in on-demand mode to exceed the
        [per-table account-level service
        quotas](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ServiceQuotas.html#default-limits-throughput)
        for read/write throughput in the current Amazon Web Services Region.

      - `KeyRangeThroughputExceeded`: The request rate directed at a
        specific partition key value has exceeded the [internal
        partition-level throughput
        limits](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/bp-partition-key-design.html),
        indicating uneven access patterns across the table's or GSI's key
        space.

      - `MaxOnDemandThroughputExceeded`: The request rate has exceeded the
        [configured maximum throughput
        limits](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/on-demand-capacity-mode-max-throughput.html)
        set for a table or index in on-demand capacity mode.

    Examples of complete throttling reasons:

    - TableReadProvisionedThroughputExceeded

    - IndexWriteAccountLimitExceeded

    This helps identify exactly what resource is being throttled, what type
    of operation caused it, and why the throttling occurred.
    """

    resource: str | None = None
    """
    The Amazon Resource Name (ARN) of the DynamoDB table or index that
    experienced the throttling event.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_THROTTLING_REASON, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.reason is not None:
            serializer.write_string(
                _SCHEMA_THROTTLING_REASON.members["reason"], self.reason
            )

        if self.resource is not None:
            serializer.write_string(
                _SCHEMA_THROTTLING_REASON.members["resource"], self.resource
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["reason"] = de.read_string(
                        _SCHEMA_THROTTLING_REASON.members["reason"]
                    )

                case 1:
                    kwargs["resource"] = de.read_string(
                        _SCHEMA_THROTTLING_REASON.members["resource"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_THROTTLING_REASON, consumer=_consumer)
        return kwargs


def _serialize_throttling_reason_list(
    serializer: ShapeSerializer, schema: Schema, value: list[ThrottlingReason]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_throttling_reason_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ThrottlingReason]:
    result: list[ThrottlingReason] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ThrottlingReason.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class RequestLimitExceeded(ServiceError):
    """
    Throughput exceeds the current throughput quota for your account. For
    detailed information about why the request was throttled and the ARN of
    the impacted resource, find the
    [ThrottlingReason](https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_ThrottlingReason.html)
    field in the returned exception. Contact [Amazon Web Services
    Support](https://aws.amazon.com/support) to request a quota increase.
    """

    fault: Literal["client", "server"] | None = "client"

    throttling_reasons: list[ThrottlingReason] | None = None
    """
    A list of
    [ThrottlingReason](https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_ThrottlingReason.html)
    that provide detailed diagnostic information about why the request was
    throttled.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REQUEST_LIMIT_EXCEEDED, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_REQUEST_LIMIT_EXCEEDED.members["message"], self.message
            )

        if self.throttling_reasons is not None:
            _serialize_throttling_reason_list(
                serializer,
                _SCHEMA_REQUEST_LIMIT_EXCEEDED.members["ThrottlingReasons"],
                self.throttling_reasons,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_REQUEST_LIMIT_EXCEEDED.members["message"]
                    )

                case 1:
                    kwargs["throttling_reasons"] = _deserialize_throttling_reason_list(
                        de, _SCHEMA_REQUEST_LIMIT_EXCEEDED.members["ThrottlingReasons"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_REQUEST_LIMIT_EXCEEDED, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ThrottlingException(ServiceError):
    """
    The request was denied due to request throttling. For detailed
    information about why the request was throttled and the ARN of the
    impacted resource, find the
    [ThrottlingReason](https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_ThrottlingReason.html)
    field in the returned exception.
    """

    fault: Literal["client", "server"] | None = "client"

    throttling_reasons: list[ThrottlingReason] | None = None
    """
    A list of
    [ThrottlingReason](https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_ThrottlingReason.html)
    that provide detailed diagnostic information about why the request was
    throttled.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_THROTTLING_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_THROTTLING_EXCEPTION.members["message"], self.message
            )

        if self.throttling_reasons is not None:
            _serialize_throttling_reason_list(
                serializer,
                _SCHEMA_THROTTLING_EXCEPTION.members["throttlingReasons"],
                self.throttling_reasons,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_THROTTLING_EXCEPTION.members["message"]
                    )

                case 1:
                    kwargs["throttling_reasons"] = _deserialize_throttling_reason_list(
                        de, _SCHEMA_THROTTLING_EXCEPTION.members["throttlingReasons"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_THROTTLING_EXCEPTION, consumer=_consumer)
        return kwargs


def _serialize_expression_attribute_name_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, str]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_string(value_schema, v))


def _deserialize_expression_attribute_name_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, str]:
    result: dict[str, str] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = d.read_string(value_schema)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class InvalidEndpointException(ServiceError):
    """Dataclass for InvalidEndpointException structure."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_ENDPOINT_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_ENDPOINT_EXCEPTION.members["Message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_ENDPOINT_EXCEPTION.members["Message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INVALID_ENDPOINT_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ProvisionedThroughputExceededException(ServiceError):
    """
    The request was denied due to request throttling. For detailed
    information about why the request was throttled and the ARN of the
    impacted resource, find the
    [ThrottlingReason](https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_ThrottlingReason.html)
    field in the returned exception. The Amazon Web Services SDKs for
    DynamoDB automatically retry requests that receive this exception. Your
    request is eventually successful, unless your retry queue is too large
    to finish. Reduce the frequency of requests and use exponential backoff.
    For more information, go to [Error Retries and Exponential
    Backoff](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Programming.Errors.html#Programming.Errors.RetryAndBackoff)
    in the *Amazon DynamoDB Developer Guide*.
    """

    fault: Literal["client", "server"] | None = "client"

    throttling_reasons: list[ThrottlingReason] | None = None
    """
    A list of
    [ThrottlingReason](https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_ThrottlingReason.html)
    that provide detailed diagnostic information about why the request was
    throttled.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION.members["message"],
                self.message,
            )

        if self.throttling_reasons is not None:
            _serialize_throttling_reason_list(
                serializer,
                _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION.members[
                    "ThrottlingReasons"
                ],
                self.throttling_reasons,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION.members[
                            "message"
                        ]
                    )

                case 1:
                    kwargs["throttling_reasons"] = _deserialize_throttling_reason_list(
                        de,
                        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION.members[
                            "ThrottlingReasons"
                        ],
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ResourceNotFoundException(ServiceError):
    """
    The operation tried to access a nonexistent table or index. The resource
    might not be specified correctly, or its status might not be `ACTIVE`.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION, consumer=_consumer
        )
        return kwargs


class ReturnItemCollectionMetrics(UnknownEnumMixin, StrEnum):
    SIZE = "SIZE"
    NONE = "NONE"


def _serialize_item_collection_size_estimate_range(
    serializer: ShapeSerializer, schema: Schema, value: list[float]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_double(member_schema, e)


def _deserialize_item_collection_size_estimate_range(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[float]:
    result: list[float] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_double(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ItemCollectionSizeLimitExceededException(ServiceError):
    """
    An item collection is too large. This exception is only returned for
    tables that have one or more local secondary indexes.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION.members[
                    "message"
                ],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION.members[
                            "message"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ReplicatedWriteConflictException(ServiceError):
    """
    The request was rejected because one or more items in the request are
    being modified by a request in another Region.
    """

    fault: Literal["client", "server"] | None = "client"
    is_retry_safe: bool | None = True

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICATED_WRITE_CONFLICT_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_REPLICATED_WRITE_CONFLICT_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_REPLICATED_WRITE_CONFLICT_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICATED_WRITE_CONFLICT_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class BillingModeSummary:
    """
    Contains the details for the read/write capacity mode. This page talks
    about `PROVISIONED` and `PAY_PER_REQUEST` billing modes. For more
    information about these modes, see [Read/write capacity
    mode](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.ReadWriteCapacityMode.html).

    Note:
        You may need to switch to on-demand mode at least once in order to
        return a `BillingModeSummary` response.
    """

    billing_mode: BillingMode | None = None
    """
    Controls how you are charged for read and write throughput and how you
    manage capacity. This setting can be changed later.

    - `PROVISIONED` - Sets the read/write capacity mode to `PROVISIONED`. We
      recommend using `PROVISIONED` for predictable workloads.

    - `PAY_PER_REQUEST` - Sets the read/write capacity mode to
      `PAY_PER_REQUEST`. We recommend using `PAY_PER_REQUEST` for
      unpredictable workloads.
    """

    last_update_to_pay_per_request_date_time: datetime | None = None
    """
    Represents the time when `PAY_PER_REQUEST` was last set as the
    read/write capacity mode.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BILLING_MODE_SUMMARY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.billing_mode is not None:
            serializer.write_string(
                _SCHEMA_BILLING_MODE_SUMMARY.members["BillingMode"], self.billing_mode
            )

        if self.last_update_to_pay_per_request_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_BILLING_MODE_SUMMARY.members[
                    "LastUpdateToPayPerRequestDateTime"
                ],
                self.last_update_to_pay_per_request_date_time,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["billing_mode"] = BillingMode(
                        de.read_string(
                            _SCHEMA_BILLING_MODE_SUMMARY.members["BillingMode"]
                        )
                    )

                case 1:
                    kwargs["last_update_to_pay_per_request_date_time"] = (
                        de.read_timestamp(
                            _SCHEMA_BILLING_MODE_SUMMARY.members[
                                "LastUpdateToPayPerRequestDateTime"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BILLING_MODE_SUMMARY, consumer=_consumer)
        return kwargs


class ComparisonOperator(UnknownEnumMixin, StrEnum):
    EQ = "EQ"
    NE = "NE"
    IN_ = "IN"
    LE = "LE"
    LT = "LT"
    GE = "GE"
    GT = "GT"
    BETWEEN = "BETWEEN"
    NOT_NULL = "NOT_NULL"
    NULL = "NULL"
    CONTAINS = "CONTAINS"
    NOT_CONTAINS = "NOT_CONTAINS"
    BEGINS_WITH = "BEGINS_WITH"


class ConditionalOperator(UnknownEnumMixin, StrEnum):
    AND_ = "AND"
    OR_ = "OR"


class ContinuousBackupsStatus(UnknownEnumMixin, StrEnum):
    ENABLED = "ENABLED"
    DISABLED = "DISABLED"


class PointInTimeRecoveryStatus(UnknownEnumMixin, StrEnum):
    ENABLED = "ENABLED"
    DISABLED = "DISABLED"


@dataclass(kw_only=True)
class PointInTimeRecoveryDescription:
    """The description of the point in time settings applied to the table."""

    point_in_time_recovery_status: PointInTimeRecoveryStatus | None = None
    """
    The current state of point in time recovery:

    - `ENABLED` - Point in time recovery is enabled.

    - `DISABLED` - Point in time recovery is disabled.
    """

    recovery_period_in_days: int | None = None
    """
    The number of preceding days for which continuous backups are taken and
    maintained. Your table data is only recoverable to any point-in-time
    from within the configured recovery period. This parameter is optional.
    """

    earliest_restorable_date_time: datetime | None = None
    """
    Specifies the earliest point in time you can restore your table to. You
    can restore your table to any point in time during the last 35 days.
    """

    latest_restorable_date_time: datetime | None = None
    """
    `LatestRestorableDateTime` is typically 5 minutes before the current
    time.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.point_in_time_recovery_status is not None:
            serializer.write_string(
                _SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION.members[
                    "PointInTimeRecoveryStatus"
                ],
                self.point_in_time_recovery_status,
            )

        if self.recovery_period_in_days is not None:
            serializer.write_integer(
                _SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION.members[
                    "RecoveryPeriodInDays"
                ],
                self.recovery_period_in_days,
            )

        if self.earliest_restorable_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION.members[
                    "EarliestRestorableDateTime"
                ],
                self.earliest_restorable_date_time,
            )

        if self.latest_restorable_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION.members[
                    "LatestRestorableDateTime"
                ],
                self.latest_restorable_date_time,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["point_in_time_recovery_status"] = PointInTimeRecoveryStatus(
                        de.read_string(
                            _SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION.members[
                                "PointInTimeRecoveryStatus"
                            ]
                        )
                    )

                case 1:
                    kwargs["recovery_period_in_days"] = de.read_integer(
                        _SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION.members[
                            "RecoveryPeriodInDays"
                        ]
                    )

                case 2:
                    kwargs["earliest_restorable_date_time"] = de.read_timestamp(
                        _SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION.members[
                            "EarliestRestorableDateTime"
                        ]
                    )

                case 3:
                    kwargs["latest_restorable_date_time"] = de.read_timestamp(
                        _SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION.members[
                            "LatestRestorableDateTime"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_POINT_IN_TIME_RECOVERY_DESCRIPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ContinuousBackupsDescription:
    """
    Represents the continuous backups and point in time recovery settings on
    the table.
    """

    continuous_backups_status: ContinuousBackupsStatus
    """
    `ContinuousBackupsStatus` can be one of the following states: ENABLED,
    DISABLED
    """

    point_in_time_recovery_description: PointInTimeRecoveryDescription | None = None
    """
    The description of the point in time recovery settings applied to the
    table.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CONTINUOUS_BACKUPS_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_CONTINUOUS_BACKUPS_DESCRIPTION.members["ContinuousBackupsStatus"],
            self.continuous_backups_status,
        )
        if self.point_in_time_recovery_description is not None:
            serializer.write_struct(
                _SCHEMA_CONTINUOUS_BACKUPS_DESCRIPTION.members[
                    "PointInTimeRecoveryDescription"
                ],
                self.point_in_time_recovery_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["continuous_backups_status"] = ContinuousBackupsStatus(
                        de.read_string(
                            _SCHEMA_CONTINUOUS_BACKUPS_DESCRIPTION.members[
                                "ContinuousBackupsStatus"
                            ]
                        )
                    )

                case 1:
                    kwargs["point_in_time_recovery_description"] = (
                        PointInTimeRecoveryDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CONTINUOUS_BACKUPS_DESCRIPTION, consumer=_consumer
        )
        if "continuous_backups_status" not in kwargs:
            kwargs["continuous_backups_status"] = ContinuousBackupsStatus._corrected("")
        return kwargs


@dataclass(kw_only=True)
class ContinuousBackupsUnavailableException(ServiceError):
    """Backups have not yet been enabled for this table."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CONTINUOUS_BACKUPS_UNAVAILABLE_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_CONTINUOUS_BACKUPS_UNAVAILABLE_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_CONTINUOUS_BACKUPS_UNAVAILABLE_EXCEPTION.members[
                            "message"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CONTINUOUS_BACKUPS_UNAVAILABLE_EXCEPTION, consumer=_consumer
        )
        return kwargs


class ContributorInsightsAction(UnknownEnumMixin, StrEnum):
    ENABLE = "ENABLE"
    DISABLE = "DISABLE"


class ContributorInsightsMode(UnknownEnumMixin, StrEnum):
    ACCESSED_AND_THROTTLED_KEYS = "ACCESSED_AND_THROTTLED_KEYS"
    THROTTLED_KEYS = "THROTTLED_KEYS"


def _serialize_contributor_insights_rule_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_contributor_insights_rule_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


class ContributorInsightsStatus(UnknownEnumMixin, StrEnum):
    ENABLING = "ENABLING"
    ENABLED = "ENABLED"
    DISABLING = "DISABLING"
    DISABLED = "DISABLED"
    FAILED = "FAILED"


@dataclass(kw_only=True)
class ContributorInsightsSummary:
    """Represents a Contributor Insights summary entry."""

    table_name: str | None = None
    """Name of the table associated with the summary."""

    index_name: str | None = None
    """Name of the index associated with the summary, if any."""

    contributor_insights_status: ContributorInsightsStatus | None = None
    """
    Describes the current status for contributor insights for the given
    table and index, if applicable.
    """

    contributor_insights_mode: ContributorInsightsMode | None = None
    """
    Indicates the current mode of CloudWatch Contributor Insights,
    specifying whether it tracks all access and throttled events or
    throttled events only for the DynamoDB table or index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY.members["TableName"],
                self.table_name,
            )

        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY.members["IndexName"],
                self.index_name,
            )

        if self.contributor_insights_status is not None:
            serializer.write_string(
                _SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY.members[
                    "ContributorInsightsStatus"
                ],
                self.contributor_insights_status,
            )

        if self.contributor_insights_mode is not None:
            serializer.write_string(
                _SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY.members["ContributorInsightsMode"],
                self.contributor_insights_mode,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY.members["TableName"]
                    )

                case 1:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY.members["IndexName"]
                    )

                case 2:
                    kwargs["contributor_insights_status"] = ContributorInsightsStatus(
                        de.read_string(
                            _SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY.members[
                                "ContributorInsightsStatus"
                            ]
                        )
                    )

                case 3:
                    kwargs["contributor_insights_mode"] = ContributorInsightsMode(
                        de.read_string(
                            _SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY.members[
                                "ContributorInsightsMode"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CONTRIBUTOR_INSIGHTS_SUMMARY, consumer=_consumer
        )
        return kwargs


def _serialize_contributor_insights_summaries(
    serializer: ShapeSerializer, schema: Schema, value: list[ContributorInsightsSummary]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_contributor_insights_summaries(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ContributorInsightsSummary]:
    result: list[ContributorInsightsSummary] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ContributorInsightsSummary.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class CreateBackupInput:
    """Dataclass for CreateBackupInput structure."""

    table_name: str | None = None
    """
    The name of the table. You can also provide the Amazon Resource Name
    (ARN) of the table in this parameter.
    """

    backup_name: str | None = None
    """Specified name for the backup."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_BACKUP_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_CREATE_BACKUP_INPUT.members["TableName"], self.table_name
            )

        if self.backup_name is not None:
            serializer.write_string(
                _SCHEMA_CREATE_BACKUP_INPUT.members["BackupName"], self.backup_name
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_CREATE_BACKUP_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["backup_name"] = de.read_string(
                        _SCHEMA_CREATE_BACKUP_INPUT.members["BackupName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREATE_BACKUP_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class CreateBackupOutput:
    """Dataclass for CreateBackupOutput structure."""

    backup_details: BackupDetails | None = None
    """Contains the details of the backup created for the table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_BACKUP_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.backup_details is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_BACKUP_OUTPUT.members["BackupDetails"],
                self.backup_details,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["backup_details"] = BackupDetails.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREATE_BACKUP_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class LimitExceededException(ServiceError):
    """
    There is no limit to the number of daily on-demand backups that can be
    taken.

    For most purposes, up to 500 simultaneous table operations are allowed
    per account. These operations include `CreateTable`, `UpdateTable`,
    `DeleteTable`,`UpdateTimeToLive`, `RestoreTableFromBackup`, and
    `RestoreTableToPointInTime`.

    When you are creating a table with one or more secondary indexes, you
    can have up to 250 such requests running at a time. However, if the
    table or index specifications are complex, then DynamoDB might
    temporarily reduce the number of concurrent operations.

    When importing into DynamoDB, up to 50 simultaneous import table
    operations are allowed per account.

    There is a soft account quota of 2,500 tables.

    GetRecords was called with a value of more than 1000 for the limit
    request parameter.

    More than 2 processes are reading from the same streams shard at the
    same time. Exceeding this limit may result in request throttling.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIMIT_EXCEEDED_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_LIMIT_EXCEEDED_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIMIT_EXCEEDED_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class TableInUseException(ServiceError):
    """
    A target table with the specified name is either being created or
    deleted.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TABLE_IN_USE_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_TABLE_IN_USE_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_TABLE_IN_USE_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TABLE_IN_USE_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class TableNotFoundException(ServiceError):
    """
    A source table with the name `TableName` does not currently exist within
    the subscriber's account or the subscriber is operating in the wrong
    Amazon Web Services Region.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TABLE_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_TABLE_NOT_FOUND_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_TABLE_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TABLE_NOT_FOUND_EXCEPTION, consumer=_consumer)
        return kwargs


CREATE_BACKUP = APIOperation(
    input=CreateBackupInput,
    output=CreateBackupOutput,
    schema=_SCHEMA_CREATE_BACKUP,
    input_schema=_SCHEMA_CREATE_BACKUP_INPUT,
    output_schema=_SCHEMA_CREATE_BACKUP_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#BackupInUseException"
            ): BackupInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ContinuousBackupsUnavailableException"
            ): ContinuousBackupsUnavailableException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID("com.amazonaws.dynamodb#TableInUseException"): TableInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#TableNotFoundException"
            ): TableNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_BACKUP_IN_USE_EXCEPTION,
        _SCHEMA_CONTINUOUS_BACKUPS_UNAVAILABLE_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_TABLE_IN_USE_EXCEPTION,
        _SCHEMA_TABLE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class WarmThroughput:
    """
    Provides visibility into the number of read and write operations your
    table or secondary index can instantaneously support. The settings can
    be modified using the `UpdateTable` operation to meet the throughput
    requirements of an upcoming peak event.
    """

    read_units_per_second: int | None = None
    """
    Represents the number of read operations your base table can
    instantaneously support.
    """

    write_units_per_second: int | None = None
    """
    Represents the number of write operations your base table can
    instantaneously support.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_WARM_THROUGHPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.read_units_per_second is not None:
            serializer.write_long(
                _SCHEMA_WARM_THROUGHPUT.members["ReadUnitsPerSecond"],
                self.read_units_per_second,
            )

        if self.write_units_per_second is not None:
            serializer.write_long(
                _SCHEMA_WARM_THROUGHPUT.members["WriteUnitsPerSecond"],
                self.write_units_per_second,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["read_units_per_second"] = de.read_long(
                        _SCHEMA_WARM_THROUGHPUT.members["ReadUnitsPerSecond"]
                    )

                case 1:
                    kwargs["write_units_per_second"] = de.read_long(
                        _SCHEMA_WARM_THROUGHPUT.members["WriteUnitsPerSecond"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_WARM_THROUGHPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class CreateGlobalSecondaryIndexAction:
    """
    Represents a new global secondary index to be added to an existing
    table.
    """

    index_name: str
    """The name of the global secondary index to be created."""

    key_schema: list[KeySchemaElement]
    """
    The key schema for the global secondary index. Global secondary index
    supports up to 4 partition and up to 4 sort keys.
    """

    projection: Projection
    """
    Represents attributes that are copied (projected) from the table into an
    index. These are in addition to the primary key attributes and index key
    attributes, which are automatically projected.
    """

    provisioned_throughput: ProvisionedThroughput | None = None
    """
    Represents the provisioned throughput settings for the specified global
    secondary index.

    For current minimum and maximum provisioned throughput values, see
    [Service, Account, and Table
    Quotas](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Limits.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    on_demand_throughput: OnDemandThroughput | None = None
    """
    The maximum number of read and write units for the global secondary
    index being created. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both. You must use
    either `OnDemand Throughput` or `ProvisionedThroughput` based on your
    table's capacity mode.
    """

    warm_throughput: WarmThroughput | None = None
    """
    Represents the warm throughput value (in read units per second and write
    units per second) when creating a secondary index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION.members["IndexName"],
            self.index_name,
        )
        _serialize_key_schema(
            serializer,
            _SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION.members["KeySchema"],
            self.key_schema,
        )
        serializer.write_struct(
            _SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION.members["Projection"],
            self.projection,
        )
        if self.provisioned_throughput is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION.members[
                    "ProvisionedThroughput"
                ],
                self.provisioned_throughput,
            )

        if self.on_demand_throughput is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION.members[
                    "OnDemandThroughput"
                ],
                self.on_demand_throughput,
            )

        if self.warm_throughput is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION.members["WarmThroughput"],
                self.warm_throughput,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION.members[
                            "IndexName"
                        ]
                    )

                case 1:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de,
                        _SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION.members[
                            "KeySchema"
                        ],
                    )

                case 2:
                    kwargs["projection"] = Projection.deserialize(de)

                case 3:
                    kwargs["provisioned_throughput"] = (
                        ProvisionedThroughput.deserialize(de)
                    )

                case 4:
                    kwargs["on_demand_throughput"] = OnDemandThroughput.deserialize(de)

                case 5:
                    kwargs["warm_throughput"] = WarmThroughput.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CREATE_GLOBAL_SECONDARY_INDEX_ACTION, consumer=_consumer
        )
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        if "key_schema" not in kwargs:
            kwargs["key_schema"] = []
        if "projection" not in kwargs:
            kwargs["projection"] = Projection._smithy_default()
        return kwargs


@dataclass(kw_only=True)
class Replica:
    """Represents the properties of a replica."""

    region_name: str | None = None
    """The Region where the replica needs to be created."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICA, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.region_name is not None:
            serializer.write_string(
                _SCHEMA_REPLICA.members["RegionName"], self.region_name
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_REPLICA.members["RegionName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_REPLICA, consumer=_consumer)
        return kwargs


def _serialize_replica_list(
    serializer: ShapeSerializer, schema: Schema, value: list[Replica]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[Replica]:
    result: list[Replica] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(Replica.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class CreateGlobalTableInput:
    """Dataclass for CreateGlobalTableInput structure."""

    global_table_name: str | None = None
    """The global table name."""

    replication_group: list[Replica] | None = None
    """The Regions where the global table needs to be created."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_GLOBAL_TABLE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_name is not None:
            serializer.write_string(
                _SCHEMA_CREATE_GLOBAL_TABLE_INPUT.members["GlobalTableName"],
                self.global_table_name,
            )

        if self.replication_group is not None:
            _serialize_replica_list(
                serializer,
                _SCHEMA_CREATE_GLOBAL_TABLE_INPUT.members["ReplicationGroup"],
                self.replication_group,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_name"] = de.read_string(
                        _SCHEMA_CREATE_GLOBAL_TABLE_INPUT.members["GlobalTableName"]
                    )

                case 1:
                    kwargs["replication_group"] = _deserialize_replica_list(
                        de,
                        _SCHEMA_CREATE_GLOBAL_TABLE_INPUT.members["ReplicationGroup"],
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREATE_GLOBAL_TABLE_INPUT, consumer=_consumer)
        return kwargs


class GlobalTableStatus(UnknownEnumMixin, StrEnum):
    CREATING = "CREATING"
    ACTIVE = "ACTIVE"
    DELETING = "DELETING"
    UPDATING = "UPDATING"


@dataclass(kw_only=True)
class OnDemandThroughputOverride:
    """
    Overrides the on-demand throughput settings for this replica table. If
    you don't specify a value for this parameter, it uses the source
    table's on-demand throughput settings.
    """

    max_read_request_units: int | None = None
    """Maximum number of read request units for the specified replica table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ON_DEMAND_THROUGHPUT_OVERRIDE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.max_read_request_units is not None:
            serializer.write_long(
                _SCHEMA_ON_DEMAND_THROUGHPUT_OVERRIDE.members["MaxReadRequestUnits"],
                self.max_read_request_units,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["max_read_request_units"] = de.read_long(
                        _SCHEMA_ON_DEMAND_THROUGHPUT_OVERRIDE.members[
                            "MaxReadRequestUnits"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_ON_DEMAND_THROUGHPUT_OVERRIDE, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ProvisionedThroughputOverride:
    """
    Replica-specific provisioned throughput settings. If not specified, uses
    the source table's provisioned throughput settings.
    """

    read_capacity_units: int | None = None
    """
    Replica-specific read capacity units. If not specified, uses the source
    table's read capacity settings.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PROVISIONED_THROUGHPUT_OVERRIDE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.read_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_PROVISIONED_THROUGHPUT_OVERRIDE.members["ReadCapacityUnits"],
                self.read_capacity_units,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["read_capacity_units"] = de.read_long(
                        _SCHEMA_PROVISIONED_THROUGHPUT_OVERRIDE.members[
                            "ReadCapacityUnits"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_PROVISIONED_THROUGHPUT_OVERRIDE, consumer=_consumer
        )
        return kwargs


class IndexStatus(UnknownEnumMixin, StrEnum):
    CREATING = "CREATING"
    UPDATING = "UPDATING"
    DELETING = "DELETING"
    ACTIVE = "ACTIVE"


@dataclass(kw_only=True)
class GlobalSecondaryIndexWarmThroughputDescription:
    """
    The description of the warm throughput value on a global secondary
    index.
    """

    read_units_per_second: int | None = None
    """
    Represents warm throughput read units per second value for a global
    secondary index.
    """

    write_units_per_second: int | None = None
    """
    Represents warm throughput write units per second value for a global
    secondary index.
    """

    status: IndexStatus | None = None
    """
    Represents the warm throughput status being created or updated on a
    global secondary index. The status can only be `UPDATING` or `ACTIVE`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_GLOBAL_SECONDARY_INDEX_WARM_THROUGHPUT_DESCRIPTION, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.read_units_per_second is not None:
            serializer.write_long(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_WARM_THROUGHPUT_DESCRIPTION.members[
                    "ReadUnitsPerSecond"
                ],
                self.read_units_per_second,
            )

        if self.write_units_per_second is not None:
            serializer.write_long(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_WARM_THROUGHPUT_DESCRIPTION.members[
                    "WriteUnitsPerSecond"
                ],
                self.write_units_per_second,
            )

        if self.status is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_WARM_THROUGHPUT_DESCRIPTION.members[
                    "Status"
                ],
                self.status,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["read_units_per_second"] = de.read_long(
                        _SCHEMA_GLOBAL_SECONDARY_INDEX_WARM_THROUGHPUT_DESCRIPTION.members[
                            "ReadUnitsPerSecond"
                        ]
                    )

                case 1:
                    kwargs["write_units_per_second"] = de.read_long(
                        _SCHEMA_GLOBAL_SECONDARY_INDEX_WARM_THROUGHPUT_DESCRIPTION.members[
                            "WriteUnitsPerSecond"
                        ]
                    )

                case 2:
                    kwargs["status"] = IndexStatus(
                        de.read_string(
                            _SCHEMA_GLOBAL_SECONDARY_INDEX_WARM_THROUGHPUT_DESCRIPTION.members[
                                "Status"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GLOBAL_SECONDARY_INDEX_WARM_THROUGHPUT_DESCRIPTION,
            consumer=_consumer,
        )
        return kwargs


@dataclass(kw_only=True)
class ReplicaGlobalSecondaryIndexDescription:
    """Represents the properties of a replica global secondary index."""

    index_name: str | None = None
    """The name of the global secondary index."""

    provisioned_throughput_override: ProvisionedThroughputOverride | None = None
    """If not described, uses the source table GSI's read capacity settings."""

    on_demand_throughput_override: OnDemandThroughputOverride | None = None
    """
    Overrides the maximum on-demand throughput for the specified global
    secondary index in the specified replica table.
    """

    warm_throughput: GlobalSecondaryIndexWarmThroughputDescription | None = None
    """
    Represents the warm throughput of the global secondary index for this
    replica.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_DESCRIPTION, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["IndexName"],
                self.index_name,
            )

        if self.provisioned_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members[
                    "ProvisionedThroughputOverride"
                ],
                self.provisioned_throughput_override,
            )

        if self.on_demand_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members[
                    "OnDemandThroughputOverride"
                ],
                self.on_demand_throughput_override,
            )

        if self.warm_throughput is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members[
                    "WarmThroughput"
                ],
                self.warm_throughput,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members[
                            "IndexName"
                        ]
                    )

                case 1:
                    kwargs["provisioned_throughput_override"] = (
                        ProvisionedThroughputOverride.deserialize(de)
                    )

                case 2:
                    kwargs["on_demand_throughput_override"] = (
                        OnDemandThroughputOverride.deserialize(de)
                    )

                case 3:
                    kwargs["warm_throughput"] = (
                        GlobalSecondaryIndexWarmThroughputDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_DESCRIPTION, consumer=_consumer
        )
        return kwargs


def _serialize_replica_global_secondary_index_description_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[ReplicaGlobalSecondaryIndexDescription],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_global_secondary_index_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaGlobalSecondaryIndexDescription]:
    result: list[ReplicaGlobalSecondaryIndexDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaGlobalSecondaryIndexDescription.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


class GlobalTableSettingsReplicationMode(UnknownEnumMixin, StrEnum):
    ENABLED = "ENABLED"
    DISABLED = "DISABLED"
    ENABLED_WITH_OVERRIDES = "ENABLED_WITH_OVERRIDES"


class ReplicaStatus(UnknownEnumMixin, StrEnum):
    CREATING = "CREATING"
    CREATION_FAILED = "CREATION_FAILED"
    UPDATING = "UPDATING"
    DELETING = "DELETING"
    ACTIVE = "ACTIVE"
    REGION_DISABLED = "REGION_DISABLED"
    INACCESSIBLE_ENCRYPTION_CREDENTIALS = "INACCESSIBLE_ENCRYPTION_CREDENTIALS"
    ARCHIVING = "ARCHIVING"
    ARCHIVED = "ARCHIVED"
    REPLICATION_NOT_AUTHORIZED = "REPLICATION_NOT_AUTHORIZED"


class TableClass(UnknownEnumMixin, StrEnum):
    STANDARD = "STANDARD"
    STANDARD_INFREQUENT_ACCESS = "STANDARD_INFREQUENT_ACCESS"


@dataclass(kw_only=True)
class TableClassSummary:
    """Contains details of the table class."""

    table_class: TableClass | None = None
    """
    The table class of the specified table. Valid values are `STANDARD` and
    `STANDARD_INFREQUENT_ACCESS`.
    """

    last_update_date_time: datetime | None = None
    """The date and time at which the table class was last updated."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TABLE_CLASS_SUMMARY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_class is not None:
            serializer.write_string(
                _SCHEMA_TABLE_CLASS_SUMMARY.members["TableClass"], self.table_class
            )

        if self.last_update_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_TABLE_CLASS_SUMMARY.members["LastUpdateDateTime"],
                self.last_update_date_time,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_class"] = TableClass(
                        de.read_string(
                            _SCHEMA_TABLE_CLASS_SUMMARY.members["TableClass"]
                        )
                    )

                case 1:
                    kwargs["last_update_date_time"] = de.read_timestamp(
                        _SCHEMA_TABLE_CLASS_SUMMARY.members["LastUpdateDateTime"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TABLE_CLASS_SUMMARY, consumer=_consumer)
        return kwargs


class TableStatus(UnknownEnumMixin, StrEnum):
    CREATING = "CREATING"
    UPDATING = "UPDATING"
    DELETING = "DELETING"
    ACTIVE = "ACTIVE"
    INACCESSIBLE_ENCRYPTION_CREDENTIALS = "INACCESSIBLE_ENCRYPTION_CREDENTIALS"
    ARCHIVING = "ARCHIVING"
    ARCHIVED = "ARCHIVED"
    REPLICATION_NOT_AUTHORIZED = "REPLICATION_NOT_AUTHORIZED"


@dataclass(kw_only=True)
class TableWarmThroughputDescription:
    """
    Represents the warm throughput value (in read units per second and write
    units per second) of the table. Warm throughput is applicable for
    DynamoDB Standard-IA tables and specifies the minimum provisioned
    capacity maintained for immediate data access.
    """

    read_units_per_second: int | None = None
    """
    Represents the base table's warm throughput value in read units per
    second.
    """

    write_units_per_second: int | None = None
    """
    Represents the base table's warm throughput value in write units per
    second.
    """

    status: TableStatus | None = None
    """Represents warm throughput value of the base table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TABLE_WARM_THROUGHPUT_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.read_units_per_second is not None:
            serializer.write_long(
                _SCHEMA_TABLE_WARM_THROUGHPUT_DESCRIPTION.members["ReadUnitsPerSecond"],
                self.read_units_per_second,
            )

        if self.write_units_per_second is not None:
            serializer.write_long(
                _SCHEMA_TABLE_WARM_THROUGHPUT_DESCRIPTION.members[
                    "WriteUnitsPerSecond"
                ],
                self.write_units_per_second,
            )

        if self.status is not None:
            serializer.write_string(
                _SCHEMA_TABLE_WARM_THROUGHPUT_DESCRIPTION.members["Status"], self.status
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["read_units_per_second"] = de.read_long(
                        _SCHEMA_TABLE_WARM_THROUGHPUT_DESCRIPTION.members[
                            "ReadUnitsPerSecond"
                        ]
                    )

                case 1:
                    kwargs["write_units_per_second"] = de.read_long(
                        _SCHEMA_TABLE_WARM_THROUGHPUT_DESCRIPTION.members[
                            "WriteUnitsPerSecond"
                        ]
                    )

                case 2:
                    kwargs["status"] = TableStatus(
                        de.read_string(
                            _SCHEMA_TABLE_WARM_THROUGHPUT_DESCRIPTION.members["Status"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_TABLE_WARM_THROUGHPUT_DESCRIPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ReplicaDescription:
    """Contains the details of the replica."""

    region_name: str | None = None
    """The name of the Region."""

    replica_status: ReplicaStatus | None = None
    """
    The current state of the replica:

    - `CREATING` - The replica is being created.

    - `UPDATING` - The replica is being updated.

    - `DELETING` - The replica is being deleted.

    - `ACTIVE` - The replica is ready for use.

    - `REGION_DISABLED` - The replica is inaccessible because the Amazon Web
      Services Region has been disabled.

      Note:
        If the Amazon Web Services Region remains inaccessible for more than
        20 hours, DynamoDB will remove this replica from the replication
        group. The replica will not be deleted and replication will stop from
        and to this region.

    - `INACCESSIBLE_ENCRYPTION_CREDENTIALS ` - The KMS key used to encrypt
      the table is inaccessible.

      Note:
        If the KMS key remains inaccessible for more than 20 hours, DynamoDB
        will remove this replica from the replication group. The replica will
        not be deleted and replication will stop from and to this region.
    """

    replica_arn: str | None = None
    """The Amazon Resource Name (ARN) of the global table replica."""

    replica_status_description: str | None = None
    """Detailed information about the replica status."""

    replica_status_percent_progress: str | None = None
    """
    Specifies the progress of a Create, Update, or Delete action on the
    replica as a percentage.
    """

    kms_master_key_id: str | None = None
    """The KMS key of the replica that will be used for KMS encryption."""

    provisioned_throughput_override: ProvisionedThroughputOverride | None = None
    """
    Replica-specific provisioned throughput. If not described, uses the
    source table's provisioned throughput settings.
    """

    on_demand_throughput_override: OnDemandThroughputOverride | None = None
    """
    Overrides the maximum on-demand throughput settings for the specified
    replica table.
    """

    warm_throughput: TableWarmThroughputDescription | None = None
    """Represents the warm throughput value for this replica."""

    global_secondary_indexes: list[ReplicaGlobalSecondaryIndexDescription] | None = None
    """Replica-specific global secondary index settings."""

    replica_inaccessible_date_time: datetime | None = None
    """
    The time at which the replica was first detected as inaccessible. To
    determine cause of inaccessibility check the `ReplicaStatus` property.
    """

    replica_table_class_summary: TableClassSummary | None = None
    """Contains details of the table class."""

    global_table_settings_replication_mode: (
        GlobalTableSettingsReplicationMode | None
    ) = None
    """
    Indicates one of the settings synchronization modes for the global table
    replica:

    - `ENABLED`: Indicates that the settings synchronization mode for the
      global table replica is enabled.

    - `DISABLED`: Indicates that the settings synchronization mode for the
      global table replica is disabled.

    - `ENABLED_WITH_OVERRIDES`: This mode is set by default for a same
      account global table. Indicates that certain global table settings can
      be overridden.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICA_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.region_name is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_DESCRIPTION.members["RegionName"], self.region_name
            )

        if self.replica_status is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_DESCRIPTION.members["ReplicaStatus"],
                self.replica_status,
            )

        if self.replica_arn is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_DESCRIPTION.members["ReplicaArn"], self.replica_arn
            )

        if self.replica_status_description is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_DESCRIPTION.members["ReplicaStatusDescription"],
                self.replica_status_description,
            )

        if self.replica_status_percent_progress is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_DESCRIPTION.members["ReplicaStatusPercentProgress"],
                self.replica_status_percent_progress,
            )

        if self.kms_master_key_id is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_DESCRIPTION.members["KMSMasterKeyId"],
                self.kms_master_key_id,
            )

        if self.provisioned_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_DESCRIPTION.members["ProvisionedThroughputOverride"],
                self.provisioned_throughput_override,
            )

        if self.on_demand_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_DESCRIPTION.members["OnDemandThroughputOverride"],
                self.on_demand_throughput_override,
            )

        if self.warm_throughput is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_DESCRIPTION.members["WarmThroughput"],
                self.warm_throughput,
            )

        if self.global_secondary_indexes is not None:
            _serialize_replica_global_secondary_index_description_list(
                serializer,
                _SCHEMA_REPLICA_DESCRIPTION.members["GlobalSecondaryIndexes"],
                self.global_secondary_indexes,
            )

        if self.replica_inaccessible_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_REPLICA_DESCRIPTION.members["ReplicaInaccessibleDateTime"],
                self.replica_inaccessible_date_time,
            )

        if self.replica_table_class_summary is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_DESCRIPTION.members["ReplicaTableClassSummary"],
                self.replica_table_class_summary,
            )

        if self.global_table_settings_replication_mode is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_DESCRIPTION.members[
                    "GlobalTableSettingsReplicationMode"
                ],
                self.global_table_settings_replication_mode,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_REPLICA_DESCRIPTION.members["RegionName"]
                    )

                case 1:
                    kwargs["replica_status"] = ReplicaStatus(
                        de.read_string(
                            _SCHEMA_REPLICA_DESCRIPTION.members["ReplicaStatus"]
                        )
                    )

                case 2:
                    kwargs["replica_arn"] = de.read_string(
                        _SCHEMA_REPLICA_DESCRIPTION.members["ReplicaArn"]
                    )

                case 3:
                    kwargs["replica_status_description"] = de.read_string(
                        _SCHEMA_REPLICA_DESCRIPTION.members["ReplicaStatusDescription"]
                    )

                case 4:
                    kwargs["replica_status_percent_progress"] = de.read_string(
                        _SCHEMA_REPLICA_DESCRIPTION.members[
                            "ReplicaStatusPercentProgress"
                        ]
                    )

                case 5:
                    kwargs["kms_master_key_id"] = de.read_string(
                        _SCHEMA_REPLICA_DESCRIPTION.members["KMSMasterKeyId"]
                    )

                case 6:
                    kwargs["provisioned_throughput_override"] = (
                        ProvisionedThroughputOverride.deserialize(de)
                    )

                case 7:
                    kwargs["on_demand_throughput_override"] = (
                        OnDemandThroughputOverride.deserialize(de)
                    )

                case 8:
                    kwargs["warm_throughput"] = (
                        TableWarmThroughputDescription.deserialize(de)
                    )

                case 9:
                    kwargs["global_secondary_indexes"] = (
                        _deserialize_replica_global_secondary_index_description_list(
                            de,
                            _SCHEMA_REPLICA_DESCRIPTION.members[
                                "GlobalSecondaryIndexes"
                            ],
                        )
                    )

                case 10:
                    kwargs["replica_inaccessible_date_time"] = de.read_timestamp(
                        _SCHEMA_REPLICA_DESCRIPTION.members[
                            "ReplicaInaccessibleDateTime"
                        ]
                    )

                case 11:
                    kwargs["replica_table_class_summary"] = (
                        TableClassSummary.deserialize(de)
                    )

                case 12:
                    kwargs["global_table_settings_replication_mode"] = (
                        GlobalTableSettingsReplicationMode(
                            de.read_string(
                                _SCHEMA_REPLICA_DESCRIPTION.members[
                                    "GlobalTableSettingsReplicationMode"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_REPLICA_DESCRIPTION, consumer=_consumer)
        return kwargs


def _serialize_replica_description_list(
    serializer: ShapeSerializer, schema: Schema, value: list[ReplicaDescription]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaDescription]:
    result: list[ReplicaDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaDescription.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class GlobalTableDescription:
    """Contains details about the global table."""

    replication_group: list[ReplicaDescription] | None = None
    """The Regions where the global table has replicas."""

    global_table_arn: str | None = None
    """The unique identifier of the global table."""

    creation_date_time: datetime | None = None
    """The creation time of the global table."""

    global_table_status: GlobalTableStatus | None = None
    """
    The current state of the global table:

    - `CREATING` - The global table is being created.

    - `UPDATING` - The global table is being updated.

    - `DELETING` - The global table is being deleted.

    - `ACTIVE` - The global table is ready for use.
    """

    global_table_name: str | None = None
    """The global table name."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GLOBAL_TABLE_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.replication_group is not None:
            _serialize_replica_description_list(
                serializer,
                _SCHEMA_GLOBAL_TABLE_DESCRIPTION.members["ReplicationGroup"],
                self.replication_group,
            )

        if self.global_table_arn is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_TABLE_DESCRIPTION.members["GlobalTableArn"],
                self.global_table_arn,
            )

        if self.creation_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_GLOBAL_TABLE_DESCRIPTION.members["CreationDateTime"],
                self.creation_date_time,
            )

        if self.global_table_status is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_TABLE_DESCRIPTION.members["GlobalTableStatus"],
                self.global_table_status,
            )

        if self.global_table_name is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_TABLE_DESCRIPTION.members["GlobalTableName"],
                self.global_table_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["replication_group"] = _deserialize_replica_description_list(
                        de, _SCHEMA_GLOBAL_TABLE_DESCRIPTION.members["ReplicationGroup"]
                    )

                case 1:
                    kwargs["global_table_arn"] = de.read_string(
                        _SCHEMA_GLOBAL_TABLE_DESCRIPTION.members["GlobalTableArn"]
                    )

                case 2:
                    kwargs["creation_date_time"] = de.read_timestamp(
                        _SCHEMA_GLOBAL_TABLE_DESCRIPTION.members["CreationDateTime"]
                    )

                case 3:
                    kwargs["global_table_status"] = GlobalTableStatus(
                        de.read_string(
                            _SCHEMA_GLOBAL_TABLE_DESCRIPTION.members[
                                "GlobalTableStatus"
                            ]
                        )
                    )

                case 4:
                    kwargs["global_table_name"] = de.read_string(
                        _SCHEMA_GLOBAL_TABLE_DESCRIPTION.members["GlobalTableName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GLOBAL_TABLE_DESCRIPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class CreateGlobalTableOutput:
    """Dataclass for CreateGlobalTableOutput structure."""

    global_table_description: GlobalTableDescription | None = None
    """Contains the details of the global table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_GLOBAL_TABLE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_description is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_GLOBAL_TABLE_OUTPUT.members["GlobalTableDescription"],
                self.global_table_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_description"] = (
                        GlobalTableDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREATE_GLOBAL_TABLE_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GlobalTableAlreadyExistsException(ServiceError):
    """The specified global table already exists."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GLOBAL_TABLE_ALREADY_EXISTS_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_TABLE_ALREADY_EXISTS_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_GLOBAL_TABLE_ALREADY_EXISTS_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GLOBAL_TABLE_ALREADY_EXISTS_EXCEPTION, consumer=_consumer
        )
        return kwargs


CREATE_GLOBAL_TABLE = APIOperation(
    input=CreateGlobalTableInput,
    output=CreateGlobalTableOutput,
    schema=_SCHEMA_CREATE_GLOBAL_TABLE,
    input_schema=_SCHEMA_CREATE_GLOBAL_TABLE_INPUT,
    output_schema=_SCHEMA_CREATE_GLOBAL_TABLE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#GlobalTableAlreadyExistsException"
            ): GlobalTableAlreadyExistsException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#TableNotFoundException"
            ): TableNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_GLOBAL_TABLE_ALREADY_EXISTS_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_TABLE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class CreateGlobalTableWitnessGroupMemberAction:
    """
    Specifies the action to add a new witness Region to a MRSC global table.
    A MRSC global table can be configured with either three replicas, or
    with two replicas and one witness.
    """

    region_name: str
    """
    The Amazon Web Services Region name to be added as a witness Region for
    the MRSC global table. The witness must be in a different Region than
    the replicas and within the same Region set:

    - US Region set: US East (N. Virginia), US East (Ohio), US West (Oregon)

    - EU Region set: Europe (Ireland), Europe (London), Europe (Paris),
      Europe (Frankfurt)

    - AP Region set: Asia Pacific (Tokyo), Asia Pacific (Seoul), Asia
      Pacific (Osaka)
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_CREATE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_CREATE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION.members[
                "RegionName"
            ],
            self.region_name,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_CREATE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION.members[
                            "RegionName"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CREATE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION, consumer=_consumer
        )
        if "region_name" not in kwargs:
            kwargs["region_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class CreateReplicaAction:
    """Represents a replica to be added."""

    region_name: str
    """The Region of the replica to be added."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_REPLICA_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_CREATE_REPLICA_ACTION.members["RegionName"], self.region_name
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_CREATE_REPLICA_ACTION.members["RegionName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREATE_REPLICA_ACTION, consumer=_consumer)
        if "region_name" not in kwargs:
            kwargs["region_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class ReplicaGlobalSecondaryIndex:
    """Represents the properties of a replica global secondary index."""

    index_name: str
    """The name of the global secondary index."""

    provisioned_throughput_override: ProvisionedThroughputOverride | None = None
    """
    Replica table GSI-specific provisioned throughput. If not specified,
    uses the source table GSI's read capacity settings.
    """

    on_demand_throughput_override: OnDemandThroughputOverride | None = None
    """
    Overrides the maximum on-demand throughput settings for the specified
    global secondary index in the specified replica table.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX.members["IndexName"], self.index_name
        )
        if self.provisioned_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX.members[
                    "ProvisionedThroughputOverride"
                ],
                self.provisioned_throughput_override,
            )

        if self.on_demand_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX.members[
                    "OnDemandThroughputOverride"
                ],
                self.on_demand_throughput_override,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX.members["IndexName"]
                    )

                case 1:
                    kwargs["provisioned_throughput_override"] = (
                        ProvisionedThroughputOverride.deserialize(de)
                    )

                case 2:
                    kwargs["on_demand_throughput_override"] = (
                        OnDemandThroughputOverride.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX, consumer=_consumer
        )
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        return kwargs


def _serialize_replica_global_secondary_index_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[ReplicaGlobalSecondaryIndex],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_global_secondary_index_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaGlobalSecondaryIndex]:
    result: list[ReplicaGlobalSecondaryIndex] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaGlobalSecondaryIndex.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class CreateReplicationGroupMemberAction:
    """Represents a replica to be created."""

    region_name: str
    """The Region where the new replica will be created."""

    kms_master_key_id: str | None = None
    """
    The KMS key that should be used for KMS encryption in the new replica.
    To specify a key, use its key ID, Amazon Resource Name (ARN), alias
    name, or alias ARN. Note that you should only provide this parameter if
    the key is different from the default DynamoDB KMS key
    `alias/aws/dynamodb`.
    """

    provisioned_throughput_override: ProvisionedThroughputOverride | None = None
    """
    Replica-specific provisioned throughput. If not specified, uses the
    source table's provisioned throughput settings.
    """

    on_demand_throughput_override: OnDemandThroughputOverride | None = None
    """
    The maximum on-demand throughput settings for the specified replica
    table being created. You can only modify `MaxReadRequestUnits`, because
    you can't modify `MaxWriteRequestUnits` for individual replica tables.
    """

    global_secondary_indexes: list[ReplicaGlobalSecondaryIndex] | None = None
    """Replica-specific global secondary index settings."""

    table_class_override: TableClass | None = None
    """
    Replica-specific table class. If not specified, uses the source table's
    table class.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION.members["RegionName"],
            self.region_name,
        )
        if self.kms_master_key_id is not None:
            serializer.write_string(
                _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                    "KMSMasterKeyId"
                ],
                self.kms_master_key_id,
            )

        if self.provisioned_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                    "ProvisionedThroughputOverride"
                ],
                self.provisioned_throughput_override,
            )

        if self.on_demand_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                    "OnDemandThroughputOverride"
                ],
                self.on_demand_throughput_override,
            )

        if self.global_secondary_indexes is not None:
            _serialize_replica_global_secondary_index_list(
                serializer,
                _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                    "GlobalSecondaryIndexes"
                ],
                self.global_secondary_indexes,
            )

        if self.table_class_override is not None:
            serializer.write_string(
                _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                    "TableClassOverride"
                ],
                self.table_class_override,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                            "RegionName"
                        ]
                    )

                case 1:
                    kwargs["kms_master_key_id"] = de.read_string(
                        _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                            "KMSMasterKeyId"
                        ]
                    )

                case 2:
                    kwargs["provisioned_throughput_override"] = (
                        ProvisionedThroughputOverride.deserialize(de)
                    )

                case 3:
                    kwargs["on_demand_throughput_override"] = (
                        OnDemandThroughputOverride.deserialize(de)
                    )

                case 4:
                    kwargs["global_secondary_indexes"] = (
                        _deserialize_replica_global_secondary_index_list(
                            de,
                            _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                                "GlobalSecondaryIndexes"
                            ],
                        )
                    )

                case 5:
                    kwargs["table_class_override"] = TableClass(
                        de.read_string(
                            _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                                "TableClassOverride"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CREATE_REPLICATION_GROUP_MEMBER_ACTION, consumer=_consumer
        )
        if "region_name" not in kwargs:
            kwargs["region_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class GlobalSecondaryIndex:
    """Represents the properties of a global secondary index."""

    index_name: str
    """
    The name of the global secondary index. The name must be unique among
    all other indexes on this table.
    """

    key_schema: list[KeySchemaElement]
    """
    The complete key schema for a global secondary index, which consists of
    one or more pairs of attribute names and key types:

    - `HASH` - partition key

    - `RANGE` - sort key

    Note:
        The partition key of an item is also known as its *hash attribute*. The
        term \"hash attribute\" derives from DynamoDB's usage of an internal
        hash function to evenly distribute data items across partitions, based
        on their partition key values.

        The sort key of an item is also known as its *range attribute*. The term
        \"range attribute\" derives from the way DynamoDB stores items with the
        same partition key physically close together, in sorted order by the
        sort key value.
    """

    projection: Projection
    """
    Represents attributes that are copied (projected) from the table into
    the global secondary index. These are in addition to the primary key
    attributes and index key attributes, which are automatically projected.
    """

    provisioned_throughput: ProvisionedThroughput | None = None
    """
    Represents the provisioned throughput settings for the specified global
    secondary index. You must use either `OnDemandThroughput` or
    `ProvisionedThroughput` based on your table's capacity mode.

    For current minimum and maximum provisioned throughput values, see
    [Service, Account, and Table
    Quotas](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Limits.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    on_demand_throughput: OnDemandThroughput | None = None
    """
    The maximum number of read and write units for the specified global
    secondary index. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both. You must use
    either `OnDemandThroughput` or `ProvisionedThroughput` based on your
    table's capacity mode.
    """

    warm_throughput: WarmThroughput | None = None
    """
    Represents the warm throughput value (in read units per second and write
    units per second) for the specified secondary index. If you use this
    parameter, you must specify `ReadUnitsPerSecond`, `WriteUnitsPerSecond`,
    or both.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GLOBAL_SECONDARY_INDEX, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_GLOBAL_SECONDARY_INDEX.members["IndexName"], self.index_name
        )
        _serialize_key_schema(
            serializer,
            _SCHEMA_GLOBAL_SECONDARY_INDEX.members["KeySchema"],
            self.key_schema,
        )
        serializer.write_struct(
            _SCHEMA_GLOBAL_SECONDARY_INDEX.members["Projection"], self.projection
        )
        if self.provisioned_throughput is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX.members["ProvisionedThroughput"],
                self.provisioned_throughput,
            )

        if self.on_demand_throughput is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX.members["OnDemandThroughput"],
                self.on_demand_throughput,
            )

        if self.warm_throughput is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX.members["WarmThroughput"],
                self.warm_throughput,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_GLOBAL_SECONDARY_INDEX.members["IndexName"]
                    )

                case 1:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de, _SCHEMA_GLOBAL_SECONDARY_INDEX.members["KeySchema"]
                    )

                case 2:
                    kwargs["projection"] = Projection.deserialize(de)

                case 3:
                    kwargs["provisioned_throughput"] = (
                        ProvisionedThroughput.deserialize(de)
                    )

                case 4:
                    kwargs["on_demand_throughput"] = OnDemandThroughput.deserialize(de)

                case 5:
                    kwargs["warm_throughput"] = WarmThroughput.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GLOBAL_SECONDARY_INDEX, consumer=_consumer)
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        if "key_schema" not in kwargs:
            kwargs["key_schema"] = []
        if "projection" not in kwargs:
            kwargs["projection"] = Projection._smithy_default()
        return kwargs


def _serialize_global_secondary_index_list(
    serializer: ShapeSerializer, schema: Schema, value: list[GlobalSecondaryIndex]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_global_secondary_index_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[GlobalSecondaryIndex]:
    result: list[GlobalSecondaryIndex] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(GlobalSecondaryIndex.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class LocalSecondaryIndex:
    """Represents the properties of a local secondary index."""

    index_name: str
    """
    The name of the local secondary index. The name must be unique among all
    other indexes on this table.
    """

    key_schema: list[KeySchemaElement]
    """
    The complete key schema for the local secondary index, consisting of one
    or more pairs of attribute names and key types:

    - `HASH` - partition key

    - `RANGE` - sort key

    Note:
        The partition key of an item is also known as its *hash attribute*. The
        term \"hash attribute\" derives from DynamoDB's usage of an internal
        hash function to evenly distribute data items across partitions, based
        on their partition key values.

        The sort key of an item is also known as its *range attribute*. The term
        \"range attribute\" derives from the way DynamoDB stores items with the
        same partition key physically close together, in sorted order by the
        sort key value.
    """

    projection: Projection
    """
    Represents attributes that are copied (projected) from the table into
    the local secondary index. These are in addition to the primary key
    attributes and index key attributes, which are automatically projected.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LOCAL_SECONDARY_INDEX, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_LOCAL_SECONDARY_INDEX.members["IndexName"], self.index_name
        )
        _serialize_key_schema(
            serializer,
            _SCHEMA_LOCAL_SECONDARY_INDEX.members["KeySchema"],
            self.key_schema,
        )
        serializer.write_struct(
            _SCHEMA_LOCAL_SECONDARY_INDEX.members["Projection"], self.projection
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_LOCAL_SECONDARY_INDEX.members["IndexName"]
                    )

                case 1:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de, _SCHEMA_LOCAL_SECONDARY_INDEX.members["KeySchema"]
                    )

                case 2:
                    kwargs["projection"] = Projection.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LOCAL_SECONDARY_INDEX, consumer=_consumer)
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        if "key_schema" not in kwargs:
            kwargs["key_schema"] = []
        if "projection" not in kwargs:
            kwargs["projection"] = Projection._smithy_default()
        return kwargs


def _serialize_local_secondary_index_list(
    serializer: ShapeSerializer, schema: Schema, value: list[LocalSecondaryIndex]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_local_secondary_index_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[LocalSecondaryIndex]:
    result: list[LocalSecondaryIndex] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(LocalSecondaryIndex.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class SSESpecification:
    """Represents the settings used to enable server-side encryption."""

    enabled: bool | None = None
    """
    Indicates whether server-side encryption is done using an Amazon Web
    Services managed key or an Amazon Web Services owned key. If enabled
    (true), server-side encryption type is set to `KMS` and an Amazon Web
    Services managed key is used (KMS charges apply). If disabled (false) or
    not specified, server-side encryption is set to Amazon Web Services
    owned key.
    """

    sse_type: SSEType | None = None
    """
    Server-side encryption type. The only supported value is:

    - `KMS` - Server-side encryption that uses Key Management Service. The
      key is stored in your account and is managed by KMS (KMS charges
      apply).
    """

    kms_master_key_id: str | None = None
    """
    The KMS key that should be used for the KMS encryption. To specify a
    key, use its key ID, Amazon Resource Name (ARN), alias name, or alias
    ARN. Note that you should only provide this parameter if the key is
    different from the default DynamoDB key `alias/aws/dynamodb`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SSE_SPECIFICATION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.enabled is not None:
            serializer.write_boolean(
                _SCHEMA_SSE_SPECIFICATION.members["Enabled"], self.enabled
            )

        if self.sse_type is not None:
            serializer.write_string(
                _SCHEMA_SSE_SPECIFICATION.members["SSEType"], self.sse_type
            )

        if self.kms_master_key_id is not None:
            serializer.write_string(
                _SCHEMA_SSE_SPECIFICATION.members["KMSMasterKeyId"],
                self.kms_master_key_id,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["enabled"] = de.read_boolean(
                        _SCHEMA_SSE_SPECIFICATION.members["Enabled"]
                    )

                case 1:
                    kwargs["sse_type"] = SSEType(
                        de.read_string(_SCHEMA_SSE_SPECIFICATION.members["SSEType"])
                    )

                case 2:
                    kwargs["kms_master_key_id"] = de.read_string(
                        _SCHEMA_SSE_SPECIFICATION.members["KMSMasterKeyId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SSE_SPECIFICATION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class Tag:
    """
    Describes a tag. A tag is a key-value pair. You can add up to 50 tags to
    a single DynamoDB table.

    Amazon Web Services-assigned tag names and values are automatically
    assigned the `aws:` prefix, which the user cannot assign. Amazon Web
    Services-assigned tag names do not count towards the tag limit of 50.
    User-assigned tag names have the prefix `user:` in the Cost Allocation
    Report. You cannot backdate the application of a tag.

    For an overview on tagging DynamoDB resources, see [Tagging for
    DynamoDB](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Tagging.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    key: str
    """
    The key of the tag. Tag keys are case sensitive. Each DynamoDB table can
    only have up to one tag with the same key. If you try to add an existing
    tag (same key), the existing tag value will be updated to the new value.
    """

    value: str
    """The value of the tag. Tag values are case-sensitive and can be null."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TAG, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_TAG.members["Key"], self.key)
        serializer.write_string(_SCHEMA_TAG.members["Value"], self.value)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["key"] = de.read_string(_SCHEMA_TAG.members["Key"])

                case 1:
                    kwargs["value"] = de.read_string(_SCHEMA_TAG.members["Value"])

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TAG, consumer=_consumer)
        if "key" not in kwargs:
            kwargs["key"] = ""
        if "value" not in kwargs:
            kwargs["value"] = ""
        return kwargs


def _serialize_tag_list(
    serializer: ShapeSerializer, schema: Schema, value: list[Tag]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_tag_list(deserializer: ShapeDeserializer, schema: Schema) -> list[Tag]:
    result: list[Tag] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(Tag.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class VectorIndex:
    """
    Contains the configuration settings for a vector index, including the
    index name, vector attribute, dimensions, distance function, search
    schema, and projection.
    """

    index_name: str
    """The name of the vector index."""

    vector_attribute: VectorAttributeDefinition
    """The vector attribute configuration for the index."""

    projection: Projection
    """
    Specifies attributes that are copied (projected) from the table into the
    vector index.
    """

    dimensions: int
    """The number of dimensions in each vector."""

    distance_function: VectorDistanceFunction
    """
    The distance function used to calculate similarity between vectors.
    Valid values: `COSINE`, `EUCLIDEAN`, `DOT_PRODUCT`.
    """

    search_schema: list[SearchSchemaElement] | None = None
    """
    The search schema that defines partition key and inline filter
    attributes for the vector index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_VECTOR_INDEX, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_VECTOR_INDEX.members["IndexName"], self.index_name
        )
        serializer.write_struct(
            _SCHEMA_VECTOR_INDEX.members["VectorAttribute"], self.vector_attribute
        )
        if self.search_schema is not None:
            _serialize_search_schema(
                serializer,
                _SCHEMA_VECTOR_INDEX.members["SearchSchema"],
                self.search_schema,
            )

        serializer.write_struct(
            _SCHEMA_VECTOR_INDEX.members["Projection"], self.projection
        )
        serializer.write_long(
            _SCHEMA_VECTOR_INDEX.members["Dimensions"], self.dimensions
        )
        serializer.write_string(
            _SCHEMA_VECTOR_INDEX.members["DistanceFunction"], self.distance_function
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_VECTOR_INDEX.members["IndexName"]
                    )

                case 1:
                    kwargs["vector_attribute"] = VectorAttributeDefinition.deserialize(
                        de
                    )

                case 2:
                    kwargs["search_schema"] = _deserialize_search_schema(
                        de, _SCHEMA_VECTOR_INDEX.members["SearchSchema"]
                    )

                case 3:
                    kwargs["projection"] = Projection.deserialize(de)

                case 4:
                    kwargs["dimensions"] = de.read_long(
                        _SCHEMA_VECTOR_INDEX.members["Dimensions"]
                    )

                case 5:
                    kwargs["distance_function"] = VectorDistanceFunction(
                        de.read_string(_SCHEMA_VECTOR_INDEX.members["DistanceFunction"])
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_VECTOR_INDEX, consumer=_consumer)
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        if "vector_attribute" not in kwargs:
            kwargs["vector_attribute"] = VectorAttributeDefinition._smithy_default()
        if "projection" not in kwargs:
            kwargs["projection"] = Projection._smithy_default()
        if "dimensions" not in kwargs:
            kwargs["dimensions"] = 0
        if "distance_function" not in kwargs:
            kwargs["distance_function"] = VectorDistanceFunction._corrected("")
        return kwargs


def _serialize_vector_index_list(
    serializer: ShapeSerializer, schema: Schema, value: list[VectorIndex]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_vector_index_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[VectorIndex]:
    result: list[VectorIndex] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(VectorIndex.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class CreateTableInput:
    """Represents the input of a `CreateTable` operation."""

    attribute_definitions: list[AttributeDefinition] | None = None
    """
    An array of attributes that describe the key schema for the table and
    indexes.
    """

    table_name: str | None = None
    """
    The name of the table to create. You can also provide the Amazon
    Resource Name (ARN) of the table in this parameter.
    """

    key_schema: list[KeySchemaElement] | None = None
    """
    Specifies the attributes that make up the primary key for a table or an
    index. The attributes in `KeySchema` must also be defined in the
    `AttributeDefinitions` array. For more information, see [Data
    Model](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DataModel.html)
    in the *Amazon DynamoDB Developer Guide*.

    Each `KeySchemaElement` in the array is composed of:

    - `AttributeName` - The name of this key attribute.

    - `KeyType` - The role that the key attribute will assume:

      - `HASH` - partition key

      - `RANGE` - sort key

    Note:
        The partition key of an item is also known as its *hash attribute*. The
        term \"hash attribute\" derives from the DynamoDB usage of an internal
        hash function to evenly distribute data items across partitions, based
        on their partition key values.

        The sort key of an item is also known as its *range attribute*. The term
        \"range attribute\" derives from the way DynamoDB stores items with the
        same partition key physically close together, in sorted order by the
        sort key value.

    For a simple primary key (partition key), you must provide exactly one
    element with a `KeyType` of `HASH`.

    For a composite primary key (partition key and sort key), you must
    provide exactly two elements, in this order: The first element must have
    a `KeyType` of `HASH`, and the second element must have a `KeyType` of
    `RANGE`.

    For more information, see [Working with
    Tables](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/WorkingWithTables.html#WorkingWithTables.primary.key)
    in the *Amazon DynamoDB Developer Guide*.
    """

    local_secondary_indexes: list[LocalSecondaryIndex] | None = None
    """
    One or more local secondary indexes (the maximum is 5) to be created on
    the table. Each index is scoped to a given partition key value. There is
    a 10 GB size limit per partition key value; otherwise, the size of a
    local secondary index is unconstrained.

    Each local secondary index in the array includes the following:

    - `IndexName` - The name of the local secondary index. Must be unique
      only for this table.

    - `KeySchema` - Specifies the key schema for the local secondary index.
      The key schema must begin with the same partition key as the table.

    - `Projection` - Specifies attributes that are copied (projected) from
      the table into the index. These are in addition to the primary key
      attributes and index key attributes, which are automatically
      projected. Each attribute specification is composed of:

      - `ProjectionType` - One of the following:

        - `KEYS_ONLY` - Only the index and primary keys are projected into
          the index.

        - `INCLUDE` - Only the specified table attributes are projected into
          the index. The list of projected attributes is in
          `NonKeyAttributes`.

        - `ALL` - All of the table attributes are projected into the index.

      - `NonKeyAttributes` - A list of one or more non-key attribute names
        that are projected into the secondary index. The total count of
        attributes provided in `NonKeyAttributes`, summed across all of the
        secondary indexes, must not exceed 100. If you project the same
        attribute into two different indexes, this counts as two distinct
        attributes when determining the total. This limit only applies when
        you specify the ProjectionType of `INCLUDE`. You still can specify
        the ProjectionType of `ALL` to project all attributes from the
        source table, even if the table has more than 100 attributes.
    """

    global_secondary_indexes: list[GlobalSecondaryIndex] | None = None
    """
    One or more global secondary indexes (the maximum is 20) to be created
    on the table. Each global secondary index in the array includes the
    following:

    - `IndexName` - The name of the global secondary index. Must be unique
      only for this table.

    - `KeySchema` - Specifies the key schema for the global secondary index.
      Each global secondary index supports up to 4 partition keys and up to
      4 sort keys.

    - `Projection` - Specifies attributes that are copied (projected) from
      the table into the index. These are in addition to the primary key
      attributes and index key attributes, which are automatically
      projected. Each attribute specification is composed of:

      - `ProjectionType` - One of the following:

        - `KEYS_ONLY` - Only the index and primary keys are projected into
          the index.

        - `INCLUDE` - Only the specified table attributes are projected into
          the index. The list of projected attributes is in
          `NonKeyAttributes`.

        - `ALL` - All of the table attributes are projected into the index.

      - `NonKeyAttributes` - A list of one or more non-key attribute names
        that are projected into the secondary index. The total count of
        attributes provided in `NonKeyAttributes`, summed across all of the
        secondary indexes, must not exceed 100. If you project the same
        attribute into two different indexes, this counts as two distinct
        attributes when determining the total. This limit only applies when
        you specify the ProjectionType of `INCLUDE`. You still can specify
        the ProjectionType of `ALL` to project all attributes from the
        source table, even if the table has more than 100 attributes.

    - `ProvisionedThroughput` - The provisioned throughput settings for the
      global secondary index, consisting of read and write capacity units.
    """

    billing_mode: BillingMode | None = None
    """
    Controls how you are charged for read and write throughput and how you
    manage capacity. This setting can be changed later.

    - `PAY_PER_REQUEST` - We recommend using `PAY_PER_REQUEST` for most
      DynamoDB workloads. `PAY_PER_REQUEST` sets the billing mode to
      [On-demand capacity
      mode](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/on-demand-capacity-mode.html).

    - `PROVISIONED` - We recommend using `PROVISIONED` for steady workloads
      with predictable growth where capacity requirements can be reliably
      forecasted. `PROVISIONED` sets the billing mode to [Provisioned
      capacity
      mode](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/provisioned-capacity-mode.html).
    """

    provisioned_throughput: ProvisionedThroughput | None = None
    """
    Represents the provisioned throughput settings for a specified table or
    index. The settings can be modified using the `UpdateTable` operation.

    If you set BillingMode as `PROVISIONED`, you must specify this property.
    If you set BillingMode as `PAY_PER_REQUEST`, you cannot specify this
    property.

    For current minimum and maximum provisioned throughput values, see
    [Service, Account, and Table
    Quotas](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Limits.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    stream_specification: StreamSpecification | None = None
    """
    The settings for DynamoDB Streams on the table. These settings consist
    of:

    - `StreamEnabled` - Indicates whether DynamoDB Streams is to be enabled
      (true) or disabled (false).

    - `StreamViewType` - When an item in the table is modified,
      `StreamViewType` determines what information is written to the
      table's stream. Valid values for `StreamViewType` are:

      - `KEYS_ONLY` - Only the key attributes of the modified item are
        written to the stream.

      - `NEW_IMAGE` - The entire item, as it appears after it was modified,
        is written to the stream.

      - `OLD_IMAGE` - The entire item, as it appeared before it was
        modified, is written to the stream.

      - `NEW_AND_OLD_IMAGES` - Both the new and the old item images of the
        item are written to the stream.
    """

    sse_specification: SSESpecification | None = None
    """Represents the settings used to enable server-side encryption."""

    tags: list[Tag] | None = None
    """
    A list of key-value pairs to label the table. For more information, see
    [Tagging for
    DynamoDB](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Tagging.html).
    """

    table_class: TableClass | None = None
    """
    The table class of the new table. Valid values are `STANDARD` and
    `STANDARD_INFREQUENT_ACCESS`.
    """

    deletion_protection_enabled: bool | None = None
    """
    Indicates whether deletion protection is to be enabled (true) or
    disabled (false) on the table.
    """

    warm_throughput: WarmThroughput | None = None
    """
    Represents the warm throughput (in read units per second and write units
    per second) for creating a table.
    """

    resource_policy: str | None = None
    """
    An Amazon Web Services resource-based policy document in JSON format
    that will be attached to the table.

    When you attach a resource-based policy while creating a table, the
    policy application is *strongly consistent*.

    The maximum size supported for a resource-based policy document is 20
    KB. DynamoDB counts whitespaces when calculating the size of a policy
    against this limit. For a full list of all considerations that apply for
    resource-based policies, see [Resource-based policy
    considerations](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/rbac-considerations.html).

    Note:
        You need to specify the `CreateTable` and `PutResourcePolicy` IAM
        actions for authorizing a user to create a table with a resource-based
        policy.
    """

    on_demand_throughput: OnDemandThroughput | None = None
    """
    Sets the maximum number of read and write units for the specified table
    in on-demand capacity mode. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    global_table_source_arn: str | None = None
    """
    The Amazon Resource Name (ARN) of the source table used for the creation
    of a multi-account global table.
    """

    global_table_settings_replication_mode: (
        GlobalTableSettingsReplicationMode | None
    ) = None
    """
    Controls the settings synchronization mode for the global table. For
    multi-account global tables, this parameter is required and the only
    supported value is ENABLED. For same-account global tables, this
    parameter is set to ENABLED_WITH_OVERRIDES.
    """

    vector_indexes: list[VectorIndex] | None = None
    """
    One or more vector indexes to be created on the table. Each vector index
    enables similarity search on a vector attribute. Each element in the
    list consists of:

    - `IndexName` - The name of the vector index. Must be unique within the
      table.

    - `VectorAttribute` - The attribute that contains vector embeddings. If
      multiple vector indexes reference the same attribute, they must all
      use the same number of dimensions.

    - `Dimensions` - The number of dimensions in each vector.

    - `DistanceFunction` - The distance function used to calculate
      similarity. Valid values: `COSINE`, `EUCLIDEAN`, `DOT_PRODUCT`.

    - `Projection` - Specifies attributes that are copied (projected) from
      the table into the vector index. The total number of projected non-key
      attributes is shared across the vector attribute (counts as 1) and
      `INLINE_FILTER` search schema elements (each counts as 1). `HASH`
      search schema elements do not count toward this limit.

    - `SearchSchema` - (Optional) Defines the partition key (`HASH`) and
      inline filter (`INLINE_FILTER`) attributes for the vector index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_TABLE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.attribute_definitions is not None:
            _serialize_attribute_definitions(
                serializer,
                _SCHEMA_CREATE_TABLE_INPUT.members["AttributeDefinitions"],
                self.attribute_definitions,
            )

        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_CREATE_TABLE_INPUT.members["TableName"], self.table_name
            )

        if self.key_schema is not None:
            _serialize_key_schema(
                serializer,
                _SCHEMA_CREATE_TABLE_INPUT.members["KeySchema"],
                self.key_schema,
            )

        if self.local_secondary_indexes is not None:
            _serialize_local_secondary_index_list(
                serializer,
                _SCHEMA_CREATE_TABLE_INPUT.members["LocalSecondaryIndexes"],
                self.local_secondary_indexes,
            )

        if self.global_secondary_indexes is not None:
            _serialize_global_secondary_index_list(
                serializer,
                _SCHEMA_CREATE_TABLE_INPUT.members["GlobalSecondaryIndexes"],
                self.global_secondary_indexes,
            )

        if self.billing_mode is not None:
            serializer.write_string(
                _SCHEMA_CREATE_TABLE_INPUT.members["BillingMode"], self.billing_mode
            )

        if self.provisioned_throughput is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_TABLE_INPUT.members["ProvisionedThroughput"],
                self.provisioned_throughput,
            )

        if self.stream_specification is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_TABLE_INPUT.members["StreamSpecification"],
                self.stream_specification,
            )

        if self.sse_specification is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_TABLE_INPUT.members["SSESpecification"],
                self.sse_specification,
            )

        if self.tags is not None:
            _serialize_tag_list(
                serializer, _SCHEMA_CREATE_TABLE_INPUT.members["Tags"], self.tags
            )

        if self.table_class is not None:
            serializer.write_string(
                _SCHEMA_CREATE_TABLE_INPUT.members["TableClass"], self.table_class
            )

        if self.deletion_protection_enabled is not None:
            serializer.write_boolean(
                _SCHEMA_CREATE_TABLE_INPUT.members["DeletionProtectionEnabled"],
                self.deletion_protection_enabled,
            )

        if self.warm_throughput is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_TABLE_INPUT.members["WarmThroughput"],
                self.warm_throughput,
            )

        if self.resource_policy is not None:
            serializer.write_string(
                _SCHEMA_CREATE_TABLE_INPUT.members["ResourcePolicy"],
                self.resource_policy,
            )

        if self.on_demand_throughput is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_TABLE_INPUT.members["OnDemandThroughput"],
                self.on_demand_throughput,
            )

        if self.global_table_source_arn is not None:
            serializer.write_string(
                _SCHEMA_CREATE_TABLE_INPUT.members["GlobalTableSourceArn"],
                self.global_table_source_arn,
            )

        if self.global_table_settings_replication_mode is not None:
            serializer.write_string(
                _SCHEMA_CREATE_TABLE_INPUT.members[
                    "GlobalTableSettingsReplicationMode"
                ],
                self.global_table_settings_replication_mode,
            )

        if self.vector_indexes is not None:
            _serialize_vector_index_list(
                serializer,
                _SCHEMA_CREATE_TABLE_INPUT.members["VectorIndexes"],
                self.vector_indexes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attribute_definitions"] = (
                        _deserialize_attribute_definitions(
                            de,
                            _SCHEMA_CREATE_TABLE_INPUT.members["AttributeDefinitions"],
                        )
                    )

                case 1:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_CREATE_TABLE_INPUT.members["TableName"]
                    )

                case 2:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de, _SCHEMA_CREATE_TABLE_INPUT.members["KeySchema"]
                    )

                case 3:
                    kwargs["local_secondary_indexes"] = (
                        _deserialize_local_secondary_index_list(
                            de,
                            _SCHEMA_CREATE_TABLE_INPUT.members["LocalSecondaryIndexes"],
                        )
                    )

                case 4:
                    kwargs["global_secondary_indexes"] = (
                        _deserialize_global_secondary_index_list(
                            de,
                            _SCHEMA_CREATE_TABLE_INPUT.members[
                                "GlobalSecondaryIndexes"
                            ],
                        )
                    )

                case 5:
                    kwargs["billing_mode"] = BillingMode(
                        de.read_string(
                            _SCHEMA_CREATE_TABLE_INPUT.members["BillingMode"]
                        )
                    )

                case 6:
                    kwargs["provisioned_throughput"] = (
                        ProvisionedThroughput.deserialize(de)
                    )

                case 7:
                    kwargs["stream_specification"] = StreamSpecification.deserialize(de)

                case 8:
                    kwargs["sse_specification"] = SSESpecification.deserialize(de)

                case 9:
                    kwargs["tags"] = _deserialize_tag_list(
                        de, _SCHEMA_CREATE_TABLE_INPUT.members["Tags"]
                    )

                case 10:
                    kwargs["table_class"] = TableClass(
                        de.read_string(_SCHEMA_CREATE_TABLE_INPUT.members["TableClass"])
                    )

                case 11:
                    kwargs["deletion_protection_enabled"] = de.read_boolean(
                        _SCHEMA_CREATE_TABLE_INPUT.members["DeletionProtectionEnabled"]
                    )

                case 12:
                    kwargs["warm_throughput"] = WarmThroughput.deserialize(de)

                case 13:
                    kwargs["resource_policy"] = de.read_string(
                        _SCHEMA_CREATE_TABLE_INPUT.members["ResourcePolicy"]
                    )

                case 14:
                    kwargs["on_demand_throughput"] = OnDemandThroughput.deserialize(de)

                case 15:
                    kwargs["global_table_source_arn"] = de.read_string(
                        _SCHEMA_CREATE_TABLE_INPUT.members["GlobalTableSourceArn"]
                    )

                case 16:
                    kwargs["global_table_settings_replication_mode"] = (
                        GlobalTableSettingsReplicationMode(
                            de.read_string(
                                _SCHEMA_CREATE_TABLE_INPUT.members[
                                    "GlobalTableSettingsReplicationMode"
                                ]
                            )
                        )
                    )

                case 17:
                    kwargs["vector_indexes"] = _deserialize_vector_index_list(
                        de, _SCHEMA_CREATE_TABLE_INPUT.members["VectorIndexes"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREATE_TABLE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ProvisionedThroughputDescription:
    """
    Represents the provisioned throughput settings for the table, consisting
    of read and write capacity units, along with data about increases and
    decreases.
    """

    last_increase_date_time: datetime | None = None
    """
    The date and time of the last provisioned throughput increase for this
    table.
    """

    last_decrease_date_time: datetime | None = None
    """
    The date and time of the last provisioned throughput decrease for this
    table.
    """

    number_of_decreases_today: int | None = None
    """
    The number of provisioned throughput decreases for this table during
    this UTC calendar day. For current maximums on provisioned throughput
    decreases, see [Service, Account, and Table
    Quotas](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Limits.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    read_capacity_units: int | None = None
    """
    The maximum number of strongly consistent reads consumed per second
    before DynamoDB returns a `ThrottlingException`. Eventually consistent
    reads require less effort than strongly consistent reads, so a setting
    of 50 `ReadCapacityUnits` per second provides 100 eventually consistent
    `ReadCapacityUnits` per second.

    For a table or global secondary index that uses on-demand capacity mode
    (`PAY_PER_REQUEST`), this value is `0`, because on-demand mode does not
    use provisioned throughput.
    """

    write_capacity_units: int | None = None
    """
    The maximum number of writes consumed per second before DynamoDB returns
    a `ThrottlingException`.

    For a table or global secondary index that uses on-demand capacity mode
    (`PAY_PER_REQUEST`), this value is `0`, because on-demand mode does not
    use provisioned throughput.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.last_increase_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION.members[
                    "LastIncreaseDateTime"
                ],
                self.last_increase_date_time,
            )

        if self.last_decrease_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION.members[
                    "LastDecreaseDateTime"
                ],
                self.last_decrease_date_time,
            )

        if self.number_of_decreases_today is not None:
            serializer.write_long(
                _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION.members[
                    "NumberOfDecreasesToday"
                ],
                self.number_of_decreases_today,
            )

        if self.read_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION.members["ReadCapacityUnits"],
                self.read_capacity_units,
            )

        if self.write_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION.members[
                    "WriteCapacityUnits"
                ],
                self.write_capacity_units,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["last_increase_date_time"] = de.read_timestamp(
                        _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION.members[
                            "LastIncreaseDateTime"
                        ]
                    )

                case 1:
                    kwargs["last_decrease_date_time"] = de.read_timestamp(
                        _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION.members[
                            "LastDecreaseDateTime"
                        ]
                    )

                case 2:
                    kwargs["number_of_decreases_today"] = de.read_long(
                        _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION.members[
                            "NumberOfDecreasesToday"
                        ]
                    )

                case 3:
                    kwargs["read_capacity_units"] = de.read_long(
                        _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION.members[
                            "ReadCapacityUnits"
                        ]
                    )

                case 4:
                    kwargs["write_capacity_units"] = de.read_long(
                        _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION.members[
                            "WriteCapacityUnits"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_PROVISIONED_THROUGHPUT_DESCRIPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class GlobalSecondaryIndexDescription:
    """Represents the properties of a global secondary index."""

    index_name: str | None = None
    """The name of the global secondary index."""

    key_schema: list[KeySchemaElement] | None = None
    """
    The complete key schema for a global secondary index, which consists of
    one or more pairs of attribute names and key types:

    - `HASH` - partition key

    - `RANGE` - sort key

    Note:
        The partition key of an item is also known as its *hash attribute*. The
        term \"hash attribute\" derives from DynamoDB's usage of an internal
        hash function to evenly distribute data items across partitions, based
        on their partition key values.

        The sort key of an item is also known as its *range attribute*. The term
        \"range attribute\" derives from the way DynamoDB stores items with the
        same partition key physically close together, in sorted order by the
        sort key value.
    """

    projection: Projection | None = None
    """
    Represents attributes that are copied (projected) from the table into
    the global secondary index. These are in addition to the primary key
    attributes and index key attributes, which are automatically projected.
    """

    index_status: IndexStatus | None = None
    """
    The current state of the global secondary index:

    - `CREATING` - The index is being created.

    - `UPDATING` - The index is being updated.

    - `DELETING` - The index is being deleted.

    - `ACTIVE` - The index is ready for use.
    """

    backfilling: bool | None = None
    """
    Indicates whether the index is currently backfilling. *Backfilling* is
    the process of reading items from the table and determining whether they
    can be added to the index. (Not all items will qualify: For example, a
    partition key cannot have any duplicate values.) If an item can be added
    to the index, DynamoDB will do so. After all items have been processed,
    the backfilling operation is complete and `Backfilling` is false.

    You can delete an index that is being created during the `Backfilling`
    phase when `IndexStatus` is set to CREATING and `Backfilling` is true.
    You can't delete the index that is being created when `IndexStatus` is
    set to CREATING and `Backfilling` is false.

    Note:
        For indexes that were created during a `CreateTable` operation, the
        `Backfilling` attribute does not appear in the `DescribeTable` output.
    """

    provisioned_throughput: ProvisionedThroughputDescription | None = None
    """
    Represents the provisioned throughput settings for the specified global
    secondary index.

    For current minimum and maximum provisioned throughput values, see
    [Service, Account, and Table
    Quotas](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Limits.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    index_size_bytes: int | None = None
    """
    The total size of the specified index, in bytes. DynamoDB updates this
    value approximately every six hours. Recent changes might not be
    reflected in this value.
    """

    item_count: int | None = None
    """
    The number of items in the specified index. DynamoDB updates this value
    approximately every six hours. Recent changes might not be reflected in
    this value.
    """

    index_arn: str | None = None
    """The Amazon Resource Name (ARN) that uniquely identifies the index."""

    on_demand_throughput: OnDemandThroughput | None = None
    """
    The maximum number of read and write units for the specified global
    secondary index. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    warm_throughput: GlobalSecondaryIndexWarmThroughputDescription | None = None
    """
    Represents the warm throughput value (in read units per second and write
    units per second) for the specified secondary index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["IndexName"],
                self.index_name,
            )

        if self.key_schema is not None:
            _serialize_key_schema(
                serializer,
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["KeySchema"],
                self.key_schema,
            )

        if self.projection is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["Projection"],
                self.projection,
            )

        if self.index_status is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["IndexStatus"],
                self.index_status,
            )

        if self.backfilling is not None:
            serializer.write_boolean(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["Backfilling"],
                self.backfilling,
            )

        if self.provisioned_throughput is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members[
                    "ProvisionedThroughput"
                ],
                self.provisioned_throughput,
            )

        if self.index_size_bytes is not None:
            serializer.write_long(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["IndexSizeBytes"],
                self.index_size_bytes,
            )

        if self.item_count is not None:
            serializer.write_long(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["ItemCount"],
                self.item_count,
            )

        if self.index_arn is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["IndexArn"],
                self.index_arn,
            )

        if self.on_demand_throughput is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members[
                    "OnDemandThroughput"
                ],
                self.on_demand_throughput,
            )

        if self.warm_throughput is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["WarmThroughput"],
                self.warm_throughput,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["IndexName"]
                    )

                case 1:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de,
                        _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["KeySchema"],
                    )

                case 2:
                    kwargs["projection"] = Projection.deserialize(de)

                case 3:
                    kwargs["index_status"] = IndexStatus(
                        de.read_string(
                            _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members[
                                "IndexStatus"
                            ]
                        )
                    )

                case 4:
                    kwargs["backfilling"] = de.read_boolean(
                        _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members[
                            "Backfilling"
                        ]
                    )

                case 5:
                    kwargs["provisioned_throughput"] = (
                        ProvisionedThroughputDescription.deserialize(de)
                    )

                case 6:
                    kwargs["index_size_bytes"] = de.read_long(
                        _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members[
                            "IndexSizeBytes"
                        ]
                    )

                case 7:
                    kwargs["item_count"] = de.read_long(
                        _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["ItemCount"]
                    )

                case 8:
                    kwargs["index_arn"] = de.read_string(
                        _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION.members["IndexArn"]
                    )

                case 9:
                    kwargs["on_demand_throughput"] = OnDemandThroughput.deserialize(de)

                case 10:
                    kwargs["warm_throughput"] = (
                        GlobalSecondaryIndexWarmThroughputDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GLOBAL_SECONDARY_INDEX_DESCRIPTION, consumer=_consumer
        )
        return kwargs


def _serialize_global_secondary_index_description_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[GlobalSecondaryIndexDescription],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_global_secondary_index_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[GlobalSecondaryIndexDescription]:
    result: list[GlobalSecondaryIndexDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(GlobalSecondaryIndexDescription.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


class WitnessStatus(UnknownEnumMixin, StrEnum):
    CREATING = "CREATING"
    DELETING = "DELETING"
    ACTIVE = "ACTIVE"


@dataclass(kw_only=True)
class GlobalTableWitnessDescription:
    """Represents the properties of a witness Region in a MRSC global table."""

    region_name: str | None = None
    """
    The name of the Amazon Web Services Region that serves as a witness for
    the MRSC global table.
    """

    witness_status: WitnessStatus | None = None
    """The current status of the witness Region in the MRSC global table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GLOBAL_TABLE_WITNESS_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.region_name is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_TABLE_WITNESS_DESCRIPTION.members["RegionName"],
                self.region_name,
            )

        if self.witness_status is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_TABLE_WITNESS_DESCRIPTION.members["WitnessStatus"],
                self.witness_status,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_GLOBAL_TABLE_WITNESS_DESCRIPTION.members["RegionName"]
                    )

                case 1:
                    kwargs["witness_status"] = WitnessStatus(
                        de.read_string(
                            _SCHEMA_GLOBAL_TABLE_WITNESS_DESCRIPTION.members[
                                "WitnessStatus"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GLOBAL_TABLE_WITNESS_DESCRIPTION, consumer=_consumer
        )
        return kwargs


def _serialize_global_table_witness_description_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[GlobalTableWitnessDescription],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_global_table_witness_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[GlobalTableWitnessDescription]:
    result: list[GlobalTableWitnessDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(GlobalTableWitnessDescription.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class LocalSecondaryIndexDescription:
    """Represents the properties of a local secondary index."""

    index_name: str | None = None
    """Represents the name of the local secondary index."""

    key_schema: list[KeySchemaElement] | None = None
    """
    The complete key schema for the local secondary index, consisting of one
    or more pairs of attribute names and key types:

    - `HASH` - partition key

    - `RANGE` - sort key

    Note:
        The partition key of an item is also known as its *hash attribute*. The
        term \"hash attribute\" derives from DynamoDB's usage of an internal
        hash function to evenly distribute data items across partitions, based
        on their partition key values.

        The sort key of an item is also known as its *range attribute*. The term
        \"range attribute\" derives from the way DynamoDB stores items with the
        same partition key physically close together, in sorted order by the
        sort key value.
    """

    projection: Projection | None = None
    """
    Represents attributes that are copied (projected) from the table into
    the global secondary index. These are in addition to the primary key
    attributes and index key attributes, which are automatically projected.
    """

    index_size_bytes: int | None = None
    """
    The total size of the specified index, in bytes. DynamoDB updates this
    value approximately every six hours. Recent changes might not be
    reflected in this value.
    """

    item_count: int | None = None
    """
    The number of items in the specified index. DynamoDB updates this value
    approximately every six hours. Recent changes might not be reflected in
    this value.
    """

    index_arn: str | None = None
    """The Amazon Resource Name (ARN) that uniquely identifies the index."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members["IndexName"],
                self.index_name,
            )

        if self.key_schema is not None:
            _serialize_key_schema(
                serializer,
                _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members["KeySchema"],
                self.key_schema,
            )

        if self.projection is not None:
            serializer.write_struct(
                _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members["Projection"],
                self.projection,
            )

        if self.index_size_bytes is not None:
            serializer.write_long(
                _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members["IndexSizeBytes"],
                self.index_size_bytes,
            )

        if self.item_count is not None:
            serializer.write_long(
                _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members["ItemCount"],
                self.item_count,
            )

        if self.index_arn is not None:
            serializer.write_string(
                _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members["IndexArn"],
                self.index_arn,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members["IndexName"]
                    )

                case 1:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de,
                        _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members["KeySchema"],
                    )

                case 2:
                    kwargs["projection"] = Projection.deserialize(de)

                case 3:
                    kwargs["index_size_bytes"] = de.read_long(
                        _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members[
                            "IndexSizeBytes"
                        ]
                    )

                case 4:
                    kwargs["item_count"] = de.read_long(
                        _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members["ItemCount"]
                    )

                case 5:
                    kwargs["index_arn"] = de.read_string(
                        _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION.members["IndexArn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_LOCAL_SECONDARY_INDEX_DESCRIPTION, consumer=_consumer
        )
        return kwargs


def _serialize_local_secondary_index_description_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[LocalSecondaryIndexDescription],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_local_secondary_index_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[LocalSecondaryIndexDescription]:
    result: list[LocalSecondaryIndexDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(LocalSecondaryIndexDescription.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


class MultiRegionConsistency(UnknownEnumMixin, StrEnum):
    EVENTUAL = "EVENTUAL"
    STRONG = "STRONG"


@dataclass(kw_only=True)
class RestoreSummary:
    """Contains details for the restore."""

    restore_date_time: datetime
    """Point in time or source backup time."""

    restore_in_progress: bool
    """Indicates if a restore is in progress or not."""

    source_backup_arn: str | None = None
    """
    The Amazon Resource Name (ARN) of the backup from which the table was
    restored.
    """

    source_table_arn: str | None = None
    """The ARN of the source table of the backup that is being restored."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RESTORE_SUMMARY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.source_backup_arn is not None:
            serializer.write_string(
                _SCHEMA_RESTORE_SUMMARY.members["SourceBackupArn"],
                self.source_backup_arn,
            )

        if self.source_table_arn is not None:
            serializer.write_string(
                _SCHEMA_RESTORE_SUMMARY.members["SourceTableArn"], self.source_table_arn
            )

        serializer.write_timestamp(
            _SCHEMA_RESTORE_SUMMARY.members["RestoreDateTime"], self.restore_date_time
        )
        serializer.write_boolean(
            _SCHEMA_RESTORE_SUMMARY.members["RestoreInProgress"],
            self.restore_in_progress,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["source_backup_arn"] = de.read_string(
                        _SCHEMA_RESTORE_SUMMARY.members["SourceBackupArn"]
                    )

                case 1:
                    kwargs["source_table_arn"] = de.read_string(
                        _SCHEMA_RESTORE_SUMMARY.members["SourceTableArn"]
                    )

                case 2:
                    kwargs["restore_date_time"] = de.read_timestamp(
                        _SCHEMA_RESTORE_SUMMARY.members["RestoreDateTime"]
                    )

                case 3:
                    kwargs["restore_in_progress"] = de.read_boolean(
                        _SCHEMA_RESTORE_SUMMARY.members["RestoreInProgress"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RESTORE_SUMMARY, consumer=_consumer)
        if "restore_date_time" not in kwargs:
            kwargs["restore_date_time"] = datetime.fromtimestamp(0, tz=timezone.utc)
        if "restore_in_progress" not in kwargs:
            kwargs["restore_in_progress"] = False
        return kwargs


@dataclass(kw_only=True)
class VectorIndexDescription:
    """
    Contains the current state and configuration of a vector index,
    including its status, size, item count, and the settings specified when
    the index was created.
    """

    index_name: str | None = None
    """The name of the vector index."""

    search_schema: list[SearchSchemaElement] | None = None
    """
    The search schema that defines partition key and inline filter
    attributes for the vector index.
    """

    projection: Projection | None = None
    """
    Specifies attributes that are copied (projected) from the table into the
    vector index.
    """

    vector_attribute: VectorAttributeDefinition | None = None
    """The vector attribute configuration for the index."""

    dimensions: int | None = None
    """The number of dimensions in each vector."""

    distance_function: VectorDistanceFunction | None = None
    """The distance function used to calculate similarity between vectors."""

    index_status: IndexStatus | None = None
    """
    The current state of the vector index:

    - `CREATING` - The index is being created.

    - `ACTIVE` - The index is ready for use.

    - `DELETING` - The index is being deleted.
    """

    backfilling: bool | None = None
    """
    Specifies whether the index is currently backfilling. During backfill,
    `SearchVectors` operations might return incomplete results.
    """

    index_size_bytes: int | None = None
    """
    The total size of the vector index, in bytes. Amazon DynamoDB updates
    this value approximately every six hours. Recent changes might not be
    reflected in this value.
    """

    item_count: int | None = None
    """
    The number of items indexed in the vector index. Amazon DynamoDB updates
    this value approximately every six hours. Recent changes might not be
    reflected in this value.
    """

    index_arn: str | None = None
    """
    The Amazon Resource Name (ARN) that uniquely identifies the vector
    index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_VECTOR_INDEX_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["IndexName"], self.index_name
            )

        if self.search_schema is not None:
            _serialize_search_schema(
                serializer,
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["SearchSchema"],
                self.search_schema,
            )

        if self.projection is not None:
            serializer.write_struct(
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["Projection"], self.projection
            )

        if self.vector_attribute is not None:
            serializer.write_struct(
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["VectorAttribute"],
                self.vector_attribute,
            )

        if self.dimensions is not None:
            serializer.write_long(
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["Dimensions"], self.dimensions
            )

        if self.distance_function is not None:
            serializer.write_string(
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["DistanceFunction"],
                self.distance_function,
            )

        if self.index_status is not None:
            serializer.write_string(
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["IndexStatus"],
                self.index_status,
            )

        if self.backfilling is not None:
            serializer.write_boolean(
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["Backfilling"],
                self.backfilling,
            )

        if self.index_size_bytes is not None:
            serializer.write_long(
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["IndexSizeBytes"],
                self.index_size_bytes,
            )

        if self.item_count is not None:
            serializer.write_long(
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["ItemCount"], self.item_count
            )

        if self.index_arn is not None:
            serializer.write_string(
                _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["IndexArn"], self.index_arn
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["IndexName"]
                    )

                case 1:
                    kwargs["search_schema"] = _deserialize_search_schema(
                        de, _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["SearchSchema"]
                    )

                case 2:
                    kwargs["projection"] = Projection.deserialize(de)

                case 3:
                    kwargs["vector_attribute"] = VectorAttributeDefinition.deserialize(
                        de
                    )

                case 4:
                    kwargs["dimensions"] = de.read_long(
                        _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["Dimensions"]
                    )

                case 5:
                    kwargs["distance_function"] = VectorDistanceFunction(
                        de.read_string(
                            _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["DistanceFunction"]
                        )
                    )

                case 6:
                    kwargs["index_status"] = IndexStatus(
                        de.read_string(
                            _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["IndexStatus"]
                        )
                    )

                case 7:
                    kwargs["backfilling"] = de.read_boolean(
                        _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["Backfilling"]
                    )

                case 8:
                    kwargs["index_size_bytes"] = de.read_long(
                        _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["IndexSizeBytes"]
                    )

                case 9:
                    kwargs["item_count"] = de.read_long(
                        _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["ItemCount"]
                    )

                case 10:
                    kwargs["index_arn"] = de.read_string(
                        _SCHEMA_VECTOR_INDEX_DESCRIPTION.members["IndexArn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_VECTOR_INDEX_DESCRIPTION, consumer=_consumer)
        return kwargs


def _serialize_vector_index_description_list(
    serializer: ShapeSerializer, schema: Schema, value: list[VectorIndexDescription]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_vector_index_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[VectorIndexDescription]:
    result: list[VectorIndexDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(VectorIndexDescription.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class TableDescription:
    """Represents the properties of a table."""

    attribute_definitions: list[AttributeDefinition] | None = None
    """
    An array of `AttributeDefinition` objects. Each of these objects
    describes one attribute in the table and index key schema.

    Each `AttributeDefinition` object in this array is composed of:

    - `AttributeName` - The name of the attribute.

    - `AttributeType` - The data type for the attribute.
    """

    table_name: str | None = None
    """The name of the table."""

    key_schema: list[KeySchemaElement] | None = None
    """
    The primary key structure for the table. Each `KeySchemaElement`
    consists of:

    - `AttributeName` - The name of the attribute.

    - `KeyType` - The role of the attribute:

      - `HASH` - partition key

      - `RANGE` - sort key

      Note:
        The partition key of an item is also known as its *hash attribute*.
        The term \"hash attribute\" derives from DynamoDB's usage of an
        internal hash function to evenly distribute data items across
        partitions, based on their partition key values.

        The sort key of an item is also known as its *range attribute*. The
        term \"range attribute\" derives from the way DynamoDB stores items
        with the same partition key physically close together, in sorted order
        by the sort key value.

    For more information about primary keys, see [Primary
    Key](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DataModel.html#DataModelPrimaryKey)
    in the *Amazon DynamoDB Developer Guide*.
    """

    table_status: TableStatus | None = None
    """
    The current state of the table:

    - `CREATING` - The table is being created.

    - `UPDATING` - The table/index configuration is being updated. The
      table/index remains available for data operations when `UPDATING`.

    - `DELETING` - The table is being deleted.

    - `ACTIVE` - The table is ready for use.

    - `INACCESSIBLE_ENCRYPTION_CREDENTIALS` - The KMS key used to encrypt
      the table in inaccessible. Table operations may fail due to failure to
      use the KMS key. DynamoDB will initiate the table archival process
      when a table's KMS key remains inaccessible for more than seven days.

    - `ARCHIVING` - The table is being archived. Operations are not allowed
      until archival is complete.

    - `ARCHIVED` - The table has been archived. See the ArchivalReason for
      more information.
    """

    creation_date_time: datetime | None = None
    """
    The date and time when the table was created, in [UNIX epoch
    time](http://www.epochconverter.com/) format.
    """

    provisioned_throughput: ProvisionedThroughputDescription | None = None
    """
    The provisioned throughput settings for the table, consisting of read
    and write capacity units, along with data about increases and decreases.
    """

    table_size_bytes: int | None = None
    """
    The total size of the specified table, in bytes. DynamoDB updates this
    value approximately every six hours. Recent changes might not be
    reflected in this value.
    """

    item_count: int | None = None
    """
    The number of items in the specified table. DynamoDB updates this value
    approximately every six hours. Recent changes might not be reflected in
    this value.
    """

    table_arn: str | None = None
    """The Amazon Resource Name (ARN) that uniquely identifies the table."""

    table_id: str | None = None
    """
    A unique identifier for the table, in UUID format, generated by DynamoDB
    when the table is created.
    """

    billing_mode_summary: BillingModeSummary | None = None
    """Contains the details for the read/write capacity mode."""

    local_secondary_indexes: list[LocalSecondaryIndexDescription] | None = None
    """
    Represents one or more local secondary indexes on the table. Each index
    is scoped to a given partition key value. Tables with one or more local
    secondary indexes are subject to an item collection size limit, where
    the amount of data within a given item collection cannot exceed 10 GB.
    Each element is composed of:

    - `IndexName` - The name of the local secondary index.

    - `KeySchema` - Specifies the complete index key schema. The attribute
      names in the key schema must be between 1 and 255 characters
      (inclusive). The key schema must begin with the same partition key as
      the table.

    - `Projection` - Specifies attributes that are copied (projected) from
      the table into the index. These are in addition to the primary key
      attributes and index key attributes, which are automatically
      projected. Each attribute specification is composed of:

      - `ProjectionType` - One of the following:

        - `KEYS_ONLY` - Only the index and primary keys are projected into
          the index.

        - `INCLUDE` - Only the specified table attributes are projected into
          the index. The list of projected attributes is in
          `NonKeyAttributes`.

        - `ALL` - All of the table attributes are projected into the index.

      - `NonKeyAttributes` - A list of one or more non-key attribute names
        that are projected into the secondary index. The total count of
        attributes provided in `NonKeyAttributes`, summed across all of the
        secondary indexes, must not exceed 100. If you project the same
        attribute into two different indexes, this counts as two distinct
        attributes when determining the total. This limit only applies when
        you specify the ProjectionType of `INCLUDE`. You still can specify
        the ProjectionType of `ALL` to project all attributes from the
        source table, even if the table has more than 100 attributes.

    - `IndexSizeBytes` - Represents the total size of the index, in bytes.
      DynamoDB updates this value approximately every six hours. Recent
      changes might not be reflected in this value.

    - `ItemCount` - Represents the number of items in the index. DynamoDB
      updates this value approximately every six hours. Recent changes might
      not be reflected in this value.

    If the table is in the `DELETING` state, no information about indexes
    will be returned.
    """

    global_secondary_indexes: list[GlobalSecondaryIndexDescription] | None = None
    """
    The global secondary indexes, if any, on the table. Each index is scoped
    to a given partition key value. Each element is composed of:

    - `Backfilling` - If true, then the index is currently in the
      backfilling phase. Backfilling occurs only when a new global secondary
      index is added to the table. It is the process by which DynamoDB
      populates the new index with data from the table. (This attribute does
      not appear for indexes that were created during a `CreateTable`
      operation.)

      You can delete an index that is being created during the `Backfilling`
      phase when `IndexStatus` is set to CREATING and `Backfilling` is true.
      You can't delete the index that is being created when `IndexStatus`
      is set to CREATING and `Backfilling` is false. (This attribute does
      not appear for indexes that were created during a `CreateTable`
      operation.)

    - `IndexName` - The name of the global secondary index.

    - `IndexSizeBytes` - The total size of the global secondary index, in
      bytes. DynamoDB updates this value approximately every six hours.
      Recent changes might not be reflected in this value.

    - `IndexStatus` - The current status of the global secondary index:

      - `CREATING` - The index is being created.

      - `UPDATING` - The index is being updated.

      - `DELETING` - The index is being deleted.

      - `ACTIVE` - The index is ready for use.

    - `ItemCount` - The number of items in the global secondary index.
      DynamoDB updates this value approximately every six hours. Recent
      changes might not be reflected in this value.

    - `KeySchema` - Specifies the complete index key schema. The attribute
      names in the key schema must be between 1 and 255 characters
      (inclusive). The key schema must begin with the same partition key as
      the table.

    - `Projection` - Specifies attributes that are copied (projected) from
      the table into the index. These are in addition to the primary key
      attributes and index key attributes, which are automatically
      projected. Each attribute specification is composed of:

      - `ProjectionType` - One of the following:

        - `KEYS_ONLY` - Only the index and primary keys are projected into
          the index.

        - `INCLUDE` - In addition to the attributes described in
          `KEYS_ONLY`, the secondary index will include other non-key
          attributes that you specify.

        - `ALL` - All of the table attributes are projected into the index.

      - `NonKeyAttributes` - A list of one or more non-key attribute names
        that are projected into the secondary index. The total count of
        attributes provided in `NonKeyAttributes`, summed across all of the
        secondary indexes, must not exceed 100. If you project the same
        attribute into two different indexes, this counts as two distinct
        attributes when determining the total. This limit only applies when
        you specify the ProjectionType of `INCLUDE`. You still can specify
        the ProjectionType of `ALL` to project all attributes from the
        source table, even if the table has more than 100 attributes.

    - `ProvisionedThroughput` - The provisioned throughput settings for the
      global secondary index, consisting of read and write capacity units,
      along with data about increases and decreases.

    If the table is in the `DELETING` state, no information about indexes
    will be returned.
    """

    stream_specification: StreamSpecification | None = None
    """The current DynamoDB Streams configuration for the table."""

    latest_stream_label: str | None = None
    """
    A timestamp, in ISO 8601 format, for this stream.

    Note that `LatestStreamLabel` is not a unique identifier for the stream,
    because it is possible that a stream from another table might have the
    same timestamp. However, the combination of the following three elements
    is guaranteed to be unique:

    - Amazon Web Services customer ID

    - Table name

    - `StreamLabel`
    """

    latest_stream_arn: str | None = None
    """
    The Amazon Resource Name (ARN) that uniquely identifies the latest
    stream for this table.
    """

    global_table_version: str | None = None
    """
    Represents the version of [global
    tables](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/GlobalTables.html)
    in use, if the table is replicated across Amazon Web Services Regions.
    """

    replicas: list[ReplicaDescription] | None = None
    """Represents replicas of the table."""

    global_table_witnesses: list[GlobalTableWitnessDescription] | None = None
    """
    The witness Region and its current status in the MRSC global table. Only
    one witness Region can be configured per MRSC global table.
    """

    global_table_settings_replication_mode: (
        GlobalTableSettingsReplicationMode | None
    ) = None
    """
    Indicates one of the settings synchronization modes for the global
    table:

    - `ENABLED`: Indicates that the settings synchronization mode for the
      global table is enabled.

    - `DISABLED`: Indicates that the settings synchronization mode for the
      global table is disabled.

    - `ENABLED_WITH_OVERRIDES`: This mode is set by default for a same
      account global table. Indicates that certain global table settings can
      be overridden.
    """

    restore_summary: RestoreSummary | None = None
    """Contains details for the restore."""

    sse_description: SSEDescription | None = None
    """
    The description of the server-side encryption status on the specified
    table.
    """

    archival_summary: ArchivalSummary | None = None
    """Contains information about the table archive."""

    table_class_summary: TableClassSummary | None = None
    """Contains details of the table class."""

    deletion_protection_enabled: bool | None = None
    """
    Indicates whether deletion protection is enabled (true) or disabled
    (false) on the table.
    """

    on_demand_throughput: OnDemandThroughput | None = None
    """
    The maximum number of read and write units for the specified on-demand
    table. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    warm_throughput: TableWarmThroughputDescription | None = None
    """Describes the warm throughput value of the base table."""

    multi_region_consistency: MultiRegionConsistency | None = None
    """
    Indicates one of the following consistency modes for a global table:

    - `EVENTUAL`: Indicates that the global table is configured for
      multi-Region eventual consistency (MREC).

    - `STRONG`: Indicates that the global table is configured for
      multi-Region strong consistency (MRSC).

    If you don't specify this field, the global table consistency mode
    defaults to `EVENTUAL`. For more information about global tables
    consistency modes, see [Consistency
    modes](https://docs.aws.amazon.com/V2globaltables_HowItWorks.html#V2globaltables_HowItWorks.consistency-modes)
    in DynamoDB developer guide.
    """

    vector_indexes: list[VectorIndexDescription] | None = None
    """
    The vector indexes, if any, on the table. Each element is composed of:

    - `IndexName` - The name of the vector index.

    - `IndexStatus` - The current status of the vector index: `CREATING`,
      `ACTIVE`, or `DELETING`.

    - `Backfilling` - Specifies whether the index is currently backfilling.
      During backfill, `SearchVectors` operations might return incomplete
      results.

    - `VectorAttribute` - The attribute that contains vector embeddings.

    - `Dimensions` - The number of dimensions in each vector.

    - `DistanceFunction` - The distance function used to calculate
      similarity (`COSINE`, `EUCLIDEAN`, or `DOT_PRODUCT`).

    - `SearchSchema` - The partition key and inline filter attributes for
      the vector index.

    - `Projection` - Specifies attributes that are copied (projected) from
      the table into the vector index.

    - `IndexArn` - The Amazon Resource Name (ARN) that uniquely identifies
      the index.

    - `IndexSizeBytes` - The total size of the vector index, in bytes.
      Amazon DynamoDB updates this value approximately every six hours.
      Recent changes might not be reflected in this value.

    - `ItemCount` - The number of items indexed in the vector index. Amazon
      DynamoDB updates this value approximately every six hours. Recent
      changes might not be reflected in this value.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TABLE_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.attribute_definitions is not None:
            _serialize_attribute_definitions(
                serializer,
                _SCHEMA_TABLE_DESCRIPTION.members["AttributeDefinitions"],
                self.attribute_definitions,
            )

        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_TABLE_DESCRIPTION.members["TableName"], self.table_name
            )

        if self.key_schema is not None:
            _serialize_key_schema(
                serializer,
                _SCHEMA_TABLE_DESCRIPTION.members["KeySchema"],
                self.key_schema,
            )

        if self.table_status is not None:
            serializer.write_string(
                _SCHEMA_TABLE_DESCRIPTION.members["TableStatus"], self.table_status
            )

        if self.creation_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_TABLE_DESCRIPTION.members["CreationDateTime"],
                self.creation_date_time,
            )

        if self.provisioned_throughput is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_DESCRIPTION.members["ProvisionedThroughput"],
                self.provisioned_throughput,
            )

        if self.table_size_bytes is not None:
            serializer.write_long(
                _SCHEMA_TABLE_DESCRIPTION.members["TableSizeBytes"],
                self.table_size_bytes,
            )

        if self.item_count is not None:
            serializer.write_long(
                _SCHEMA_TABLE_DESCRIPTION.members["ItemCount"], self.item_count
            )

        if self.table_arn is not None:
            serializer.write_string(
                _SCHEMA_TABLE_DESCRIPTION.members["TableArn"], self.table_arn
            )

        if self.table_id is not None:
            serializer.write_string(
                _SCHEMA_TABLE_DESCRIPTION.members["TableId"], self.table_id
            )

        if self.billing_mode_summary is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_DESCRIPTION.members["BillingModeSummary"],
                self.billing_mode_summary,
            )

        if self.local_secondary_indexes is not None:
            _serialize_local_secondary_index_description_list(
                serializer,
                _SCHEMA_TABLE_DESCRIPTION.members["LocalSecondaryIndexes"],
                self.local_secondary_indexes,
            )

        if self.global_secondary_indexes is not None:
            _serialize_global_secondary_index_description_list(
                serializer,
                _SCHEMA_TABLE_DESCRIPTION.members["GlobalSecondaryIndexes"],
                self.global_secondary_indexes,
            )

        if self.stream_specification is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_DESCRIPTION.members["StreamSpecification"],
                self.stream_specification,
            )

        if self.latest_stream_label is not None:
            serializer.write_string(
                _SCHEMA_TABLE_DESCRIPTION.members["LatestStreamLabel"],
                self.latest_stream_label,
            )

        if self.latest_stream_arn is not None:
            serializer.write_string(
                _SCHEMA_TABLE_DESCRIPTION.members["LatestStreamArn"],
                self.latest_stream_arn,
            )

        if self.global_table_version is not None:
            serializer.write_string(
                _SCHEMA_TABLE_DESCRIPTION.members["GlobalTableVersion"],
                self.global_table_version,
            )

        if self.replicas is not None:
            _serialize_replica_description_list(
                serializer, _SCHEMA_TABLE_DESCRIPTION.members["Replicas"], self.replicas
            )

        if self.global_table_witnesses is not None:
            _serialize_global_table_witness_description_list(
                serializer,
                _SCHEMA_TABLE_DESCRIPTION.members["GlobalTableWitnesses"],
                self.global_table_witnesses,
            )

        if self.global_table_settings_replication_mode is not None:
            serializer.write_string(
                _SCHEMA_TABLE_DESCRIPTION.members["GlobalTableSettingsReplicationMode"],
                self.global_table_settings_replication_mode,
            )

        if self.restore_summary is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_DESCRIPTION.members["RestoreSummary"],
                self.restore_summary,
            )

        if self.sse_description is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_DESCRIPTION.members["SSEDescription"],
                self.sse_description,
            )

        if self.archival_summary is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_DESCRIPTION.members["ArchivalSummary"],
                self.archival_summary,
            )

        if self.table_class_summary is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_DESCRIPTION.members["TableClassSummary"],
                self.table_class_summary,
            )

        if self.deletion_protection_enabled is not None:
            serializer.write_boolean(
                _SCHEMA_TABLE_DESCRIPTION.members["DeletionProtectionEnabled"],
                self.deletion_protection_enabled,
            )

        if self.on_demand_throughput is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_DESCRIPTION.members["OnDemandThroughput"],
                self.on_demand_throughput,
            )

        if self.warm_throughput is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_DESCRIPTION.members["WarmThroughput"],
                self.warm_throughput,
            )

        if self.multi_region_consistency is not None:
            serializer.write_string(
                _SCHEMA_TABLE_DESCRIPTION.members["MultiRegionConsistency"],
                self.multi_region_consistency,
            )

        if self.vector_indexes is not None:
            _serialize_vector_index_description_list(
                serializer,
                _SCHEMA_TABLE_DESCRIPTION.members["VectorIndexes"],
                self.vector_indexes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attribute_definitions"] = (
                        _deserialize_attribute_definitions(
                            de,
                            _SCHEMA_TABLE_DESCRIPTION.members["AttributeDefinitions"],
                        )
                    )

                case 1:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_TABLE_DESCRIPTION.members["TableName"]
                    )

                case 2:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de, _SCHEMA_TABLE_DESCRIPTION.members["KeySchema"]
                    )

                case 3:
                    kwargs["table_status"] = TableStatus(
                        de.read_string(_SCHEMA_TABLE_DESCRIPTION.members["TableStatus"])
                    )

                case 4:
                    kwargs["creation_date_time"] = de.read_timestamp(
                        _SCHEMA_TABLE_DESCRIPTION.members["CreationDateTime"]
                    )

                case 5:
                    kwargs["provisioned_throughput"] = (
                        ProvisionedThroughputDescription.deserialize(de)
                    )

                case 6:
                    kwargs["table_size_bytes"] = de.read_long(
                        _SCHEMA_TABLE_DESCRIPTION.members["TableSizeBytes"]
                    )

                case 7:
                    kwargs["item_count"] = de.read_long(
                        _SCHEMA_TABLE_DESCRIPTION.members["ItemCount"]
                    )

                case 8:
                    kwargs["table_arn"] = de.read_string(
                        _SCHEMA_TABLE_DESCRIPTION.members["TableArn"]
                    )

                case 9:
                    kwargs["table_id"] = de.read_string(
                        _SCHEMA_TABLE_DESCRIPTION.members["TableId"]
                    )

                case 10:
                    kwargs["billing_mode_summary"] = BillingModeSummary.deserialize(de)

                case 11:
                    kwargs["local_secondary_indexes"] = (
                        _deserialize_local_secondary_index_description_list(
                            de,
                            _SCHEMA_TABLE_DESCRIPTION.members["LocalSecondaryIndexes"],
                        )
                    )

                case 12:
                    kwargs["global_secondary_indexes"] = (
                        _deserialize_global_secondary_index_description_list(
                            de,
                            _SCHEMA_TABLE_DESCRIPTION.members["GlobalSecondaryIndexes"],
                        )
                    )

                case 13:
                    kwargs["stream_specification"] = StreamSpecification.deserialize(de)

                case 14:
                    kwargs["latest_stream_label"] = de.read_string(
                        _SCHEMA_TABLE_DESCRIPTION.members["LatestStreamLabel"]
                    )

                case 15:
                    kwargs["latest_stream_arn"] = de.read_string(
                        _SCHEMA_TABLE_DESCRIPTION.members["LatestStreamArn"]
                    )

                case 16:
                    kwargs["global_table_version"] = de.read_string(
                        _SCHEMA_TABLE_DESCRIPTION.members["GlobalTableVersion"]
                    )

                case 17:
                    kwargs["replicas"] = _deserialize_replica_description_list(
                        de, _SCHEMA_TABLE_DESCRIPTION.members["Replicas"]
                    )

                case 18:
                    kwargs["global_table_witnesses"] = (
                        _deserialize_global_table_witness_description_list(
                            de,
                            _SCHEMA_TABLE_DESCRIPTION.members["GlobalTableWitnesses"],
                        )
                    )

                case 19:
                    kwargs["global_table_settings_replication_mode"] = (
                        GlobalTableSettingsReplicationMode(
                            de.read_string(
                                _SCHEMA_TABLE_DESCRIPTION.members[
                                    "GlobalTableSettingsReplicationMode"
                                ]
                            )
                        )
                    )

                case 20:
                    kwargs["restore_summary"] = RestoreSummary.deserialize(de)

                case 21:
                    kwargs["sse_description"] = SSEDescription.deserialize(de)

                case 22:
                    kwargs["archival_summary"] = ArchivalSummary.deserialize(de)

                case 23:
                    kwargs["table_class_summary"] = TableClassSummary.deserialize(de)

                case 24:
                    kwargs["deletion_protection_enabled"] = de.read_boolean(
                        _SCHEMA_TABLE_DESCRIPTION.members["DeletionProtectionEnabled"]
                    )

                case 25:
                    kwargs["on_demand_throughput"] = OnDemandThroughput.deserialize(de)

                case 26:
                    kwargs["warm_throughput"] = (
                        TableWarmThroughputDescription.deserialize(de)
                    )

                case 27:
                    kwargs["multi_region_consistency"] = MultiRegionConsistency(
                        de.read_string(
                            _SCHEMA_TABLE_DESCRIPTION.members["MultiRegionConsistency"]
                        )
                    )

                case 28:
                    kwargs["vector_indexes"] = (
                        _deserialize_vector_index_description_list(
                            de, _SCHEMA_TABLE_DESCRIPTION.members["VectorIndexes"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TABLE_DESCRIPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class CreateTableOutput:
    """Represents the output of a `CreateTable` operation."""

    table_description: TableDescription | None = None
    """Represents the properties of the table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_TABLE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_description is not None:
            serializer.write_struct(
                _SCHEMA_CREATE_TABLE_OUTPUT.members["TableDescription"],
                self.table_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_description"] = TableDescription.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREATE_TABLE_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ResourceInUseException(ServiceError):
    """
    The operation conflicts with the resource's availability. For example:

    - You attempted to recreate an existing table.

    - You tried to delete a table currently in the `CREATING` state.

    - You tried to update a resource that was already being updated.

    When appropriate, wait for the ongoing update to complete and attempt
    the request again.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RESOURCE_IN_USE_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_RESOURCE_IN_USE_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_RESOURCE_IN_USE_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RESOURCE_IN_USE_EXCEPTION, consumer=_consumer)
        return kwargs


CREATE_TABLE = APIOperation(
    input=CreateTableInput,
    output=CreateTableOutput,
    schema=_SCHEMA_CREATE_TABLE,
    input_schema=_SCHEMA_CREATE_TABLE_INPUT,
    output_schema=_SCHEMA_CREATE_TABLE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class CreateVectorIndexAction:
    """A new vector index to be added to a table."""

    index_name: str
    """The name of the vector index. Must be unique within the table."""

    vector_attribute: VectorAttributeDefinition
    """
    The attribute that contains vector embeddings. If multiple vector
    indexes reference the same attribute, they must all use the same number
    of dimensions.
    """

    projection: Projection
    """
    Specifies attributes that are copied (projected) from the table into the
    vector index.
    """

    dimensions: int
    """The number of dimensions in each vector."""

    distance_function: VectorDistanceFunction
    """
    The distance function used to calculate similarity. Valid values:
    `COSINE`, `EUCLIDEAN`, `DOT_PRODUCT`.
    """

    search_schema: list[SearchSchemaElement] | None = None
    """
    The partition key and inline filter attribute definitions for the vector
    index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_VECTOR_INDEX_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_CREATE_VECTOR_INDEX_ACTION.members["IndexName"], self.index_name
        )
        serializer.write_struct(
            _SCHEMA_CREATE_VECTOR_INDEX_ACTION.members["VectorAttribute"],
            self.vector_attribute,
        )
        if self.search_schema is not None:
            _serialize_search_schema(
                serializer,
                _SCHEMA_CREATE_VECTOR_INDEX_ACTION.members["SearchSchema"],
                self.search_schema,
            )

        serializer.write_struct(
            _SCHEMA_CREATE_VECTOR_INDEX_ACTION.members["Projection"], self.projection
        )
        serializer.write_long(
            _SCHEMA_CREATE_VECTOR_INDEX_ACTION.members["Dimensions"], self.dimensions
        )
        serializer.write_string(
            _SCHEMA_CREATE_VECTOR_INDEX_ACTION.members["DistanceFunction"],
            self.distance_function,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_CREATE_VECTOR_INDEX_ACTION.members["IndexName"]
                    )

                case 1:
                    kwargs["vector_attribute"] = VectorAttributeDefinition.deserialize(
                        de
                    )

                case 2:
                    kwargs["search_schema"] = _deserialize_search_schema(
                        de, _SCHEMA_CREATE_VECTOR_INDEX_ACTION.members["SearchSchema"]
                    )

                case 3:
                    kwargs["projection"] = Projection.deserialize(de)

                case 4:
                    kwargs["dimensions"] = de.read_long(
                        _SCHEMA_CREATE_VECTOR_INDEX_ACTION.members["Dimensions"]
                    )

                case 5:
                    kwargs["distance_function"] = VectorDistanceFunction(
                        de.read_string(
                            _SCHEMA_CREATE_VECTOR_INDEX_ACTION.members[
                                "DistanceFunction"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREATE_VECTOR_INDEX_ACTION, consumer=_consumer)
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        if "vector_attribute" not in kwargs:
            kwargs["vector_attribute"] = VectorAttributeDefinition._smithy_default()
        if "projection" not in kwargs:
            kwargs["projection"] = Projection._smithy_default()
        if "dimensions" not in kwargs:
            kwargs["dimensions"] = 0
        if "distance_function" not in kwargs:
            kwargs["distance_function"] = VectorDistanceFunction._corrected("")
        return kwargs


def _serialize_csv_header_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_csv_header_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class CsvOptions:
    """Processing options for the CSV file being imported."""

    delimiter: str | None = None
    """The delimiter used for separating items in the CSV file being imported."""

    header_list: list[str] | None = None
    """
    List of the headers used to specify a common header for all source CSV
    files being imported. If this field is specified then the first line of
    each CSV file is treated as data instead of the header. If this field is
    not specified the the first line of each CSV file is treated as the
    header.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CSV_OPTIONS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.delimiter is not None:
            serializer.write_string(
                _SCHEMA_CSV_OPTIONS.members["Delimiter"], self.delimiter
            )

        if self.header_list is not None:
            _serialize_csv_header_list(
                serializer, _SCHEMA_CSV_OPTIONS.members["HeaderList"], self.header_list
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["delimiter"] = de.read_string(
                        _SCHEMA_CSV_OPTIONS.members["Delimiter"]
                    )

                case 1:
                    kwargs["header_list"] = _deserialize_csv_header_list(
                        de, _SCHEMA_CSV_OPTIONS.members["HeaderList"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CSV_OPTIONS, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DeleteBackupInput:
    """Dataclass for DeleteBackupInput structure."""

    backup_arn: str | None = None
    """The ARN associated with the backup."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_BACKUP_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.backup_arn is not None:
            serializer.write_string(
                _SCHEMA_DELETE_BACKUP_INPUT.members["BackupArn"], self.backup_arn
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["backup_arn"] = de.read_string(
                        _SCHEMA_DELETE_BACKUP_INPUT.members["BackupArn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_BACKUP_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DeleteBackupOutput:
    """Dataclass for DeleteBackupOutput structure."""

    backup_description: BackupDescription | None = None
    """Contains the description of the backup created for the table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_BACKUP_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.backup_description is not None:
            serializer.write_struct(
                _SCHEMA_DELETE_BACKUP_OUTPUT.members["BackupDescription"],
                self.backup_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["backup_description"] = BackupDescription.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_BACKUP_OUTPUT, consumer=_consumer)
        return kwargs


DELETE_BACKUP = APIOperation(
    input=DeleteBackupInput,
    output=DeleteBackupOutput,
    schema=_SCHEMA_DELETE_BACKUP,
    input_schema=_SCHEMA_DELETE_BACKUP_INPUT,
    output_schema=_SCHEMA_DELETE_BACKUP_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#BackupInUseException"
            ): BackupInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#BackupNotFoundException"
            ): BackupNotFoundException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_BACKUP_IN_USE_EXCEPTION,
        _SCHEMA_BACKUP_NOT_FOUND_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DeleteGlobalSecondaryIndexAction:
    """
    Represents a global secondary index to be deleted from an existing
    table.
    """

    index_name: str
    """The name of the global secondary index to be deleted."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_GLOBAL_SECONDARY_INDEX_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_DELETE_GLOBAL_SECONDARY_INDEX_ACTION.members["IndexName"],
            self.index_name,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_DELETE_GLOBAL_SECONDARY_INDEX_ACTION.members[
                            "IndexName"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DELETE_GLOBAL_SECONDARY_INDEX_ACTION, consumer=_consumer
        )
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class DeleteGlobalTableWitnessGroupMemberAction:
    """
    Specifies the action to remove a witness Region from a MRSC global
    table. You cannot delete a single witness from a MRSC global table - you
    must delete both a replica and the witness together. The deletion of
    both a witness and replica converts the remaining replica to a
    single-Region DynamoDB table.
    """

    region_name: str
    """The witness Region name to be removed from the MRSC global table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_DELETE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_DELETE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION.members[
                "RegionName"
            ],
            self.region_name,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_DELETE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION.members[
                            "RegionName"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DELETE_GLOBAL_TABLE_WITNESS_GROUP_MEMBER_ACTION, consumer=_consumer
        )
        if "region_name" not in kwargs:
            kwargs["region_name"] = ""
        return kwargs


class ReturnValue(UnknownEnumMixin, StrEnum):
    NONE = "NONE"
    ALL_OLD = "ALL_OLD"
    UPDATED_OLD = "UPDATED_OLD"
    ALL_NEW = "ALL_NEW"
    UPDATED_NEW = "UPDATED_NEW"


@dataclass(kw_only=True)
class TransactionConflictException(ServiceError):
    """
    Operation was rejected because there is an ongoing transaction for the
    item.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSACTION_CONFLICT_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_TRANSACTION_CONFLICT_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_TRANSACTION_CONFLICT_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_TRANSACTION_CONFLICT_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class DeleteReplicaAction:
    """Represents a replica to be removed."""

    region_name: str
    """The Region of the replica to be removed."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_REPLICA_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_DELETE_REPLICA_ACTION.members["RegionName"], self.region_name
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_DELETE_REPLICA_ACTION.members["RegionName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_REPLICA_ACTION, consumer=_consumer)
        if "region_name" not in kwargs:
            kwargs["region_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class DeleteReplicationGroupMemberAction:
    """Represents a replica to be deleted."""

    region_name: str
    """The Region where the replica exists."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_REPLICATION_GROUP_MEMBER_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_DELETE_REPLICATION_GROUP_MEMBER_ACTION.members["RegionName"],
            self.region_name,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_DELETE_REPLICATION_GROUP_MEMBER_ACTION.members[
                            "RegionName"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DELETE_REPLICATION_GROUP_MEMBER_ACTION, consumer=_consumer
        )
        if "region_name" not in kwargs:
            kwargs["region_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class DeleteResourcePolicyInput:
    """Dataclass for DeleteResourcePolicyInput structure."""

    resource_arn: str | None = None
    """
    The Amazon Resource Name (ARN) of the DynamoDB resource from which the
    policy will be removed. The resources you can specify include tables and
    streams. If you remove the policy of a table, it will also remove the
    permissions for the table's indexes defined in that policy document.
    This is because index permissions are defined in the table's policy.
    """

    expected_revision_id: str | None = None
    """
    A string value that you can use to conditionally delete your policy.
    When you provide an expected revision ID, if the revision ID of the
    existing policy on the resource doesn't match or if there's no policy
    attached to the resource, the request will fail and return a
    `PolicyNotFoundException`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_RESOURCE_POLICY_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.resource_arn is not None:
            serializer.write_string(
                _SCHEMA_DELETE_RESOURCE_POLICY_INPUT.members["ResourceArn"],
                self.resource_arn,
            )

        if self.expected_revision_id is not None:
            serializer.write_string(
                _SCHEMA_DELETE_RESOURCE_POLICY_INPUT.members["ExpectedRevisionId"],
                self.expected_revision_id,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["resource_arn"] = de.read_string(
                        _SCHEMA_DELETE_RESOURCE_POLICY_INPUT.members["ResourceArn"]
                    )

                case 1:
                    kwargs["expected_revision_id"] = de.read_string(
                        _SCHEMA_DELETE_RESOURCE_POLICY_INPUT.members[
                            "ExpectedRevisionId"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DELETE_RESOURCE_POLICY_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class DeleteResourcePolicyOutput:
    """Dataclass for DeleteResourcePolicyOutput structure."""

    revision_id: str | None = None
    """
    A unique string that represents the revision ID of the policy. If
    you're comparing revision IDs, make sure to always use string
    comparison logic.

    This value will be empty if you make a request against a resource
    without a policy.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_RESOURCE_POLICY_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.revision_id is not None:
            serializer.write_string(
                _SCHEMA_DELETE_RESOURCE_POLICY_OUTPUT.members["RevisionId"],
                self.revision_id,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["revision_id"] = de.read_string(
                        _SCHEMA_DELETE_RESOURCE_POLICY_OUTPUT.members["RevisionId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DELETE_RESOURCE_POLICY_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class PolicyNotFoundException(ServiceError):
    """
    The operation tried to access a nonexistent resource-based policy.

    If you specified an `ExpectedRevisionId`, it's possible that a policy
    is present for the resource but its revision ID didn't match the
    expected value.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_POLICY_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_POLICY_NOT_FOUND_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_POLICY_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_POLICY_NOT_FOUND_EXCEPTION, consumer=_consumer)
        return kwargs


DELETE_RESOURCE_POLICY = APIOperation(
    input=DeleteResourcePolicyInput,
    output=DeleteResourcePolicyOutput,
    schema=_SCHEMA_DELETE_RESOURCE_POLICY,
    input_schema=_SCHEMA_DELETE_RESOURCE_POLICY_INPUT,
    output_schema=_SCHEMA_DELETE_RESOURCE_POLICY_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#PolicyNotFoundException"
            ): PolicyNotFoundException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_POLICY_NOT_FOUND_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DeleteTableInput:
    """Represents the input of a `DeleteTable` operation."""

    table_name: str | None = None
    """
    The name of the table to delete. You can also provide the Amazon
    Resource Name (ARN) of the table in this parameter.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_TABLE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DELETE_TABLE_INPUT.members["TableName"], self.table_name
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DELETE_TABLE_INPUT.members["TableName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_TABLE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DeleteTableOutput:
    """Represents the output of a `DeleteTable` operation."""

    table_description: TableDescription | None = None
    """Represents the properties of a table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_TABLE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_description is not None:
            serializer.write_struct(
                _SCHEMA_DELETE_TABLE_OUTPUT.members["TableDescription"],
                self.table_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_description"] = TableDescription.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_TABLE_OUTPUT, consumer=_consumer)
        return kwargs


DELETE_TABLE = APIOperation(
    input=DeleteTableInput,
    output=DeleteTableOutput,
    schema=_SCHEMA_DELETE_TABLE,
    input_schema=_SCHEMA_DELETE_TABLE_INPUT,
    output_schema=_SCHEMA_DELETE_TABLE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DeleteVectorIndexAction:
    """A vector index to be removed from a table."""

    index_name: str
    """The name of the vector index to delete."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_VECTOR_INDEX_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_DELETE_VECTOR_INDEX_ACTION.members["IndexName"], self.index_name
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_DELETE_VECTOR_INDEX_ACTION.members["IndexName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_VECTOR_INDEX_ACTION, consumer=_consumer)
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class DescribeBackupInput:
    """Dataclass for DescribeBackupInput structure."""

    backup_arn: str | None = None
    """The Amazon Resource Name (ARN) associated with the backup."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_BACKUP_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.backup_arn is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_BACKUP_INPUT.members["BackupArn"], self.backup_arn
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["backup_arn"] = de.read_string(
                        _SCHEMA_DESCRIBE_BACKUP_INPUT.members["BackupArn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_BACKUP_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DescribeBackupOutput:
    """Dataclass for DescribeBackupOutput structure."""

    backup_description: BackupDescription | None = None
    """Contains the description of the backup created for the table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_BACKUP_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.backup_description is not None:
            serializer.write_struct(
                _SCHEMA_DESCRIBE_BACKUP_OUTPUT.members["BackupDescription"],
                self.backup_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["backup_description"] = BackupDescription.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_BACKUP_OUTPUT, consumer=_consumer)
        return kwargs


DESCRIBE_BACKUP = APIOperation(
    input=DescribeBackupInput,
    output=DescribeBackupOutput,
    schema=_SCHEMA_DESCRIBE_BACKUP,
    input_schema=_SCHEMA_DESCRIBE_BACKUP_INPUT,
    output_schema=_SCHEMA_DESCRIBE_BACKUP_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#BackupNotFoundException"
            ): BackupNotFoundException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_BACKUP_NOT_FOUND_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DescribeContinuousBackupsInput:
    """Dataclass for DescribeContinuousBackupsInput structure."""

    table_name: str | None = None
    """
    Name of the table for which the customer wants to check the continuous
    backups and point in time recovery settings.

    You can also provide the Amazon Resource Name (ARN) of the table in this
    parameter.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_INPUT.members["TableName"],
                self.table_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_INPUT.members["TableName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class DescribeContinuousBackupsOutput:
    """Dataclass for DescribeContinuousBackupsOutput structure."""

    continuous_backups_description: ContinuousBackupsDescription | None = None
    """
    Represents the continuous backups and point in time recovery settings on
    the table.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.continuous_backups_description is not None:
            serializer.write_struct(
                _SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_OUTPUT.members[
                    "ContinuousBackupsDescription"
                ],
                self.continuous_backups_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["continuous_backups_description"] = (
                        ContinuousBackupsDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_OUTPUT, consumer=_consumer
        )
        return kwargs


DESCRIBE_CONTINUOUS_BACKUPS = APIOperation(
    input=DescribeContinuousBackupsInput,
    output=DescribeContinuousBackupsOutput,
    schema=_SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS,
    input_schema=_SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_INPUT,
    output_schema=_SCHEMA_DESCRIBE_CONTINUOUS_BACKUPS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#TableNotFoundException"
            ): TableNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_TABLE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DescribeContributorInsightsInput:
    """Dataclass for DescribeContributorInsightsInput structure."""

    table_name: str | None = None
    """
    The name of the table to describe. You can also provide the Amazon
    Resource Name (ARN) of the table in this parameter.
    """

    index_name: str | None = None
    """The name of the global secondary index to describe, if applicable."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_INPUT.members["TableName"],
                self.table_name,
            )

        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_INPUT.members["IndexName"],
                self.index_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_INPUT.members["IndexName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class FailureException:
    """Represents a failure a contributor insights operation."""

    exception_name: str | None = None
    """Exception name."""

    exception_description: str | None = None
    """Description of the failure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_FAILURE_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.exception_name is not None:
            serializer.write_string(
                _SCHEMA_FAILURE_EXCEPTION.members["ExceptionName"], self.exception_name
            )

        if self.exception_description is not None:
            serializer.write_string(
                _SCHEMA_FAILURE_EXCEPTION.members["ExceptionDescription"],
                self.exception_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["exception_name"] = de.read_string(
                        _SCHEMA_FAILURE_EXCEPTION.members["ExceptionName"]
                    )

                case 1:
                    kwargs["exception_description"] = de.read_string(
                        _SCHEMA_FAILURE_EXCEPTION.members["ExceptionDescription"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_FAILURE_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DescribeContributorInsightsOutput:
    """Dataclass for DescribeContributorInsightsOutput structure."""

    table_name: str | None = None
    """The name of the table being described."""

    index_name: str | None = None
    """The name of the global secondary index being described."""

    contributor_insights_rule_list: list[str] | None = None
    """List of names of the associated contributor insights rules."""

    contributor_insights_status: ContributorInsightsStatus | None = None
    """Current status of contributor insights."""

    last_update_date_time: datetime | None = None
    """Timestamp of the last time the status was changed."""

    failure_exception: FailureException | None = None
    """
    Returns information about the last failure that was encountered.

    The most common exceptions for a FAILED status are:

    - LimitExceededException - Per-account Amazon CloudWatch Contributor
      Insights rule limit reached. Please disable Contributor Insights for
      other tables/indexes OR disable Contributor Insights rules before
      retrying.

    - AccessDeniedException - Amazon CloudWatch Contributor Insights rules
      cannot be modified due to insufficient permissions.

    - AccessDeniedException - Failed to create service-linked role for
      Contributor Insights due to insufficient permissions.

    - InternalServerError - Failed to create Amazon CloudWatch Contributor
      Insights rules. Please retry request.
    """

    contributor_insights_mode: ContributorInsightsMode | None = None
    """
    The mode of CloudWatch Contributor Insights for DynamoDB that determines
    which events are emitted. Can be set to track all access and throttled
    events or throttled events only.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members["TableName"],
                self.table_name,
            )

        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members["IndexName"],
                self.index_name,
            )

        if self.contributor_insights_rule_list is not None:
            _serialize_contributor_insights_rule_list(
                serializer,
                _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                    "ContributorInsightsRuleList"
                ],
                self.contributor_insights_rule_list,
            )

        if self.contributor_insights_status is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                    "ContributorInsightsStatus"
                ],
                self.contributor_insights_status,
            )

        if self.last_update_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                    "LastUpdateDateTime"
                ],
                self.last_update_date_time,
            )

        if self.failure_exception is not None:
            serializer.write_struct(
                _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                    "FailureException"
                ],
                self.failure_exception,
            )

        if self.contributor_insights_mode is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                    "ContributorInsightsMode"
                ],
                self.contributor_insights_mode,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                            "TableName"
                        ]
                    )

                case 1:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                            "IndexName"
                        ]
                    )

                case 2:
                    kwargs["contributor_insights_rule_list"] = (
                        _deserialize_contributor_insights_rule_list(
                            de,
                            _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                                "ContributorInsightsRuleList"
                            ],
                        )
                    )

                case 3:
                    kwargs["contributor_insights_status"] = ContributorInsightsStatus(
                        de.read_string(
                            _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                                "ContributorInsightsStatus"
                            ]
                        )
                    )

                case 4:
                    kwargs["last_update_date_time"] = de.read_timestamp(
                        _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                            "LastUpdateDateTime"
                        ]
                    )

                case 5:
                    kwargs["failure_exception"] = FailureException.deserialize(de)

                case 6:
                    kwargs["contributor_insights_mode"] = ContributorInsightsMode(
                        de.read_string(
                            _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                                "ContributorInsightsMode"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT, consumer=_consumer
        )
        return kwargs


DESCRIBE_CONTRIBUTOR_INSIGHTS = APIOperation(
    input=DescribeContributorInsightsInput,
    output=DescribeContributorInsightsOutput,
    schema=_SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS,
    input_schema=_SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_INPUT,
    output_schema=_SCHEMA_DESCRIBE_CONTRIBUTOR_INSIGHTS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_INTERNAL_SERVER_ERROR, _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION],
)


@dataclass(kw_only=True)
class DescribeEndpointsInput:
    """Dataclass for DescribeEndpointsInput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_ENDPOINTS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_ENDPOINTS_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class Endpoint:
    """An endpoint information details."""

    address: str
    """IP address of the endpoint."""

    cache_period_in_minutes: int = 0
    """Endpoint cache time to live (TTL) value."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ENDPOINT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_ENDPOINT.members["Address"], self.address)
        serializer.write_long(
            _SCHEMA_ENDPOINT.members["CachePeriodInMinutes"],
            self.cache_period_in_minutes,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["address"] = de.read_string(
                        _SCHEMA_ENDPOINT.members["Address"]
                    )

                case 1:
                    kwargs["cache_period_in_minutes"] = de.read_long(
                        _SCHEMA_ENDPOINT.members["CachePeriodInMinutes"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ENDPOINT, consumer=_consumer)
        if "address" not in kwargs:
            kwargs["address"] = ""
        return kwargs


def _serialize_endpoints(
    serializer: ShapeSerializer, schema: Schema, value: list[Endpoint]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_endpoints(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[Endpoint]:
    result: list[Endpoint] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(Endpoint.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class DescribeEndpointsOutput:
    """Dataclass for DescribeEndpointsOutput structure."""

    endpoints: list[Endpoint]
    """List of endpoints."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_ENDPOINTS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_endpoints(
            serializer,
            _SCHEMA_DESCRIBE_ENDPOINTS_OUTPUT.members["Endpoints"],
            self.endpoints,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["endpoints"] = _deserialize_endpoints(
                        de, _SCHEMA_DESCRIBE_ENDPOINTS_OUTPUT.members["Endpoints"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_ENDPOINTS_OUTPUT, consumer=_consumer)
        if "endpoints" not in kwargs:
            kwargs["endpoints"] = []
        return kwargs


DESCRIBE_ENDPOINTS = APIOperation(
    input=DescribeEndpointsInput,
    output=DescribeEndpointsOutput,
    schema=_SCHEMA_DESCRIBE_ENDPOINTS,
    input_schema=_SCHEMA_DESCRIBE_ENDPOINTS_INPUT,
    output_schema=_SCHEMA_DESCRIBE_ENDPOINTS_OUTPUT,
    error_registry=TypeRegistry({}),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[],
)


@dataclass(kw_only=True)
class DescribeExportInput:
    """Dataclass for DescribeExportInput structure."""

    export_arn: str | None = None
    """The Amazon Resource Name (ARN) associated with the export."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_EXPORT_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.export_arn is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_EXPORT_INPUT.members["ExportArn"], self.export_arn
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["export_arn"] = de.read_string(
                        _SCHEMA_DESCRIBE_EXPORT_INPUT.members["ExportArn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_EXPORT_INPUT, consumer=_consumer)
        return kwargs


class ExportFormat(UnknownEnumMixin, StrEnum):
    DYNAMODB_JSON = "DYNAMODB_JSON"
    ION = "ION"


class ExportStatus(UnknownEnumMixin, StrEnum):
    IN_PROGRESS = "IN_PROGRESS"
    COMPLETED = "COMPLETED"
    FAILED = "FAILED"


class ExportType(UnknownEnumMixin, StrEnum):
    FULL_EXPORT = "FULL_EXPORT"
    INCREMENTAL_EXPORT = "INCREMENTAL_EXPORT"


class ExportViewType(UnknownEnumMixin, StrEnum):
    NEW_IMAGE = "NEW_IMAGE"
    NEW_AND_OLD_IMAGES = "NEW_AND_OLD_IMAGES"


@dataclass(kw_only=True)
class IncrementalExportSpecification:
    """
    Optional object containing the parameters specific to an incremental
    export.
    """

    export_from_time: datetime | None = None
    """
    Time in the past which provides the inclusive start range for the export
    table's data, counted in seconds from the start of the Unix epoch. The
    incremental export will reflect the table's state including and after
    this point in time.
    """

    export_to_time: datetime | None = None
    """
    Time in the past which provides the exclusive end range for the export
    table's data, counted in seconds from the start of the Unix epoch. The
    incremental export will reflect the table's state just prior to this
    point in time. If this is not provided, the latest time with data
    available will be used.
    """

    export_view_type: ExportViewType | None = None
    """
    The view type that was chosen for the export. Valid values are
    `NEW_AND_OLD_IMAGES` and `NEW_IMAGES`. The default value is
    `NEW_AND_OLD_IMAGES`.

    `NEW_AND_OLD_IMAGES` exports both the new and old images of each changed
    item, while `NEW_IMAGES` exports only the new (latest) image. The view
    type you choose determines the structure of each item in the output for
    `insert`, `update`, and `delete` operations. For details and examples of
    how each view type shapes the export output, see [DynamoDB table export
    output
    format](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/S3DataExport.Output.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INCREMENTAL_EXPORT_SPECIFICATION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.export_from_time is not None:
            serializer.write_timestamp(
                _SCHEMA_INCREMENTAL_EXPORT_SPECIFICATION.members["ExportFromTime"],
                self.export_from_time,
            )

        if self.export_to_time is not None:
            serializer.write_timestamp(
                _SCHEMA_INCREMENTAL_EXPORT_SPECIFICATION.members["ExportToTime"],
                self.export_to_time,
            )

        if self.export_view_type is not None:
            serializer.write_string(
                _SCHEMA_INCREMENTAL_EXPORT_SPECIFICATION.members["ExportViewType"],
                self.export_view_type,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["export_from_time"] = de.read_timestamp(
                        _SCHEMA_INCREMENTAL_EXPORT_SPECIFICATION.members[
                            "ExportFromTime"
                        ]
                    )

                case 1:
                    kwargs["export_to_time"] = de.read_timestamp(
                        _SCHEMA_INCREMENTAL_EXPORT_SPECIFICATION.members["ExportToTime"]
                    )

                case 2:
                    kwargs["export_view_type"] = ExportViewType(
                        de.read_string(
                            _SCHEMA_INCREMENTAL_EXPORT_SPECIFICATION.members[
                                "ExportViewType"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_INCREMENTAL_EXPORT_SPECIFICATION, consumer=_consumer
        )
        return kwargs


class S3SseAlgorithm(UnknownEnumMixin, StrEnum):
    AES256 = "AES256"
    KMS = "KMS"


@dataclass(kw_only=True)
class ExportDescription:
    """Represents the properties of the exported table."""

    export_arn: str | None = None
    """The Amazon Resource Name (ARN) of the table export."""

    export_status: ExportStatus | None = None
    """
    Export can be in one of the following states: IN_PROGRESS, COMPLETED, or
    FAILED.
    """

    start_time: datetime | None = None
    """The time at which the export task began."""

    end_time: datetime | None = None
    """The time at which the export task completed."""

    export_manifest: str | None = None
    """The name of the manifest file for the export task."""

    table_arn: str | None = None
    """The Amazon Resource Name (ARN) of the table that was exported."""

    table_id: str | None = None
    """Unique ID of the table that was exported."""

    export_time: datetime | None = None
    """Point in time from which table data was exported."""

    client_token: str | None = None
    """
    The client token that was provided for the export task. A client token
    makes calls to `ExportTableToPointInTimeInput` idempotent, meaning that
    multiple identical calls have the same effect as one single call.
    """

    s3_bucket: str | None = None
    """The name of the Amazon S3 bucket containing the export."""

    s3_bucket_owner: str | None = None
    """
    The ID of the Amazon Web Services account that owns the bucket
    containing the export.
    """

    s3_prefix: str | None = None
    """
    The Amazon S3 bucket prefix used as the file name and path of the
    exported snapshot.
    """

    s3_sse_algorithm: S3SseAlgorithm | None = None
    """
    Type of encryption used on the bucket where export data is stored. Valid
    values for `S3SseAlgorithm` are:

    - `AES256` - server-side encryption with Amazon S3 managed keys

    - `KMS` - server-side encryption with KMS managed keys
    """

    s3_sse_kms_key_id: str | None = None
    """
    The ID of the KMS managed key used to encrypt the S3 bucket where export
    data is stored (if applicable).
    """

    failure_code: str | None = None
    """Status code for the result of the failed export."""

    failure_message: str | None = None
    """Export failure reason description."""

    export_format: ExportFormat | None = None
    """
    The format of the exported data. Valid values for `ExportFormat` are
    `DYNAMODB_JSON` or `ION`.
    """

    billed_size_bytes: int | None = None
    """The billable size of the table export."""

    item_count: int | None = None
    """The number of items exported."""

    export_type: ExportType | None = None
    """
    The type of export that was performed. Valid values are `FULL_EXPORT` or
    `INCREMENTAL_EXPORT`.
    """

    incremental_export_specification: IncrementalExportSpecification | None = None
    """
    Optional object containing the parameters specific to an incremental
    export.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXPORT_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.export_arn is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["ExportArn"], self.export_arn
            )

        if self.export_status is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["ExportStatus"], self.export_status
            )

        if self.start_time is not None:
            serializer.write_timestamp(
                _SCHEMA_EXPORT_DESCRIPTION.members["StartTime"], self.start_time
            )

        if self.end_time is not None:
            serializer.write_timestamp(
                _SCHEMA_EXPORT_DESCRIPTION.members["EndTime"], self.end_time
            )

        if self.export_manifest is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["ExportManifest"],
                self.export_manifest,
            )

        if self.table_arn is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["TableArn"], self.table_arn
            )

        if self.table_id is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["TableId"], self.table_id
            )

        if self.export_time is not None:
            serializer.write_timestamp(
                _SCHEMA_EXPORT_DESCRIPTION.members["ExportTime"], self.export_time
            )

        if self.client_token is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["ClientToken"], self.client_token
            )

        if self.s3_bucket is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["S3Bucket"], self.s3_bucket
            )

        if self.s3_bucket_owner is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["S3BucketOwner"],
                self.s3_bucket_owner,
            )

        if self.s3_prefix is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["S3Prefix"], self.s3_prefix
            )

        if self.s3_sse_algorithm is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["S3SseAlgorithm"],
                self.s3_sse_algorithm,
            )

        if self.s3_sse_kms_key_id is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["S3SseKmsKeyId"],
                self.s3_sse_kms_key_id,
            )

        if self.failure_code is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["FailureCode"], self.failure_code
            )

        if self.failure_message is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["FailureMessage"],
                self.failure_message,
            )

        if self.export_format is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["ExportFormat"], self.export_format
            )

        if self.billed_size_bytes is not None:
            serializer.write_long(
                _SCHEMA_EXPORT_DESCRIPTION.members["BilledSizeBytes"],
                self.billed_size_bytes,
            )

        if self.item_count is not None:
            serializer.write_long(
                _SCHEMA_EXPORT_DESCRIPTION.members["ItemCount"], self.item_count
            )

        if self.export_type is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_DESCRIPTION.members["ExportType"], self.export_type
            )

        if self.incremental_export_specification is not None:
            serializer.write_struct(
                _SCHEMA_EXPORT_DESCRIPTION.members["IncrementalExportSpecification"],
                self.incremental_export_specification,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["export_arn"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["ExportArn"]
                    )

                case 1:
                    kwargs["export_status"] = ExportStatus(
                        de.read_string(
                            _SCHEMA_EXPORT_DESCRIPTION.members["ExportStatus"]
                        )
                    )

                case 2:
                    kwargs["start_time"] = de.read_timestamp(
                        _SCHEMA_EXPORT_DESCRIPTION.members["StartTime"]
                    )

                case 3:
                    kwargs["end_time"] = de.read_timestamp(
                        _SCHEMA_EXPORT_DESCRIPTION.members["EndTime"]
                    )

                case 4:
                    kwargs["export_manifest"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["ExportManifest"]
                    )

                case 5:
                    kwargs["table_arn"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["TableArn"]
                    )

                case 6:
                    kwargs["table_id"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["TableId"]
                    )

                case 7:
                    kwargs["export_time"] = de.read_timestamp(
                        _SCHEMA_EXPORT_DESCRIPTION.members["ExportTime"]
                    )

                case 8:
                    kwargs["client_token"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["ClientToken"]
                    )

                case 9:
                    kwargs["s3_bucket"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["S3Bucket"]
                    )

                case 10:
                    kwargs["s3_bucket_owner"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["S3BucketOwner"]
                    )

                case 11:
                    kwargs["s3_prefix"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["S3Prefix"]
                    )

                case 12:
                    kwargs["s3_sse_algorithm"] = S3SseAlgorithm(
                        de.read_string(
                            _SCHEMA_EXPORT_DESCRIPTION.members["S3SseAlgorithm"]
                        )
                    )

                case 13:
                    kwargs["s3_sse_kms_key_id"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["S3SseKmsKeyId"]
                    )

                case 14:
                    kwargs["failure_code"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["FailureCode"]
                    )

                case 15:
                    kwargs["failure_message"] = de.read_string(
                        _SCHEMA_EXPORT_DESCRIPTION.members["FailureMessage"]
                    )

                case 16:
                    kwargs["export_format"] = ExportFormat(
                        de.read_string(
                            _SCHEMA_EXPORT_DESCRIPTION.members["ExportFormat"]
                        )
                    )

                case 17:
                    kwargs["billed_size_bytes"] = de.read_long(
                        _SCHEMA_EXPORT_DESCRIPTION.members["BilledSizeBytes"]
                    )

                case 18:
                    kwargs["item_count"] = de.read_long(
                        _SCHEMA_EXPORT_DESCRIPTION.members["ItemCount"]
                    )

                case 19:
                    kwargs["export_type"] = ExportType(
                        de.read_string(_SCHEMA_EXPORT_DESCRIPTION.members["ExportType"])
                    )

                case 20:
                    kwargs["incremental_export_specification"] = (
                        IncrementalExportSpecification.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EXPORT_DESCRIPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DescribeExportOutput:
    """Dataclass for DescribeExportOutput structure."""

    export_description: ExportDescription | None = None
    """Represents the properties of the export."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_EXPORT_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.export_description is not None:
            serializer.write_struct(
                _SCHEMA_DESCRIBE_EXPORT_OUTPUT.members["ExportDescription"],
                self.export_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["export_description"] = ExportDescription.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_EXPORT_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ExportNotFoundException(ServiceError):
    """The specified export was not found."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXPORT_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_NOT_FOUND_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_EXPORT_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EXPORT_NOT_FOUND_EXCEPTION, consumer=_consumer)
        return kwargs


DESCRIBE_EXPORT = APIOperation(
    input=DescribeExportInput,
    output=DescribeExportOutput,
    schema=_SCHEMA_DESCRIBE_EXPORT,
    input_schema=_SCHEMA_DESCRIBE_EXPORT_INPUT,
    output_schema=_SCHEMA_DESCRIBE_EXPORT_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#ExportNotFoundException"
            ): ExportNotFoundException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_EXPORT_NOT_FOUND_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DescribeGlobalTableInput:
    """Dataclass for DescribeGlobalTableInput structure."""

    global_table_name: str | None = None
    """The name of the global table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_GLOBAL_TABLE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_GLOBAL_TABLE_INPUT.members["GlobalTableName"],
                self.global_table_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_GLOBAL_TABLE_INPUT.members["GlobalTableName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_GLOBAL_TABLE_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class DescribeGlobalTableOutput:
    """Dataclass for DescribeGlobalTableOutput structure."""

    global_table_description: GlobalTableDescription | None = None
    """Contains the details of the global table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_GLOBAL_TABLE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_description is not None:
            serializer.write_struct(
                _SCHEMA_DESCRIBE_GLOBAL_TABLE_OUTPUT.members["GlobalTableDescription"],
                self.global_table_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_description"] = (
                        GlobalTableDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_GLOBAL_TABLE_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class GlobalTableNotFoundException(ServiceError):
    """The specified global table does not exist."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GLOBAL_TABLE_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_TABLE_NOT_FOUND_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_GLOBAL_TABLE_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GLOBAL_TABLE_NOT_FOUND_EXCEPTION, consumer=_consumer
        )
        return kwargs


DESCRIBE_GLOBAL_TABLE = APIOperation(
    input=DescribeGlobalTableInput,
    output=DescribeGlobalTableOutput,
    schema=_SCHEMA_DESCRIBE_GLOBAL_TABLE,
    input_schema=_SCHEMA_DESCRIBE_GLOBAL_TABLE_INPUT,
    output_schema=_SCHEMA_DESCRIBE_GLOBAL_TABLE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#GlobalTableNotFoundException"
            ): GlobalTableNotFoundException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_GLOBAL_TABLE_NOT_FOUND_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DescribeGlobalTableSettingsInput:
    """Dataclass for DescribeGlobalTableSettingsInput structure."""

    global_table_name: str | None = None
    """The name of the global table to describe."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_INPUT.members["GlobalTableName"],
                self.global_table_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                            "GlobalTableName"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ReplicaGlobalSecondaryIndexSettingsDescription:
    """Represents the properties of a global secondary index."""

    index_name: str
    """
    The name of the global secondary index. The name must be unique among
    all other indexes on this table.
    """

    index_status: IndexStatus | None = None
    """
    The current status of the global secondary index:

    - `CREATING` - The global secondary index is being created.

    - `UPDATING` - The global secondary index is being updated.

    - `DELETING` - The global secondary index is being deleted.

    - `ACTIVE` - The global secondary index is ready for use.
    """

    provisioned_read_capacity_units: int | None = None
    """
    The maximum number of strongly consistent reads consumed per second
    before DynamoDB returns a `ThrottlingException`.
    """

    provisioned_read_capacity_auto_scaling_settings: (
        AutoScalingSettingsDescription | None
    ) = None
    """
    Auto scaling settings for a global secondary index replica's read
    capacity units.
    """

    provisioned_write_capacity_units: int | None = None
    """
    The maximum number of writes consumed per second before DynamoDB returns
    a `ThrottlingException`.
    """

    provisioned_write_capacity_auto_scaling_settings: (
        AutoScalingSettingsDescription | None
    ) = None
    """
    Auto scaling settings for a global secondary index replica's write
    capacity units.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION.members[
                "IndexName"
            ],
            self.index_name,
        )
        if self.index_status is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION.members[
                    "IndexStatus"
                ],
                self.index_status,
            )

        if self.provisioned_read_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION.members[
                    "ProvisionedReadCapacityUnits"
                ],
                self.provisioned_read_capacity_units,
            )

        if self.provisioned_read_capacity_auto_scaling_settings is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION.members[
                    "ProvisionedReadCapacityAutoScalingSettings"
                ],
                self.provisioned_read_capacity_auto_scaling_settings,
            )

        if self.provisioned_write_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION.members[
                    "ProvisionedWriteCapacityUnits"
                ],
                self.provisioned_write_capacity_units,
            )

        if self.provisioned_write_capacity_auto_scaling_settings is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION.members[
                    "ProvisionedWriteCapacityAutoScalingSettings"
                ],
                self.provisioned_write_capacity_auto_scaling_settings,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION.members[
                            "IndexName"
                        ]
                    )

                case 1:
                    kwargs["index_status"] = IndexStatus(
                        de.read_string(
                            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION.members[
                                "IndexStatus"
                            ]
                        )
                    )

                case 2:
                    kwargs["provisioned_read_capacity_units"] = de.read_long(
                        _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION.members[
                            "ProvisionedReadCapacityUnits"
                        ]
                    )

                case 3:
                    kwargs["provisioned_read_capacity_auto_scaling_settings"] = (
                        AutoScalingSettingsDescription.deserialize(de)
                    )

                case 4:
                    kwargs["provisioned_write_capacity_units"] = de.read_long(
                        _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION.members[
                            "ProvisionedWriteCapacityUnits"
                        ]
                    )

                case 5:
                    kwargs["provisioned_write_capacity_auto_scaling_settings"] = (
                        AutoScalingSettingsDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_DESCRIPTION,
            consumer=_consumer,
        )
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        return kwargs


def _serialize_replica_global_secondary_index_settings_description_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[ReplicaGlobalSecondaryIndexSettingsDescription],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_global_secondary_index_settings_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaGlobalSecondaryIndexSettingsDescription]:
    result: list[ReplicaGlobalSecondaryIndexSettingsDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaGlobalSecondaryIndexSettingsDescription.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ReplicaSettingsDescription:
    """Represents the properties of a replica."""

    region_name: str
    """The Region name of the replica."""

    replica_status: ReplicaStatus | None = None
    """
    The current state of the Region:

    - `CREATING` - The Region is being created.

    - `UPDATING` - The Region is being updated.

    - `DELETING` - The Region is being deleted.

    - `ACTIVE` - The Region is ready for use.
    """

    replica_billing_mode_summary: BillingModeSummary | None = None
    """The read/write capacity mode of the replica."""

    replica_provisioned_read_capacity_units: int | None = None
    """
    The maximum number of strongly consistent reads consumed per second
    before DynamoDB returns a `ThrottlingException`. For more information,
    see [Specifying Read and Write
    Requirements](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/WorkingWithTables.html#ProvisionedThroughput)
    in the *Amazon DynamoDB Developer Guide*.
    """

    replica_provisioned_read_capacity_auto_scaling_settings: (
        AutoScalingSettingsDescription | None
    ) = None
    """Auto scaling settings for a global table replica's read capacity units."""

    replica_provisioned_write_capacity_units: int | None = None
    """
    The maximum number of writes consumed per second before DynamoDB returns
    a `ThrottlingException`. For more information, see [Specifying Read and
    Write
    Requirements](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/WorkingWithTables.html#ProvisionedThroughput)
    in the *Amazon DynamoDB Developer Guide*.
    """

    replica_provisioned_write_capacity_auto_scaling_settings: (
        AutoScalingSettingsDescription | None
    ) = None
    """
    Auto scaling settings for a global table replica's write capacity
    units.
    """

    replica_global_secondary_index_settings: (
        list[ReplicaGlobalSecondaryIndexSettingsDescription] | None
    ) = None
    """Replica global secondary index settings for the global table."""

    replica_table_class_summary: TableClassSummary | None = None
    """Contains details of the table class."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICA_SETTINGS_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members["RegionName"], self.region_name
        )
        if self.replica_status is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members["ReplicaStatus"],
                self.replica_status,
            )

        if self.replica_billing_mode_summary is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                    "ReplicaBillingModeSummary"
                ],
                self.replica_billing_mode_summary,
            )

        if self.replica_provisioned_read_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                    "ReplicaProvisionedReadCapacityUnits"
                ],
                self.replica_provisioned_read_capacity_units,
            )

        if self.replica_provisioned_read_capacity_auto_scaling_settings is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                    "ReplicaProvisionedReadCapacityAutoScalingSettings"
                ],
                self.replica_provisioned_read_capacity_auto_scaling_settings,
            )

        if self.replica_provisioned_write_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                    "ReplicaProvisionedWriteCapacityUnits"
                ],
                self.replica_provisioned_write_capacity_units,
            )

        if self.replica_provisioned_write_capacity_auto_scaling_settings is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                    "ReplicaProvisionedWriteCapacityAutoScalingSettings"
                ],
                self.replica_provisioned_write_capacity_auto_scaling_settings,
            )

        if self.replica_global_secondary_index_settings is not None:
            _serialize_replica_global_secondary_index_settings_description_list(
                serializer,
                _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                    "ReplicaGlobalSecondaryIndexSettings"
                ],
                self.replica_global_secondary_index_settings,
            )

        if self.replica_table_class_summary is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                    "ReplicaTableClassSummary"
                ],
                self.replica_table_class_summary,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members["RegionName"]
                    )

                case 1:
                    kwargs["replica_status"] = ReplicaStatus(
                        de.read_string(
                            _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                                "ReplicaStatus"
                            ]
                        )
                    )

                case 2:
                    kwargs["replica_billing_mode_summary"] = (
                        BillingModeSummary.deserialize(de)
                    )

                case 3:
                    kwargs["replica_provisioned_read_capacity_units"] = de.read_long(
                        _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                            "ReplicaProvisionedReadCapacityUnits"
                        ]
                    )

                case 4:
                    kwargs[
                        "replica_provisioned_read_capacity_auto_scaling_settings"
                    ] = AutoScalingSettingsDescription.deserialize(de)

                case 5:
                    kwargs["replica_provisioned_write_capacity_units"] = de.read_long(
                        _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                            "ReplicaProvisionedWriteCapacityUnits"
                        ]
                    )

                case 6:
                    kwargs[
                        "replica_provisioned_write_capacity_auto_scaling_settings"
                    ] = AutoScalingSettingsDescription.deserialize(de)

                case 7:
                    kwargs["replica_global_secondary_index_settings"] = (
                        _deserialize_replica_global_secondary_index_settings_description_list(
                            de,
                            _SCHEMA_REPLICA_SETTINGS_DESCRIPTION.members[
                                "ReplicaGlobalSecondaryIndexSettings"
                            ],
                        )
                    )

                case 8:
                    kwargs["replica_table_class_summary"] = (
                        TableClassSummary.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_SETTINGS_DESCRIPTION, consumer=_consumer
        )
        if "region_name" not in kwargs:
            kwargs["region_name"] = ""
        return kwargs


def _serialize_replica_settings_description_list(
    serializer: ShapeSerializer, schema: Schema, value: list[ReplicaSettingsDescription]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_settings_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaSettingsDescription]:
    result: list[ReplicaSettingsDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaSettingsDescription.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class DescribeGlobalTableSettingsOutput:
    """Dataclass for DescribeGlobalTableSettingsOutput structure."""

    global_table_name: str | None = None
    """The name of the global table."""

    replica_settings: list[ReplicaSettingsDescription] | None = None
    """The Region-specific settings for the global table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_OUTPUT.members[
                    "GlobalTableName"
                ],
                self.global_table_name,
            )

        if self.replica_settings is not None:
            _serialize_replica_settings_description_list(
                serializer,
                _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_OUTPUT.members[
                    "ReplicaSettings"
                ],
                self.replica_settings,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_OUTPUT.members[
                            "GlobalTableName"
                        ]
                    )

                case 1:
                    kwargs["replica_settings"] = (
                        _deserialize_replica_settings_description_list(
                            de,
                            _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_OUTPUT.members[
                                "ReplicaSettings"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_OUTPUT, consumer=_consumer
        )
        return kwargs


DESCRIBE_GLOBAL_TABLE_SETTINGS = APIOperation(
    input=DescribeGlobalTableSettingsInput,
    output=DescribeGlobalTableSettingsOutput,
    schema=_SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS,
    input_schema=_SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_INPUT,
    output_schema=_SCHEMA_DESCRIBE_GLOBAL_TABLE_SETTINGS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#GlobalTableNotFoundException"
            ): GlobalTableNotFoundException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_GLOBAL_TABLE_NOT_FOUND_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DescribeImportInput:
    """Dataclass for DescribeImportInput structure."""

    import_arn: str | None = None
    """
    The Amazon Resource Name (ARN) associated with the table you're
    importing to.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_IMPORT_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.import_arn is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_IMPORT_INPUT.members["ImportArn"], self.import_arn
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["import_arn"] = de.read_string(
                        _SCHEMA_DESCRIBE_IMPORT_INPUT.members["ImportArn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_IMPORT_INPUT, consumer=_consumer)
        return kwargs


class ImportStatus(UnknownEnumMixin, StrEnum):
    IN_PROGRESS = "IN_PROGRESS"
    COMPLETED = "COMPLETED"
    CANCELLING = "CANCELLING"
    CANCELLED = "CANCELLED"
    FAILED = "FAILED"


class InputCompressionType(UnknownEnumMixin, StrEnum):
    GZIP = "GZIP"
    ZSTD = "ZSTD"
    NONE = "NONE"


class InputFormat(UnknownEnumMixin, StrEnum):
    DYNAMODB_JSON = "DYNAMODB_JSON"
    ION = "ION"
    CSV = "CSV"


@dataclass(kw_only=True)
class InputFormatOptions:
    """
    The format options for the data that was imported into the target table.
    There is one value, CsvOption.
    """

    csv: CsvOptions | None = None
    """
    The options for imported source files in CSV format. The values are
    Delimiter and HeaderList.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INPUT_FORMAT_OPTIONS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.csv is not None:
            serializer.write_struct(
                _SCHEMA_INPUT_FORMAT_OPTIONS.members["Csv"], self.csv
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["csv"] = CsvOptions.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INPUT_FORMAT_OPTIONS, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class S3BucketSource:
    """The S3 bucket that is being imported from."""

    s3_bucket: str
    """The S3 bucket that is being imported from."""

    s3_bucket_owner: str | None = None
    """
    The account number of the S3 bucket that is being imported from. If the
    bucket is owned by the requester this is optional.
    """

    s3_key_prefix: str | None = None
    """The key prefix shared by all S3 Objects that are being imported."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_S3_BUCKET_SOURCE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.s3_bucket_owner is not None:
            serializer.write_string(
                _SCHEMA_S3_BUCKET_SOURCE.members["S3BucketOwner"], self.s3_bucket_owner
            )

        serializer.write_string(
            _SCHEMA_S3_BUCKET_SOURCE.members["S3Bucket"], self.s3_bucket
        )
        if self.s3_key_prefix is not None:
            serializer.write_string(
                _SCHEMA_S3_BUCKET_SOURCE.members["S3KeyPrefix"], self.s3_key_prefix
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["s3_bucket_owner"] = de.read_string(
                        _SCHEMA_S3_BUCKET_SOURCE.members["S3BucketOwner"]
                    )

                case 1:
                    kwargs["s3_bucket"] = de.read_string(
                        _SCHEMA_S3_BUCKET_SOURCE.members["S3Bucket"]
                    )

                case 2:
                    kwargs["s3_key_prefix"] = de.read_string(
                        _SCHEMA_S3_BUCKET_SOURCE.members["S3KeyPrefix"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_S3_BUCKET_SOURCE, consumer=_consumer)
        if "s3_bucket" not in kwargs:
            kwargs["s3_bucket"] = ""
        return kwargs


@dataclass(kw_only=True)
class TableCreationParameters:
    """The parameters for the table created as part of the import operation."""

    table_name: str
    """The name of the table created as part of the import operation."""

    attribute_definitions: list[AttributeDefinition]
    """The attributes of the table created as part of the import operation."""

    key_schema: list[KeySchemaElement]
    """
    The primary key and option sort key of the table created as part of the
    import operation.
    """

    billing_mode: BillingMode | None = None
    """
    The billing mode for provisioning the table created as part of the
    import operation.
    """

    provisioned_throughput: ProvisionedThroughput | None = None
    """
    Represents the provisioned throughput settings for the specified global
    secondary index. You must use `ProvisionedThroughput` or
    `OnDemandThroughput` based on your table's capacity mode.

    For current minimum and maximum provisioned throughput values, see
    [Service, Account, and Table
    Quotas](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Limits.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    on_demand_throughput: OnDemandThroughput | None = None
    """
    Sets the maximum number of read and write units for the specified
    on-demand table. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    sse_specification: SSESpecification | None = None
    """Represents the settings used to enable server-side encryption."""

    global_secondary_indexes: list[GlobalSecondaryIndex] | None = None
    """
    The Global Secondary Indexes (GSI) of the table to be created as part of
    the import operation.
    """

    vector_indexes: list[VectorIndex] | None = None
    """
    The vector indexes of the table to be created as part of the import
    operation.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TABLE_CREATION_PARAMETERS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_TABLE_CREATION_PARAMETERS.members["TableName"], self.table_name
        )
        _serialize_attribute_definitions(
            serializer,
            _SCHEMA_TABLE_CREATION_PARAMETERS.members["AttributeDefinitions"],
            self.attribute_definitions,
        )
        _serialize_key_schema(
            serializer,
            _SCHEMA_TABLE_CREATION_PARAMETERS.members["KeySchema"],
            self.key_schema,
        )
        if self.billing_mode is not None:
            serializer.write_string(
                _SCHEMA_TABLE_CREATION_PARAMETERS.members["BillingMode"],
                self.billing_mode,
            )

        if self.provisioned_throughput is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_CREATION_PARAMETERS.members["ProvisionedThroughput"],
                self.provisioned_throughput,
            )

        if self.on_demand_throughput is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_CREATION_PARAMETERS.members["OnDemandThroughput"],
                self.on_demand_throughput,
            )

        if self.sse_specification is not None:
            serializer.write_struct(
                _SCHEMA_TABLE_CREATION_PARAMETERS.members["SSESpecification"],
                self.sse_specification,
            )

        if self.global_secondary_indexes is not None:
            _serialize_global_secondary_index_list(
                serializer,
                _SCHEMA_TABLE_CREATION_PARAMETERS.members["GlobalSecondaryIndexes"],
                self.global_secondary_indexes,
            )

        if self.vector_indexes is not None:
            _serialize_vector_index_list(
                serializer,
                _SCHEMA_TABLE_CREATION_PARAMETERS.members["VectorIndexes"],
                self.vector_indexes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_TABLE_CREATION_PARAMETERS.members["TableName"]
                    )

                case 1:
                    kwargs["attribute_definitions"] = (
                        _deserialize_attribute_definitions(
                            de,
                            _SCHEMA_TABLE_CREATION_PARAMETERS.members[
                                "AttributeDefinitions"
                            ],
                        )
                    )

                case 2:
                    kwargs["key_schema"] = _deserialize_key_schema(
                        de, _SCHEMA_TABLE_CREATION_PARAMETERS.members["KeySchema"]
                    )

                case 3:
                    kwargs["billing_mode"] = BillingMode(
                        de.read_string(
                            _SCHEMA_TABLE_CREATION_PARAMETERS.members["BillingMode"]
                        )
                    )

                case 4:
                    kwargs["provisioned_throughput"] = (
                        ProvisionedThroughput.deserialize(de)
                    )

                case 5:
                    kwargs["on_demand_throughput"] = OnDemandThroughput.deserialize(de)

                case 6:
                    kwargs["sse_specification"] = SSESpecification.deserialize(de)

                case 7:
                    kwargs["global_secondary_indexes"] = (
                        _deserialize_global_secondary_index_list(
                            de,
                            _SCHEMA_TABLE_CREATION_PARAMETERS.members[
                                "GlobalSecondaryIndexes"
                            ],
                        )
                    )

                case 8:
                    kwargs["vector_indexes"] = _deserialize_vector_index_list(
                        de, _SCHEMA_TABLE_CREATION_PARAMETERS.members["VectorIndexes"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TABLE_CREATION_PARAMETERS, consumer=_consumer)
        if "table_name" not in kwargs:
            kwargs["table_name"] = ""
        if "attribute_definitions" not in kwargs:
            kwargs["attribute_definitions"] = []
        if "key_schema" not in kwargs:
            kwargs["key_schema"] = []
        return kwargs


@dataclass(kw_only=True)
class ImportTableDescription:
    """Represents the properties of the table being imported into."""

    import_arn: str | None = None
    """The Amazon Resource Number (ARN) corresponding to the import request."""

    import_status: ImportStatus | None = None
    """The status of the import."""

    table_arn: str | None = None
    """The Amazon Resource Number (ARN) of the table being imported into."""

    table_id: str | None = None
    """The table id corresponding to the table created by import table process."""

    client_token: str | None = None
    """
    The client token that was provided for the import task. Reusing the
    client token on retry makes a call to `ImportTable` idempotent.
    """

    s3_bucket_source: S3BucketSource | None = None
    """
    Values for the S3 bucket the source file is imported from. Includes
    bucket name (required), key prefix (optional) and bucket account owner
    ID (optional).
    """

    error_count: int = 0
    """
    The number of errors occurred on importing the source file into the
    target table.
    """

    cloud_watch_log_group_arn: str | None = None
    """
    The Amazon Resource Number (ARN) of the Cloudwatch Log Group associated
    with the target table.
    """

    input_format: InputFormat | None = None
    """The format of the source data going into the target table."""

    input_format_options: InputFormatOptions | None = None
    """
    The format options for the data that was imported into the target table.
    There is one value, CsvOption.
    """

    input_compression_type: InputCompressionType | None = None
    """
    The compression options for the data that has been imported into the
    target table. The values are NONE, GZIP, or ZSTD.
    """

    table_creation_parameters: TableCreationParameters | None = None
    """The parameters for the new table that is being imported into."""

    start_time: datetime | None = None
    """The time when this import task started."""

    end_time: datetime | None = None
    """
    The time at which the creation of the table associated with this import
    task completed.
    """

    processed_size_bytes: int | None = None
    """The total size of data processed from the source file, in Bytes."""

    processed_item_count: int = 0
    """The total number of items processed from the source file."""

    imported_item_count: int = 0
    """The number of items successfully imported into the new table."""

    failure_code: str | None = None
    """
    The error code corresponding to the failure that the import job ran into
    during execution.
    """

    failure_message: str | None = None
    """
    The error message corresponding to the failure that the import job ran
    into during execution.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_IMPORT_TABLE_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.import_arn is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ImportArn"], self.import_arn
            )

        if self.import_status is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ImportStatus"],
                self.import_status,
            )

        if self.table_arn is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["TableArn"], self.table_arn
            )

        if self.table_id is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["TableId"], self.table_id
            )

        if self.client_token is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ClientToken"],
                self.client_token,
            )

        if self.s3_bucket_source is not None:
            serializer.write_struct(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["S3BucketSource"],
                self.s3_bucket_source,
            )

        serializer.write_long(
            _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ErrorCount"], self.error_count
        )
        if self.cloud_watch_log_group_arn is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["CloudWatchLogGroupArn"],
                self.cloud_watch_log_group_arn,
            )

        if self.input_format is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["InputFormat"],
                self.input_format,
            )

        if self.input_format_options is not None:
            serializer.write_struct(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["InputFormatOptions"],
                self.input_format_options,
            )

        if self.input_compression_type is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["InputCompressionType"],
                self.input_compression_type,
            )

        if self.table_creation_parameters is not None:
            serializer.write_struct(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["TableCreationParameters"],
                self.table_creation_parameters,
            )

        if self.start_time is not None:
            serializer.write_timestamp(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["StartTime"], self.start_time
            )

        if self.end_time is not None:
            serializer.write_timestamp(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["EndTime"], self.end_time
            )

        if self.processed_size_bytes is not None:
            serializer.write_long(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ProcessedSizeBytes"],
                self.processed_size_bytes,
            )

        serializer.write_long(
            _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ProcessedItemCount"],
            self.processed_item_count,
        )
        serializer.write_long(
            _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ImportedItemCount"],
            self.imported_item_count,
        )
        if self.failure_code is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["FailureCode"],
                self.failure_code,
            )

        if self.failure_message is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["FailureMessage"],
                self.failure_message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["import_arn"] = de.read_string(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ImportArn"]
                    )

                case 1:
                    kwargs["import_status"] = ImportStatus(
                        de.read_string(
                            _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ImportStatus"]
                        )
                    )

                case 2:
                    kwargs["table_arn"] = de.read_string(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["TableArn"]
                    )

                case 3:
                    kwargs["table_id"] = de.read_string(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["TableId"]
                    )

                case 4:
                    kwargs["client_token"] = de.read_string(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ClientToken"]
                    )

                case 5:
                    kwargs["s3_bucket_source"] = S3BucketSource.deserialize(de)

                case 6:
                    kwargs["error_count"] = de.read_long(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ErrorCount"]
                    )

                case 7:
                    kwargs["cloud_watch_log_group_arn"] = de.read_string(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members[
                            "CloudWatchLogGroupArn"
                        ]
                    )

                case 8:
                    kwargs["input_format"] = InputFormat(
                        de.read_string(
                            _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["InputFormat"]
                        )
                    )

                case 9:
                    kwargs["input_format_options"] = InputFormatOptions.deserialize(de)

                case 10:
                    kwargs["input_compression_type"] = InputCompressionType(
                        de.read_string(
                            _SCHEMA_IMPORT_TABLE_DESCRIPTION.members[
                                "InputCompressionType"
                            ]
                        )
                    )

                case 11:
                    kwargs["table_creation_parameters"] = (
                        TableCreationParameters.deserialize(de)
                    )

                case 12:
                    kwargs["start_time"] = de.read_timestamp(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["StartTime"]
                    )

                case 13:
                    kwargs["end_time"] = de.read_timestamp(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["EndTime"]
                    )

                case 14:
                    kwargs["processed_size_bytes"] = de.read_long(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ProcessedSizeBytes"]
                    )

                case 15:
                    kwargs["processed_item_count"] = de.read_long(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ProcessedItemCount"]
                    )

                case 16:
                    kwargs["imported_item_count"] = de.read_long(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["ImportedItemCount"]
                    )

                case 17:
                    kwargs["failure_code"] = de.read_string(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["FailureCode"]
                    )

                case 18:
                    kwargs["failure_message"] = de.read_string(
                        _SCHEMA_IMPORT_TABLE_DESCRIPTION.members["FailureMessage"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_IMPORT_TABLE_DESCRIPTION, consumer=_consumer)
        return kwargs

    @classmethod
    def _smithy_default(cls) -> Self:
        return cls()


@dataclass(kw_only=True)
class DescribeImportOutput:
    """Dataclass for DescribeImportOutput structure."""

    import_table_description: ImportTableDescription
    """
    Represents the properties of the table created for the import, and
    parameters of the import. The import parameters include import status,
    how many items were processed, and how many errors were encountered.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_IMPORT_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_DESCRIBE_IMPORT_OUTPUT.members["ImportTableDescription"],
            self.import_table_description,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["import_table_description"] = (
                        ImportTableDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_IMPORT_OUTPUT, consumer=_consumer)
        if "import_table_description" not in kwargs:
            kwargs["import_table_description"] = (
                ImportTableDescription._smithy_default()
            )
        return kwargs


@dataclass(kw_only=True)
class ImportNotFoundException(ServiceError):
    """The specified import was not found."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_IMPORT_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_NOT_FOUND_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_IMPORT_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_IMPORT_NOT_FOUND_EXCEPTION, consumer=_consumer)
        return kwargs


DESCRIBE_IMPORT = APIOperation(
    input=DescribeImportInput,
    output=DescribeImportOutput,
    schema=_SCHEMA_DESCRIBE_IMPORT,
    input_schema=_SCHEMA_DESCRIBE_IMPORT_INPUT,
    output_schema=_SCHEMA_DESCRIBE_IMPORT_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#ImportNotFoundException"
            ): ImportNotFoundException
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_IMPORT_NOT_FOUND_EXCEPTION],
)


@dataclass(kw_only=True)
class DescribeKinesisStreamingDestinationInput:
    """Dataclass for DescribeKinesisStreamingDestinationInput structure."""

    table_name: str | None = None
    """
    The name of the table being described. You can also provide the Amazon
    Resource Name (ARN) of the table in this parameter.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_INPUT, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                    "TableName"
                ],
                self.table_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                            "TableName"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_INPUT, consumer=_consumer
        )
        return kwargs


class DestinationStatus(UnknownEnumMixin, StrEnum):
    ENABLING = "ENABLING"
    ACTIVE = "ACTIVE"
    DISABLING = "DISABLING"
    DISABLED = "DISABLED"
    ENABLE_FAILED = "ENABLE_FAILED"
    UPDATING = "UPDATING"


@dataclass(kw_only=True)
class KinesisDataStreamDestination:
    """Describes a Kinesis data stream destination."""

    stream_arn: str | None = None
    """The ARN for a specific Kinesis data stream."""

    destination_status: DestinationStatus | None = None
    """The current status of replication."""

    destination_status_description: str | None = None
    """The human-readable string that corresponds to the replica status."""

    approximate_creation_date_time_precision: (
        ApproximateCreationDateTimePrecision | None
    ) = None
    """
    The precision of the Kinesis data stream timestamp. The values are
    either `MILLISECOND` or `MICROSECOND`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_KINESIS_DATA_STREAM_DESTINATION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.stream_arn is not None:
            serializer.write_string(
                _SCHEMA_KINESIS_DATA_STREAM_DESTINATION.members["StreamArn"],
                self.stream_arn,
            )

        if self.destination_status is not None:
            serializer.write_string(
                _SCHEMA_KINESIS_DATA_STREAM_DESTINATION.members["DestinationStatus"],
                self.destination_status,
            )

        if self.destination_status_description is not None:
            serializer.write_string(
                _SCHEMA_KINESIS_DATA_STREAM_DESTINATION.members[
                    "DestinationStatusDescription"
                ],
                self.destination_status_description,
            )

        if self.approximate_creation_date_time_precision is not None:
            serializer.write_string(
                _SCHEMA_KINESIS_DATA_STREAM_DESTINATION.members[
                    "ApproximateCreationDateTimePrecision"
                ],
                self.approximate_creation_date_time_precision,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["stream_arn"] = de.read_string(
                        _SCHEMA_KINESIS_DATA_STREAM_DESTINATION.members["StreamArn"]
                    )

                case 1:
                    kwargs["destination_status"] = DestinationStatus(
                        de.read_string(
                            _SCHEMA_KINESIS_DATA_STREAM_DESTINATION.members[
                                "DestinationStatus"
                            ]
                        )
                    )

                case 2:
                    kwargs["destination_status_description"] = de.read_string(
                        _SCHEMA_KINESIS_DATA_STREAM_DESTINATION.members[
                            "DestinationStatusDescription"
                        ]
                    )

                case 3:
                    kwargs["approximate_creation_date_time_precision"] = (
                        ApproximateCreationDateTimePrecision(
                            de.read_string(
                                _SCHEMA_KINESIS_DATA_STREAM_DESTINATION.members[
                                    "ApproximateCreationDateTimePrecision"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_KINESIS_DATA_STREAM_DESTINATION, consumer=_consumer
        )
        return kwargs


def _serialize_kinesis_data_stream_destinations(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[KinesisDataStreamDestination],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_kinesis_data_stream_destinations(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[KinesisDataStreamDestination]:
    result: list[KinesisDataStreamDestination] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(KinesisDataStreamDestination.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class DescribeKinesisStreamingDestinationOutput:
    """Dataclass for DescribeKinesisStreamingDestinationOutput structure."""

    table_name: str | None = None
    """The name of the table being described."""

    kinesis_data_stream_destinations: list[KinesisDataStreamDestination] | None = None
    """The list of replica structures for the table being described."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_OUTPUT, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "TableName"
                ],
                self.table_name,
            )

        if self.kinesis_data_stream_destinations is not None:
            _serialize_kinesis_data_stream_destinations(
                serializer,
                _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "KinesisDataStreamDestinations"
                ],
                self.kinesis_data_stream_destinations,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                            "TableName"
                        ]
                    )

                case 1:
                    kwargs["kinesis_data_stream_destinations"] = (
                        _deserialize_kinesis_data_stream_destinations(
                            de,
                            _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                                "KinesisDataStreamDestinations"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_OUTPUT, consumer=_consumer
        )
        return kwargs


DESCRIBE_KINESIS_STREAMING_DESTINATION = APIOperation(
    input=DescribeKinesisStreamingDestinationInput,
    output=DescribeKinesisStreamingDestinationOutput,
    schema=_SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION,
    input_schema=_SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_INPUT,
    output_schema=_SCHEMA_DESCRIBE_KINESIS_STREAMING_DESTINATION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DescribeLimitsInput:
    """Represents the input of a `DescribeLimits` operation. Has no content."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_LIMITS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_LIMITS_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DescribeLimitsOutput:
    """Represents the output of a `DescribeLimits` operation."""

    account_max_read_capacity_units: int | None = None
    """
    The maximum total read capacity units that your account allows you to
    provision across all of your tables in this Region.
    """

    account_max_write_capacity_units: int | None = None
    """
    The maximum total write capacity units that your account allows you to
    provision across all of your tables in this Region.
    """

    table_max_read_capacity_units: int | None = None
    """
    The maximum read capacity units that your account allows you to
    provision for a new table that you are creating in this Region,
    including the read capacity units provisioned for its global secondary
    indexes (GSIs).
    """

    table_max_write_capacity_units: int | None = None
    """
    The maximum write capacity units that your account allows you to
    provision for a new table that you are creating in this Region,
    including the write capacity units provisioned for its global secondary
    indexes (GSIs).
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_LIMITS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.account_max_read_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_DESCRIBE_LIMITS_OUTPUT.members["AccountMaxReadCapacityUnits"],
                self.account_max_read_capacity_units,
            )

        if self.account_max_write_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_DESCRIBE_LIMITS_OUTPUT.members["AccountMaxWriteCapacityUnits"],
                self.account_max_write_capacity_units,
            )

        if self.table_max_read_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_DESCRIBE_LIMITS_OUTPUT.members["TableMaxReadCapacityUnits"],
                self.table_max_read_capacity_units,
            )

        if self.table_max_write_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_DESCRIBE_LIMITS_OUTPUT.members["TableMaxWriteCapacityUnits"],
                self.table_max_write_capacity_units,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["account_max_read_capacity_units"] = de.read_long(
                        _SCHEMA_DESCRIBE_LIMITS_OUTPUT.members[
                            "AccountMaxReadCapacityUnits"
                        ]
                    )

                case 1:
                    kwargs["account_max_write_capacity_units"] = de.read_long(
                        _SCHEMA_DESCRIBE_LIMITS_OUTPUT.members[
                            "AccountMaxWriteCapacityUnits"
                        ]
                    )

                case 2:
                    kwargs["table_max_read_capacity_units"] = de.read_long(
                        _SCHEMA_DESCRIBE_LIMITS_OUTPUT.members[
                            "TableMaxReadCapacityUnits"
                        ]
                    )

                case 3:
                    kwargs["table_max_write_capacity_units"] = de.read_long(
                        _SCHEMA_DESCRIBE_LIMITS_OUTPUT.members[
                            "TableMaxWriteCapacityUnits"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_LIMITS_OUTPUT, consumer=_consumer)
        return kwargs


DESCRIBE_LIMITS = APIOperation(
    input=DescribeLimitsInput,
    output=DescribeLimitsOutput,
    schema=_SCHEMA_DESCRIBE_LIMITS,
    input_schema=_SCHEMA_DESCRIBE_LIMITS_INPUT,
    output_schema=_SCHEMA_DESCRIBE_LIMITS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_INTERNAL_SERVER_ERROR, _SCHEMA_INVALID_ENDPOINT_EXCEPTION],
)


@dataclass(kw_only=True)
class DescribeTableInput:
    """Represents the input of a `DescribeTable` operation."""

    table_name: str | None = None
    """
    The name of the table to describe. You can also provide the Amazon
    Resource Name (ARN) of the table in this parameter.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_TABLE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_TABLE_INPUT.members["TableName"], self.table_name
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_TABLE_INPUT.members["TableName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_TABLE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DescribeTableOutput:
    """Represents the output of a `DescribeTable` operation."""

    table: TableDescription | None = None
    """The properties of the table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_TABLE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table is not None:
            serializer.write_struct(
                _SCHEMA_DESCRIBE_TABLE_OUTPUT.members["Table"], self.table
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table"] = TableDescription.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DESCRIBE_TABLE_OUTPUT, consumer=_consumer)
        return kwargs


DESCRIBE_TABLE = APIOperation(
    input=DescribeTableInput,
    output=DescribeTableOutput,
    schema=_SCHEMA_DESCRIBE_TABLE,
    input_schema=_SCHEMA_DESCRIBE_TABLE_INPUT,
    output_schema=_SCHEMA_DESCRIBE_TABLE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DescribeTableReplicaAutoScalingInput:
    """Dataclass for DescribeTableReplicaAutoScalingInput structure."""

    table_name: str | None = None
    """
    The name of the table. You can also provide the Amazon Resource Name
    (ARN) of the table in this parameter.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_INPUT.members["TableName"],
                self.table_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_INPUT.members[
                            "TableName"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ReplicaGlobalSecondaryIndexAutoScalingDescription:
    """
    Represents the auto scaling configuration for a replica global secondary
    index.
    """

    index_name: str | None = None
    """The name of the global secondary index."""

    index_status: IndexStatus | None = None
    """
    The current state of the replica global secondary index:

    - `CREATING` - The index is being created.

    - `UPDATING` - The table/index configuration is being updated. The
      table/index remains available for data operations when `UPDATING`

    - `DELETING` - The index is being deleted.

    - `ACTIVE` - The index is ready for use.
    """

    provisioned_read_capacity_auto_scaling_settings: (
        AutoScalingSettingsDescription | None
    ) = None
    """
    Represents the auto scaling settings for a global table or global
    secondary index.
    """

    provisioned_write_capacity_auto_scaling_settings: (
        AutoScalingSettingsDescription | None
    ) = None
    """
    Represents the auto scaling settings for a global table or global
    secondary index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_DESCRIPTION, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_DESCRIPTION.members[
                    "IndexName"
                ],
                self.index_name,
            )

        if self.index_status is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_DESCRIPTION.members[
                    "IndexStatus"
                ],
                self.index_status,
            )

        if self.provisioned_read_capacity_auto_scaling_settings is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_DESCRIPTION.members[
                    "ProvisionedReadCapacityAutoScalingSettings"
                ],
                self.provisioned_read_capacity_auto_scaling_settings,
            )

        if self.provisioned_write_capacity_auto_scaling_settings is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_DESCRIPTION.members[
                    "ProvisionedWriteCapacityAutoScalingSettings"
                ],
                self.provisioned_write_capacity_auto_scaling_settings,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_DESCRIPTION.members[
                            "IndexName"
                        ]
                    )

                case 1:
                    kwargs["index_status"] = IndexStatus(
                        de.read_string(
                            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_DESCRIPTION.members[
                                "IndexStatus"
                            ]
                        )
                    )

                case 2:
                    kwargs["provisioned_read_capacity_auto_scaling_settings"] = (
                        AutoScalingSettingsDescription.deserialize(de)
                    )

                case 3:
                    kwargs["provisioned_write_capacity_auto_scaling_settings"] = (
                        AutoScalingSettingsDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_DESCRIPTION,
            consumer=_consumer,
        )
        return kwargs


def _serialize_replica_global_secondary_index_auto_scaling_description_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[ReplicaGlobalSecondaryIndexAutoScalingDescription],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_global_secondary_index_auto_scaling_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaGlobalSecondaryIndexAutoScalingDescription]:
    result: list[ReplicaGlobalSecondaryIndexAutoScalingDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(
                ReplicaGlobalSecondaryIndexAutoScalingDescription.deserialize(d)
            )

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ReplicaAutoScalingDescription:
    """Represents the auto scaling settings of the replica."""

    region_name: str | None = None
    """The Region where the replica exists."""

    global_secondary_indexes: (
        list[ReplicaGlobalSecondaryIndexAutoScalingDescription] | None
    ) = None
    """Replica-specific global secondary index auto scaling settings."""

    replica_provisioned_read_capacity_auto_scaling_settings: (
        AutoScalingSettingsDescription | None
    ) = None
    """
    Represents the auto scaling settings for a global table or global
    secondary index.
    """

    replica_provisioned_write_capacity_auto_scaling_settings: (
        AutoScalingSettingsDescription | None
    ) = None
    """
    Represents the auto scaling settings for a global table or global
    secondary index.
    """

    replica_status: ReplicaStatus | None = None
    """
    The current state of the replica:

    - `CREATING` - The replica is being created.

    - `UPDATING` - The replica is being updated.

    - `DELETING` - The replica is being deleted.

    - `ACTIVE` - The replica is ready for use.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.region_name is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION.members["RegionName"],
                self.region_name,
            )

        if self.global_secondary_indexes is not None:
            _serialize_replica_global_secondary_index_auto_scaling_description_list(
                serializer,
                _SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION.members[
                    "GlobalSecondaryIndexes"
                ],
                self.global_secondary_indexes,
            )

        if self.replica_provisioned_read_capacity_auto_scaling_settings is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION.members[
                    "ReplicaProvisionedReadCapacityAutoScalingSettings"
                ],
                self.replica_provisioned_read_capacity_auto_scaling_settings,
            )

        if self.replica_provisioned_write_capacity_auto_scaling_settings is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION.members[
                    "ReplicaProvisionedWriteCapacityAutoScalingSettings"
                ],
                self.replica_provisioned_write_capacity_auto_scaling_settings,
            )

        if self.replica_status is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION.members["ReplicaStatus"],
                self.replica_status,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION.members["RegionName"]
                    )

                case 1:
                    kwargs["global_secondary_indexes"] = (
                        _deserialize_replica_global_secondary_index_auto_scaling_description_list(
                            de,
                            _SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION.members[
                                "GlobalSecondaryIndexes"
                            ],
                        )
                    )

                case 2:
                    kwargs[
                        "replica_provisioned_read_capacity_auto_scaling_settings"
                    ] = AutoScalingSettingsDescription.deserialize(de)

                case 3:
                    kwargs[
                        "replica_provisioned_write_capacity_auto_scaling_settings"
                    ] = AutoScalingSettingsDescription.deserialize(de)

                case 4:
                    kwargs["replica_status"] = ReplicaStatus(
                        de.read_string(
                            _SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION.members[
                                "ReplicaStatus"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_AUTO_SCALING_DESCRIPTION, consumer=_consumer
        )
        return kwargs


def _serialize_replica_auto_scaling_description_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[ReplicaAutoScalingDescription],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_auto_scaling_description_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaAutoScalingDescription]:
    result: list[ReplicaAutoScalingDescription] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaAutoScalingDescription.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class TableAutoScalingDescription:
    """Represents the auto scaling configuration for a global table."""

    table_name: str | None = None
    """The name of the table."""

    table_status: TableStatus | None = None
    """
    The current state of the table:

    - `CREATING` - The table is being created.

    - `UPDATING` - The table is being updated.

    - `DELETING` - The table is being deleted.

    - `ACTIVE` - The table is ready for use.
    """

    replicas: list[ReplicaAutoScalingDescription] | None = None
    """Represents replicas of the global table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TABLE_AUTO_SCALING_DESCRIPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_TABLE_AUTO_SCALING_DESCRIPTION.members["TableName"],
                self.table_name,
            )

        if self.table_status is not None:
            serializer.write_string(
                _SCHEMA_TABLE_AUTO_SCALING_DESCRIPTION.members["TableStatus"],
                self.table_status,
            )

        if self.replicas is not None:
            _serialize_replica_auto_scaling_description_list(
                serializer,
                _SCHEMA_TABLE_AUTO_SCALING_DESCRIPTION.members["Replicas"],
                self.replicas,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_TABLE_AUTO_SCALING_DESCRIPTION.members["TableName"]
                    )

                case 1:
                    kwargs["table_status"] = TableStatus(
                        de.read_string(
                            _SCHEMA_TABLE_AUTO_SCALING_DESCRIPTION.members[
                                "TableStatus"
                            ]
                        )
                    )

                case 2:
                    kwargs["replicas"] = (
                        _deserialize_replica_auto_scaling_description_list(
                            de,
                            _SCHEMA_TABLE_AUTO_SCALING_DESCRIPTION.members["Replicas"],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_TABLE_AUTO_SCALING_DESCRIPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class DescribeTableReplicaAutoScalingOutput:
    """Dataclass for DescribeTableReplicaAutoScalingOutput structure."""

    table_auto_scaling_description: TableAutoScalingDescription | None = None
    """Represents the auto scaling properties of the table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_OUTPUT, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_auto_scaling_description is not None:
            serializer.write_struct(
                _SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_OUTPUT.members[
                    "TableAutoScalingDescription"
                ],
                self.table_auto_scaling_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_auto_scaling_description"] = (
                        TableAutoScalingDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_OUTPUT, consumer=_consumer
        )
        return kwargs


DESCRIBE_TABLE_REPLICA_AUTO_SCALING = APIOperation(
    input=DescribeTableReplicaAutoScalingInput,
    output=DescribeTableReplicaAutoScalingOutput,
    schema=_SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING,
    input_schema=_SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_INPUT,
    output_schema=_SCHEMA_DESCRIBE_TABLE_REPLICA_AUTO_SCALING_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_INTERNAL_SERVER_ERROR, _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION],
)


@dataclass(kw_only=True)
class DescribeTimeToLiveInput:
    """Dataclass for DescribeTimeToLiveInput structure."""

    table_name: str | None = None
    """
    The name of the table to be described. You can also provide the Amazon
    Resource Name (ARN) of the table in this parameter.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_TIME_TO_LIVE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DESCRIBE_TIME_TO_LIVE_INPUT.members["TableName"],
                self.table_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DESCRIBE_TIME_TO_LIVE_INPUT.members["TableName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_TIME_TO_LIVE_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class DescribeTimeToLiveOutput:
    """Dataclass for DescribeTimeToLiveOutput structure."""

    time_to_live_description: TimeToLiveDescription | None = None
    """"""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DESCRIBE_TIME_TO_LIVE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.time_to_live_description is not None:
            serializer.write_struct(
                _SCHEMA_DESCRIBE_TIME_TO_LIVE_OUTPUT.members["TimeToLiveDescription"],
                self.time_to_live_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["time_to_live_description"] = (
                        TimeToLiveDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DESCRIBE_TIME_TO_LIVE_OUTPUT, consumer=_consumer
        )
        return kwargs


DESCRIBE_TIME_TO_LIVE = APIOperation(
    input=DescribeTimeToLiveInput,
    output=DescribeTimeToLiveOutput,
    schema=_SCHEMA_DESCRIBE_TIME_TO_LIVE,
    input_schema=_SCHEMA_DESCRIBE_TIME_TO_LIVE_INPUT,
    output_schema=_SCHEMA_DESCRIBE_TIME_TO_LIVE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class EnableKinesisStreamingConfiguration:
    """Enables setting the configuration for Kinesis Streaming."""

    approximate_creation_date_time_precision: (
        ApproximateCreationDateTimePrecision | None
    ) = None
    """
    Toggle for the precision of Kinesis data stream timestamp. The values
    are either `MILLISECOND` or `MICROSECOND`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ENABLE_KINESIS_STREAMING_CONFIGURATION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.approximate_creation_date_time_precision is not None:
            serializer.write_string(
                _SCHEMA_ENABLE_KINESIS_STREAMING_CONFIGURATION.members[
                    "ApproximateCreationDateTimePrecision"
                ],
                self.approximate_creation_date_time_precision,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["approximate_creation_date_time_precision"] = (
                        ApproximateCreationDateTimePrecision(
                            de.read_string(
                                _SCHEMA_ENABLE_KINESIS_STREAMING_CONFIGURATION.members[
                                    "ApproximateCreationDateTimePrecision"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_ENABLE_KINESIS_STREAMING_CONFIGURATION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class DisableKinesisStreamingDestinationInput:
    """Dataclass for DisableKinesisStreamingDestinationInput structure."""

    table_name: str | None = None
    """
    The name of the DynamoDB table. You can also provide the Amazon Resource
    Name (ARN) of the table in this parameter.
    """

    stream_arn: str | None = None
    """The ARN for a Kinesis data stream."""

    enable_kinesis_streaming_configuration: (
        EnableKinesisStreamingConfiguration | None
    ) = None
    """The source for the Kinesis streaming information that is being enabled."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_INPUT, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                    "TableName"
                ],
                self.table_name,
            )

        if self.stream_arn is not None:
            serializer.write_string(
                _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                    "StreamArn"
                ],
                self.stream_arn,
            )

        if self.enable_kinesis_streaming_configuration is not None:
            serializer.write_struct(
                _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                    "EnableKinesisStreamingConfiguration"
                ],
                self.enable_kinesis_streaming_configuration,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                            "TableName"
                        ]
                    )

                case 1:
                    kwargs["stream_arn"] = de.read_string(
                        _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                            "StreamArn"
                        ]
                    )

                case 2:
                    kwargs["enable_kinesis_streaming_configuration"] = (
                        EnableKinesisStreamingConfiguration.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class DisableKinesisStreamingDestinationOutput:
    """Dataclass for DisableKinesisStreamingDestinationOutput structure."""

    table_name: str | None = None
    """The name of the table being modified."""

    stream_arn: str | None = None
    """The ARN for the specific Kinesis data stream."""

    destination_status: DestinationStatus | None = None
    """The current status of the replication."""

    enable_kinesis_streaming_configuration: (
        EnableKinesisStreamingConfiguration | None
    ) = None
    """
    The destination for the Kinesis streaming information that is being
    enabled.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "TableName"
                ],
                self.table_name,
            )

        if self.stream_arn is not None:
            serializer.write_string(
                _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "StreamArn"
                ],
                self.stream_arn,
            )

        if self.destination_status is not None:
            serializer.write_string(
                _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "DestinationStatus"
                ],
                self.destination_status,
            )

        if self.enable_kinesis_streaming_configuration is not None:
            serializer.write_struct(
                _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "EnableKinesisStreamingConfiguration"
                ],
                self.enable_kinesis_streaming_configuration,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                            "TableName"
                        ]
                    )

                case 1:
                    kwargs["stream_arn"] = de.read_string(
                        _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                            "StreamArn"
                        ]
                    )

                case 2:
                    kwargs["destination_status"] = DestinationStatus(
                        de.read_string(
                            _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                                "DestinationStatus"
                            ]
                        )
                    )

                case 3:
                    kwargs["enable_kinesis_streaming_configuration"] = (
                        EnableKinesisStreamingConfiguration.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT, consumer=_consumer
        )
        return kwargs


DISABLE_KINESIS_STREAMING_DESTINATION = APIOperation(
    input=DisableKinesisStreamingDestinationInput,
    output=DisableKinesisStreamingDestinationOutput,
    schema=_SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION,
    input_schema=_SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_INPUT,
    output_schema=_SCHEMA_DISABLE_KINESIS_STREAMING_DESTINATION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class DuplicateItemException(ServiceError):
    """
    There was an attempt to insert an item with the same primary key as an
    item that already exists in the DynamoDB table.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DUPLICATE_ITEM_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_DUPLICATE_ITEM_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_DUPLICATE_ITEM_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DUPLICATE_ITEM_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class EnableKinesisStreamingDestinationInput:
    """Dataclass for EnableKinesisStreamingDestinationInput structure."""

    table_name: str | None = None
    """
    The name of the DynamoDB table. You can also provide the Amazon Resource
    Name (ARN) of the table in this parameter.
    """

    stream_arn: str | None = None
    """The ARN for a Kinesis data stream."""

    enable_kinesis_streaming_configuration: (
        EnableKinesisStreamingConfiguration | None
    ) = None
    """The source for the Kinesis streaming information that is being enabled."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_INPUT, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_INPUT.members["TableName"],
                self.table_name,
            )

        if self.stream_arn is not None:
            serializer.write_string(
                _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_INPUT.members["StreamArn"],
                self.stream_arn,
            )

        if self.enable_kinesis_streaming_configuration is not None:
            serializer.write_struct(
                _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                    "EnableKinesisStreamingConfiguration"
                ],
                self.enable_kinesis_streaming_configuration,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                            "TableName"
                        ]
                    )

                case 1:
                    kwargs["stream_arn"] = de.read_string(
                        _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                            "StreamArn"
                        ]
                    )

                case 2:
                    kwargs["enable_kinesis_streaming_configuration"] = (
                        EnableKinesisStreamingConfiguration.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class EnableKinesisStreamingDestinationOutput:
    """Dataclass for EnableKinesisStreamingDestinationOutput structure."""

    table_name: str | None = None
    """The name of the table being modified."""

    stream_arn: str | None = None
    """The ARN for the specific Kinesis data stream."""

    destination_status: DestinationStatus | None = None
    """The current status of the replication."""

    enable_kinesis_streaming_configuration: (
        EnableKinesisStreamingConfiguration | None
    ) = None
    """
    The destination for the Kinesis streaming information that is being
    enabled.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "TableName"
                ],
                self.table_name,
            )

        if self.stream_arn is not None:
            serializer.write_string(
                _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "StreamArn"
                ],
                self.stream_arn,
            )

        if self.destination_status is not None:
            serializer.write_string(
                _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "DestinationStatus"
                ],
                self.destination_status,
            )

        if self.enable_kinesis_streaming_configuration is not None:
            serializer.write_struct(
                _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "EnableKinesisStreamingConfiguration"
                ],
                self.enable_kinesis_streaming_configuration,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                            "TableName"
                        ]
                    )

                case 1:
                    kwargs["stream_arn"] = de.read_string(
                        _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                            "StreamArn"
                        ]
                    )

                case 2:
                    kwargs["destination_status"] = DestinationStatus(
                        de.read_string(
                            _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                                "DestinationStatus"
                            ]
                        )
                    )

                case 3:
                    kwargs["enable_kinesis_streaming_configuration"] = (
                        EnableKinesisStreamingConfiguration.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT, consumer=_consumer
        )
        return kwargs


ENABLE_KINESIS_STREAMING_DESTINATION = APIOperation(
    input=EnableKinesisStreamingDestinationInput,
    output=EnableKinesisStreamingDestinationOutput,
    schema=_SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION,
    input_schema=_SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_INPUT,
    output_schema=_SCHEMA_ENABLE_KINESIS_STREAMING_DESTINATION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class IdempotentParameterMismatchException(ServiceError):
    """
    DynamoDB rejected the request because you retried a request with a
    different payload but with an idempotent token that was already used.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_IDEMPOTENT_PARAMETER_MISMATCH_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_IDEMPOTENT_PARAMETER_MISMATCH_EXCEPTION.members["Message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_IDEMPOTENT_PARAMETER_MISMATCH_EXCEPTION.members[
                            "Message"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_IDEMPOTENT_PARAMETER_MISMATCH_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class TransactionInProgressException(ServiceError):
    """
    The transaction with the given request token is already in progress.

    Recommended Settings

    Note:
        This is a general recommendation for handling the
        `TransactionInProgressException`. These settings help ensure that the
        client retries will trigger completion of the ongoing
        `TransactWriteItems` request.

    - Set `clientExecutionTimeout` to a value that allows at least one retry
      to be processed after 5 seconds have elapsed since the first attempt
      for the `TransactWriteItems` operation.

    - Set `socketTimeout` to a value a little lower than the
      `requestTimeout` setting.

    - `requestTimeout` should be set based on the time taken for the
      individual retries of a single HTTP request for your use case, but
      setting it to 1 second or higher should work well to reduce chances of
      retries and `TransactionInProgressException` errors.

    - Use exponential backoff when retrying and tune backoff if needed.

    Assuming [default retry
    policy](https://github.com/aws/aws-sdk-java/blob/fd409dee8ae23fb8953e0bb4dbde65536a7e0514/aws-java-sdk-core/src/main/java/com/amazonaws/retry/PredefinedRetryPolicies.java#L97),
    example timeout settings based on the guidelines above are as follows:

    Example timeline:

    - 0-1000 first attempt

    - 1000-1500 first sleep/delay (default retry policy uses 500 ms as base
      delay for 4xx errors)

    - 1500-2500 second attempt

    - 2500-3500 second sleep/delay (500 * 2, exponential backoff)

    - 3500-4500 third attempt

    - 4500-6500 third sleep/delay (500 * 2^2)

    - 6500-7500 fourth attempt (this can trigger inline recovery since 5
      seconds have elapsed since the first attempt reached TC)
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSACTION_IN_PROGRESS_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_TRANSACTION_IN_PROGRESS_EXCEPTION.members["Message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_TRANSACTION_IN_PROGRESS_EXCEPTION.members["Message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_TRANSACTION_IN_PROGRESS_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ExportConflictException(ServiceError):
    """There was a conflict when writing to the specified S3 bucket."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXPORT_CONFLICT_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_CONFLICT_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_EXPORT_CONFLICT_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EXPORT_CONFLICT_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ExportTableToPointInTimeInput:
    """Dataclass for ExportTableToPointInTimeInput structure."""

    table_arn: str | None = None
    """The Amazon Resource Name (ARN) associated with the table to export."""

    export_time: datetime | None = None
    """
    Time in the past from which to export table data, counted in seconds
    from the start of the Unix epoch. The table export will be a snapshot of
    the table's state at this point in time.
    """

    client_token: str | None = None
    """
    Providing a `ClientToken` makes the call to
    `ExportTableToPointInTimeInput` idempotent, meaning that multiple
    identical calls have the same effect as one single call.

    A client token is valid for 8 hours after the first request that uses it
    is completed. After 8 hours, any request with the same client token is
    treated as a new request. Do not resubmit the same request with the same
    client token for more than 8 hours, or the result might not be
    idempotent.

    If you submit a request with the same client token but a change in other
    parameters within the 8-hour idempotency window, DynamoDB returns an
    `ExportConflictException`.
    """

    s3_bucket: str | None = None
    """The name of the Amazon S3 bucket to export the snapshot to."""

    s3_bucket_owner: str | None = None
    """
    The ID of the Amazon Web Services account that owns the bucket the
    export will be stored in.

    Note:
        S3BucketOwner is a required parameter when exporting to a S3 bucket in
        another account.
    """

    s3_prefix: str | None = None
    """
    The Amazon S3 bucket prefix to use as the file name and path of the
    exported snapshot.
    """

    s3_sse_algorithm: S3SseAlgorithm | None = None
    """
    Type of encryption used on the bucket where export data will be stored.
    Valid values for `S3SseAlgorithm` are:

    - `AES256` - server-side encryption with Amazon S3 managed keys

    - `KMS` - server-side encryption with KMS managed keys
    """

    s3_sse_kms_key_id: str | None = None
    """
    The ID of the KMS managed key used to encrypt the S3 bucket where export
    data will be stored (if applicable).
    """

    export_format: ExportFormat | None = None
    """
    The format for the exported data. Valid values for `ExportFormat` are
    `DYNAMODB_JSON` or `ION`.
    """

    export_type: ExportType | None = None
    """
    Choice of whether to execute as a full export or incremental export.
    Valid values are FULL_EXPORT or INCREMENTAL_EXPORT. The default value is
    FULL_EXPORT. If INCREMENTAL_EXPORT is provided, the
    IncrementalExportSpecification must also be used.
    """

    incremental_export_specification: IncrementalExportSpecification | None = None
    """
    Optional object containing the parameters specific to an incremental
    export.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_arn is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["TableArn"],
                self.table_arn,
            )

        if self.export_time is not None:
            serializer.write_timestamp(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["ExportTime"],
                self.export_time,
            )

        if self.client_token is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["ClientToken"],
                self.client_token,
            )

        if self.s3_bucket is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["S3Bucket"],
                self.s3_bucket,
            )

        if self.s3_bucket_owner is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["S3BucketOwner"],
                self.s3_bucket_owner,
            )

        if self.s3_prefix is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["S3Prefix"],
                self.s3_prefix,
            )

        if self.s3_sse_algorithm is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["S3SseAlgorithm"],
                self.s3_sse_algorithm,
            )

        if self.s3_sse_kms_key_id is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["S3SseKmsKeyId"],
                self.s3_sse_kms_key_id,
            )

        if self.export_format is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["ExportFormat"],
                self.export_format,
            )

        if self.export_type is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["ExportType"],
                self.export_type,
            )

        if self.incremental_export_specification is not None:
            serializer.write_struct(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members[
                    "IncrementalExportSpecification"
                ],
                self.incremental_export_specification,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_arn"] = de.read_string(
                        _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["TableArn"]
                    )

                case 1:
                    kwargs["export_time"] = de.read_timestamp(
                        _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members[
                            "ExportTime"
                        ]
                    )

                case 2:
                    kwargs["client_token"] = de.read_string(
                        _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members[
                            "ClientToken"
                        ]
                    )

                case 3:
                    kwargs["s3_bucket"] = de.read_string(
                        _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["S3Bucket"]
                    )

                case 4:
                    kwargs["s3_bucket_owner"] = de.read_string(
                        _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members[
                            "S3BucketOwner"
                        ]
                    )

                case 5:
                    kwargs["s3_prefix"] = de.read_string(
                        _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members["S3Prefix"]
                    )

                case 6:
                    kwargs["s3_sse_algorithm"] = S3SseAlgorithm(
                        de.read_string(
                            _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members[
                                "S3SseAlgorithm"
                            ]
                        )
                    )

                case 7:
                    kwargs["s3_sse_kms_key_id"] = de.read_string(
                        _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members[
                            "S3SseKmsKeyId"
                        ]
                    )

                case 8:
                    kwargs["export_format"] = ExportFormat(
                        de.read_string(
                            _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members[
                                "ExportFormat"
                            ]
                        )
                    )

                case 9:
                    kwargs["export_type"] = ExportType(
                        de.read_string(
                            _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT.members[
                                "ExportType"
                            ]
                        )
                    )

                case 10:
                    kwargs["incremental_export_specification"] = (
                        IncrementalExportSpecification.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ExportTableToPointInTimeOutput:
    """Dataclass for ExportTableToPointInTimeOutput structure."""

    export_description: ExportDescription | None = None
    """Contains a description of the table export."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.export_description is not None:
            serializer.write_struct(
                _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_OUTPUT.members[
                    "ExportDescription"
                ],
                self.export_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["export_description"] = ExportDescription.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class InvalidExportTimeException(ServiceError):
    """
    The specified `ExportTime` is outside of the point in time recovery
    window.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_EXPORT_TIME_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_EXPORT_TIME_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_EXPORT_TIME_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_INVALID_EXPORT_TIME_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class PointInTimeRecoveryUnavailableException(ServiceError):
    """Point in time recovery has not yet been enabled for this source table."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_POINT_IN_TIME_RECOVERY_UNAVAILABLE_EXCEPTION, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_POINT_IN_TIME_RECOVERY_UNAVAILABLE_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_POINT_IN_TIME_RECOVERY_UNAVAILABLE_EXCEPTION.members[
                            "message"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_POINT_IN_TIME_RECOVERY_UNAVAILABLE_EXCEPTION, consumer=_consumer
        )
        return kwargs


EXPORT_TABLE_TO_POINT_IN_TIME = APIOperation(
    input=ExportTableToPointInTimeInput,
    output=ExportTableToPointInTimeOutput,
    schema=_SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME,
    input_schema=_SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_INPUT,
    output_schema=_SCHEMA_EXPORT_TABLE_TO_POINT_IN_TIME_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#ExportConflictException"
            ): ExportConflictException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidExportTimeException"
            ): InvalidExportTimeException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#PointInTimeRecoveryUnavailableException"
            ): PointInTimeRecoveryUnavailableException,
            ShapeID(
                "com.amazonaws.dynamodb#TableNotFoundException"
            ): TableNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_EXPORT_CONFLICT_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_EXPORT_TIME_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_POINT_IN_TIME_RECOVERY_UNAVAILABLE_EXCEPTION,
        _SCHEMA_TABLE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class GetResourcePolicyInput:
    """Dataclass for GetResourcePolicyInput structure."""

    resource_arn: str | None = None
    """
    The Amazon Resource Name (ARN) of the DynamoDB resource to which the
    policy is attached. The resources you can specify include tables and
    streams.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_RESOURCE_POLICY_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.resource_arn is not None:
            serializer.write_string(
                _SCHEMA_GET_RESOURCE_POLICY_INPUT.members["ResourceArn"],
                self.resource_arn,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["resource_arn"] = de.read_string(
                        _SCHEMA_GET_RESOURCE_POLICY_INPUT.members["ResourceArn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_RESOURCE_POLICY_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GetResourcePolicyOutput:
    """Dataclass for GetResourcePolicyOutput structure."""

    policy: str | None = None
    """
    The resource-based policy document attached to the resource, which can
    be a table or stream, in JSON format.
    """

    revision_id: str | None = None
    """
    A unique string that represents the revision ID of the policy. If
    you're comparing revision IDs, make sure to always use string
    comparison logic.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_RESOURCE_POLICY_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.policy is not None:
            serializer.write_string(
                _SCHEMA_GET_RESOURCE_POLICY_OUTPUT.members["Policy"], self.policy
            )

        if self.revision_id is not None:
            serializer.write_string(
                _SCHEMA_GET_RESOURCE_POLICY_OUTPUT.members["RevisionId"],
                self.revision_id,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["policy"] = de.read_string(
                        _SCHEMA_GET_RESOURCE_POLICY_OUTPUT.members["Policy"]
                    )

                case 1:
                    kwargs["revision_id"] = de.read_string(
                        _SCHEMA_GET_RESOURCE_POLICY_OUTPUT.members["RevisionId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_RESOURCE_POLICY_OUTPUT, consumer=_consumer)
        return kwargs


GET_RESOURCE_POLICY = APIOperation(
    input=GetResourcePolicyInput,
    output=GetResourcePolicyOutput,
    schema=_SCHEMA_GET_RESOURCE_POLICY,
    input_schema=_SCHEMA_GET_RESOURCE_POLICY_INPUT,
    output_schema=_SCHEMA_GET_RESOURCE_POLICY_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#PolicyNotFoundException"
            ): PolicyNotFoundException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_POLICY_NOT_FOUND_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class ImportConflictException(ServiceError):
    """
    There was a conflict when importing from the specified S3 source. This
    can occur when the current import conflicts with a previous import
    request that had the same client token.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_IMPORT_CONFLICT_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_CONFLICT_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_IMPORT_CONFLICT_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_IMPORT_CONFLICT_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ImportTableInput:
    """Dataclass for ImportTableInput structure."""

    client_token: str | None = None
    """
    Providing a `ClientToken` makes the call to `ImportTableInput`
    idempotent, meaning that multiple identical calls have the same effect
    as one single call.

    A client token is valid for 8 hours after the first request that uses it
    is completed. After 8 hours, any request with the same client token is
    treated as a new request. Do not resubmit the same request with the same
    client token for more than 8 hours, or the result might not be
    idempotent.

    If you submit a request with the same client token but a change in other
    parameters within the 8-hour idempotency window, DynamoDB returns an
    `IdempotentParameterMismatch` exception.
    """

    s3_bucket_source: S3BucketSource | None = None
    """The S3 bucket that provides the source for the import."""

    input_format: InputFormat | None = None
    """
    The format of the source data. Valid values for `ImportFormat` are
    `CSV`, `DYNAMODB_JSON` or `ION`.
    """

    input_format_options: InputFormatOptions | None = None
    """Additional properties that specify how the input is formatted,"""

    input_compression_type: InputCompressionType | None = None
    """
    Type of compression to be used on the input coming from the imported
    table.
    """

    table_creation_parameters: TableCreationParameters | None = None
    """Parameters for the table to import the data into."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_IMPORT_TABLE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.client_token is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_INPUT.members["ClientToken"], self.client_token
            )

        if self.s3_bucket_source is not None:
            serializer.write_struct(
                _SCHEMA_IMPORT_TABLE_INPUT.members["S3BucketSource"],
                self.s3_bucket_source,
            )

        if self.input_format is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_INPUT.members["InputFormat"], self.input_format
            )

        if self.input_format_options is not None:
            serializer.write_struct(
                _SCHEMA_IMPORT_TABLE_INPUT.members["InputFormatOptions"],
                self.input_format_options,
            )

        if self.input_compression_type is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_TABLE_INPUT.members["InputCompressionType"],
                self.input_compression_type,
            )

        if self.table_creation_parameters is not None:
            serializer.write_struct(
                _SCHEMA_IMPORT_TABLE_INPUT.members["TableCreationParameters"],
                self.table_creation_parameters,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["client_token"] = de.read_string(
                        _SCHEMA_IMPORT_TABLE_INPUT.members["ClientToken"]
                    )

                case 1:
                    kwargs["s3_bucket_source"] = S3BucketSource.deserialize(de)

                case 2:
                    kwargs["input_format"] = InputFormat(
                        de.read_string(
                            _SCHEMA_IMPORT_TABLE_INPUT.members["InputFormat"]
                        )
                    )

                case 3:
                    kwargs["input_format_options"] = InputFormatOptions.deserialize(de)

                case 4:
                    kwargs["input_compression_type"] = InputCompressionType(
                        de.read_string(
                            _SCHEMA_IMPORT_TABLE_INPUT.members["InputCompressionType"]
                        )
                    )

                case 5:
                    kwargs["table_creation_parameters"] = (
                        TableCreationParameters.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_IMPORT_TABLE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ImportTableOutput:
    """Dataclass for ImportTableOutput structure."""

    import_table_description: ImportTableDescription
    """
    Represents the properties of the table created for the import, and
    parameters of the import. The import parameters include import status,
    how many items were processed, and how many errors were encountered.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_IMPORT_TABLE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_IMPORT_TABLE_OUTPUT.members["ImportTableDescription"],
            self.import_table_description,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["import_table_description"] = (
                        ImportTableDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_IMPORT_TABLE_OUTPUT, consumer=_consumer)
        if "import_table_description" not in kwargs:
            kwargs["import_table_description"] = (
                ImportTableDescription._smithy_default()
            )
        return kwargs


IMPORT_TABLE = APIOperation(
    input=ImportTableInput,
    output=ImportTableOutput,
    schema=_SCHEMA_IMPORT_TABLE,
    input_schema=_SCHEMA_IMPORT_TABLE_INPUT,
    output_schema=_SCHEMA_IMPORT_TABLE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#ImportConflictException"
            ): ImportConflictException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_IMPORT_CONFLICT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class ListBackupsInput:
    """Dataclass for ListBackupsInput structure."""

    table_name: str | None = None
    """
    Lists the backups from the table specified in `TableName`. You can also
    provide the Amazon Resource Name (ARN) of the table in this parameter.
    """

    limit: int | None = None
    """Maximum number of backups to return at once."""

    time_range_lower_bound: datetime | None = None
    """
    Only backups created after this time are listed. `TimeRangeLowerBound`
    is inclusive.
    """

    time_range_upper_bound: datetime | None = None
    """
    Only backups created before this time are listed. `TimeRangeUpperBound`
    is exclusive.
    """

    exclusive_start_backup_arn: str | None = None
    """
    `LastEvaluatedBackupArn` is the Amazon Resource Name (ARN) of the backup
    last evaluated when the current page of results was returned, inclusive
    of the current page of results. This value may be specified as the
    `ExclusiveStartBackupArn` of a new `ListBackups` operation in order to
    fetch the next page of results.
    """

    backup_type: BackupTypeFilter | None = None
    """
    The backups from the table specified by `BackupType` are listed.

    Where `BackupType` can be:

    - `USER` - On-demand backup created by you. (The default setting if no
      other backup types are specified.)

    - `SYSTEM` - On-demand backup automatically created by DynamoDB.

    - `ALL` - All types of on-demand backups (USER and SYSTEM).
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_BACKUPS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_LIST_BACKUPS_INPUT.members["TableName"], self.table_name
            )

        if self.limit is not None:
            serializer.write_integer(
                _SCHEMA_LIST_BACKUPS_INPUT.members["Limit"], self.limit
            )

        if self.time_range_lower_bound is not None:
            serializer.write_timestamp(
                _SCHEMA_LIST_BACKUPS_INPUT.members["TimeRangeLowerBound"],
                self.time_range_lower_bound,
            )

        if self.time_range_upper_bound is not None:
            serializer.write_timestamp(
                _SCHEMA_LIST_BACKUPS_INPUT.members["TimeRangeUpperBound"],
                self.time_range_upper_bound,
            )

        if self.exclusive_start_backup_arn is not None:
            serializer.write_string(
                _SCHEMA_LIST_BACKUPS_INPUT.members["ExclusiveStartBackupArn"],
                self.exclusive_start_backup_arn,
            )

        if self.backup_type is not None:
            serializer.write_string(
                _SCHEMA_LIST_BACKUPS_INPUT.members["BackupType"], self.backup_type
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_LIST_BACKUPS_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["limit"] = de.read_integer(
                        _SCHEMA_LIST_BACKUPS_INPUT.members["Limit"]
                    )

                case 2:
                    kwargs["time_range_lower_bound"] = de.read_timestamp(
                        _SCHEMA_LIST_BACKUPS_INPUT.members["TimeRangeLowerBound"]
                    )

                case 3:
                    kwargs["time_range_upper_bound"] = de.read_timestamp(
                        _SCHEMA_LIST_BACKUPS_INPUT.members["TimeRangeUpperBound"]
                    )

                case 4:
                    kwargs["exclusive_start_backup_arn"] = de.read_string(
                        _SCHEMA_LIST_BACKUPS_INPUT.members["ExclusiveStartBackupArn"]
                    )

                case 5:
                    kwargs["backup_type"] = BackupTypeFilter(
                        de.read_string(_SCHEMA_LIST_BACKUPS_INPUT.members["BackupType"])
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_BACKUPS_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ListBackupsOutput:
    """Dataclass for ListBackupsOutput structure."""

    backup_summaries: list[BackupSummary] | None = None
    """List of `BackupSummary` objects."""

    last_evaluated_backup_arn: str | None = None
    """
    The ARN of the backup last evaluated when the current page of results
    was returned, inclusive of the current page of results. This value may
    be specified as the `ExclusiveStartBackupArn` of a new `ListBackups`
    operation in order to fetch the next page of results.

    If `LastEvaluatedBackupArn` is empty, then the last page of results has
    been processed and there are no more results to be retrieved.

    If `LastEvaluatedBackupArn` is not empty, this may or may not indicate
    that there is more data to be returned. All results are guaranteed to
    have been returned if and only if no value for `LastEvaluatedBackupArn`
    is returned.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_BACKUPS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.backup_summaries is not None:
            _serialize_backup_summaries(
                serializer,
                _SCHEMA_LIST_BACKUPS_OUTPUT.members["BackupSummaries"],
                self.backup_summaries,
            )

        if self.last_evaluated_backup_arn is not None:
            serializer.write_string(
                _SCHEMA_LIST_BACKUPS_OUTPUT.members["LastEvaluatedBackupArn"],
                self.last_evaluated_backup_arn,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["backup_summaries"] = _deserialize_backup_summaries(
                        de, _SCHEMA_LIST_BACKUPS_OUTPUT.members["BackupSummaries"]
                    )

                case 1:
                    kwargs["last_evaluated_backup_arn"] = de.read_string(
                        _SCHEMA_LIST_BACKUPS_OUTPUT.members["LastEvaluatedBackupArn"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_BACKUPS_OUTPUT, consumer=_consumer)
        return kwargs


LIST_BACKUPS = APIOperation(
    input=ListBackupsInput,
    output=ListBackupsOutput,
    schema=_SCHEMA_LIST_BACKUPS,
    input_schema=_SCHEMA_LIST_BACKUPS_INPUT,
    output_schema=_SCHEMA_LIST_BACKUPS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_INTERNAL_SERVER_ERROR, _SCHEMA_INVALID_ENDPOINT_EXCEPTION],
)


@dataclass(kw_only=True)
class ListContributorInsightsInput:
    """Dataclass for ListContributorInsightsInput structure."""

    table_name: str | None = None
    """
    The name of the table. You can also provide the Amazon Resource Name
    (ARN) of the table in this parameter.
    """

    next_token: str | None = None
    """A token to for the desired page, if there is one."""

    max_results: int = 0
    """Maximum number of results to return per page."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_INPUT.members["TableName"],
                self.table_name,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_INPUT.members["NextToken"],
                self.next_token,
            )

        if self.max_results is not None:
            serializer.write_integer(
                _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_INPUT.members["MaxResults"],
                self.max_results,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_INPUT.members["NextToken"]
                    )

                case 2:
                    kwargs["max_results"] = de.read_integer(
                        _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_INPUT.members["MaxResults"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ListContributorInsightsOutput:
    """Dataclass for ListContributorInsightsOutput structure."""

    contributor_insights_summaries: list[ContributorInsightsSummary] | None = None
    """A list of ContributorInsightsSummary."""

    next_token: str | None = None
    """A token to go to the next page if there is one."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.contributor_insights_summaries is not None:
            _serialize_contributor_insights_summaries(
                serializer,
                _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                    "ContributorInsightsSummaries"
                ],
                self.contributor_insights_summaries,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_OUTPUT.members["NextToken"],
                self.next_token,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["contributor_insights_summaries"] = (
                        _deserialize_contributor_insights_summaries(
                            de,
                            _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                                "ContributorInsightsSummaries"
                            ],
                        )
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_OUTPUT.members["NextToken"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_OUTPUT, consumer=_consumer
        )
        return kwargs


LIST_CONTRIBUTOR_INSIGHTS = APIOperation(
    input=ListContributorInsightsInput,
    output=ListContributorInsightsOutput,
    schema=_SCHEMA_LIST_CONTRIBUTOR_INSIGHTS,
    input_schema=_SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_INPUT,
    output_schema=_SCHEMA_LIST_CONTRIBUTOR_INSIGHTS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_INTERNAL_SERVER_ERROR, _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION],
)


@dataclass(kw_only=True)
class ListExportsInput:
    """Dataclass for ListExportsInput structure."""

    table_arn: str | None = None
    """The Amazon Resource Name (ARN) associated with the exported table."""

    max_results: int | None = None
    """Maximum number of results to return per page."""

    next_token: str | None = None
    """
    An optional string that, if supplied, must be copied from the output of
    a previous call to `ListExports`. When provided in this manner, the API
    fetches the next page of results.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_EXPORTS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_arn is not None:
            serializer.write_string(
                _SCHEMA_LIST_EXPORTS_INPUT.members["TableArn"], self.table_arn
            )

        if self.max_results is not None:
            serializer.write_integer(
                _SCHEMA_LIST_EXPORTS_INPUT.members["MaxResults"], self.max_results
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_EXPORTS_INPUT.members["NextToken"], self.next_token
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_arn"] = de.read_string(
                        _SCHEMA_LIST_EXPORTS_INPUT.members["TableArn"]
                    )

                case 1:
                    kwargs["max_results"] = de.read_integer(
                        _SCHEMA_LIST_EXPORTS_INPUT.members["MaxResults"]
                    )

                case 2:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_EXPORTS_INPUT.members["NextToken"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_EXPORTS_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ExportSummary:
    """Summary information about an export task."""

    export_arn: str | None = None
    """The Amazon Resource Name (ARN) of the export."""

    export_status: ExportStatus | None = None
    """
    Export can be in one of the following states: IN_PROGRESS, COMPLETED, or
    FAILED.
    """

    export_type: ExportType | None = None
    """
    The type of export that was performed. Valid values are `FULL_EXPORT` or
    `INCREMENTAL_EXPORT`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXPORT_SUMMARY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.export_arn is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_SUMMARY.members["ExportArn"], self.export_arn
            )

        if self.export_status is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_SUMMARY.members["ExportStatus"], self.export_status
            )

        if self.export_type is not None:
            serializer.write_string(
                _SCHEMA_EXPORT_SUMMARY.members["ExportType"], self.export_type
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["export_arn"] = de.read_string(
                        _SCHEMA_EXPORT_SUMMARY.members["ExportArn"]
                    )

                case 1:
                    kwargs["export_status"] = ExportStatus(
                        de.read_string(_SCHEMA_EXPORT_SUMMARY.members["ExportStatus"])
                    )

                case 2:
                    kwargs["export_type"] = ExportType(
                        de.read_string(_SCHEMA_EXPORT_SUMMARY.members["ExportType"])
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EXPORT_SUMMARY, consumer=_consumer)
        return kwargs


def _serialize_export_summaries(
    serializer: ShapeSerializer, schema: Schema, value: list[ExportSummary]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_export_summaries(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ExportSummary]:
    result: list[ExportSummary] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ExportSummary.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ListExportsOutput:
    """Dataclass for ListExportsOutput structure."""

    export_summaries: list[ExportSummary] | None = None
    """A list of `ExportSummary` objects."""

    next_token: str | None = None
    """
    If this value is returned, there are additional results to be displayed.
    To retrieve them, call `ListExports` again, with `NextToken` set to this
    value.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_EXPORTS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.export_summaries is not None:
            _serialize_export_summaries(
                serializer,
                _SCHEMA_LIST_EXPORTS_OUTPUT.members["ExportSummaries"],
                self.export_summaries,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_EXPORTS_OUTPUT.members["NextToken"], self.next_token
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["export_summaries"] = _deserialize_export_summaries(
                        de, _SCHEMA_LIST_EXPORTS_OUTPUT.members["ExportSummaries"]
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_EXPORTS_OUTPUT.members["NextToken"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_EXPORTS_OUTPUT, consumer=_consumer)
        return kwargs


LIST_EXPORTS = APIOperation(
    input=ListExportsInput,
    output=ListExportsOutput,
    schema=_SCHEMA_LIST_EXPORTS,
    input_schema=_SCHEMA_LIST_EXPORTS_INPUT,
    output_schema=_SCHEMA_LIST_EXPORTS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_INTERNAL_SERVER_ERROR, _SCHEMA_LIMIT_EXCEEDED_EXCEPTION],
)


@dataclass(kw_only=True)
class ListGlobalTablesInput:
    """Dataclass for ListGlobalTablesInput structure."""

    exclusive_start_global_table_name: str | None = None
    """The first global table name that this operation will evaluate."""

    limit: int | None = None
    """
    The maximum number of table names to return, if the parameter is not
    specified DynamoDB defaults to 100.

    If the number of global tables DynamoDB finds reaches this limit, it
    stops the operation and returns the table names collected up to that
    point, with a table name in the `LastEvaluatedGlobalTableName` to apply
    in a subsequent operation to the `ExclusiveStartGlobalTableName`
    parameter.
    """

    region_name: str | None = None
    """Lists the global tables in a specific Region."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_GLOBAL_TABLES_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.exclusive_start_global_table_name is not None:
            serializer.write_string(
                _SCHEMA_LIST_GLOBAL_TABLES_INPUT.members[
                    "ExclusiveStartGlobalTableName"
                ],
                self.exclusive_start_global_table_name,
            )

        if self.limit is not None:
            serializer.write_integer(
                _SCHEMA_LIST_GLOBAL_TABLES_INPUT.members["Limit"], self.limit
            )

        if self.region_name is not None:
            serializer.write_string(
                _SCHEMA_LIST_GLOBAL_TABLES_INPUT.members["RegionName"], self.region_name
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["exclusive_start_global_table_name"] = de.read_string(
                        _SCHEMA_LIST_GLOBAL_TABLES_INPUT.members[
                            "ExclusiveStartGlobalTableName"
                        ]
                    )

                case 1:
                    kwargs["limit"] = de.read_integer(
                        _SCHEMA_LIST_GLOBAL_TABLES_INPUT.members["Limit"]
                    )

                case 2:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_LIST_GLOBAL_TABLES_INPUT.members["RegionName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_GLOBAL_TABLES_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GlobalTable:
    """Represents the properties of a global table."""

    global_table_name: str | None = None
    """The global table name."""

    replication_group: list[Replica] | None = None
    """The Regions where the global table has replicas."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GLOBAL_TABLE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_name is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_TABLE.members["GlobalTableName"], self.global_table_name
            )

        if self.replication_group is not None:
            _serialize_replica_list(
                serializer,
                _SCHEMA_GLOBAL_TABLE.members["ReplicationGroup"],
                self.replication_group,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_name"] = de.read_string(
                        _SCHEMA_GLOBAL_TABLE.members["GlobalTableName"]
                    )

                case 1:
                    kwargs["replication_group"] = _deserialize_replica_list(
                        de, _SCHEMA_GLOBAL_TABLE.members["ReplicationGroup"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GLOBAL_TABLE, consumer=_consumer)
        return kwargs


def _serialize_global_table_list(
    serializer: ShapeSerializer, schema: Schema, value: list[GlobalTable]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_global_table_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[GlobalTable]:
    result: list[GlobalTable] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(GlobalTable.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ListGlobalTablesOutput:
    """Dataclass for ListGlobalTablesOutput structure."""

    global_tables: list[GlobalTable] | None = None
    """List of global table names."""

    last_evaluated_global_table_name: str | None = None
    """Last evaluated global table name."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_GLOBAL_TABLES_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_tables is not None:
            _serialize_global_table_list(
                serializer,
                _SCHEMA_LIST_GLOBAL_TABLES_OUTPUT.members["GlobalTables"],
                self.global_tables,
            )

        if self.last_evaluated_global_table_name is not None:
            serializer.write_string(
                _SCHEMA_LIST_GLOBAL_TABLES_OUTPUT.members[
                    "LastEvaluatedGlobalTableName"
                ],
                self.last_evaluated_global_table_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_tables"] = _deserialize_global_table_list(
                        de, _SCHEMA_LIST_GLOBAL_TABLES_OUTPUT.members["GlobalTables"]
                    )

                case 1:
                    kwargs["last_evaluated_global_table_name"] = de.read_string(
                        _SCHEMA_LIST_GLOBAL_TABLES_OUTPUT.members[
                            "LastEvaluatedGlobalTableName"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_GLOBAL_TABLES_OUTPUT, consumer=_consumer)
        return kwargs


LIST_GLOBAL_TABLES = APIOperation(
    input=ListGlobalTablesInput,
    output=ListGlobalTablesOutput,
    schema=_SCHEMA_LIST_GLOBAL_TABLES,
    input_schema=_SCHEMA_LIST_GLOBAL_TABLES_INPUT,
    output_schema=_SCHEMA_LIST_GLOBAL_TABLES_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_INTERNAL_SERVER_ERROR, _SCHEMA_INVALID_ENDPOINT_EXCEPTION],
)


@dataclass(kw_only=True)
class ListImportsInput:
    """Dataclass for ListImportsInput structure."""

    table_arn: str | None = None
    """
    The Amazon Resource Name (ARN) associated with the table that was
    imported to.
    """

    page_size: int | None = None
    """The number of `ImportSummary `objects returned in a single page."""

    next_token: str | None = None
    """
    An optional string that, if supplied, must be copied from the output of
    a previous call to `ListImports`. When provided in this manner, the API
    fetches the next page of results.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_IMPORTS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_arn is not None:
            serializer.write_string(
                _SCHEMA_LIST_IMPORTS_INPUT.members["TableArn"], self.table_arn
            )

        if self.page_size is not None:
            serializer.write_integer(
                _SCHEMA_LIST_IMPORTS_INPUT.members["PageSize"], self.page_size
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_IMPORTS_INPUT.members["NextToken"], self.next_token
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_arn"] = de.read_string(
                        _SCHEMA_LIST_IMPORTS_INPUT.members["TableArn"]
                    )

                case 1:
                    kwargs["page_size"] = de.read_integer(
                        _SCHEMA_LIST_IMPORTS_INPUT.members["PageSize"]
                    )

                case 2:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_IMPORTS_INPUT.members["NextToken"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_IMPORTS_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ImportSummary:
    """Summary information about the source file for the import."""

    import_arn: str | None = None
    """The Amazon Resource Number (ARN) corresponding to the import request."""

    import_status: ImportStatus | None = None
    """The status of the import operation."""

    table_arn: str | None = None
    """The Amazon Resource Number (ARN) of the table being imported into."""

    s3_bucket_source: S3BucketSource | None = None
    """
    The path and S3 bucket of the source file that is being imported. This
    includes the S3Bucket (required), S3KeyPrefix (optional) and
    S3BucketOwner (optional if the bucket is owned by the requester).
    """

    cloud_watch_log_group_arn: str | None = None
    """
    The Amazon Resource Number (ARN) of the Cloudwatch Log Group associated
    with this import task.
    """

    input_format: InputFormat | None = None
    """
    The format of the source data. Valid values are `CSV`, `DYNAMODB_JSON`
    or `ION`.
    """

    start_time: datetime | None = None
    """The time at which this import task began."""

    end_time: datetime | None = None
    """The time at which this import task ended."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_IMPORT_SUMMARY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.import_arn is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_SUMMARY.members["ImportArn"], self.import_arn
            )

        if self.import_status is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_SUMMARY.members["ImportStatus"], self.import_status
            )

        if self.table_arn is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_SUMMARY.members["TableArn"], self.table_arn
            )

        if self.s3_bucket_source is not None:
            serializer.write_struct(
                _SCHEMA_IMPORT_SUMMARY.members["S3BucketSource"], self.s3_bucket_source
            )

        if self.cloud_watch_log_group_arn is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_SUMMARY.members["CloudWatchLogGroupArn"],
                self.cloud_watch_log_group_arn,
            )

        if self.input_format is not None:
            serializer.write_string(
                _SCHEMA_IMPORT_SUMMARY.members["InputFormat"], self.input_format
            )

        if self.start_time is not None:
            serializer.write_timestamp(
                _SCHEMA_IMPORT_SUMMARY.members["StartTime"], self.start_time
            )

        if self.end_time is not None:
            serializer.write_timestamp(
                _SCHEMA_IMPORT_SUMMARY.members["EndTime"], self.end_time
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["import_arn"] = de.read_string(
                        _SCHEMA_IMPORT_SUMMARY.members["ImportArn"]
                    )

                case 1:
                    kwargs["import_status"] = ImportStatus(
                        de.read_string(_SCHEMA_IMPORT_SUMMARY.members["ImportStatus"])
                    )

                case 2:
                    kwargs["table_arn"] = de.read_string(
                        _SCHEMA_IMPORT_SUMMARY.members["TableArn"]
                    )

                case 3:
                    kwargs["s3_bucket_source"] = S3BucketSource.deserialize(de)

                case 4:
                    kwargs["cloud_watch_log_group_arn"] = de.read_string(
                        _SCHEMA_IMPORT_SUMMARY.members["CloudWatchLogGroupArn"]
                    )

                case 5:
                    kwargs["input_format"] = InputFormat(
                        de.read_string(_SCHEMA_IMPORT_SUMMARY.members["InputFormat"])
                    )

                case 6:
                    kwargs["start_time"] = de.read_timestamp(
                        _SCHEMA_IMPORT_SUMMARY.members["StartTime"]
                    )

                case 7:
                    kwargs["end_time"] = de.read_timestamp(
                        _SCHEMA_IMPORT_SUMMARY.members["EndTime"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_IMPORT_SUMMARY, consumer=_consumer)
        return kwargs


def _serialize_import_summary_list(
    serializer: ShapeSerializer, schema: Schema, value: list[ImportSummary]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_import_summary_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ImportSummary]:
    result: list[ImportSummary] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ImportSummary.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ListImportsOutput:
    """Dataclass for ListImportsOutput structure."""

    import_summary_list: list[ImportSummary] | None = None
    """A list of `ImportSummary` objects."""

    next_token: str | None = None
    """
    If this value is returned, there are additional results to be displayed.
    To retrieve them, call `ListImports` again, with `NextToken` set to this
    value.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_IMPORTS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.import_summary_list is not None:
            _serialize_import_summary_list(
                serializer,
                _SCHEMA_LIST_IMPORTS_OUTPUT.members["ImportSummaryList"],
                self.import_summary_list,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_IMPORTS_OUTPUT.members["NextToken"], self.next_token
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["import_summary_list"] = _deserialize_import_summary_list(
                        de, _SCHEMA_LIST_IMPORTS_OUTPUT.members["ImportSummaryList"]
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_IMPORTS_OUTPUT.members["NextToken"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_IMPORTS_OUTPUT, consumer=_consumer)
        return kwargs


LIST_IMPORTS = APIOperation(
    input=ListImportsInput,
    output=ListImportsOutput,
    schema=_SCHEMA_LIST_IMPORTS,
    input_schema=_SCHEMA_LIST_IMPORTS_INPUT,
    output_schema=_SCHEMA_LIST_IMPORTS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_LIMIT_EXCEEDED_EXCEPTION],
)


@dataclass(kw_only=True)
class ListTablesInput:
    """Represents the input of a `ListTables` operation."""

    exclusive_start_table_name: str | None = None
    """
    The first table name that this operation will evaluate. Use the value
    that was returned for `LastEvaluatedTableName` in a previous operation,
    so that you can obtain the next page of results.
    """

    limit: int | None = None
    """
    A maximum number of table names to return. If this parameter is not
    specified, the limit is 100.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_TABLES_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.exclusive_start_table_name is not None:
            serializer.write_string(
                _SCHEMA_LIST_TABLES_INPUT.members["ExclusiveStartTableName"],
                self.exclusive_start_table_name,
            )

        if self.limit is not None:
            serializer.write_integer(
                _SCHEMA_LIST_TABLES_INPUT.members["Limit"], self.limit
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["exclusive_start_table_name"] = de.read_string(
                        _SCHEMA_LIST_TABLES_INPUT.members["ExclusiveStartTableName"]
                    )

                case 1:
                    kwargs["limit"] = de.read_integer(
                        _SCHEMA_LIST_TABLES_INPUT.members["Limit"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_TABLES_INPUT, consumer=_consumer)
        return kwargs


def _serialize_table_name_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_table_name_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ListTablesOutput:
    """Represents the output of a `ListTables` operation."""

    table_names: list[str] | None = None
    """
    The names of the tables associated with the current account at the
    current endpoint. The maximum size of this array is 100.

    If `LastEvaluatedTableName` also appears in the output, you can use this
    value as the `ExclusiveStartTableName` parameter in a subsequent
    `ListTables` request and obtain the next page of results.
    """

    last_evaluated_table_name: str | None = None
    """
    The name of the last table in the current page of results. Use this
    value as the `ExclusiveStartTableName` in a new request to obtain the
    next page of results, until all the table names are returned.

    If you do not receive a `LastEvaluatedTableName` value in the response,
    this means that there are no more table names to be retrieved.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_TABLES_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_names is not None:
            _serialize_table_name_list(
                serializer,
                _SCHEMA_LIST_TABLES_OUTPUT.members["TableNames"],
                self.table_names,
            )

        if self.last_evaluated_table_name is not None:
            serializer.write_string(
                _SCHEMA_LIST_TABLES_OUTPUT.members["LastEvaluatedTableName"],
                self.last_evaluated_table_name,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_names"] = _deserialize_table_name_list(
                        de, _SCHEMA_LIST_TABLES_OUTPUT.members["TableNames"]
                    )

                case 1:
                    kwargs["last_evaluated_table_name"] = de.read_string(
                        _SCHEMA_LIST_TABLES_OUTPUT.members["LastEvaluatedTableName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_TABLES_OUTPUT, consumer=_consumer)
        return kwargs


LIST_TABLES = APIOperation(
    input=ListTablesInput,
    output=ListTablesOutput,
    schema=_SCHEMA_LIST_TABLES,
    input_schema=_SCHEMA_LIST_TABLES_INPUT,
    output_schema=_SCHEMA_LIST_TABLES_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_INTERNAL_SERVER_ERROR, _SCHEMA_INVALID_ENDPOINT_EXCEPTION],
)


@dataclass(kw_only=True)
class ListTagsOfResourceInput:
    """Dataclass for ListTagsOfResourceInput structure."""

    resource_arn: str | None = None
    """
    The Amazon DynamoDB resource with tags to be listed. This value is an
    Amazon Resource Name (ARN).
    """

    next_token: str | None = None
    """
    An optional string that, if supplied, must be copied from the output of
    a previous call to ListTagOfResource. When provided in this manner, this
    API fetches the next page of results.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_TAGS_OF_RESOURCE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.resource_arn is not None:
            serializer.write_string(
                _SCHEMA_LIST_TAGS_OF_RESOURCE_INPUT.members["ResourceArn"],
                self.resource_arn,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_TAGS_OF_RESOURCE_INPUT.members["NextToken"],
                self.next_token,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["resource_arn"] = de.read_string(
                        _SCHEMA_LIST_TAGS_OF_RESOURCE_INPUT.members["ResourceArn"]
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_TAGS_OF_RESOURCE_INPUT.members["NextToken"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_LIST_TAGS_OF_RESOURCE_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ListTagsOfResourceOutput:
    """Dataclass for ListTagsOfResourceOutput structure."""

    tags: list[Tag] | None = None
    """The tags currently associated with the Amazon DynamoDB resource."""

    next_token: str | None = None
    """
    If this value is returned, there are additional results to be displayed.
    To retrieve them, call ListTagsOfResource again, with NextToken set to
    this value.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_TAGS_OF_RESOURCE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.tags is not None:
            _serialize_tag_list(
                serializer,
                _SCHEMA_LIST_TAGS_OF_RESOURCE_OUTPUT.members["Tags"],
                self.tags,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_TAGS_OF_RESOURCE_OUTPUT.members["NextToken"],
                self.next_token,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["tags"] = _deserialize_tag_list(
                        de, _SCHEMA_LIST_TAGS_OF_RESOURCE_OUTPUT.members["Tags"]
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_TAGS_OF_RESOURCE_OUTPUT.members["NextToken"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_LIST_TAGS_OF_RESOURCE_OUTPUT, consumer=_consumer
        )
        return kwargs


LIST_TAGS_OF_RESOURCE = APIOperation(
    input=ListTagsOfResourceInput,
    output=ListTagsOfResourceOutput,
    schema=_SCHEMA_LIST_TAGS_OF_RESOURCE,
    input_schema=_SCHEMA_LIST_TAGS_OF_RESOURCE_INPUT,
    output_schema=_SCHEMA_LIST_TAGS_OF_RESOURCE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class PutResourcePolicyInput:
    """Dataclass for PutResourcePolicyInput structure."""

    resource_arn: str | None = None
    """
    The Amazon Resource Name (ARN) of the DynamoDB resource to which the
    policy will be attached. The resources you can specify include tables
    and streams.

    You can control index permissions using the base table's policy. To
    specify the same permission level for your table and its indexes, you
    can provide both the table and index Amazon Resource Name (ARN)s in the
    `Resource` field of a given `Statement` in your policy document.
    Alternatively, to specify different permissions for your table, indexes,
    or both, you can define multiple `Statement` fields in your policy
    document.
    """

    policy: str | None = None
    """
    An Amazon Web Services resource-based policy document in JSON format.

    - The maximum size supported for a resource-based policy document is 20
      KB. DynamoDB counts whitespaces when calculating the size of a policy
      against this limit.

    - Within a resource-based policy, if the action for a DynamoDB
      service-linked role (SLR) to replicate data for a global table is
      denied, adding or deleting a replica will fail with an error.

    For a full list of all considerations that apply while attaching a
    resource-based policy, see [Resource-based policy
    considerations](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/rbac-considerations.html).
    """

    expected_revision_id: str | None = None
    """
    A string value that you can use to conditionally update your policy. You
    can provide the revision ID of your existing policy to make mutating
    requests against that policy.

    Note:
        When you provide an expected revision ID, if the revision ID of the
        existing policy on the resource doesn't match or if there's no policy
        attached to the resource, your request will be rejected with a
        `PolicyNotFoundException`.

    To conditionally attach a policy when no policy exists for the resource,
    specify `NO_POLICY` for the revision ID.
    """

    confirm_remove_self_resource_access: bool = False
    """
    Set this parameter to `true` to confirm that you want to remove your
    permissions to change the policy of this resource in the future.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PUT_RESOURCE_POLICY_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.resource_arn is not None:
            serializer.write_string(
                _SCHEMA_PUT_RESOURCE_POLICY_INPUT.members["ResourceArn"],
                self.resource_arn,
            )

        if self.policy is not None:
            serializer.write_string(
                _SCHEMA_PUT_RESOURCE_POLICY_INPUT.members["Policy"], self.policy
            )

        if self.expected_revision_id is not None:
            serializer.write_string(
                _SCHEMA_PUT_RESOURCE_POLICY_INPUT.members["ExpectedRevisionId"],
                self.expected_revision_id,
            )

        if self.confirm_remove_self_resource_access is not None:
            serializer.write_boolean(
                _SCHEMA_PUT_RESOURCE_POLICY_INPUT.members[
                    "ConfirmRemoveSelfResourceAccess"
                ],
                self.confirm_remove_self_resource_access,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["resource_arn"] = de.read_string(
                        _SCHEMA_PUT_RESOURCE_POLICY_INPUT.members["ResourceArn"]
                    )

                case 1:
                    kwargs["policy"] = de.read_string(
                        _SCHEMA_PUT_RESOURCE_POLICY_INPUT.members["Policy"]
                    )

                case 2:
                    kwargs["expected_revision_id"] = de.read_string(
                        _SCHEMA_PUT_RESOURCE_POLICY_INPUT.members["ExpectedRevisionId"]
                    )

                case 3:
                    kwargs["confirm_remove_self_resource_access"] = de.read_boolean(
                        _SCHEMA_PUT_RESOURCE_POLICY_INPUT.members[
                            "ConfirmRemoveSelfResourceAccess"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PUT_RESOURCE_POLICY_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class PutResourcePolicyOutput:
    """Dataclass for PutResourcePolicyOutput structure."""

    revision_id: str | None = None
    """
    A unique string that represents the revision ID of the policy. If
    you're comparing revision IDs, make sure to always use string
    comparison logic.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PUT_RESOURCE_POLICY_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.revision_id is not None:
            serializer.write_string(
                _SCHEMA_PUT_RESOURCE_POLICY_OUTPUT.members["RevisionId"],
                self.revision_id,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["revision_id"] = de.read_string(
                        _SCHEMA_PUT_RESOURCE_POLICY_OUTPUT.members["RevisionId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PUT_RESOURCE_POLICY_OUTPUT, consumer=_consumer)
        return kwargs


PUT_RESOURCE_POLICY = APIOperation(
    input=PutResourcePolicyInput,
    output=PutResourcePolicyOutput,
    schema=_SCHEMA_PUT_RESOURCE_POLICY,
    input_schema=_SCHEMA_PUT_RESOURCE_POLICY_INPUT,
    output_schema=_SCHEMA_PUT_RESOURCE_POLICY_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#PolicyNotFoundException"
            ): PolicyNotFoundException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_POLICY_NOT_FOUND_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


class Select(UnknownEnumMixin, StrEnum):
    ALL_ATTRIBUTES = "ALL_ATTRIBUTES"
    ALL_PROJECTED_ATTRIBUTES = "ALL_PROJECTED_ATTRIBUTES"
    SPECIFIC_ATTRIBUTES = "SPECIFIC_ATTRIBUTES"
    COUNT = "COUNT"


@dataclass(kw_only=True)
class RestoreTableFromBackupInput:
    """Dataclass for RestoreTableFromBackupInput structure."""

    target_table_name: str | None = None
    """The name of the new table to which the backup must be restored."""

    backup_arn: str | None = None
    """The Amazon Resource Name (ARN) associated with the backup."""

    billing_mode_override: BillingMode | None = None
    """The billing mode of the restored table."""

    global_secondary_index_override: list[GlobalSecondaryIndex] | None = None
    """
    List of global secondary indexes for the restored table. The indexes
    provided should match existing secondary indexes. You can choose to
    exclude some or all of the indexes at the time of restore.
    """

    local_secondary_index_override: list[LocalSecondaryIndex] | None = None
    """
    List of local secondary indexes for the restored table. The indexes
    provided should match existing secondary indexes. You can choose to
    exclude some or all of the indexes at the time of restore.
    """

    provisioned_throughput_override: ProvisionedThroughput | None = None
    """Provisioned throughput settings for the restored table."""

    on_demand_throughput_override: OnDemandThroughput | None = None
    """
    Sets the maximum number of read and write units for the specified
    on-demand table. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    sse_specification_override: SSESpecification | None = None
    """The new server-side encryption settings for the restored table."""

    vector_index_override: list[VectorIndex] | None = None
    """
    The vector indexes for the restored table. If not specified, all vector
    indexes from the backup are restored. The indexes provided must match
    existing vector indexes from the backup. You can choose to exclude some
    or all of the vector indexes at the time of restore.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.target_table_name is not None:
            serializer.write_string(
                _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members["TargetTableName"],
                self.target_table_name,
            )

        if self.backup_arn is not None:
            serializer.write_string(
                _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members["BackupArn"],
                self.backup_arn,
            )

        if self.billing_mode_override is not None:
            serializer.write_string(
                _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members["BillingModeOverride"],
                self.billing_mode_override,
            )

        if self.global_secondary_index_override is not None:
            _serialize_global_secondary_index_list(
                serializer,
                _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members[
                    "GlobalSecondaryIndexOverride"
                ],
                self.global_secondary_index_override,
            )

        if self.local_secondary_index_override is not None:
            _serialize_local_secondary_index_list(
                serializer,
                _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members[
                    "LocalSecondaryIndexOverride"
                ],
                self.local_secondary_index_override,
            )

        if self.provisioned_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members[
                    "ProvisionedThroughputOverride"
                ],
                self.provisioned_throughput_override,
            )

        if self.on_demand_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members[
                    "OnDemandThroughputOverride"
                ],
                self.on_demand_throughput_override,
            )

        if self.sse_specification_override is not None:
            serializer.write_struct(
                _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members[
                    "SSESpecificationOverride"
                ],
                self.sse_specification_override,
            )

        if self.vector_index_override is not None:
            _serialize_vector_index_list(
                serializer,
                _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members["VectorIndexOverride"],
                self.vector_index_override,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["target_table_name"] = de.read_string(
                        _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members[
                            "TargetTableName"
                        ]
                    )

                case 1:
                    kwargs["backup_arn"] = de.read_string(
                        _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members["BackupArn"]
                    )

                case 2:
                    kwargs["billing_mode_override"] = BillingMode(
                        de.read_string(
                            _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members[
                                "BillingModeOverride"
                            ]
                        )
                    )

                case 3:
                    kwargs["global_secondary_index_override"] = (
                        _deserialize_global_secondary_index_list(
                            de,
                            _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members[
                                "GlobalSecondaryIndexOverride"
                            ],
                        )
                    )

                case 4:
                    kwargs["local_secondary_index_override"] = (
                        _deserialize_local_secondary_index_list(
                            de,
                            _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members[
                                "LocalSecondaryIndexOverride"
                            ],
                        )
                    )

                case 5:
                    kwargs["provisioned_throughput_override"] = (
                        ProvisionedThroughput.deserialize(de)
                    )

                case 6:
                    kwargs["on_demand_throughput_override"] = (
                        OnDemandThroughput.deserialize(de)
                    )

                case 7:
                    kwargs["sse_specification_override"] = SSESpecification.deserialize(
                        de
                    )

                case 8:
                    kwargs["vector_index_override"] = _deserialize_vector_index_list(
                        de,
                        _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT.members[
                            "VectorIndexOverride"
                        ],
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class RestoreTableFromBackupOutput:
    """Dataclass for RestoreTableFromBackupOutput structure."""

    table_description: TableDescription | None = None
    """The description of the table created from an existing backup."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RESTORE_TABLE_FROM_BACKUP_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_description is not None:
            serializer.write_struct(
                _SCHEMA_RESTORE_TABLE_FROM_BACKUP_OUTPUT.members["TableDescription"],
                self.table_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_description"] = TableDescription.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_RESTORE_TABLE_FROM_BACKUP_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class TableAlreadyExistsException(ServiceError):
    """A target table with the specified name already exists."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TABLE_ALREADY_EXISTS_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_TABLE_ALREADY_EXISTS_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_TABLE_ALREADY_EXISTS_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_TABLE_ALREADY_EXISTS_EXCEPTION, consumer=_consumer
        )
        return kwargs


RESTORE_TABLE_FROM_BACKUP = APIOperation(
    input=RestoreTableFromBackupInput,
    output=RestoreTableFromBackupOutput,
    schema=_SCHEMA_RESTORE_TABLE_FROM_BACKUP,
    input_schema=_SCHEMA_RESTORE_TABLE_FROM_BACKUP_INPUT,
    output_schema=_SCHEMA_RESTORE_TABLE_FROM_BACKUP_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#BackupInUseException"
            ): BackupInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#BackupNotFoundException"
            ): BackupNotFoundException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#TableAlreadyExistsException"
            ): TableAlreadyExistsException,
            ShapeID("com.amazonaws.dynamodb#TableInUseException"): TableInUseException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_BACKUP_IN_USE_EXCEPTION,
        _SCHEMA_BACKUP_NOT_FOUND_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_TABLE_ALREADY_EXISTS_EXCEPTION,
        _SCHEMA_TABLE_IN_USE_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class InvalidRestoreTimeException(ServiceError):
    """
    An invalid restore time was specified. RestoreDateTime must be between
    EarliestRestorableDateTime and LatestRestorableDateTime.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_RESTORE_TIME_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_RESTORE_TIME_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_RESTORE_TIME_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_INVALID_RESTORE_TIME_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class RestoreTableToPointInTimeInput:
    """Dataclass for RestoreTableToPointInTimeInput structure."""

    source_table_arn: str | None = None
    """
    The DynamoDB table that will be restored. This value is an Amazon
    Resource Name (ARN).
    """

    source_table_name: str | None = None
    """Name of the source table that is being restored."""

    target_table_name: str | None = None
    """The name of the new table to which it must be restored to."""

    use_latest_restorable_time: bool | None = None
    """
    Restore the table to the latest possible time.
    `LatestRestorableDateTime` is typically 5 minutes before the current
    time.
    """

    restore_date_time: datetime | None = None
    """Time in the past to restore the table to."""

    billing_mode_override: BillingMode | None = None
    """The billing mode of the restored table."""

    global_secondary_index_override: list[GlobalSecondaryIndex] | None = None
    """
    List of global secondary indexes for the restored table. The indexes
    provided should match existing secondary indexes. You can choose to
    exclude some or all of the indexes at the time of restore.

    The `WarmThroughput` setting is not supported on global secondary
    indexes when you use `RestoreTableToPointInTime`. Although
    `WarmThroughput` appears in the shared index definition, including it in
    a `GlobalSecondaryIndexOverride` entry causes the request to fail with a
    validation error.
    """

    local_secondary_index_override: list[LocalSecondaryIndex] | None = None
    """
    List of local secondary indexes for the restored table. The indexes
    provided should match existing secondary indexes. You can choose to
    exclude some or all of the indexes at the time of restore.
    """

    provisioned_throughput_override: ProvisionedThroughput | None = None
    """Provisioned throughput settings for the restored table."""

    on_demand_throughput_override: OnDemandThroughput | None = None
    """
    Sets the maximum number of read and write units for the specified
    on-demand table. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    sse_specification_override: SSESpecification | None = None
    """The new server-side encryption settings for the restored table."""

    vector_index_override: list[VectorIndex] | None = None
    """
    The vector indexes for the restored table. If not specified, all vector
    indexes from the source table are restored. The indexes provided must
    match existing vector indexes from the source table. You can choose to
    exclude some or all of the vector indexes at the time of restore.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.source_table_arn is not None:
            serializer.write_string(
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members["SourceTableArn"],
                self.source_table_arn,
            )

        if self.source_table_name is not None:
            serializer.write_string(
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members["SourceTableName"],
                self.source_table_name,
            )

        if self.target_table_name is not None:
            serializer.write_string(
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members["TargetTableName"],
                self.target_table_name,
            )

        if self.use_latest_restorable_time is not None:
            serializer.write_boolean(
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                    "UseLatestRestorableTime"
                ],
                self.use_latest_restorable_time,
            )

        if self.restore_date_time is not None:
            serializer.write_timestamp(
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members["RestoreDateTime"],
                self.restore_date_time,
            )

        if self.billing_mode_override is not None:
            serializer.write_string(
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                    "BillingModeOverride"
                ],
                self.billing_mode_override,
            )

        if self.global_secondary_index_override is not None:
            _serialize_global_secondary_index_list(
                serializer,
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                    "GlobalSecondaryIndexOverride"
                ],
                self.global_secondary_index_override,
            )

        if self.local_secondary_index_override is not None:
            _serialize_local_secondary_index_list(
                serializer,
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                    "LocalSecondaryIndexOverride"
                ],
                self.local_secondary_index_override,
            )

        if self.provisioned_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                    "ProvisionedThroughputOverride"
                ],
                self.provisioned_throughput_override,
            )

        if self.on_demand_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                    "OnDemandThroughputOverride"
                ],
                self.on_demand_throughput_override,
            )

        if self.sse_specification_override is not None:
            serializer.write_struct(
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                    "SSESpecificationOverride"
                ],
                self.sse_specification_override,
            )

        if self.vector_index_override is not None:
            _serialize_vector_index_list(
                serializer,
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                    "VectorIndexOverride"
                ],
                self.vector_index_override,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["source_table_arn"] = de.read_string(
                        _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                            "SourceTableArn"
                        ]
                    )

                case 1:
                    kwargs["source_table_name"] = de.read_string(
                        _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                            "SourceTableName"
                        ]
                    )

                case 2:
                    kwargs["target_table_name"] = de.read_string(
                        _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                            "TargetTableName"
                        ]
                    )

                case 3:
                    kwargs["use_latest_restorable_time"] = de.read_boolean(
                        _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                            "UseLatestRestorableTime"
                        ]
                    )

                case 4:
                    kwargs["restore_date_time"] = de.read_timestamp(
                        _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                            "RestoreDateTime"
                        ]
                    )

                case 5:
                    kwargs["billing_mode_override"] = BillingMode(
                        de.read_string(
                            _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                                "BillingModeOverride"
                            ]
                        )
                    )

                case 6:
                    kwargs["global_secondary_index_override"] = (
                        _deserialize_global_secondary_index_list(
                            de,
                            _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                                "GlobalSecondaryIndexOverride"
                            ],
                        )
                    )

                case 7:
                    kwargs["local_secondary_index_override"] = (
                        _deserialize_local_secondary_index_list(
                            de,
                            _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                                "LocalSecondaryIndexOverride"
                            ],
                        )
                    )

                case 8:
                    kwargs["provisioned_throughput_override"] = (
                        ProvisionedThroughput.deserialize(de)
                    )

                case 9:
                    kwargs["on_demand_throughput_override"] = (
                        OnDemandThroughput.deserialize(de)
                    )

                case 10:
                    kwargs["sse_specification_override"] = SSESpecification.deserialize(
                        de
                    )

                case 11:
                    kwargs["vector_index_override"] = _deserialize_vector_index_list(
                        de,
                        _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT.members[
                            "VectorIndexOverride"
                        ],
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class RestoreTableToPointInTimeOutput:
    """Dataclass for RestoreTableToPointInTimeOutput structure."""

    table_description: TableDescription | None = None
    """Represents the properties of a table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_description is not None:
            serializer.write_struct(
                _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_OUTPUT.members[
                    "TableDescription"
                ],
                self.table_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_description"] = TableDescription.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_OUTPUT, consumer=_consumer
        )
        return kwargs


RESTORE_TABLE_TO_POINT_IN_TIME = APIOperation(
    input=RestoreTableToPointInTimeInput,
    output=RestoreTableToPointInTimeOutput,
    schema=_SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME,
    input_schema=_SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_INPUT,
    output_schema=_SCHEMA_RESTORE_TABLE_TO_POINT_IN_TIME_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidRestoreTimeException"
            ): InvalidRestoreTimeException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#PointInTimeRecoveryUnavailableException"
            ): PointInTimeRecoveryUnavailableException,
            ShapeID(
                "com.amazonaws.dynamodb#TableAlreadyExistsException"
            ): TableAlreadyExistsException,
            ShapeID("com.amazonaws.dynamodb#TableInUseException"): TableInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#TableNotFoundException"
            ): TableNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_INVALID_RESTORE_TIME_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_POINT_IN_TIME_RECOVERY_UNAVAILABLE_EXCEPTION,
        _SCHEMA_TABLE_ALREADY_EXISTS_EXCEPTION,
        _SCHEMA_TABLE_IN_USE_EXCEPTION,
        _SCHEMA_TABLE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class TagResourceInput:
    """Dataclass for TagResourceInput structure."""

    resource_arn: str | None = None
    """
    Identifies the Amazon DynamoDB resource to which tags should be added.
    This value is an Amazon Resource Name (ARN).
    """

    tags: list[Tag] | None = None
    """The tags to be assigned to the Amazon DynamoDB resource."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TAG_RESOURCE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.resource_arn is not None:
            serializer.write_string(
                _SCHEMA_TAG_RESOURCE_INPUT.members["ResourceArn"], self.resource_arn
            )

        if self.tags is not None:
            _serialize_tag_list(
                serializer, _SCHEMA_TAG_RESOURCE_INPUT.members["Tags"], self.tags
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["resource_arn"] = de.read_string(
                        _SCHEMA_TAG_RESOURCE_INPUT.members["ResourceArn"]
                    )

                case 1:
                    kwargs["tags"] = _deserialize_tag_list(
                        de, _SCHEMA_TAG_RESOURCE_INPUT.members["Tags"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TAG_RESOURCE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class TagResourceOutput:
    """Dataclass for TagResourceOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TAG_RESOURCE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TAG_RESOURCE_OUTPUT, consumer=_consumer)
        return kwargs


TAG_RESOURCE = APIOperation(
    input=TagResourceInput,
    output=TagResourceOutput,
    schema=_SCHEMA_TAG_RESOURCE,
    input_schema=_SCHEMA_TAG_RESOURCE_INPUT,
    output_schema=_SCHEMA_TAG_RESOURCE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


def _serialize_tag_key_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_tag_key_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class UntagResourceInput:
    """Dataclass for UntagResourceInput structure."""

    resource_arn: str | None = None
    """
    The DynamoDB resource that the tags will be removed from. This value is
    an Amazon Resource Name (ARN).
    """

    tag_keys: list[str] | None = None
    """
    A list of tag keys. Existing tags of the resource whose keys are members
    of this list will be removed from the DynamoDB resource.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UNTAG_RESOURCE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.resource_arn is not None:
            serializer.write_string(
                _SCHEMA_UNTAG_RESOURCE_INPUT.members["ResourceArn"], self.resource_arn
            )

        if self.tag_keys is not None:
            _serialize_tag_key_list(
                serializer,
                _SCHEMA_UNTAG_RESOURCE_INPUT.members["TagKeys"],
                self.tag_keys,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["resource_arn"] = de.read_string(
                        _SCHEMA_UNTAG_RESOURCE_INPUT.members["ResourceArn"]
                    )

                case 1:
                    kwargs["tag_keys"] = _deserialize_tag_key_list(
                        de, _SCHEMA_UNTAG_RESOURCE_INPUT.members["TagKeys"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UNTAG_RESOURCE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class UntagResourceOutput:
    """Dataclass for UntagResourceOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UNTAG_RESOURCE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UNTAG_RESOURCE_OUTPUT, consumer=_consumer)
        return kwargs


UNTAG_RESOURCE = APIOperation(
    input=UntagResourceInput,
    output=UntagResourceOutput,
    schema=_SCHEMA_UNTAG_RESOURCE,
    input_schema=_SCHEMA_UNTAG_RESOURCE_INPUT,
    output_schema=_SCHEMA_UNTAG_RESOURCE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class PointInTimeRecoverySpecification:
    """Represents the settings used to enable point in time recovery."""

    point_in_time_recovery_enabled: bool
    """
    Indicates whether point in time recovery is enabled (true) or disabled
    (false) on the table.
    """

    recovery_period_in_days: int | None = None
    """
    The number of preceding days for which continuous backups are taken and
    maintained. Your table data is only recoverable to any point-in-time
    from within the configured recovery period. This parameter is optional.
    If no value is provided, the value will default to 35.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_POINT_IN_TIME_RECOVERY_SPECIFICATION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_boolean(
            _SCHEMA_POINT_IN_TIME_RECOVERY_SPECIFICATION.members[
                "PointInTimeRecoveryEnabled"
            ],
            self.point_in_time_recovery_enabled,
        )
        if self.recovery_period_in_days is not None:
            serializer.write_integer(
                _SCHEMA_POINT_IN_TIME_RECOVERY_SPECIFICATION.members[
                    "RecoveryPeriodInDays"
                ],
                self.recovery_period_in_days,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["point_in_time_recovery_enabled"] = de.read_boolean(
                        _SCHEMA_POINT_IN_TIME_RECOVERY_SPECIFICATION.members[
                            "PointInTimeRecoveryEnabled"
                        ]
                    )

                case 1:
                    kwargs["recovery_period_in_days"] = de.read_integer(
                        _SCHEMA_POINT_IN_TIME_RECOVERY_SPECIFICATION.members[
                            "RecoveryPeriodInDays"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_POINT_IN_TIME_RECOVERY_SPECIFICATION, consumer=_consumer
        )
        if "point_in_time_recovery_enabled" not in kwargs:
            kwargs["point_in_time_recovery_enabled"] = False
        return kwargs


@dataclass(kw_only=True)
class UpdateContinuousBackupsInput:
    """Dataclass for UpdateContinuousBackupsInput structure."""

    table_name: str | None = None
    """
    The name of the table. You can also provide the Amazon Resource Name
    (ARN) of the table in this parameter.
    """

    point_in_time_recovery_specification: PointInTimeRecoverySpecification | None = None
    """Represents the settings used to enable point in time recovery."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_CONTINUOUS_BACKUPS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_CONTINUOUS_BACKUPS_INPUT.members["TableName"],
                self.table_name,
            )

        if self.point_in_time_recovery_specification is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_CONTINUOUS_BACKUPS_INPUT.members[
                    "PointInTimeRecoverySpecification"
                ],
                self.point_in_time_recovery_specification,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_UPDATE_CONTINUOUS_BACKUPS_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["point_in_time_recovery_specification"] = (
                        PointInTimeRecoverySpecification.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_CONTINUOUS_BACKUPS_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class UpdateContinuousBackupsOutput:
    """Dataclass for UpdateContinuousBackupsOutput structure."""

    continuous_backups_description: ContinuousBackupsDescription | None = None
    """
    Represents the continuous backups and point in time recovery settings on
    the table.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_CONTINUOUS_BACKUPS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.continuous_backups_description is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_CONTINUOUS_BACKUPS_OUTPUT.members[
                    "ContinuousBackupsDescription"
                ],
                self.continuous_backups_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["continuous_backups_description"] = (
                        ContinuousBackupsDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_CONTINUOUS_BACKUPS_OUTPUT, consumer=_consumer
        )
        return kwargs


UPDATE_CONTINUOUS_BACKUPS = APIOperation(
    input=UpdateContinuousBackupsInput,
    output=UpdateContinuousBackupsOutput,
    schema=_SCHEMA_UPDATE_CONTINUOUS_BACKUPS,
    input_schema=_SCHEMA_UPDATE_CONTINUOUS_BACKUPS_INPUT,
    output_schema=_SCHEMA_UPDATE_CONTINUOUS_BACKUPS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#ContinuousBackupsUnavailableException"
            ): ContinuousBackupsUnavailableException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#TableNotFoundException"
            ): TableNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_CONTINUOUS_BACKUPS_UNAVAILABLE_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_TABLE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class UpdateContributorInsightsInput:
    """Dataclass for UpdateContributorInsightsInput structure."""

    table_name: str | None = None
    """
    The name of the table. You can also provide the Amazon Resource Name
    (ARN) of the table in this parameter.
    """

    index_name: str | None = None
    """The global secondary index name, if applicable."""

    contributor_insights_action: ContributorInsightsAction | None = None
    """Represents the contributor insights action."""

    contributor_insights_mode: ContributorInsightsMode | None = None
    """
    Specifies whether to track all access and throttled events or throttled
    events only for the DynamoDB table or index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT.members["TableName"],
                self.table_name,
            )

        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT.members["IndexName"],
                self.index_name,
            )

        if self.contributor_insights_action is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT.members[
                    "ContributorInsightsAction"
                ],
                self.contributor_insights_action,
            )

        if self.contributor_insights_mode is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT.members[
                    "ContributorInsightsMode"
                ],
                self.contributor_insights_mode,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT.members["IndexName"]
                    )

                case 2:
                    kwargs["contributor_insights_action"] = ContributorInsightsAction(
                        de.read_string(
                            _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT.members[
                                "ContributorInsightsAction"
                            ]
                        )
                    )

                case 3:
                    kwargs["contributor_insights_mode"] = ContributorInsightsMode(
                        de.read_string(
                            _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT.members[
                                "ContributorInsightsMode"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class UpdateContributorInsightsOutput:
    """Dataclass for UpdateContributorInsightsOutput structure."""

    table_name: str | None = None
    """The name of the table."""

    index_name: str | None = None
    """The name of the global secondary index, if applicable."""

    contributor_insights_status: ContributorInsightsStatus | None = None
    """The status of contributor insights"""

    contributor_insights_mode: ContributorInsightsMode | None = None
    """
    The updated mode of CloudWatch Contributor Insights that determines
    whether to monitor all access and throttled events or to track throttled
    events exclusively.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT.members["TableName"],
                self.table_name,
            )

        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT.members["IndexName"],
                self.index_name,
            )

        if self.contributor_insights_status is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                    "ContributorInsightsStatus"
                ],
                self.contributor_insights_status,
            )

        if self.contributor_insights_mode is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                    "ContributorInsightsMode"
                ],
                self.contributor_insights_mode,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT.members["TableName"]
                    )

                case 1:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT.members["IndexName"]
                    )

                case 2:
                    kwargs["contributor_insights_status"] = ContributorInsightsStatus(
                        de.read_string(
                            _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                                "ContributorInsightsStatus"
                            ]
                        )
                    )

                case 3:
                    kwargs["contributor_insights_mode"] = ContributorInsightsMode(
                        de.read_string(
                            _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT.members[
                                "ContributorInsightsMode"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT, consumer=_consumer
        )
        return kwargs


UPDATE_CONTRIBUTOR_INSIGHTS = APIOperation(
    input=UpdateContributorInsightsInput,
    output=UpdateContributorInsightsOutput,
    schema=_SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS,
    input_schema=_SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_INPUT,
    output_schema=_SCHEMA_UPDATE_CONTRIBUTOR_INSIGHTS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[_SCHEMA_INTERNAL_SERVER_ERROR, _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION],
)


@dataclass(kw_only=True)
class ReplicaAlreadyExistsException(ServiceError):
    """The specified replica is already part of the global table."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICA_ALREADY_EXISTS_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_ALREADY_EXISTS_EXCEPTION.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_REPLICA_ALREADY_EXISTS_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_ALREADY_EXISTS_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ReplicaNotFoundException(ServiceError):
    """The specified replica is no longer part of the global table."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICA_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_NOT_FOUND_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_REPLICA_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_NOT_FOUND_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ReplicaUpdate:
    """
    Represents one of the following:

    - A new replica to be added to an existing global table.

    - New parameters for an existing replica.

    - An existing replica to be removed from an existing global table.
    """

    create: CreateReplicaAction | None = None
    """
    The parameters required for creating a replica on an existing global
    table.
    """

    delete: DeleteReplicaAction | None = None
    """The name of the existing replica to be removed."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICA_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.create is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_UPDATE.members["Create"], self.create
            )

        if self.delete is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_UPDATE.members["Delete"], self.delete
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["create"] = CreateReplicaAction.deserialize(de)

                case 1:
                    kwargs["delete"] = DeleteReplicaAction.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_REPLICA_UPDATE, consumer=_consumer)
        return kwargs


def _serialize_replica_update_list(
    serializer: ShapeSerializer, schema: Schema, value: list[ReplicaUpdate]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaUpdate]:
    result: list[ReplicaUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class UpdateGlobalTableInput:
    """Dataclass for UpdateGlobalTableInput structure."""

    global_table_name: str | None = None
    """The global table name."""

    replica_updates: list[ReplicaUpdate] | None = None
    """A list of Regions that should be added or removed from the global table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_GLOBAL_TABLE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_GLOBAL_TABLE_INPUT.members["GlobalTableName"],
                self.global_table_name,
            )

        if self.replica_updates is not None:
            _serialize_replica_update_list(
                serializer,
                _SCHEMA_UPDATE_GLOBAL_TABLE_INPUT.members["ReplicaUpdates"],
                self.replica_updates,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_name"] = de.read_string(
                        _SCHEMA_UPDATE_GLOBAL_TABLE_INPUT.members["GlobalTableName"]
                    )

                case 1:
                    kwargs["replica_updates"] = _deserialize_replica_update_list(
                        de, _SCHEMA_UPDATE_GLOBAL_TABLE_INPUT.members["ReplicaUpdates"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UPDATE_GLOBAL_TABLE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class UpdateGlobalTableOutput:
    """Dataclass for UpdateGlobalTableOutput structure."""

    global_table_description: GlobalTableDescription | None = None
    """Contains the details of the global table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_GLOBAL_TABLE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_description is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_GLOBAL_TABLE_OUTPUT.members["GlobalTableDescription"],
                self.global_table_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_description"] = (
                        GlobalTableDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UPDATE_GLOBAL_TABLE_OUTPUT, consumer=_consumer)
        return kwargs


UPDATE_GLOBAL_TABLE = APIOperation(
    input=UpdateGlobalTableInput,
    output=UpdateGlobalTableOutput,
    schema=_SCHEMA_UPDATE_GLOBAL_TABLE,
    input_schema=_SCHEMA_UPDATE_GLOBAL_TABLE_INPUT,
    output_schema=_SCHEMA_UPDATE_GLOBAL_TABLE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#GlobalTableNotFoundException"
            ): GlobalTableNotFoundException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ReplicaAlreadyExistsException"
            ): ReplicaAlreadyExistsException,
            ShapeID(
                "com.amazonaws.dynamodb#ReplicaNotFoundException"
            ): ReplicaNotFoundException,
            ShapeID(
                "com.amazonaws.dynamodb#TableNotFoundException"
            ): TableNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_GLOBAL_TABLE_NOT_FOUND_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_REPLICA_ALREADY_EXISTS_EXCEPTION,
        _SCHEMA_REPLICA_NOT_FOUND_EXCEPTION,
        _SCHEMA_TABLE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class IndexNotFoundException(ServiceError):
    """The operation tried to access a nonexistent index."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INDEX_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INDEX_NOT_FOUND_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INDEX_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INDEX_NOT_FOUND_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GlobalTableGlobalSecondaryIndexSettingsUpdate:
    """
    Represents the settings of a global secondary index for a global table
    that will be modified.
    """

    index_name: str
    """
    The name of the global secondary index. The name must be unique among
    all other indexes on this table.
    """

    provisioned_write_capacity_units: int | None = None
    """
    The maximum number of writes consumed per second before DynamoDB returns
    a `ThrottlingException.`
    """

    provisioned_write_capacity_auto_scaling_settings_update: (
        AutoScalingSettingsUpdate | None
    ) = None
    """
    Auto scaling settings for managing a global secondary index's write
    capacity units.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_GLOBAL_TABLE_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_GLOBAL_TABLE_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE.members[
                "IndexName"
            ],
            self.index_name,
        )
        if self.provisioned_write_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_GLOBAL_TABLE_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE.members[
                    "ProvisionedWriteCapacityUnits"
                ],
                self.provisioned_write_capacity_units,
            )

        if self.provisioned_write_capacity_auto_scaling_settings_update is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_TABLE_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE.members[
                    "ProvisionedWriteCapacityAutoScalingSettingsUpdate"
                ],
                self.provisioned_write_capacity_auto_scaling_settings_update,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_GLOBAL_TABLE_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE.members[
                            "IndexName"
                        ]
                    )

                case 1:
                    kwargs["provisioned_write_capacity_units"] = de.read_long(
                        _SCHEMA_GLOBAL_TABLE_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE.members[
                            "ProvisionedWriteCapacityUnits"
                        ]
                    )

                case 2:
                    kwargs[
                        "provisioned_write_capacity_auto_scaling_settings_update"
                    ] = AutoScalingSettingsUpdate.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GLOBAL_TABLE_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE,
            consumer=_consumer,
        )
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        return kwargs


def _serialize_global_table_global_secondary_index_settings_update_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[GlobalTableGlobalSecondaryIndexSettingsUpdate],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_global_table_global_secondary_index_settings_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[GlobalTableGlobalSecondaryIndexSettingsUpdate]:
    result: list[GlobalTableGlobalSecondaryIndexSettingsUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(GlobalTableGlobalSecondaryIndexSettingsUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ReplicaGlobalSecondaryIndexSettingsUpdate:
    """
    Represents the settings of a global secondary index for a global table
    that will be modified.
    """

    index_name: str
    """
    The name of the global secondary index. The name must be unique among
    all other indexes on this table.
    """

    provisioned_read_capacity_units: int | None = None
    """
    The maximum number of strongly consistent reads consumed per second
    before DynamoDB returns a `ThrottlingException`.
    """

    provisioned_read_capacity_auto_scaling_settings_update: (
        AutoScalingSettingsUpdate | None
    ) = None
    """
    Auto scaling settings for managing a global secondary index replica's
    read capacity units.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE.members["IndexName"],
            self.index_name,
        )
        if self.provisioned_read_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE.members[
                    "ProvisionedReadCapacityUnits"
                ],
                self.provisioned_read_capacity_units,
            )

        if self.provisioned_read_capacity_auto_scaling_settings_update is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE.members[
                    "ProvisionedReadCapacityAutoScalingSettingsUpdate"
                ],
                self.provisioned_read_capacity_auto_scaling_settings_update,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE.members[
                            "IndexName"
                        ]
                    )

                case 1:
                    kwargs["provisioned_read_capacity_units"] = de.read_long(
                        _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE.members[
                            "ProvisionedReadCapacityUnits"
                        ]
                    )

                case 2:
                    kwargs["provisioned_read_capacity_auto_scaling_settings_update"] = (
                        AutoScalingSettingsUpdate.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_SETTINGS_UPDATE, consumer=_consumer
        )
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        return kwargs


def _serialize_replica_global_secondary_index_settings_update_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[ReplicaGlobalSecondaryIndexSettingsUpdate],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_global_secondary_index_settings_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaGlobalSecondaryIndexSettingsUpdate]:
    result: list[ReplicaGlobalSecondaryIndexSettingsUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaGlobalSecondaryIndexSettingsUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ReplicaSettingsUpdate:
    """
    Represents the settings for a global table in a Region that will be
    modified.
    """

    region_name: str
    """The Region of the replica to be added."""

    replica_provisioned_read_capacity_units: int | None = None
    """
    The maximum number of strongly consistent reads consumed per second
    before DynamoDB returns a `ThrottlingException`. For more information,
    see [Specifying Read and Write
    Requirements](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/WorkingWithTables.html#ProvisionedThroughput)
    in the *Amazon DynamoDB Developer Guide*.
    """

    replica_provisioned_read_capacity_auto_scaling_settings_update: (
        AutoScalingSettingsUpdate | None
    ) = None
    """
    Auto scaling settings for managing a global table replica's read
    capacity units.
    """

    replica_global_secondary_index_settings_update: (
        list[ReplicaGlobalSecondaryIndexSettingsUpdate] | None
    ) = None
    """
    Represents the settings of a global secondary index for a global table
    that will be modified.
    """

    replica_table_class: TableClass | None = None
    """
    Replica-specific table class. If not specified, uses the source table's
    table class.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICA_SETTINGS_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_REPLICA_SETTINGS_UPDATE.members["RegionName"], self.region_name
        )
        if self.replica_provisioned_read_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_REPLICA_SETTINGS_UPDATE.members[
                    "ReplicaProvisionedReadCapacityUnits"
                ],
                self.replica_provisioned_read_capacity_units,
            )

        if (
            self.replica_provisioned_read_capacity_auto_scaling_settings_update
            is not None
        ):
            serializer.write_struct(
                _SCHEMA_REPLICA_SETTINGS_UPDATE.members[
                    "ReplicaProvisionedReadCapacityAutoScalingSettingsUpdate"
                ],
                self.replica_provisioned_read_capacity_auto_scaling_settings_update,
            )

        if self.replica_global_secondary_index_settings_update is not None:
            _serialize_replica_global_secondary_index_settings_update_list(
                serializer,
                _SCHEMA_REPLICA_SETTINGS_UPDATE.members[
                    "ReplicaGlobalSecondaryIndexSettingsUpdate"
                ],
                self.replica_global_secondary_index_settings_update,
            )

        if self.replica_table_class is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_SETTINGS_UPDATE.members["ReplicaTableClass"],
                self.replica_table_class,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_REPLICA_SETTINGS_UPDATE.members["RegionName"]
                    )

                case 1:
                    kwargs["replica_provisioned_read_capacity_units"] = de.read_long(
                        _SCHEMA_REPLICA_SETTINGS_UPDATE.members[
                            "ReplicaProvisionedReadCapacityUnits"
                        ]
                    )

                case 2:
                    kwargs[
                        "replica_provisioned_read_capacity_auto_scaling_settings_update"
                    ] = AutoScalingSettingsUpdate.deserialize(de)

                case 3:
                    kwargs["replica_global_secondary_index_settings_update"] = (
                        _deserialize_replica_global_secondary_index_settings_update_list(
                            de,
                            _SCHEMA_REPLICA_SETTINGS_UPDATE.members[
                                "ReplicaGlobalSecondaryIndexSettingsUpdate"
                            ],
                        )
                    )

                case 4:
                    kwargs["replica_table_class"] = TableClass(
                        de.read_string(
                            _SCHEMA_REPLICA_SETTINGS_UPDATE.members["ReplicaTableClass"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_REPLICA_SETTINGS_UPDATE, consumer=_consumer)
        if "region_name" not in kwargs:
            kwargs["region_name"] = ""
        return kwargs


def _serialize_replica_settings_update_list(
    serializer: ShapeSerializer, schema: Schema, value: list[ReplicaSettingsUpdate]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_settings_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaSettingsUpdate]:
    result: list[ReplicaSettingsUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaSettingsUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class UpdateGlobalTableSettingsInput:
    """Dataclass for UpdateGlobalTableSettingsInput structure."""

    global_table_name: str | None = None
    """The name of the global table"""

    global_table_billing_mode: BillingMode | None = None
    """
    The billing mode of the global table. If `GlobalTableBillingMode` is not
    specified, the global table defaults to `PROVISIONED` capacity billing
    mode.

    - `PROVISIONED` - We recommend using `PROVISIONED` for predictable
      workloads. `PROVISIONED` sets the billing mode to [Provisioned
      capacity
      mode](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/provisioned-capacity-mode.html).

    - `PAY_PER_REQUEST` - We recommend using `PAY_PER_REQUEST` for
      unpredictable workloads. `PAY_PER_REQUEST` sets the billing mode to
      [On-demand capacity
      mode](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/on-demand-capacity-mode.html).
    """

    global_table_provisioned_write_capacity_units: int | None = None
    """
    The maximum number of writes consumed per second before DynamoDB returns
    a `ThrottlingException.`
    """

    global_table_provisioned_write_capacity_auto_scaling_settings_update: (
        AutoScalingSettingsUpdate | None
    ) = None
    """
    Auto scaling settings for managing provisioned write capacity for the
    global table.
    """

    global_table_global_secondary_index_settings_update: (
        list[GlobalTableGlobalSecondaryIndexSettingsUpdate] | None
    ) = None
    """
    Represents the settings of a global secondary index for a global table
    that will be modified.
    """

    replica_settings_update: list[ReplicaSettingsUpdate] | None = None
    """
    Represents the settings for a global table in a Region that will be
    modified.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members["GlobalTableName"],
                self.global_table_name,
            )

        if self.global_table_billing_mode is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                    "GlobalTableBillingMode"
                ],
                self.global_table_billing_mode,
            )

        if self.global_table_provisioned_write_capacity_units is not None:
            serializer.write_long(
                _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                    "GlobalTableProvisionedWriteCapacityUnits"
                ],
                self.global_table_provisioned_write_capacity_units,
            )

        if (
            self.global_table_provisioned_write_capacity_auto_scaling_settings_update
            is not None
        ):
            serializer.write_struct(
                _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                    "GlobalTableProvisionedWriteCapacityAutoScalingSettingsUpdate"
                ],
                self.global_table_provisioned_write_capacity_auto_scaling_settings_update,
            )

        if self.global_table_global_secondary_index_settings_update is not None:
            _serialize_global_table_global_secondary_index_settings_update_list(
                serializer,
                _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                    "GlobalTableGlobalSecondaryIndexSettingsUpdate"
                ],
                self.global_table_global_secondary_index_settings_update,
            )

        if self.replica_settings_update is not None:
            _serialize_replica_settings_update_list(
                serializer,
                _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                    "ReplicaSettingsUpdate"
                ],
                self.replica_settings_update,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_name"] = de.read_string(
                        _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                            "GlobalTableName"
                        ]
                    )

                case 1:
                    kwargs["global_table_billing_mode"] = BillingMode(
                        de.read_string(
                            _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                                "GlobalTableBillingMode"
                            ]
                        )
                    )

                case 2:
                    kwargs["global_table_provisioned_write_capacity_units"] = (
                        de.read_long(
                            _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                                "GlobalTableProvisionedWriteCapacityUnits"
                            ]
                        )
                    )

                case 3:
                    kwargs[
                        "global_table_provisioned_write_capacity_auto_scaling_settings_update"
                    ] = AutoScalingSettingsUpdate.deserialize(de)

                case 4:
                    kwargs["global_table_global_secondary_index_settings_update"] = (
                        _deserialize_global_table_global_secondary_index_settings_update_list(
                            de,
                            _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                                "GlobalTableGlobalSecondaryIndexSettingsUpdate"
                            ],
                        )
                    )

                case 5:
                    kwargs["replica_settings_update"] = (
                        _deserialize_replica_settings_update_list(
                            de,
                            _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT.members[
                                "ReplicaSettingsUpdate"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class UpdateGlobalTableSettingsOutput:
    """Dataclass for UpdateGlobalTableSettingsOutput structure."""

    global_table_name: str | None = None
    """The name of the global table."""

    replica_settings: list[ReplicaSettingsDescription] | None = None
    """The Region-specific settings for the global table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_OUTPUT.members["GlobalTableName"],
                self.global_table_name,
            )

        if self.replica_settings is not None:
            _serialize_replica_settings_description_list(
                serializer,
                _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_OUTPUT.members["ReplicaSettings"],
                self.replica_settings,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_table_name"] = de.read_string(
                        _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_OUTPUT.members[
                            "GlobalTableName"
                        ]
                    )

                case 1:
                    kwargs["replica_settings"] = (
                        _deserialize_replica_settings_description_list(
                            de,
                            _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_OUTPUT.members[
                                "ReplicaSettings"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_OUTPUT, consumer=_consumer
        )
        return kwargs


UPDATE_GLOBAL_TABLE_SETTINGS = APIOperation(
    input=UpdateGlobalTableSettingsInput,
    output=UpdateGlobalTableSettingsOutput,
    schema=_SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS,
    input_schema=_SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_INPUT,
    output_schema=_SCHEMA_UPDATE_GLOBAL_TABLE_SETTINGS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#GlobalTableNotFoundException"
            ): GlobalTableNotFoundException,
            ShapeID(
                "com.amazonaws.dynamodb#IndexNotFoundException"
            ): IndexNotFoundException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ReplicaNotFoundException"
            ): ReplicaNotFoundException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_GLOBAL_TABLE_NOT_FOUND_EXCEPTION,
        _SCHEMA_INDEX_NOT_FOUND_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_REPLICA_NOT_FOUND_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class UpdateKinesisStreamingConfiguration:
    """Enables updating the configuration for Kinesis Streaming."""

    approximate_creation_date_time_precision: (
        ApproximateCreationDateTimePrecision | None
    ) = None
    """Enables updating the precision of Kinesis data stream timestamp."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_KINESIS_STREAMING_CONFIGURATION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.approximate_creation_date_time_precision is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_KINESIS_STREAMING_CONFIGURATION.members[
                    "ApproximateCreationDateTimePrecision"
                ],
                self.approximate_creation_date_time_precision,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["approximate_creation_date_time_precision"] = (
                        ApproximateCreationDateTimePrecision(
                            de.read_string(
                                _SCHEMA_UPDATE_KINESIS_STREAMING_CONFIGURATION.members[
                                    "ApproximateCreationDateTimePrecision"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_KINESIS_STREAMING_CONFIGURATION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class UpdateKinesisStreamingDestinationInput:
    """Dataclass for UpdateKinesisStreamingDestinationInput structure."""

    table_name: str | None = None
    """
    The table name for the Kinesis streaming destination input. You can also
    provide the ARN of the table in this parameter.
    """

    stream_arn: str | None = None
    """The Amazon Resource Name (ARN) for the Kinesis stream input."""

    update_kinesis_streaming_configuration: (
        UpdateKinesisStreamingConfiguration | None
    ) = None
    """The command to update the Kinesis stream configuration."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_INPUT, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_INPUT.members["TableName"],
                self.table_name,
            )

        if self.stream_arn is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_INPUT.members["StreamArn"],
                self.stream_arn,
            )

        if self.update_kinesis_streaming_configuration is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                    "UpdateKinesisStreamingConfiguration"
                ],
                self.update_kinesis_streaming_configuration,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                            "TableName"
                        ]
                    )

                case 1:
                    kwargs["stream_arn"] = de.read_string(
                        _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_INPUT.members[
                            "StreamArn"
                        ]
                    )

                case 2:
                    kwargs["update_kinesis_streaming_configuration"] = (
                        UpdateKinesisStreamingConfiguration.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class UpdateKinesisStreamingDestinationOutput:
    """Dataclass for UpdateKinesisStreamingDestinationOutput structure."""

    table_name: str | None = None
    """The table name for the Kinesis streaming destination output."""

    stream_arn: str | None = None
    """The ARN for the Kinesis stream input."""

    destination_status: DestinationStatus | None = None
    """
    The status of the attempt to update the Kinesis streaming destination
    output.
    """

    update_kinesis_streaming_configuration: (
        UpdateKinesisStreamingConfiguration | None
    ) = None
    """The command to update the Kinesis streaming destination configuration."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "TableName"
                ],
                self.table_name,
            )

        if self.stream_arn is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "StreamArn"
                ],
                self.stream_arn,
            )

        if self.destination_status is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "DestinationStatus"
                ],
                self.destination_status,
            )

        if self.update_kinesis_streaming_configuration is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                    "UpdateKinesisStreamingConfiguration"
                ],
                self.update_kinesis_streaming_configuration,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                            "TableName"
                        ]
                    )

                case 1:
                    kwargs["stream_arn"] = de.read_string(
                        _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                            "StreamArn"
                        ]
                    )

                case 2:
                    kwargs["destination_status"] = DestinationStatus(
                        de.read_string(
                            _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT.members[
                                "DestinationStatus"
                            ]
                        )
                    )

                case 3:
                    kwargs["update_kinesis_streaming_configuration"] = (
                        UpdateKinesisStreamingConfiguration.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT, consumer=_consumer
        )
        return kwargs


UPDATE_KINESIS_STREAMING_DESTINATION = APIOperation(
    input=UpdateKinesisStreamingDestinationInput,
    output=UpdateKinesisStreamingDestinationOutput,
    schema=_SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION,
    input_schema=_SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_INPUT,
    output_schema=_SCHEMA_UPDATE_KINESIS_STREAMING_DESTINATION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class UpdateGlobalSecondaryIndexAction:
    """
    Represents the new provisioned throughput settings to be applied to a
    global secondary index.
    """

    index_name: str
    """The name of the global secondary index to be updated."""

    provisioned_throughput: ProvisionedThroughput | None = None
    """
    Represents the provisioned throughput settings for the specified global
    secondary index.

    For current minimum and maximum provisioned throughput values, see
    [Service, Account, and Table
    Quotas](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Limits.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    on_demand_throughput: OnDemandThroughput | None = None
    """
    Updates the maximum number of read and write units for the specified
    global secondary index. If you use this parameter, you must specify
    `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    warm_throughput: WarmThroughput | None = None
    """
    Represents the warm throughput value of the new provisioned throughput
    settings to be applied to a global secondary index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_GLOBAL_SECONDARY_INDEX_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_UPDATE_GLOBAL_SECONDARY_INDEX_ACTION.members["IndexName"],
            self.index_name,
        )
        if self.provisioned_throughput is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_GLOBAL_SECONDARY_INDEX_ACTION.members[
                    "ProvisionedThroughput"
                ],
                self.provisioned_throughput,
            )

        if self.on_demand_throughput is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_GLOBAL_SECONDARY_INDEX_ACTION.members[
                    "OnDemandThroughput"
                ],
                self.on_demand_throughput,
            )

        if self.warm_throughput is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_GLOBAL_SECONDARY_INDEX_ACTION.members["WarmThroughput"],
                self.warm_throughput,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_UPDATE_GLOBAL_SECONDARY_INDEX_ACTION.members[
                            "IndexName"
                        ]
                    )

                case 1:
                    kwargs["provisioned_throughput"] = (
                        ProvisionedThroughput.deserialize(de)
                    )

                case 2:
                    kwargs["on_demand_throughput"] = OnDemandThroughput.deserialize(de)

                case 3:
                    kwargs["warm_throughput"] = WarmThroughput.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_GLOBAL_SECONDARY_INDEX_ACTION, consumer=_consumer
        )
        if "index_name" not in kwargs:
            kwargs["index_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class GlobalSecondaryIndexUpdate:
    """
    Represents one of the following:

    - A new global secondary index to be added to an existing table.

    - New provisioned throughput parameters for an existing global secondary
      index.

    - An existing global secondary index to be removed from an existing
      table.
    """

    update: UpdateGlobalSecondaryIndexAction | None = None
    """
    The name of an existing global secondary index, along with new
    provisioned throughput settings to be applied to that index.
    """

    create: CreateGlobalSecondaryIndexAction | None = None
    """
    The parameters required for creating a global secondary index on an
    existing table:

    - `IndexName `

    - `KeySchema `

    - `AttributeDefinitions `

    - `Projection `

    - `ProvisionedThroughput `
    """

    delete: DeleteGlobalSecondaryIndexAction | None = None
    """The name of an existing global secondary index to be removed."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GLOBAL_SECONDARY_INDEX_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.update is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_UPDATE.members["Update"], self.update
            )

        if self.create is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_UPDATE.members["Create"], self.create
            )

        if self.delete is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_UPDATE.members["Delete"], self.delete
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["update"] = UpdateGlobalSecondaryIndexAction.deserialize(de)

                case 1:
                    kwargs["create"] = CreateGlobalSecondaryIndexAction.deserialize(de)

                case 2:
                    kwargs["delete"] = DeleteGlobalSecondaryIndexAction.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GLOBAL_SECONDARY_INDEX_UPDATE, consumer=_consumer
        )
        return kwargs


def _serialize_global_secondary_index_update_list(
    serializer: ShapeSerializer, schema: Schema, value: list[GlobalSecondaryIndexUpdate]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_global_secondary_index_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[GlobalSecondaryIndexUpdate]:
    result: list[GlobalSecondaryIndexUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(GlobalSecondaryIndexUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class GlobalTableWitnessGroupUpdate:
    """
    Represents one of the following:

    - A new witness to be added to a new global table.

    - An existing witness to be removed from an existing global table.

    You can configure one witness per MRSC global table.
    """

    create: CreateGlobalTableWitnessGroupMemberAction | None = None
    """
    Specifies a witness Region to be added to a new MRSC global table. The
    witness must be added when creating the MRSC global table.
    """

    delete: DeleteGlobalTableWitnessGroupMemberAction | None = None
    """
    Specifies a witness Region to be removed from an existing global table.
    Must be done in conjunction with removing a replica. The deletion of
    both a witness and replica converts the remaining replica to a
    single-Region DynamoDB table.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GLOBAL_TABLE_WITNESS_GROUP_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.create is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_TABLE_WITNESS_GROUP_UPDATE.members["Create"], self.create
            )

        if self.delete is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_TABLE_WITNESS_GROUP_UPDATE.members["Delete"], self.delete
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["create"] = (
                        CreateGlobalTableWitnessGroupMemberAction.deserialize(de)
                    )

                case 1:
                    kwargs["delete"] = (
                        DeleteGlobalTableWitnessGroupMemberAction.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GLOBAL_TABLE_WITNESS_GROUP_UPDATE, consumer=_consumer
        )
        return kwargs


def _serialize_global_table_witness_group_update_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[GlobalTableWitnessGroupUpdate],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_global_table_witness_group_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[GlobalTableWitnessGroupUpdate]:
    result: list[GlobalTableWitnessGroupUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(GlobalTableWitnessGroupUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class UpdateReplicationGroupMemberAction:
    """Represents a replica to be modified."""

    region_name: str
    """The Region where the replica exists."""

    kms_master_key_id: str | None = None
    """
    The KMS key of the replica that should be used for KMS encryption. To
    specify a key, use its key ID, Amazon Resource Name (ARN), alias name,
    or alias ARN. Note that you should only provide this parameter if the
    key is different from the default DynamoDB KMS key `alias/aws/dynamodb`.
    """

    provisioned_throughput_override: ProvisionedThroughputOverride | None = None
    """
    Replica-specific provisioned throughput. If not specified, uses the
    source table's provisioned throughput settings.
    """

    on_demand_throughput_override: OnDemandThroughputOverride | None = None
    """Overrides the maximum on-demand throughput for the replica table."""

    global_secondary_indexes: list[ReplicaGlobalSecondaryIndex] | None = None
    """Replica-specific global secondary index settings."""

    table_class_override: TableClass | None = None
    """
    Replica-specific table class. If not specified, uses the source table's
    table class.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION.members["RegionName"],
            self.region_name,
        )
        if self.kms_master_key_id is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                    "KMSMasterKeyId"
                ],
                self.kms_master_key_id,
            )

        if self.provisioned_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                    "ProvisionedThroughputOverride"
                ],
                self.provisioned_throughput_override,
            )

        if self.on_demand_throughput_override is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                    "OnDemandThroughputOverride"
                ],
                self.on_demand_throughput_override,
            )

        if self.global_secondary_indexes is not None:
            _serialize_replica_global_secondary_index_list(
                serializer,
                _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                    "GlobalSecondaryIndexes"
                ],
                self.global_secondary_indexes,
            )

        if self.table_class_override is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                    "TableClassOverride"
                ],
                self.table_class_override,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                            "RegionName"
                        ]
                    )

                case 1:
                    kwargs["kms_master_key_id"] = de.read_string(
                        _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                            "KMSMasterKeyId"
                        ]
                    )

                case 2:
                    kwargs["provisioned_throughput_override"] = (
                        ProvisionedThroughputOverride.deserialize(de)
                    )

                case 3:
                    kwargs["on_demand_throughput_override"] = (
                        OnDemandThroughputOverride.deserialize(de)
                    )

                case 4:
                    kwargs["global_secondary_indexes"] = (
                        _deserialize_replica_global_secondary_index_list(
                            de,
                            _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                                "GlobalSecondaryIndexes"
                            ],
                        )
                    )

                case 5:
                    kwargs["table_class_override"] = TableClass(
                        de.read_string(
                            _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION.members[
                                "TableClassOverride"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_REPLICATION_GROUP_MEMBER_ACTION, consumer=_consumer
        )
        if "region_name" not in kwargs:
            kwargs["region_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class ReplicationGroupUpdate:
    """
    Represents one of the following:

    - A new replica to be added to an existing regional table or global
      table. This request invokes the `CreateTableReplica` action in the
      destination Region.

    - New parameters for an existing replica. This request invokes the
      `UpdateTable` action in the destination Region.

    - An existing replica to be deleted. The request invokes the
      `DeleteTableReplica` action in the destination Region, deleting the
      replica and all if its items in the destination Region.

    Note:
        When you manually remove a table or global table replica, you do not
        automatically remove any associated scalable targets, scaling policies,
        or CloudWatch alarms.
    """

    create: CreateReplicationGroupMemberAction | None = None
    """The parameters required for creating a replica for the table."""

    update: UpdateReplicationGroupMemberAction | None = None
    """The parameters required for updating a replica for the table."""

    delete: DeleteReplicationGroupMemberAction | None = None
    """The parameters required for deleting a replica for the table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICATION_GROUP_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.create is not None:
            serializer.write_struct(
                _SCHEMA_REPLICATION_GROUP_UPDATE.members["Create"], self.create
            )

        if self.update is not None:
            serializer.write_struct(
                _SCHEMA_REPLICATION_GROUP_UPDATE.members["Update"], self.update
            )

        if self.delete is not None:
            serializer.write_struct(
                _SCHEMA_REPLICATION_GROUP_UPDATE.members["Delete"], self.delete
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["create"] = CreateReplicationGroupMemberAction.deserialize(
                        de
                    )

                case 1:
                    kwargs["update"] = UpdateReplicationGroupMemberAction.deserialize(
                        de
                    )

                case 2:
                    kwargs["delete"] = DeleteReplicationGroupMemberAction.deserialize(
                        de
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_REPLICATION_GROUP_UPDATE, consumer=_consumer)
        return kwargs


def _serialize_replication_group_update_list(
    serializer: ShapeSerializer, schema: Schema, value: list[ReplicationGroupUpdate]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replication_group_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicationGroupUpdate]:
    result: list[ReplicationGroupUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicationGroupUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class VectorIndexUpdate:
    """A vector index to be added to or removed from a table."""

    create: CreateVectorIndexAction | None = None
    """The configuration for creating a new vector index on the table."""

    delete: DeleteVectorIndexAction | None = None
    """The configuration for deleting an existing vector index from the table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_VECTOR_INDEX_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.create is not None:
            serializer.write_struct(
                _SCHEMA_VECTOR_INDEX_UPDATE.members["Create"], self.create
            )

        if self.delete is not None:
            serializer.write_struct(
                _SCHEMA_VECTOR_INDEX_UPDATE.members["Delete"], self.delete
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["create"] = CreateVectorIndexAction.deserialize(de)

                case 1:
                    kwargs["delete"] = DeleteVectorIndexAction.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_VECTOR_INDEX_UPDATE, consumer=_consumer)
        return kwargs


def _serialize_vector_index_update_list(
    serializer: ShapeSerializer, schema: Schema, value: list[VectorIndexUpdate]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_vector_index_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[VectorIndexUpdate]:
    result: list[VectorIndexUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(VectorIndexUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class UpdateTableInput:
    """Represents the input of an `UpdateTable` operation."""

    attribute_definitions: list[AttributeDefinition] | None = None
    """
    An array of attributes that describe the key schema for the table and
    indexes. If you are adding a new global secondary index to the table,
    `AttributeDefinitions` must include the key element(s) of the new index.
    """

    table_name: str | None = None
    """
    The name of the table to be updated. You can also provide the Amazon
    Resource Name (ARN) of the table in this parameter.
    """

    billing_mode: BillingMode | None = None
    """
    Controls how you are charged for read and write throughput and how you
    manage capacity. When switching from pay-per-request to provisioned
    capacity, initial provisioned capacity values must be set. The initial
    provisioned capacity values are estimated based on the consumed read and
    write capacity of your table and global secondary indexes over the past
    30 minutes.

    - `PAY_PER_REQUEST` - We recommend using `PAY_PER_REQUEST` for most
      DynamoDB workloads. `PAY_PER_REQUEST` sets the billing mode to
      [On-demand capacity
      mode](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/on-demand-capacity-mode.html).

    - `PROVISIONED` - We recommend using `PROVISIONED` for steady workloads
      with predictable growth where capacity requirements can be reliably
      forecasted. `PROVISIONED` sets the billing mode to [Provisioned
      capacity
      mode](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/provisioned-capacity-mode.html).
    """

    provisioned_throughput: ProvisionedThroughput | None = None
    """
    The new provisioned throughput settings for the specified table or
    index.
    """

    global_secondary_index_updates: list[GlobalSecondaryIndexUpdate] | None = None
    """
    An array of one or more global secondary indexes for the table. For each
    index in the array, you can request one action:

    - `Create` - add a new global secondary index to the table.

    - `Update` - modify the provisioned throughput settings of an existing
      global secondary index.

    - `Delete` - remove a global secondary index from the table.

    You can create or delete only one global secondary index per
    `UpdateTable` operation.

    For more information, see [Managing Global Secondary
    Indexes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/GSI.OnlineOps.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    stream_specification: StreamSpecification | None = None
    """
    Represents the DynamoDB Streams configuration for the table.

    Note:
        You receive a `ValidationException` if you try to enable a stream on a
        table that already has a stream, or if you try to disable a stream on a
        table that doesn't have a stream.
    """

    sse_specification: SSESpecification | None = None
    """The new server-side encryption settings for the specified table."""

    replica_updates: list[ReplicationGroupUpdate] | None = None
    """
    A list of replica update actions (create, delete, or update) for the
    table.
    """

    table_class: TableClass | None = None
    """
    The table class of the table to be updated. Valid values are `STANDARD`
    and `STANDARD_INFREQUENT_ACCESS`.
    """

    deletion_protection_enabled: bool | None = None
    """
    Indicates whether deletion protection is to be enabled (true) or
    disabled (false) on the table.
    """

    multi_region_consistency: MultiRegionConsistency | None = None
    """
    Specifies the consistency mode for a new global table. This parameter is
    only valid when you create a global table by specifying one or more
    [Create](https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_ReplicationGroupUpdate.html#DDB-Type-ReplicationGroupUpdate-Create)
    actions in the
    [ReplicaUpdates](https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_UpdateTable.html#DDB-UpdateTable-request-ReplicaUpdates)
    action list.

    You can specify one of the following consistency modes:

    - `EVENTUAL`: Configures a new global table for multi-Region eventual
      consistency (MREC). This is the default consistency mode for global
      tables.

    - `STRONG`: Configures a new global table for multi-Region strong
      consistency (MRSC).

    If you don't specify this field, the global table consistency mode
    defaults to `EVENTUAL`. For more information about global tables
    consistency modes, see [Consistency
    modes](https://docs.aws.amazon.com/V2globaltables_HowItWorks.html#V2globaltables_HowItWorks.consistency-modes)
    in DynamoDB developer guide.
    """

    global_table_witness_updates: list[GlobalTableWitnessGroupUpdate] | None = None
    """
    A list of witness updates for a MRSC global table. A witness provides a
    cost-effective alternative to a full replica in a MRSC global table by
    maintaining replicated change data written to global table replicas. You
    cannot perform read or write operations on a witness. For each witness,
    you can request one action:

    - `Create` - add a new witness to the global table.

    - `Delete` - remove a witness from the global table.

    You can create or delete only one witness per `UpdateTable` operation.

    For more information, see [Multi-Region strong consistency
    (MRSC)](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/V2globaltables_HowItWorks.html#V2globaltables_HowItWorks.consistency-modes)
    in the Amazon DynamoDB Developer Guide
    """

    on_demand_throughput: OnDemandThroughput | None = None
    """
    Updates the maximum number of read and write units for the specified
    table in on-demand capacity mode. If you use this parameter, you must
    specify `MaxReadRequestUnits`, `MaxWriteRequestUnits`, or both.
    """

    warm_throughput: WarmThroughput | None = None
    """
    Represents the warm throughput (in read units per second and write units
    per second) for updating a table.
    """

    global_table_settings_replication_mode: (
        GlobalTableSettingsReplicationMode | None
    ) = None
    """
    Controls the settings replication mode for a global table replica. This
    attribute can be defined using UpdateTable operation only on a regional
    table with values:

    - `ENABLED`: Defines settings replication on a regional table to be used
      as a source table for creating Multi-Account Global Table.

    - `DISABLED`: Remove settings replication on a regional table. Settings
      replication needs to be defined to ENABLED again in order to create a
      Multi-Account Global Table using this table.
    """

    vector_index_updates: list[VectorIndexUpdate] | None = None
    """
    A list of vector indexes to be added to or removed from the table. You
    can add or remove one vector index for each `UpdateTable` operation.

    To add a vector index, specify `IndexName`, `VectorAttribute`,
    `Dimensions`, `DistanceFunction`, and `Projection`. To remove a vector
    index, specify only the `IndexName`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_TABLE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.attribute_definitions is not None:
            _serialize_attribute_definitions(
                serializer,
                _SCHEMA_UPDATE_TABLE_INPUT.members["AttributeDefinitions"],
                self.attribute_definitions,
            )

        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_TABLE_INPUT.members["TableName"], self.table_name
            )

        if self.billing_mode is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_TABLE_INPUT.members["BillingMode"], self.billing_mode
            )

        if self.provisioned_throughput is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_TABLE_INPUT.members["ProvisionedThroughput"],
                self.provisioned_throughput,
            )

        if self.global_secondary_index_updates is not None:
            _serialize_global_secondary_index_update_list(
                serializer,
                _SCHEMA_UPDATE_TABLE_INPUT.members["GlobalSecondaryIndexUpdates"],
                self.global_secondary_index_updates,
            )

        if self.stream_specification is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_TABLE_INPUT.members["StreamSpecification"],
                self.stream_specification,
            )

        if self.sse_specification is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_TABLE_INPUT.members["SSESpecification"],
                self.sse_specification,
            )

        if self.replica_updates is not None:
            _serialize_replication_group_update_list(
                serializer,
                _SCHEMA_UPDATE_TABLE_INPUT.members["ReplicaUpdates"],
                self.replica_updates,
            )

        if self.table_class is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_TABLE_INPUT.members["TableClass"], self.table_class
            )

        if self.deletion_protection_enabled is not None:
            serializer.write_boolean(
                _SCHEMA_UPDATE_TABLE_INPUT.members["DeletionProtectionEnabled"],
                self.deletion_protection_enabled,
            )

        if self.multi_region_consistency is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_TABLE_INPUT.members["MultiRegionConsistency"],
                self.multi_region_consistency,
            )

        if self.global_table_witness_updates is not None:
            _serialize_global_table_witness_group_update_list(
                serializer,
                _SCHEMA_UPDATE_TABLE_INPUT.members["GlobalTableWitnessUpdates"],
                self.global_table_witness_updates,
            )

        if self.on_demand_throughput is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_TABLE_INPUT.members["OnDemandThroughput"],
                self.on_demand_throughput,
            )

        if self.warm_throughput is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_TABLE_INPUT.members["WarmThroughput"],
                self.warm_throughput,
            )

        if self.global_table_settings_replication_mode is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_TABLE_INPUT.members[
                    "GlobalTableSettingsReplicationMode"
                ],
                self.global_table_settings_replication_mode,
            )

        if self.vector_index_updates is not None:
            _serialize_vector_index_update_list(
                serializer,
                _SCHEMA_UPDATE_TABLE_INPUT.members["VectorIndexUpdates"],
                self.vector_index_updates,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attribute_definitions"] = (
                        _deserialize_attribute_definitions(
                            de,
                            _SCHEMA_UPDATE_TABLE_INPUT.members["AttributeDefinitions"],
                        )
                    )

                case 1:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_UPDATE_TABLE_INPUT.members["TableName"]
                    )

                case 2:
                    kwargs["billing_mode"] = BillingMode(
                        de.read_string(
                            _SCHEMA_UPDATE_TABLE_INPUT.members["BillingMode"]
                        )
                    )

                case 3:
                    kwargs["provisioned_throughput"] = (
                        ProvisionedThroughput.deserialize(de)
                    )

                case 4:
                    kwargs["global_secondary_index_updates"] = (
                        _deserialize_global_secondary_index_update_list(
                            de,
                            _SCHEMA_UPDATE_TABLE_INPUT.members[
                                "GlobalSecondaryIndexUpdates"
                            ],
                        )
                    )

                case 5:
                    kwargs["stream_specification"] = StreamSpecification.deserialize(de)

                case 6:
                    kwargs["sse_specification"] = SSESpecification.deserialize(de)

                case 7:
                    kwargs["replica_updates"] = (
                        _deserialize_replication_group_update_list(
                            de, _SCHEMA_UPDATE_TABLE_INPUT.members["ReplicaUpdates"]
                        )
                    )

                case 8:
                    kwargs["table_class"] = TableClass(
                        de.read_string(_SCHEMA_UPDATE_TABLE_INPUT.members["TableClass"])
                    )

                case 9:
                    kwargs["deletion_protection_enabled"] = de.read_boolean(
                        _SCHEMA_UPDATE_TABLE_INPUT.members["DeletionProtectionEnabled"]
                    )

                case 10:
                    kwargs["multi_region_consistency"] = MultiRegionConsistency(
                        de.read_string(
                            _SCHEMA_UPDATE_TABLE_INPUT.members["MultiRegionConsistency"]
                        )
                    )

                case 11:
                    kwargs["global_table_witness_updates"] = (
                        _deserialize_global_table_witness_group_update_list(
                            de,
                            _SCHEMA_UPDATE_TABLE_INPUT.members[
                                "GlobalTableWitnessUpdates"
                            ],
                        )
                    )

                case 12:
                    kwargs["on_demand_throughput"] = OnDemandThroughput.deserialize(de)

                case 13:
                    kwargs["warm_throughput"] = WarmThroughput.deserialize(de)

                case 14:
                    kwargs["global_table_settings_replication_mode"] = (
                        GlobalTableSettingsReplicationMode(
                            de.read_string(
                                _SCHEMA_UPDATE_TABLE_INPUT.members[
                                    "GlobalTableSettingsReplicationMode"
                                ]
                            )
                        )
                    )

                case 15:
                    kwargs["vector_index_updates"] = (
                        _deserialize_vector_index_update_list(
                            de, _SCHEMA_UPDATE_TABLE_INPUT.members["VectorIndexUpdates"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UPDATE_TABLE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class UpdateTableOutput:
    """Represents the output of an `UpdateTable` operation."""

    table_description: TableDescription | None = None
    """Represents the properties of the table."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_TABLE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_description is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_TABLE_OUTPUT.members["TableDescription"],
                self.table_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_description"] = TableDescription.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UPDATE_TABLE_OUTPUT, consumer=_consumer)
        return kwargs


UPDATE_TABLE = APIOperation(
    input=UpdateTableInput,
    output=UpdateTableOutput,
    schema=_SCHEMA_UPDATE_TABLE,
    input_schema=_SCHEMA_UPDATE_TABLE_INPUT,
    output_schema=_SCHEMA_UPDATE_TABLE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class GlobalSecondaryIndexAutoScalingUpdate:
    """
    Represents the auto scaling settings of a global secondary index for a
    global table that will be modified.
    """

    index_name: str | None = None
    """The name of the global secondary index."""

    provisioned_write_capacity_auto_scaling_update: AutoScalingSettingsUpdate | None = (
        None
    )
    """
    Represents the auto scaling settings to be modified for a global table
    or global secondary index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE.members["IndexName"],
                self.index_name,
            )

        if self.provisioned_write_capacity_auto_scaling_update is not None:
            serializer.write_struct(
                _SCHEMA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE.members[
                    "ProvisionedWriteCapacityAutoScalingUpdate"
                ],
                self.provisioned_write_capacity_auto_scaling_update,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE.members[
                            "IndexName"
                        ]
                    )

                case 1:
                    kwargs["provisioned_write_capacity_auto_scaling_update"] = (
                        AutoScalingSettingsUpdate.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE, consumer=_consumer
        )
        return kwargs


def _serialize_global_secondary_index_auto_scaling_update_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[GlobalSecondaryIndexAutoScalingUpdate],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_global_secondary_index_auto_scaling_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[GlobalSecondaryIndexAutoScalingUpdate]:
    result: list[GlobalSecondaryIndexAutoScalingUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(GlobalSecondaryIndexAutoScalingUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ReplicaGlobalSecondaryIndexAutoScalingUpdate:
    """
    Represents the auto scaling settings of a global secondary index for a
    replica that will be modified.
    """

    index_name: str | None = None
    """The name of the global secondary index."""

    provisioned_read_capacity_auto_scaling_update: AutoScalingSettingsUpdate | None = (
        None
    )
    """
    Represents the auto scaling settings to be modified for a global table
    or global secondary index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE.members[
                    "IndexName"
                ],
                self.index_name,
            )

        if self.provisioned_read_capacity_auto_scaling_update is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE.members[
                    "ProvisionedReadCapacityAutoScalingUpdate"
                ],
                self.provisioned_read_capacity_auto_scaling_update,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE.members[
                            "IndexName"
                        ]
                    )

                case 1:
                    kwargs["provisioned_read_capacity_auto_scaling_update"] = (
                        AutoScalingSettingsUpdate.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_GLOBAL_SECONDARY_INDEX_AUTO_SCALING_UPDATE,
            consumer=_consumer,
        )
        return kwargs


def _serialize_replica_global_secondary_index_auto_scaling_update_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[ReplicaGlobalSecondaryIndexAutoScalingUpdate],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_global_secondary_index_auto_scaling_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaGlobalSecondaryIndexAutoScalingUpdate]:
    result: list[ReplicaGlobalSecondaryIndexAutoScalingUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaGlobalSecondaryIndexAutoScalingUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ReplicaAutoScalingUpdate:
    """Represents the auto scaling settings of a replica that will be modified."""

    region_name: str
    """The Region where the replica exists."""

    replica_global_secondary_index_updates: (
        list[ReplicaGlobalSecondaryIndexAutoScalingUpdate] | None
    ) = None
    """
    Represents the auto scaling settings of global secondary indexes that
    will be modified.
    """

    replica_provisioned_read_capacity_auto_scaling_update: (
        AutoScalingSettingsUpdate | None
    ) = None
    """
    Represents the auto scaling settings to be modified for a global table
    or global secondary index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REPLICA_AUTO_SCALING_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_REPLICA_AUTO_SCALING_UPDATE.members["RegionName"], self.region_name
        )
        if self.replica_global_secondary_index_updates is not None:
            _serialize_replica_global_secondary_index_auto_scaling_update_list(
                serializer,
                _SCHEMA_REPLICA_AUTO_SCALING_UPDATE.members[
                    "ReplicaGlobalSecondaryIndexUpdates"
                ],
                self.replica_global_secondary_index_updates,
            )

        if self.replica_provisioned_read_capacity_auto_scaling_update is not None:
            serializer.write_struct(
                _SCHEMA_REPLICA_AUTO_SCALING_UPDATE.members[
                    "ReplicaProvisionedReadCapacityAutoScalingUpdate"
                ],
                self.replica_provisioned_read_capacity_auto_scaling_update,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["region_name"] = de.read_string(
                        _SCHEMA_REPLICA_AUTO_SCALING_UPDATE.members["RegionName"]
                    )

                case 1:
                    kwargs["replica_global_secondary_index_updates"] = (
                        _deserialize_replica_global_secondary_index_auto_scaling_update_list(
                            de,
                            _SCHEMA_REPLICA_AUTO_SCALING_UPDATE.members[
                                "ReplicaGlobalSecondaryIndexUpdates"
                            ],
                        )
                    )

                case 2:
                    kwargs["replica_provisioned_read_capacity_auto_scaling_update"] = (
                        AutoScalingSettingsUpdate.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_REPLICA_AUTO_SCALING_UPDATE, consumer=_consumer
        )
        if "region_name" not in kwargs:
            kwargs["region_name"] = ""
        return kwargs


def _serialize_replica_auto_scaling_update_list(
    serializer: ShapeSerializer, schema: Schema, value: list[ReplicaAutoScalingUpdate]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_replica_auto_scaling_update_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ReplicaAutoScalingUpdate]:
    result: list[ReplicaAutoScalingUpdate] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ReplicaAutoScalingUpdate.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class UpdateTableReplicaAutoScalingInput:
    """Dataclass for UpdateTableReplicaAutoScalingInput structure."""

    global_secondary_index_updates: (
        list[GlobalSecondaryIndexAutoScalingUpdate] | None
    ) = None
    """
    Represents the auto scaling settings of the global secondary indexes of
    the replica to be updated.
    """

    table_name: str | None = None
    """
    The name of the global table to be updated. You can also provide the
    Amazon Resource Name (ARN) of the table in this parameter.
    """

    provisioned_write_capacity_auto_scaling_update: AutoScalingSettingsUpdate | None = (
        None
    )
    """
    Represents the auto scaling settings to be modified for a global table
    or global secondary index.
    """

    replica_updates: list[ReplicaAutoScalingUpdate] | None = None
    """
    Represents the auto scaling settings of replicas of the table that will
    be modified.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.global_secondary_index_updates is not None:
            _serialize_global_secondary_index_auto_scaling_update_list(
                serializer,
                _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT.members[
                    "GlobalSecondaryIndexUpdates"
                ],
                self.global_secondary_index_updates,
            )

        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT.members["TableName"],
                self.table_name,
            )

        if self.provisioned_write_capacity_auto_scaling_update is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT.members[
                    "ProvisionedWriteCapacityAutoScalingUpdate"
                ],
                self.provisioned_write_capacity_auto_scaling_update,
            )

        if self.replica_updates is not None:
            _serialize_replica_auto_scaling_update_list(
                serializer,
                _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT.members[
                    "ReplicaUpdates"
                ],
                self.replica_updates,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["global_secondary_index_updates"] = (
                        _deserialize_global_secondary_index_auto_scaling_update_list(
                            de,
                            _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT.members[
                                "GlobalSecondaryIndexUpdates"
                            ],
                        )
                    )

                case 1:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT.members[
                            "TableName"
                        ]
                    )

                case 2:
                    kwargs["provisioned_write_capacity_auto_scaling_update"] = (
                        AutoScalingSettingsUpdate.deserialize(de)
                    )

                case 3:
                    kwargs["replica_updates"] = (
                        _deserialize_replica_auto_scaling_update_list(
                            de,
                            _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT.members[
                                "ReplicaUpdates"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class UpdateTableReplicaAutoScalingOutput:
    """Dataclass for UpdateTableReplicaAutoScalingOutput structure."""

    table_auto_scaling_description: TableAutoScalingDescription | None = None
    """
    Returns information about the auto scaling settings of a table with
    replicas.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_auto_scaling_description is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_OUTPUT.members[
                    "TableAutoScalingDescription"
                ],
                self.table_auto_scaling_description,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_auto_scaling_description"] = (
                        TableAutoScalingDescription.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_OUTPUT, consumer=_consumer
        )
        return kwargs


UPDATE_TABLE_REPLICA_AUTO_SCALING = APIOperation(
    input=UpdateTableReplicaAutoScalingInput,
    output=UpdateTableReplicaAutoScalingOutput,
    schema=_SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING,
    input_schema=_SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_INPUT,
    output_schema=_SCHEMA_UPDATE_TABLE_REPLICA_AUTO_SCALING_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class TimeToLiveSpecification:
    """
    Represents the settings used to enable or disable Time to Live (TTL) for
    the specified table.
    """

    enabled: bool
    """
    Indicates whether TTL is to be enabled (true) or disabled (false) on the
    table.
    """

    attribute_name: str
    """
    The name of the TTL attribute used to store the expiration time for
    items in the table.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TIME_TO_LIVE_SPECIFICATION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_boolean(
            _SCHEMA_TIME_TO_LIVE_SPECIFICATION.members["Enabled"], self.enabled
        )
        serializer.write_string(
            _SCHEMA_TIME_TO_LIVE_SPECIFICATION.members["AttributeName"],
            self.attribute_name,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["enabled"] = de.read_boolean(
                        _SCHEMA_TIME_TO_LIVE_SPECIFICATION.members["Enabled"]
                    )

                case 1:
                    kwargs["attribute_name"] = de.read_string(
                        _SCHEMA_TIME_TO_LIVE_SPECIFICATION.members["AttributeName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TIME_TO_LIVE_SPECIFICATION, consumer=_consumer)
        if "enabled" not in kwargs:
            kwargs["enabled"] = False
        if "attribute_name" not in kwargs:
            kwargs["attribute_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class UpdateTimeToLiveInput:
    """Represents the input of an `UpdateTimeToLive` operation."""

    table_name: str | None = None
    """
    The name of the table to be configured. You can also provide the Amazon
    Resource Name (ARN) of the table in this parameter.
    """

    time_to_live_specification: TimeToLiveSpecification | None = None
    """
    Represents the settings used to enable or disable Time to Live for the
    specified table.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_TIME_TO_LIVE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_TIME_TO_LIVE_INPUT.members["TableName"], self.table_name
            )

        if self.time_to_live_specification is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_TIME_TO_LIVE_INPUT.members["TimeToLiveSpecification"],
                self.time_to_live_specification,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_UPDATE_TIME_TO_LIVE_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["time_to_live_specification"] = (
                        TimeToLiveSpecification.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UPDATE_TIME_TO_LIVE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class UpdateTimeToLiveOutput:
    """Dataclass for UpdateTimeToLiveOutput structure."""

    time_to_live_specification: TimeToLiveSpecification | None = None
    """Represents the output of an `UpdateTimeToLive` operation."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_TIME_TO_LIVE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.time_to_live_specification is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_TIME_TO_LIVE_OUTPUT.members["TimeToLiveSpecification"],
                self.time_to_live_specification,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["time_to_live_specification"] = (
                        TimeToLiveSpecification.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UPDATE_TIME_TO_LIVE_OUTPUT, consumer=_consumer)
        return kwargs


UPDATE_TIME_TO_LIVE = APIOperation(
    input=UpdateTimeToLiveInput,
    output=UpdateTimeToLiveOutput,
    schema=_SCHEMA_UPDATE_TIME_TO_LIVE,
    input_schema=_SCHEMA_UPDATE_TIME_TO_LIVE_INPUT,
    output_schema=_SCHEMA_UPDATE_TIME_TO_LIVE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#LimitExceededException"
            ): LimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceInUseException"
            ): ResourceInUseException,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_RESOURCE_IN_USE_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    ],
)


@dataclass
class AttributeValueS:
    """
    An attribute of type String. For example:

    `"S": "Hello"`
    """

    value: str

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_ATTRIBUTE_VALUE.members["S"], self.value)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=deserializer.read_string(_SCHEMA_ATTRIBUTE_VALUE.members["S"]))


@dataclass
class AttributeValueN:
    """
    An attribute of type Number. For example:

    `"N": "123.45"`

    Numbers are sent across the network to DynamoDB as strings, to maximize
    compatibility across languages and libraries. However, DynamoDB treats
    them as number type attributes for mathematical operations.
    """

    value: str

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_ATTRIBUTE_VALUE.members["N"], self.value)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=deserializer.read_string(_SCHEMA_ATTRIBUTE_VALUE.members["N"]))


@dataclass
class AttributeValueB:
    """
    An attribute of type Binary. For example:

    `"B": "dGhpcyB0ZXh0IGlzIGJhc2U2NC1lbmNvZGVk"`
    """

    value: bytes

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_blob(_SCHEMA_ATTRIBUTE_VALUE.members["B"], self.value)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=deserializer.read_blob(_SCHEMA_ATTRIBUTE_VALUE.members["B"]))


@dataclass
class AttributeValueSS:
    """
    An attribute of type String Set. For example:

    `"SS": ["Giraffe", "Hippo" ,"Zebra"]`
    """

    value: list[str]

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_string_set_attribute_value(
            serializer, _SCHEMA_ATTRIBUTE_VALUE.members["SS"], self.value
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(
            value=_deserialize_string_set_attribute_value(
                deserializer, _SCHEMA_ATTRIBUTE_VALUE.members["SS"]
            )
        )


@dataclass
class AttributeValueNS:
    """
    An attribute of type Number Set. For example:

    `"NS": ["42.2", "-19", "7.5", "3.14"]`

    Numbers are sent across the network to DynamoDB as strings, to maximize
    compatibility across languages and libraries. However, DynamoDB treats
    them as number type attributes for mathematical operations.
    """

    value: list[str]

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_number_set_attribute_value(
            serializer, _SCHEMA_ATTRIBUTE_VALUE.members["NS"], self.value
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(
            value=_deserialize_number_set_attribute_value(
                deserializer, _SCHEMA_ATTRIBUTE_VALUE.members["NS"]
            )
        )


@dataclass
class AttributeValueBS:
    """
    An attribute of type Binary Set. For example:

    `"BS": ["U3Vubnk=", "UmFpbnk=", "U25vd3k="]`
    """

    value: list[bytes]

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_binary_set_attribute_value(
            serializer, _SCHEMA_ATTRIBUTE_VALUE.members["BS"], self.value
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(
            value=_deserialize_binary_set_attribute_value(
                deserializer, _SCHEMA_ATTRIBUTE_VALUE.members["BS"]
            )
        )


@dataclass
class AttributeValueM:
    """
    An attribute of type Map. For example:

    `"M": {"Name": {"S": "Joe"}, "Age": {"N": "35"}}`
    """

    value: "dict[str, AttributeValue]"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_map_attribute_value(
            serializer, _SCHEMA_ATTRIBUTE_VALUE.members["M"], self.value
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(
            value=_deserialize_map_attribute_value(
                deserializer, _SCHEMA_ATTRIBUTE_VALUE.members["M"]
            )
        )


@dataclass
class AttributeValueL:
    """
    An attribute of type List. For example:

    `"L": [ {"S": "Cookies"} , {"S": "Coffee"}, {"N": "3.14159"}]`
    """

    value: "list[AttributeValue]"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_list_attribute_value(
            serializer, _SCHEMA_ATTRIBUTE_VALUE.members["L"], self.value
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(
            value=_deserialize_list_attribute_value(
                deserializer, _SCHEMA_ATTRIBUTE_VALUE.members["L"]
            )
        )


@dataclass
class AttributeValueNULL:
    """
    An attribute of type Null. For example:

    `"NULL": true`
    """

    value: bool

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_boolean(_SCHEMA_ATTRIBUTE_VALUE.members["NULL"], self.value)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(
            value=deserializer.read_boolean(_SCHEMA_ATTRIBUTE_VALUE.members["NULL"])
        )


@dataclass
class AttributeValueBOOL:
    """
    An attribute of type Boolean. For example:

    `"BOOL": true`
    """

    value: bool

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_boolean(_SCHEMA_ATTRIBUTE_VALUE.members["BOOL"], self.value)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(
            value=deserializer.read_boolean(_SCHEMA_ATTRIBUTE_VALUE.members["BOOL"])
        )


@dataclass
class AttributeValueUnknown:
    """
    Represents an unknown variant.

    If you receive this value, you will need to update your library to receive the
    parsed value.

    This value may not be deliberately sent.
    """

    tag: str

    def serialize(self, serializer: ShapeSerializer):
        raise SerializationError("Unknown union variants may not be serialized.")

    def serialize_members(self, serializer: ShapeSerializer):
        raise SerializationError("Unknown union variants may not be serialized.")

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        raise NotImplementedError()


AttributeValue = Union[
    AttributeValueS
    | AttributeValueN
    | AttributeValueB
    | AttributeValueSS
    | AttributeValueNS
    | AttributeValueBS
    | AttributeValueM
    | AttributeValueL
    | AttributeValueNULL
    | AttributeValueBOOL
    | AttributeValueUnknown
]
"""
Represents the data for an attribute.

Each attribute value is described as a name-value pair. The name is the
data type, and the value is the data itself.

For more information, see [Data
Types](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.NamingRulesDataTypes.html#HowItWorks.DataTypes)
in the *Amazon DynamoDB Developer Guide*.
"""


class _AttributeValueDeserializer:
    _result: AttributeValue | None = None

    def deserialize(self, deserializer: ShapeDeserializer) -> AttributeValue:
        self._result = None
        deserializer.read_struct(_SCHEMA_ATTRIBUTE_VALUE, self._consumer)

        if self._result is None:
            raise SerializationError(
                "Unions must have exactly one value, but found none."
            )

        return self._result

    def _consumer(self, schema: Schema, de: ShapeDeserializer) -> None:
        match schema.expect_member_index():
            case 0:
                self._set_result(AttributeValueS.deserialize(de))

            case 1:
                self._set_result(AttributeValueN.deserialize(de))

            case 2:
                self._set_result(AttributeValueB.deserialize(de))

            case 3:
                self._set_result(AttributeValueSS.deserialize(de))

            case 4:
                self._set_result(AttributeValueNS.deserialize(de))

            case 5:
                self._set_result(AttributeValueBS.deserialize(de))

            case 6:
                self._set_result(AttributeValueM.deserialize(de))

            case 7:
                self._set_result(AttributeValueL.deserialize(de))

            case 8:
                self._set_result(AttributeValueNULL.deserialize(de))

            case 9:
                self._set_result(AttributeValueBOOL.deserialize(de))

            case _:
                self._set_result(AttributeValueUnknown(tag=schema.expect_member_name()))

    def _set_result(self, value: AttributeValue) -> None:
        if self._result is not None:
            raise SerializationError(
                "Unions must have exactly one value, but found more than one."
            )
        self._result = value


def _serialize_list_attribute_value(
    serializer: ShapeSerializer, schema: Schema, value: list[AttributeValue]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_list_attribute_value(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[AttributeValue]:
    result: list[AttributeValue] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(_AttributeValueDeserializer().deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_map_attribute_value(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, AttributeValue]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_map_attribute_value(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, AttributeValue]:
    result: dict[str, AttributeValue] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = _AttributeValueDeserializer().deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_attribute_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, AttributeValue]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_attribute_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, AttributeValue]:
    result: dict[str, AttributeValue] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = _AttributeValueDeserializer().deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_attribute_value_list(
    serializer: ShapeSerializer, schema: Schema, value: list[AttributeValue]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_attribute_value_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[AttributeValue]:
    result: list[AttributeValue] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(_AttributeValueDeserializer().deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class AttributeValueUpdate:
    """
    For the `UpdateItem` operation, represents the attributes to be
    modified, the action to perform on each, and the new value for each.

    Note:
        You cannot use `UpdateItem` to update any primary key attributes.
        Instead, you will need to delete the item, and then use `PutItem` to
        create a new item with new attributes.

    Attribute values cannot be null; string and binary type attributes must
    have lengths greater than zero; and set type attributes must not be
    empty. Requests with empty values will be rejected with a
    `ValidationException` exception.
    """

    value: "AttributeValue | None" = None
    """
    Represents the data for an attribute.

    Each attribute value is described as a name-value pair. The name is the
    data type, and the value is the data itself.

    For more information, see [Data
    Types](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.NamingRulesDataTypes.html#HowItWorks.DataTypes)
    in the *Amazon DynamoDB Developer Guide*.
    """

    action: AttributeAction | None = None
    """
    Specifies how to perform the update. Valid values are `PUT` (default),
    `DELETE`, and `ADD`. The behavior depends on whether the specified
    primary key already exists in the table.

    **If an item with the specified *Key* is found in the table:**

    - `PUT` - Adds the specified attribute to the item. If the attribute
      already exists, it is replaced by the new value.

    - `DELETE` - If no value is specified, the attribute and its value are
      removed from the item. The data type of the specified value must match
      the existing value's data type.

      If a *set* of values is specified, then those values are subtracted
      from the old set. For example, if the attribute value was the set
      `[a,b,c]` and the `DELETE` action specified `[a,c]`, then the final
      attribute value would be `[b]`. Specifying an empty set is an error.

    - `ADD` - If the attribute does not already exist, then the attribute
      and its values are added to the item. If the attribute does exist,
      then the behavior of `ADD` depends on the data type of the attribute:

      - If the existing attribute is a number, and if `Value` is also a
        number, then the `Value` is mathematically added to the existing
        attribute. If `Value` is a negative number, then it is subtracted
        from the existing attribute.

        Note:
        If you use `ADD` to increment or decrement a number value for an
        item that doesn't exist before the update, DynamoDB uses 0 as the
        initial value.

        In addition, if you use `ADD` to update an existing item, and intend
        to increment or decrement an attribute value which does not yet
        exist, DynamoDB uses `0` as the initial value. For example, suppose
        that the item you want to update does not yet have an attribute
        named *itemcount*, but you decide to `ADD` the number `3` to this
        attribute anyway, even though it currently does not exist. DynamoDB
        will create the *itemcount* attribute, set its initial value to `0`,
        and finally add `3` to it. The result will be a new *itemcount*
        attribute in the item, with a value of `3`.

      - If the existing data type is a set, and if the `Value` is also a
        set, then the `Value` is added to the existing set. (This is a *set*
        operation, not mathematical addition.) For example, if the attribute
        value was the set `[1,2]`, and the `ADD` action specified `[3]`,
        then the final attribute value would be `[1,2,3]`. An error occurs
        if an Add action is specified for a set attribute and the attribute
        type specified does not match the existing set type.

        Both sets must have the same primitive data type. For example, if
        the existing data type is a set of strings, the `Value` must also be
        a set of strings. The same holds true for number sets and binary
        sets.

      This action is only valid for an existing attribute whose data type is
      number or is a set. Do not use `ADD` for any other data types.

    **If no item with the specified *Key* is found:**

    - `PUT` - DynamoDB creates a new item with the specified primary key,
      and then adds the attribute.

    - `DELETE` - Nothing happens; there is no attribute to delete.

    - `ADD` - DynamoDB creates a new item with the supplied primary key and
      number (or set) for the attribute value. The only data types allowed
      are number, number set, string set or binary set.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ATTRIBUTE_VALUE_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.value is not None:
            serializer.write_struct(
                _SCHEMA_ATTRIBUTE_VALUE_UPDATE.members["Value"], self.value
            )

        if self.action is not None:
            serializer.write_string(
                _SCHEMA_ATTRIBUTE_VALUE_UPDATE.members["Action"], self.action
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["value"] = _AttributeValueDeserializer().deserialize(de)

                case 1:
                    kwargs["action"] = AttributeAction(
                        de.read_string(_SCHEMA_ATTRIBUTE_VALUE_UPDATE.members["Action"])
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ATTRIBUTE_VALUE_UPDATE, consumer=_consumer)
        return kwargs


def _serialize_expression_attribute_value_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, AttributeValue]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_expression_attribute_value_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, AttributeValue]:
    result: dict[str, AttributeValue] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = _AttributeValueDeserializer().deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_item_collection_key_attribute_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, AttributeValue]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_item_collection_key_attribute_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, AttributeValue]:
    result: dict[str, AttributeValue] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = _AttributeValueDeserializer().deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_key(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, AttributeValue]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_key(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, AttributeValue]:
    result: dict[str, AttributeValue] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = _AttributeValueDeserializer().deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_prepared_statement_parameters(
    serializer: ShapeSerializer, schema: Schema, value: list[AttributeValue]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_prepared_statement_parameters(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[AttributeValue]:
    result: list[AttributeValue] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(_AttributeValueDeserializer().deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_put_item_input_attribute_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, AttributeValue]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_put_item_input_attribute_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, AttributeValue]:
    result: dict[str, AttributeValue] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = _AttributeValueDeserializer().deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_search_vector_list(
    serializer: ShapeSerializer, schema: Schema, value: list[AttributeValue]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_search_vector_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[AttributeValue]:
    result: list[AttributeValue] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(_AttributeValueDeserializer().deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_attribute_updates(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, AttributeValueUpdate]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_attribute_updates(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, AttributeValueUpdate]:
    result: dict[str, AttributeValueUpdate] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = AttributeValueUpdate.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class BatchStatementError:
    """An error associated with a statement in a PartiQL batch that was run."""

    code: BatchStatementErrorCodeEnum | None = None
    """The error code associated with the failed PartiQL batch statement."""

    message: str | None = None
    """The error message associated with the PartiQL batch response."""

    item: "dict[str, AttributeValue] | None" = None
    """
    The item which caused the condition check to fail. This will be set if
    ReturnValuesOnConditionCheckFailure is specified as `ALL_OLD`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_STATEMENT_ERROR, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.code is not None:
            serializer.write_string(
                _SCHEMA_BATCH_STATEMENT_ERROR.members["Code"], self.code
            )

        if self.message is not None:
            serializer.write_string(
                _SCHEMA_BATCH_STATEMENT_ERROR.members["Message"], self.message
            )

        if self.item is not None:
            _serialize_attribute_map(
                serializer, _SCHEMA_BATCH_STATEMENT_ERROR.members["Item"], self.item
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["code"] = BatchStatementErrorCodeEnum(
                        de.read_string(_SCHEMA_BATCH_STATEMENT_ERROR.members["Code"])
                    )

                case 1:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_BATCH_STATEMENT_ERROR.members["Message"]
                    )

                case 2:
                    kwargs["item"] = _deserialize_attribute_map(
                        de, _SCHEMA_BATCH_STATEMENT_ERROR.members["Item"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BATCH_STATEMENT_ERROR, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class BatchStatementRequest:
    """A PartiQL batch statement request."""

    statement: str
    """A valid PartiQL statement."""

    parameters: "list[AttributeValue] | None" = None
    """The parameters associated with a PartiQL statement in the batch request."""

    consistent_read: bool | None = None
    """The read consistency of the PartiQL batch request."""

    return_values_on_condition_check_failure: (
        ReturnValuesOnConditionCheckFailure | None
    ) = None
    """
    An optional parameter that returns the item attributes for a PartiQL
    batch request operation that failed a condition check.

    There is no additional cost associated with requesting a return value
    aside from the small network and processing overhead of receiving a
    larger response. No read capacity units are consumed.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_STATEMENT_REQUEST, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_BATCH_STATEMENT_REQUEST.members["Statement"], self.statement
        )
        if self.parameters is not None:
            _serialize_prepared_statement_parameters(
                serializer,
                _SCHEMA_BATCH_STATEMENT_REQUEST.members["Parameters"],
                self.parameters,
            )

        if self.consistent_read is not None:
            serializer.write_boolean(
                _SCHEMA_BATCH_STATEMENT_REQUEST.members["ConsistentRead"],
                self.consistent_read,
            )

        if self.return_values_on_condition_check_failure is not None:
            serializer.write_string(
                _SCHEMA_BATCH_STATEMENT_REQUEST.members[
                    "ReturnValuesOnConditionCheckFailure"
                ],
                self.return_values_on_condition_check_failure,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["statement"] = de.read_string(
                        _SCHEMA_BATCH_STATEMENT_REQUEST.members["Statement"]
                    )

                case 1:
                    kwargs["parameters"] = _deserialize_prepared_statement_parameters(
                        de, _SCHEMA_BATCH_STATEMENT_REQUEST.members["Parameters"]
                    )

                case 2:
                    kwargs["consistent_read"] = de.read_boolean(
                        _SCHEMA_BATCH_STATEMENT_REQUEST.members["ConsistentRead"]
                    )

                case 3:
                    kwargs["return_values_on_condition_check_failure"] = (
                        ReturnValuesOnConditionCheckFailure(
                            de.read_string(
                                _SCHEMA_BATCH_STATEMENT_REQUEST.members[
                                    "ReturnValuesOnConditionCheckFailure"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BATCH_STATEMENT_REQUEST, consumer=_consumer)
        if "statement" not in kwargs:
            kwargs["statement"] = ""
        return kwargs


@dataclass(kw_only=True)
class CancellationReason:
    """
    An ordered list of errors for each item in the request which caused the
    transaction to get cancelled. The values of the list are ordered
    according to the ordering of the `TransactWriteItems` request parameter.
    If no error occurred for the associated item an error with a Null code
    and Null message will be present.
    """

    item: "dict[str, AttributeValue] | None" = None
    """Item in the request which caused the transaction to get cancelled."""

    code: str | None = None
    """Status code for the result of the cancelled transaction."""

    message: str | None = None
    """Cancellation reason message description."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CANCELLATION_REASON, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.item is not None:
            _serialize_attribute_map(
                serializer, _SCHEMA_CANCELLATION_REASON.members["Item"], self.item
            )

        if self.code is not None:
            serializer.write_string(
                _SCHEMA_CANCELLATION_REASON.members["Code"], self.code
            )

        if self.message is not None:
            serializer.write_string(
                _SCHEMA_CANCELLATION_REASON.members["Message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["item"] = _deserialize_attribute_map(
                        de, _SCHEMA_CANCELLATION_REASON.members["Item"]
                    )

                case 1:
                    kwargs["code"] = de.read_string(
                        _SCHEMA_CANCELLATION_REASON.members["Code"]
                    )

                case 2:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_CANCELLATION_REASON.members["Message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CANCELLATION_REASON, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class Condition:
    """
    Represents the selection criteria for a `Query` or `Scan` operation:

    - For a `Query` operation, `Condition` is used for specifying the
      `KeyConditions` to use when querying a table or an index. For
      `KeyConditions`, only the following comparison operators are
      supported:

      `EQ | LE | LT | GE | GT | BEGINS_WITH | BETWEEN`

      `Condition` is also used in a `QueryFilter`, which evaluates the query
      results and returns only the desired values.

    - For a `Scan` operation, `Condition` is used in a `ScanFilter`, which
      evaluates the scan results and returns only the desired values.
    """

    comparison_operator: ComparisonOperator
    """
    A comparator for evaluating attributes. For example, equals, greater
    than, less than, etc.

    The following comparison operators are available:

    `EQ | NE | LE | LT | GE | GT | NOT_NULL | NULL | CONTAINS | NOT_CONTAINS | BEGINS_WITH | IN | BETWEEN`

    The following are descriptions of each comparison operator.

    - `EQ` : Equal. `EQ` is supported for all data types, including lists
      and maps.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, Binary, String Set, Number Set, or Binary Set. If
      an item contains an `AttributeValue` element of a different type than
      the one provided in the request, the value does not match. For
      example, `{"S":"6"}` does not equal `{"N":"6"}`. Also, `{"N":"6"}`
      does not equal `{"NS":["6", "2", "1"]}`.

    - `NE` : Not equal. `NE` is supported for all data types, including
      lists and maps.

      `AttributeValueList` can contain only one `AttributeValue` of type
      String, Number, Binary, String Set, Number Set, or Binary Set. If an
      item contains an `AttributeValue` of a different type than the one
      provided in the request, the value does not match. For example,
      `{"S":"6"}` does not equal `{"N":"6"}`. Also, `{"N":"6"}` does not
      equal `{"NS":["6", "2", "1"]}`.

    - `LE` : Less than or equal.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, or Binary (not a set type). If an item contains
      an `AttributeValue` element of a different type than the one provided
      in the request, the value does not match. For example, `{"S":"6"}`
      does not equal `{"N":"6"}`. Also, `{"N":"6"}` does not compare to
      `{"NS":["6", "2", "1"]}`.

    - `LT` : Less than.

      `AttributeValueList` can contain only one `AttributeValue` of type
      String, Number, or Binary (not a set type). If an item contains an
      `AttributeValue` element of a different type than the one provided in
      the request, the value does not match. For example, `{"S":"6"}` does
      not equal `{"N":"6"}`. Also, `{"N":"6"}` does not compare to
      `{"NS":["6", "2", "1"]}`.

    - `GE` : Greater than or equal.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, or Binary (not a set type). If an item contains
      an `AttributeValue` element of a different type than the one provided
      in the request, the value does not match. For example, `{"S":"6"}`
      does not equal `{"N":"6"}`. Also, `{"N":"6"}` does not compare to
      `{"NS":["6", "2", "1"]}`.

    - `GT` : Greater than.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, or Binary (not a set type). If an item contains
      an `AttributeValue` element of a different type than the one provided
      in the request, the value does not match. For example, `{"S":"6"}`
      does not equal `{"N":"6"}`. Also, `{"N":"6"}` does not compare to
      `{"NS":["6", "2", "1"]}`.

    - `NOT_NULL` : The attribute exists. `NOT_NULL` is supported for all
      data types, including lists and maps.

      Note:
        This operator tests for the existence of an attribute, not its data
        type. If the data type of attribute \"`a`\" is null, and you evaluate
        it using `NOT_NULL`, the result is a Boolean `true`. This result is
        because the attribute \"`a`\" exists; its data type is not relevant to
        the `NOT_NULL` comparison operator.

    - `NULL` : The attribute does not exist. `NULL` is supported for all
      data types, including lists and maps.

      Note:
        This operator tests for the nonexistence of an attribute, not its data
        type. If the data type of attribute \"`a`\" is null, and you evaluate
        it using `NULL`, the result is a Boolean `false`. This is because the
        attribute \"`a`\" exists; its data type is not relevant to the `NULL`
        comparison operator.

    - `CONTAINS` : Checks for a subsequence, or value in a set.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, or Binary (not a set type). If the target
      attribute of the comparison is of type String, then the operator
      checks for a substring match. If the target attribute of the
      comparison is of type Binary, then the operator looks for a
      subsequence of the target that matches the input. If the target
      attribute of the comparison is a set (\"`SS`\", \"`NS`\", or
      \"`BS`\"), then the operator evaluates to true if it finds an exact
      match with any member of the set.

      CONTAINS is supported for lists: When evaluating \"`a CONTAINS b`\",
      \"`a`\" can be a list; however, \"`b`\" cannot be a set, a map, or a
      list.

    - `NOT_CONTAINS` : Checks for absence of a subsequence, or absence of a
      value in a set.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, or Binary (not a set type). If the target
      attribute of the comparison is a String, then the operator checks for
      the absence of a substring match. If the target attribute of the
      comparison is Binary, then the operator checks for the absence of a
      subsequence of the target that matches the input. If the target
      attribute of the comparison is a set (\"`SS`\", \"`NS`\", or
      \"`BS`\"), then the operator evaluates to true if it *does not* find
      an exact match with any member of the set.

      NOT_CONTAINS is supported for lists: When evaluating
      \"`a NOT CONTAINS b`\", \"`a`\" can be a list; however, \"`b`\" cannot
      be a set, a map, or a list.

    - `BEGINS_WITH` : Checks for a prefix.

      `AttributeValueList` can contain only one `AttributeValue` of type
      String or Binary (not a Number or a set type). The target attribute of
      the comparison must be of type String or Binary (not a Number or a set
      type).

    - `IN` : Checks for matching elements in a list.

      `AttributeValueList` can contain one or more `AttributeValue` elements
      of type String, Number, or Binary. These attributes are compared
      against an existing attribute of an item. If any elements of the input
      are equal to the item attribute, the expression evaluates to true.

    - `BETWEEN` : Greater than or equal to the first value, and less than or
      equal to the second value.

      `AttributeValueList` must contain two `AttributeValue` elements of the
      same type, either String, Number, or Binary (not a set type). A target
      attribute matches if the target value is greater than, or equal to,
      the first element and less than, or equal to, the second element. If
      an item contains an `AttributeValue` element of a different type than
      the one provided in the request, the value does not match. For
      example, `{"S":"6"}` does not compare to `{"N":"6"}`. Also,
      `{"N":"6"}` does not compare to `{"NS":["6", "2", "1"]}`

    For usage examples of `AttributeValueList` and `ComparisonOperator`, see
    [Legacy Conditional
    Parameters](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    attribute_value_list: "list[AttributeValue] | None" = None
    """
    One or more values to evaluate against the supplied attribute. The
    number of values in the list depends on the `ComparisonOperator` being
    used.

    For type Number, value comparisons are numeric.

    String value comparisons for greater than, equals, or less than are
    based on ASCII character code values. For example, `a` is greater than
    `A`, and `a` is greater than `B`. For a list of code values, see
    <http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters>.

    For Binary, DynamoDB treats each byte of the binary data as unsigned
    when it compares binary values.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CONDITION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.attribute_value_list is not None:
            _serialize_attribute_value_list(
                serializer,
                _SCHEMA_CONDITION.members["AttributeValueList"],
                self.attribute_value_list,
            )

        serializer.write_string(
            _SCHEMA_CONDITION.members["ComparisonOperator"], self.comparison_operator
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attribute_value_list"] = _deserialize_attribute_value_list(
                        de, _SCHEMA_CONDITION.members["AttributeValueList"]
                    )

                case 1:
                    kwargs["comparison_operator"] = ComparisonOperator(
                        de.read_string(_SCHEMA_CONDITION.members["ComparisonOperator"])
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CONDITION, consumer=_consumer)
        if "comparison_operator" not in kwargs:
            kwargs["comparison_operator"] = ComparisonOperator._corrected("")
        return kwargs


@dataclass(kw_only=True)
class ConditionalCheckFailedException(ServiceError):
    """A condition specified in the operation failed to be evaluated."""

    fault: Literal["client", "server"] | None = "client"

    item: "dict[str, AttributeValue] | None" = None
    """Item which caused the `ConditionalCheckFailedException`."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION.members["message"],
                self.message,
            )

        if self.item is not None:
            _serialize_attribute_map(
                serializer,
                _SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION.members["Item"],
                self.item,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION.members["message"]
                    )

                case 1:
                    kwargs["item"] = _deserialize_attribute_map(
                        de, _SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION.members["Item"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class DeleteRequest:
    """Represents a request to perform a `DeleteItem` operation on an item."""

    key: "dict[str, AttributeValue]"
    """
    A map of attribute name to attribute values, representing the primary
    key of the item to delete. All of the table's primary key attributes
    must be specified, and their data types must match those of the table's
    key schema.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_REQUEST, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_key(serializer, _SCHEMA_DELETE_REQUEST.members["Key"], self.key)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["key"] = _deserialize_key(
                        de, _SCHEMA_DELETE_REQUEST.members["Key"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_REQUEST, consumer=_consumer)
        if "key" not in kwargs:
            kwargs["key"] = {}
        return kwargs


@dataclass(kw_only=True)
class ExecuteStatementInput:
    """Dataclass for ExecuteStatementInput structure."""

    statement: str | None = None
    """The PartiQL statement representing the operation to run."""

    parameters: "list[AttributeValue] | None" = None
    """The parameters for the PartiQL statement, if any."""

    consistent_read: bool | None = None
    """
    The consistency of a read operation. If set to `true`, then a strongly
    consistent read is used; otherwise, an eventually consistent read is
    used.
    """

    next_token: str | None = None
    """
    Set this value to get remaining results, if `NextToken` was returned in
    the statement response.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    limit: int | None = None
    """
    The maximum number of items to evaluate (not necessarily the number of
    matching items). If DynamoDB processes the number of items up to the
    limit while processing the results, it stops the operation and returns
    the matching values up to that point, along with a key in
    `LastEvaluatedKey` to apply in a subsequent operation so you can pick up
    where you left off. Also, if the processed dataset size exceeds 1 MB
    before DynamoDB reaches this limit, it stops the operation and returns
    the matching values up to the limit, and a key in `LastEvaluatedKey` to
    apply in a subsequent operation to continue the operation.
    """

    return_values_on_condition_check_failure: (
        ReturnValuesOnConditionCheckFailure | None
    ) = None
    """
    An optional parameter that returns the item attributes for an
    `ExecuteStatement` operation that failed a condition check.

    There is no additional cost associated with requesting a return value
    aside from the small network and processing overhead of receiving a
    larger response. No read capacity units are consumed.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXECUTE_STATEMENT_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.statement is not None:
            serializer.write_string(
                _SCHEMA_EXECUTE_STATEMENT_INPUT.members["Statement"], self.statement
            )

        if self.parameters is not None:
            _serialize_prepared_statement_parameters(
                serializer,
                _SCHEMA_EXECUTE_STATEMENT_INPUT.members["Parameters"],
                self.parameters,
            )

        if self.consistent_read is not None:
            serializer.write_boolean(
                _SCHEMA_EXECUTE_STATEMENT_INPUT.members["ConsistentRead"],
                self.consistent_read,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_EXECUTE_STATEMENT_INPUT.members["NextToken"], self.next_token
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_EXECUTE_STATEMENT_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

        if self.limit is not None:
            serializer.write_integer(
                _SCHEMA_EXECUTE_STATEMENT_INPUT.members["Limit"], self.limit
            )

        if self.return_values_on_condition_check_failure is not None:
            serializer.write_string(
                _SCHEMA_EXECUTE_STATEMENT_INPUT.members[
                    "ReturnValuesOnConditionCheckFailure"
                ],
                self.return_values_on_condition_check_failure,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["statement"] = de.read_string(
                        _SCHEMA_EXECUTE_STATEMENT_INPUT.members["Statement"]
                    )

                case 1:
                    kwargs["parameters"] = _deserialize_prepared_statement_parameters(
                        de, _SCHEMA_EXECUTE_STATEMENT_INPUT.members["Parameters"]
                    )

                case 2:
                    kwargs["consistent_read"] = de.read_boolean(
                        _SCHEMA_EXECUTE_STATEMENT_INPUT.members["ConsistentRead"]
                    )

                case 3:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_EXECUTE_STATEMENT_INPUT.members["NextToken"]
                    )

                case 4:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_EXECUTE_STATEMENT_INPUT.members[
                                "ReturnConsumedCapacity"
                            ]
                        )
                    )

                case 5:
                    kwargs["limit"] = de.read_integer(
                        _SCHEMA_EXECUTE_STATEMENT_INPUT.members["Limit"]
                    )

                case 6:
                    kwargs["return_values_on_condition_check_failure"] = (
                        ReturnValuesOnConditionCheckFailure(
                            de.read_string(
                                _SCHEMA_EXECUTE_STATEMENT_INPUT.members[
                                    "ReturnValuesOnConditionCheckFailure"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EXECUTE_STATEMENT_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class Get:
    """
    Specifies an item and related attribute values to retrieve in a
    `TransactGetItem` object.
    """

    key: "dict[str, AttributeValue]"
    """
    A map of attribute names to `AttributeValue` objects that specifies the
    primary key of the item to retrieve.
    """

    table_name: str
    """
    The name of the table from which to retrieve the specified item. You can
    also provide the Amazon Resource Name (ARN) of the table in this
    parameter.
    """

    projection_expression: str | None = None
    """
    A string that identifies one or more attributes of the specified item to
    retrieve from the table. The attributes in the expression must be
    separated by commas. If no attribute names are specified, then all
    attributes of the specified item are returned. If any of the requested
    attributes are not found, they do not appear in the result.
    """

    expression_attribute_names: dict[str, str] | None = None
    """
    One or more substitution tokens for attribute names in the
    ProjectionExpression parameter.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_key(serializer, _SCHEMA_GET.members["Key"], self.key)
        serializer.write_string(_SCHEMA_GET.members["TableName"], self.table_name)
        if self.projection_expression is not None:
            serializer.write_string(
                _SCHEMA_GET.members["ProjectionExpression"], self.projection_expression
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_GET.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["key"] = _deserialize_key(de, _SCHEMA_GET.members["Key"])

                case 1:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_GET.members["TableName"]
                    )

                case 2:
                    kwargs["projection_expression"] = de.read_string(
                        _SCHEMA_GET.members["ProjectionExpression"]
                    )

                case 3:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de, _SCHEMA_GET.members["ExpressionAttributeNames"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET, consumer=_consumer)
        if "key" not in kwargs:
            kwargs["key"] = {}
        if "table_name" not in kwargs:
            kwargs["table_name"] = ""
        return kwargs

    @classmethod
    def _smithy_default(cls) -> Self:
        return cls(key={}, table_name="")


@dataclass(kw_only=True)
class GetItemInput:
    """Represents the input of a `GetItem` operation."""

    table_name: str | None = None
    """
    The name of the table containing the requested item. You can also
    provide the Amazon Resource Name (ARN) of the table in this parameter.
    """

    key: "dict[str, AttributeValue] | None" = None
    """
    A map of attribute names to `AttributeValue` objects, representing the
    primary key of the item to retrieve.

    For the primary key, you must provide all of the attributes. For
    example, with a simple primary key, you only need to provide a value for
    the partition key. For a composite primary key, you must provide values
    for both the partition key and the sort key.
    """

    attributes_to_get: list[str] | None = None
    """
    This is a legacy parameter. Use `ProjectionExpression` instead. For more
    information, see
    [AttributesToGet](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.AttributesToGet.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    consistent_read: bool | None = None
    """
    Determines the read consistency model: If set to `true`, then the
    operation uses strongly consistent reads; otherwise, the operation uses
    eventually consistent reads.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    projection_expression: str | None = None
    """
    A string that identifies one or more attributes to retrieve from the
    table. These attributes can include scalars, sets, or elements of a JSON
    document. The attributes in the expression must be separated by commas.

    If no attribute names are specified, then all attributes are returned.
    If any of the requested attributes are not found, they do not appear in
    the result.

    For more information, see [Specifying Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_names: dict[str, str] | None = None
    """
    One or more substitution tokens for attribute names in an expression.
    The following are some use cases for using `ExpressionAttributeNames`:

    - To access an attribute whose name conflicts with a DynamoDB reserved
      word.

    - To create a placeholder for repeating occurrences of an attribute name
      in an expression.

    - To prevent special characters in an attribute name from being
      misinterpreted in an expression.

    Use the **#** character in an expression to dereference an attribute
    name. For example, consider the following attribute name:

    - `Percentile`

    The name of this attribute conflicts with a reserved word, so it cannot
    be used directly in an expression. (For the complete list of reserved
    words, see [Reserved
    Words](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
    in the *Amazon DynamoDB Developer Guide*). To work around this, you
    could specify the following for `ExpressionAttributeNames`:

    - `{"#P":"Percentile"}`

    You could then use this substitution in an expression, as in this
    example:

    - `#P = :val`

    Note:
        Tokens that begin with the **:** character are *expression attribute
        values*, which are placeholders for the actual value at runtime.

    For more information on expression attribute names, see [Specifying Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_ITEM_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_GET_ITEM_INPUT.members["TableName"], self.table_name
            )

        if self.key is not None:
            _serialize_key(serializer, _SCHEMA_GET_ITEM_INPUT.members["Key"], self.key)

        if self.attributes_to_get is not None:
            _serialize_attribute_name_list(
                serializer,
                _SCHEMA_GET_ITEM_INPUT.members["AttributesToGet"],
                self.attributes_to_get,
            )

        if self.consistent_read is not None:
            serializer.write_boolean(
                _SCHEMA_GET_ITEM_INPUT.members["ConsistentRead"], self.consistent_read
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_GET_ITEM_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

        if self.projection_expression is not None:
            serializer.write_string(
                _SCHEMA_GET_ITEM_INPUT.members["ProjectionExpression"],
                self.projection_expression,
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_GET_ITEM_INPUT.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_GET_ITEM_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["key"] = _deserialize_key(
                        de, _SCHEMA_GET_ITEM_INPUT.members["Key"]
                    )

                case 2:
                    kwargs["attributes_to_get"] = _deserialize_attribute_name_list(
                        de, _SCHEMA_GET_ITEM_INPUT.members["AttributesToGet"]
                    )

                case 3:
                    kwargs["consistent_read"] = de.read_boolean(
                        _SCHEMA_GET_ITEM_INPUT.members["ConsistentRead"]
                    )

                case 4:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_GET_ITEM_INPUT.members["ReturnConsumedCapacity"]
                        )
                    )

                case 5:
                    kwargs["projection_expression"] = de.read_string(
                        _SCHEMA_GET_ITEM_INPUT.members["ProjectionExpression"]
                    )

                case 6:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de,
                            _SCHEMA_GET_ITEM_INPUT.members["ExpressionAttributeNames"],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_ITEM_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GetItemOutput:
    """Represents the output of a `GetItem` operation."""

    item: "dict[str, AttributeValue] | None" = None
    """
    A map of attribute names to `AttributeValue` objects, as specified by
    `ProjectionExpression`.
    """

    consumed_capacity: ConsumedCapacity | None = None
    """
    The capacity units consumed by the `GetItem` operation. The data
    returned includes the total provisioned throughput consumed, along with
    statistics for the table and any indexes involved in the operation.
    `ConsumedCapacity` is only returned if the `ReturnConsumedCapacity`
    parameter was specified. For more information, see [Capacity unit
    consumption for read
    operations](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/read-write-operations.html#read-operation-consumption)
    in the *Amazon DynamoDB Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_ITEM_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.item is not None:
            _serialize_attribute_map(
                serializer, _SCHEMA_GET_ITEM_OUTPUT.members["Item"], self.item
            )

        if self.consumed_capacity is not None:
            serializer.write_struct(
                _SCHEMA_GET_ITEM_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["item"] = _deserialize_attribute_map(
                        de, _SCHEMA_GET_ITEM_OUTPUT.members["Item"]
                    )

                case 1:
                    kwargs["consumed_capacity"] = ConsumedCapacity.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_ITEM_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ItemCollectionMetrics:
    """
    Information about item collections, if any, that were affected by the
    operation. `ItemCollectionMetrics` is only returned if the request asked
    for it. If the table does not have any local secondary indexes, this
    information is not returned in the response.
    """

    item_collection_key: "dict[str, AttributeValue] | None" = None
    """
    The partition key value of the item collection. This value is the same
    as the partition key value of the item.
    """

    size_estimate_range_gb: list[float] | None = None
    """
    An estimate of item collection size, in gigabytes. This value is a
    two-element array containing a lower bound and an upper bound for the
    estimate. The estimate includes the size of all the items in the table,
    plus the size of all attributes projected into all of the local
    secondary indexes on that table. Use this estimate to measure whether a
    local secondary index is approaching its size limit.

    The estimate is subject to change over time; therefore, do not rely on
    the precision or accuracy of the estimate.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ITEM_COLLECTION_METRICS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.item_collection_key is not None:
            _serialize_item_collection_key_attribute_map(
                serializer,
                _SCHEMA_ITEM_COLLECTION_METRICS.members["ItemCollectionKey"],
                self.item_collection_key,
            )

        if self.size_estimate_range_gb is not None:
            _serialize_item_collection_size_estimate_range(
                serializer,
                _SCHEMA_ITEM_COLLECTION_METRICS.members["SizeEstimateRangeGB"],
                self.size_estimate_range_gb,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["item_collection_key"] = (
                        _deserialize_item_collection_key_attribute_map(
                            de,
                            _SCHEMA_ITEM_COLLECTION_METRICS.members[
                                "ItemCollectionKey"
                            ],
                        )
                    )

                case 1:
                    kwargs["size_estimate_range_gb"] = (
                        _deserialize_item_collection_size_estimate_range(
                            de,
                            _SCHEMA_ITEM_COLLECTION_METRICS.members[
                                "SizeEstimateRangeGB"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ITEM_COLLECTION_METRICS, consumer=_consumer)
        return kwargs


def _serialize_item_list(
    serializer: ShapeSerializer, schema: Schema, value: list[dict[str, AttributeValue]]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            _serialize_attribute_map(ls, member_schema, e)


def _deserialize_item_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[dict[str, AttributeValue]]:
    result: list[dict[str, AttributeValue]] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(_deserialize_attribute_map(d, member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ItemResponse:
    """Details for the requested item."""

    item: "dict[str, AttributeValue] | None" = None
    """Map of attribute data consisting of the data type and attribute value."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ITEM_RESPONSE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.item is not None:
            _serialize_attribute_map(
                serializer, _SCHEMA_ITEM_RESPONSE.members["Item"], self.item
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["item"] = _deserialize_attribute_map(
                        de, _SCHEMA_ITEM_RESPONSE.members["Item"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ITEM_RESPONSE, consumer=_consumer)
        return kwargs


def _serialize_key_list(
    serializer: ShapeSerializer, schema: Schema, value: list[dict[str, AttributeValue]]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            _serialize_key(ls, member_schema, e)


def _deserialize_key_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[dict[str, AttributeValue]]:
    result: list[dict[str, AttributeValue]] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(_deserialize_key(d, member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ParameterizedStatement:
    """Represents a PartiQL statement that uses parameters."""

    statement: str
    """A PartiQL statement that uses parameters."""

    parameters: "list[AttributeValue] | None" = None
    """The parameter values."""

    return_values_on_condition_check_failure: (
        ReturnValuesOnConditionCheckFailure | None
    ) = None
    """
    An optional parameter that returns the item attributes for a PartiQL
    `ParameterizedStatement` operation that failed a condition check.

    There is no additional cost associated with requesting a return value
    aside from the small network and processing overhead of receiving a
    larger response. No read capacity units are consumed.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PARAMETERIZED_STATEMENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_PARAMETERIZED_STATEMENT.members["Statement"], self.statement
        )
        if self.parameters is not None:
            _serialize_prepared_statement_parameters(
                serializer,
                _SCHEMA_PARAMETERIZED_STATEMENT.members["Parameters"],
                self.parameters,
            )

        if self.return_values_on_condition_check_failure is not None:
            serializer.write_string(
                _SCHEMA_PARAMETERIZED_STATEMENT.members[
                    "ReturnValuesOnConditionCheckFailure"
                ],
                self.return_values_on_condition_check_failure,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["statement"] = de.read_string(
                        _SCHEMA_PARAMETERIZED_STATEMENT.members["Statement"]
                    )

                case 1:
                    kwargs["parameters"] = _deserialize_prepared_statement_parameters(
                        de, _SCHEMA_PARAMETERIZED_STATEMENT.members["Parameters"]
                    )

                case 2:
                    kwargs["return_values_on_condition_check_failure"] = (
                        ReturnValuesOnConditionCheckFailure(
                            de.read_string(
                                _SCHEMA_PARAMETERIZED_STATEMENT.members[
                                    "ReturnValuesOnConditionCheckFailure"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PARAMETERIZED_STATEMENT, consumer=_consumer)
        if "statement" not in kwargs:
            kwargs["statement"] = ""
        return kwargs


@dataclass(kw_only=True)
class PutRequest:
    """Represents a request to perform a `PutItem` operation on an item."""

    item: "dict[str, AttributeValue]"
    """
    A map of attribute name to attribute values, representing the primary
    key of an item to be processed by `PutItem`. All of the table's primary
    key attributes must be specified, and their data types must match those
    of the table's key schema. If any attributes are present in the item
    that are part of an index key schema for the table, their types must
    match the index key schema.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PUT_REQUEST, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_put_item_input_attribute_map(
            serializer, _SCHEMA_PUT_REQUEST.members["Item"], self.item
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["item"] = _deserialize_put_item_input_attribute_map(
                        de, _SCHEMA_PUT_REQUEST.members["Item"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PUT_REQUEST, consumer=_consumer)
        if "item" not in kwargs:
            kwargs["item"] = {}
        return kwargs


@dataclass(kw_only=True)
class SearchResultItem:
    """A single result from a `SearchVectors` operation."""

    item: "dict[str, AttributeValue] | None" = None
    """
    A map of attribute names to `AttributeValue` objects, representing the
    projected attributes of the item returned by the vector search.
    """

    score: float = 0
    """
    The similarity score for this item relative to the search vector. The
    interpretation depends on the distance function configured for the
    vector index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SEARCH_RESULT_ITEM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.item is not None:
            _serialize_attribute_map(
                serializer, _SCHEMA_SEARCH_RESULT_ITEM.members["Item"], self.item
            )

        serializer.write_double(_SCHEMA_SEARCH_RESULT_ITEM.members["Score"], self.score)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["item"] = _deserialize_attribute_map(
                        de, _SCHEMA_SEARCH_RESULT_ITEM.members["Item"]
                    )

                case 1:
                    kwargs["score"] = de.read_double(
                        _SCHEMA_SEARCH_RESULT_ITEM.members["Score"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SEARCH_RESULT_ITEM, consumer=_consumer)
        return kwargs


def _serialize_batch_get_response_map(
    serializer: ShapeSerializer,
    schema: Schema,
    value: dict[str, list[dict[str, AttributeValue]]],
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: _serialize_item_list(vs, value_schema, v))


def _deserialize_batch_get_response_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, list[dict[str, AttributeValue]]]:
    result: dict[str, list[dict[str, AttributeValue]]] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = _deserialize_item_list(d, value_schema)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_cancellation_reason_list(
    serializer: ShapeSerializer, schema: Schema, value: list[CancellationReason]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_cancellation_reason_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[CancellationReason]:
    result: list[CancellationReason] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(CancellationReason.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_filter_condition_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, Condition]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_filter_condition_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, Condition]:
    result: dict[str, Condition] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = Condition.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_item_collection_metrics_multiple(
    serializer: ShapeSerializer, schema: Schema, value: list[ItemCollectionMetrics]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_item_collection_metrics_multiple(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ItemCollectionMetrics]:
    result: list[ItemCollectionMetrics] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ItemCollectionMetrics.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_item_response_list(
    serializer: ShapeSerializer, schema: Schema, value: list[ItemResponse]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_item_response_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ItemResponse]:
    result: list[ItemResponse] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ItemResponse.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_key_conditions(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, Condition]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_key_conditions(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, Condition]:
    result: dict[str, Condition] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = Condition.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class KeysAndAttributes:
    """
    Represents a set of primary keys and, for each key, the attributes to
    retrieve from the table.

    For each primary key, you must provide *all* of the key attributes. For
    example, with a simple primary key, you only need to provide the
    partition key. For a composite primary key, you must provide *both* the
    partition key and the sort key.
    """

    keys: "list[dict[str, AttributeValue]]"
    """
    The primary key attribute values that define the items and the
    attributes associated with the items.
    """

    attributes_to_get: list[str] | None = None
    """
    This is a legacy parameter. Use `ProjectionExpression` instead. For more
    information, see [Legacy Conditional
    Parameters](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    consistent_read: bool | None = None
    """
    The consistency of a read operation. If set to `true`, then a strongly
    consistent read is used; otherwise, an eventually consistent read is
    used.
    """

    projection_expression: str | None = None
    """
    A string that identifies one or more attributes to retrieve from the
    table. These attributes can include scalars, sets, or elements of a JSON
    document. The attributes in the `ProjectionExpression` must be separated
    by commas.

    If no attribute names are specified, then all attributes will be
    returned. If any of the requested attributes are not found, they will
    not appear in the result.

    For more information, see [Accessing Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_names: dict[str, str] | None = None
    """
    One or more substitution tokens for attribute names in an expression.
    The following are some use cases for using `ExpressionAttributeNames`:

    - To access an attribute whose name conflicts with a DynamoDB reserved
      word.

    - To create a placeholder for repeating occurrences of an attribute name
      in an expression.

    - To prevent special characters in an attribute name from being
      misinterpreted in an expression.

    Use the **#** character in an expression to dereference an attribute
    name. For example, consider the following attribute name:

    - `Percentile`

    The name of this attribute conflicts with a reserved word, so it cannot
    be used directly in an expression. (For the complete list of reserved
    words, see [Reserved
    Words](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
    in the *Amazon DynamoDB Developer Guide*). To work around this, you
    could specify the following for `ExpressionAttributeNames`:

    - `{"#P":"Percentile"}`

    You could then use this substitution in an expression, as in this
    example:

    - `#P = :val`

    Note:
        Tokens that begin with the **:** character are *expression attribute
        values*, which are placeholders for the actual value at runtime.

    For more information on expression attribute names, see [Accessing Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_KEYS_AND_ATTRIBUTES, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_key_list(
            serializer, _SCHEMA_KEYS_AND_ATTRIBUTES.members["Keys"], self.keys
        )
        if self.attributes_to_get is not None:
            _serialize_attribute_name_list(
                serializer,
                _SCHEMA_KEYS_AND_ATTRIBUTES.members["AttributesToGet"],
                self.attributes_to_get,
            )

        if self.consistent_read is not None:
            serializer.write_boolean(
                _SCHEMA_KEYS_AND_ATTRIBUTES.members["ConsistentRead"],
                self.consistent_read,
            )

        if self.projection_expression is not None:
            serializer.write_string(
                _SCHEMA_KEYS_AND_ATTRIBUTES.members["ProjectionExpression"],
                self.projection_expression,
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_KEYS_AND_ATTRIBUTES.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["keys"] = _deserialize_key_list(
                        de, _SCHEMA_KEYS_AND_ATTRIBUTES.members["Keys"]
                    )

                case 1:
                    kwargs["attributes_to_get"] = _deserialize_attribute_name_list(
                        de, _SCHEMA_KEYS_AND_ATTRIBUTES.members["AttributesToGet"]
                    )

                case 2:
                    kwargs["consistent_read"] = de.read_boolean(
                        _SCHEMA_KEYS_AND_ATTRIBUTES.members["ConsistentRead"]
                    )

                case 3:
                    kwargs["projection_expression"] = de.read_string(
                        _SCHEMA_KEYS_AND_ATTRIBUTES.members["ProjectionExpression"]
                    )

                case 4:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de,
                            _SCHEMA_KEYS_AND_ATTRIBUTES.members[
                                "ExpressionAttributeNames"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_KEYS_AND_ATTRIBUTES, consumer=_consumer)
        if "keys" not in kwargs:
            kwargs["keys"] = []
        return kwargs


def _serialize_parameterized_statements(
    serializer: ShapeSerializer, schema: Schema, value: list[ParameterizedStatement]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_parameterized_statements(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ParameterizedStatement]:
    result: list[ParameterizedStatement] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ParameterizedStatement.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_parti_ql_batch_request(
    serializer: ShapeSerializer, schema: Schema, value: list[BatchStatementRequest]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_parti_ql_batch_request(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[BatchStatementRequest]:
    result: list[BatchStatementRequest] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(BatchStatementRequest.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_search_result_list(
    serializer: ShapeSerializer, schema: Schema, value: list[SearchResultItem]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_search_result_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[SearchResultItem]:
    result: list[SearchResultItem] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(SearchResultItem.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class TransactGetItem:
    """Specifies an item to be retrieved as part of the transaction."""

    get: "Get"
    """
    Contains the primary key that identifies the item to get, together with
    the name of the table that contains the item, and optionally the
    specific attributes of the item to retrieve.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSACT_GET_ITEM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSACT_GET_ITEM.members["Get"], self.get)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["get"] = Get.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TRANSACT_GET_ITEM, consumer=_consumer)
        if "get" not in kwargs:
            kwargs["get"] = Get._smithy_default()
        return kwargs


@dataclass(kw_only=True)
class BatchExecuteStatementInput:
    """Dataclass for BatchExecuteStatementInput structure."""

    statements: "list[BatchStatementRequest] | None" = None
    """The list of PartiQL statements representing the batch to run."""

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_EXECUTE_STATEMENT_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.statements is not None:
            _serialize_parti_ql_batch_request(
                serializer,
                _SCHEMA_BATCH_EXECUTE_STATEMENT_INPUT.members["Statements"],
                self.statements,
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_BATCH_EXECUTE_STATEMENT_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["statements"] = _deserialize_parti_ql_batch_request(
                        de, _SCHEMA_BATCH_EXECUTE_STATEMENT_INPUT.members["Statements"]
                    )

                case 1:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_BATCH_EXECUTE_STATEMENT_INPUT.members[
                                "ReturnConsumedCapacity"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_BATCH_EXECUTE_STATEMENT_INPUT, consumer=_consumer
        )
        return kwargs


def _serialize_batch_get_request_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, KeysAndAttributes]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_batch_get_request_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, KeysAndAttributes]:
    result: dict[str, KeysAndAttributes] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = KeysAndAttributes.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ExecuteTransactionInput:
    """Dataclass for ExecuteTransactionInput structure."""

    transact_statements: "list[ParameterizedStatement] | None" = None
    """The list of PartiQL statements representing the transaction to run."""

    client_request_token: str | None = None
    """
    Set this value to get remaining results, if `NextToken` was returned in
    the statement response.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response. For more
    information, see
    [TransactGetItems](https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_TransactGetItems.html)
    and
    [TransactWriteItems](https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_TransactWriteItems.html).
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXECUTE_TRANSACTION_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.transact_statements is not None:
            _serialize_parameterized_statements(
                serializer,
                _SCHEMA_EXECUTE_TRANSACTION_INPUT.members["TransactStatements"],
                self.transact_statements,
            )

        if self.client_request_token is not None:
            serializer.write_string(
                _SCHEMA_EXECUTE_TRANSACTION_INPUT.members["ClientRequestToken"],
                self.client_request_token,
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_EXECUTE_TRANSACTION_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["transact_statements"] = (
                        _deserialize_parameterized_statements(
                            de,
                            _SCHEMA_EXECUTE_TRANSACTION_INPUT.members[
                                "TransactStatements"
                            ],
                        )
                    )

                case 1:
                    kwargs["client_request_token"] = de.read_string(
                        _SCHEMA_EXECUTE_TRANSACTION_INPUT.members["ClientRequestToken"]
                    )

                case 2:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_EXECUTE_TRANSACTION_INPUT.members[
                                "ReturnConsumedCapacity"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EXECUTE_TRANSACTION_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ExecuteTransactionOutput:
    """Dataclass for ExecuteTransactionOutput structure."""

    responses: "list[ItemResponse] | None" = None
    """The response to a PartiQL transaction."""

    consumed_capacity: list[ConsumedCapacity] | None = None
    """
    The capacity units consumed by the entire operation. The values of the
    list are ordered according to the ordering of the statements.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXECUTE_TRANSACTION_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.responses is not None:
            _serialize_item_response_list(
                serializer,
                _SCHEMA_EXECUTE_TRANSACTION_OUTPUT.members["Responses"],
                self.responses,
            )

        if self.consumed_capacity is not None:
            _serialize_consumed_capacity_multiple(
                serializer,
                _SCHEMA_EXECUTE_TRANSACTION_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["responses"] = _deserialize_item_response_list(
                        de, _SCHEMA_EXECUTE_TRANSACTION_OUTPUT.members["Responses"]
                    )

                case 1:
                    kwargs["consumed_capacity"] = (
                        _deserialize_consumed_capacity_multiple(
                            de,
                            _SCHEMA_EXECUTE_TRANSACTION_OUTPUT.members[
                                "ConsumedCapacity"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EXECUTE_TRANSACTION_OUTPUT, consumer=_consumer)
        return kwargs


def _serialize_item_collection_metrics_per_table(
    serializer: ShapeSerializer,
    schema: Schema,
    value: dict[str, list[ItemCollectionMetrics]],
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(
                k,
                lambda vs: _serialize_item_collection_metrics_multiple(
                    vs, value_schema, v
                ),
            )


def _deserialize_item_collection_metrics_per_table(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, list[ItemCollectionMetrics]]:
    result: dict[str, list[ItemCollectionMetrics]] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = _deserialize_item_collection_metrics_multiple(d, value_schema)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class SearchVectorsOutput:
    """Dataclass for SearchVectorsOutput structure."""

    consumed_capacity: VectorCapacity | None = None
    """
    The capacity units consumed by the `SearchVectors` operation. Contains
    `VectorSearchRequestBytes`, which represents the vector search capacity
    consumed.
    """

    search_results: "list[SearchResultItem] | None" = None
    """
    A list of items returned by the vector similarity search, sorted by
    similarity with the most similar item first. Each item contains the
    projected attributes and a similarity score.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SEARCH_VECTORS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.consumed_capacity is not None:
            serializer.write_struct(
                _SCHEMA_SEARCH_VECTORS_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

        if self.search_results is not None:
            _serialize_search_result_list(
                serializer,
                _SCHEMA_SEARCH_VECTORS_OUTPUT.members["SearchResults"],
                self.search_results,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["consumed_capacity"] = VectorCapacity.deserialize(de)

                case 1:
                    kwargs["search_results"] = _deserialize_search_result_list(
                        de, _SCHEMA_SEARCH_VECTORS_OUTPUT.members["SearchResults"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SEARCH_VECTORS_OUTPUT, consumer=_consumer)
        return kwargs


def _serialize_transact_get_item_list(
    serializer: ShapeSerializer, schema: Schema, value: list[TransactGetItem]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_transact_get_item_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[TransactGetItem]:
    result: list[TransactGetItem] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(TransactGetItem.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class TransactGetItemsOutput:
    """Dataclass for TransactGetItemsOutput structure."""

    consumed_capacity: list[ConsumedCapacity] | None = None
    """
    If the *ReturnConsumedCapacity* value was `TOTAL`, this is an array of
    `ConsumedCapacity` objects, one for each table addressed by
    `TransactGetItem` objects in the *TransactItems* parameter. These
    `ConsumedCapacity` objects report the read-capacity units consumed by
    the `TransactGetItems` call in that table.
    """

    responses: "list[ItemResponse] | None" = None
    """
    An ordered array of up to 100 `ItemResponse` objects, each of which
    corresponds to the `TransactGetItem` object in the same position in the
    *TransactItems* array. Each `ItemResponse` object contains a Map of the
    name-value pairs that are the projected attributes of the requested
    item.

    If a requested item could not be retrieved, the corresponding
    `ItemResponse` object is Null, or if the requested item has no projected
    attributes, the corresponding `ItemResponse` object is an empty Map.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSACT_GET_ITEMS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.consumed_capacity is not None:
            _serialize_consumed_capacity_multiple(
                serializer,
                _SCHEMA_TRANSACT_GET_ITEMS_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

        if self.responses is not None:
            _serialize_item_response_list(
                serializer,
                _SCHEMA_TRANSACT_GET_ITEMS_OUTPUT.members["Responses"],
                self.responses,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["consumed_capacity"] = (
                        _deserialize_consumed_capacity_multiple(
                            de,
                            _SCHEMA_TRANSACT_GET_ITEMS_OUTPUT.members[
                                "ConsumedCapacity"
                            ],
                        )
                    )

                case 1:
                    kwargs["responses"] = _deserialize_item_response_list(
                        de, _SCHEMA_TRANSACT_GET_ITEMS_OUTPUT.members["Responses"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TRANSACT_GET_ITEMS_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class TransactionCanceledException(ServiceError):
    """
    The entire transaction request was canceled.

    DynamoDB cancels a `TransactWriteItems` request under the following
    circumstances:

    - A condition in one of the condition expressions is not met.

    - A table in the `TransactWriteItems` request is in a different account
      or region.

    - More than one action in the `TransactWriteItems` operation targets the
      same item.

    - There is insufficient provisioned capacity for the transaction to be
      completed.

    - An item size becomes too large (larger than 400 KB), or a local
      secondary index (LSI) becomes too large, or a similar validation error
      occurs because of changes made by the transaction.

    - There is a user error, such as an invalid data format.

    - There is an ongoing `TransactWriteItems` operation that conflicts with
      a concurrent `TransactWriteItems` request. In this case the
      `TransactWriteItems` operation fails with a
      `TransactionCanceledException`.

    DynamoDB cancels a `TransactGetItems` request under the following
    circumstances:

    - There is an ongoing `TransactGetItems` operation that conflicts with a
      concurrent `PutItem`, `UpdateItem`, `DeleteItem` or
      `TransactWriteItems` request. In this case the `TransactGetItems`
      operation fails with a `TransactionCanceledException`.

    - A table in the `TransactGetItems` request is in a different account or
      region.

    - There is insufficient provisioned capacity for the transaction to be
      completed.

    - There is a user error, such as an invalid data format.

    Note:
        DynamoDB lists the cancellation reasons on the `CancellationReasons`
        property. Transaction cancellation reasons are ordered in the order of
        requested items, if an item has no error it will have `None` code and
        `Null` message. The `None` code is returned as the literal string
        `"None"`, not a null or absent value; the message field is omitted
        entirely for an item that has no error. This is important to note when
        using an SDK that surfaces the code as an optional or nullable type.

    Cancellation reason codes and possible error messages:

    - No Errors:

      - Code: `None`

      - Message: `null`

    - Conditional Check Failed:

      - Code: `ConditionalCheckFailed`

      - Message: The conditional request failed.

    - Item Collection Size Limit Exceeded:

      - Code: `ItemCollectionSizeLimitExceeded`

      - Message: Collection size exceeded.

    - Transaction Conflict:

      - Code: `TransactionConflict`

      - Message: Transaction is ongoing for the item.

    - Provisioned Throughput Exceeded:

      - Code: `ProvisionedThroughputExceeded`

      - Messages:

        - The level of configured provisioned throughput for the table was
          exceeded. Consider increasing your provisioning level with the
          UpdateTable API.

          Note:
        This Message is received when provisioned throughput is exceeded
        is on a provisioned DynamoDB table.

        - The level of configured provisioned throughput for one or more
          global secondary indexes of the table was exceeded. Consider
          increasing your provisioning level for the under-provisioned
          global secondary indexes with the UpdateTable API.

          Note:
        This message is returned when provisioned throughput is exceeded
        is on a provisioned GSI.

    - Throttling Error:

      - Code: `ThrottlingError`

      - Messages:

        - Throughput exceeds the current capacity of your table or index.
          DynamoDB is automatically scaling your table or index so please
          try again shortly. If exceptions persist, check if you have a hot
          key:
          https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/bp-partition-key-design.html.

          Note:
        This message is returned when writes get throttled on an On-Demand
        table as DynamoDB is automatically scaling the table.

        - Throughput exceeds the current capacity for one or more global
          secondary indexes. DynamoDB is automatically scaling your index so
          please try again shortly.

          Note:
        This message is returned when writes get throttled on an On-Demand
        GSI as DynamoDB is automatically scaling the GSI.

    - Validation Error:

      - Code: `ValidationError`

      - Messages:

        - One or more parameter values were invalid.

        - The update expression attempted to update the secondary index key
          beyond allowed size limits.

        - The update expression attempted to update the secondary index key
          to unsupported type.

        - An operand in the update expression has an incorrect data type.

        - Item size to update has exceeded the maximum allowed size.

        - Number overflow. Attempting to store a number with magnitude
          larger than supported range.

        - Type mismatch for attribute to update.

        - Nesting Levels have exceeded supported limits.

        - The document path provided in the update expression is invalid for
          update.

        - The provided expression refers to an attribute that does not exist
          in the item.
    """

    fault: Literal["client", "server"] | None = "client"

    cancellation_reasons: "list[CancellationReason] | None" = None
    """A list of cancellation reasons."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSACTION_CANCELED_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_TRANSACTION_CANCELED_EXCEPTION.members["Message"], self.message
            )

        if self.cancellation_reasons is not None:
            _serialize_cancellation_reason_list(
                serializer,
                _SCHEMA_TRANSACTION_CANCELED_EXCEPTION.members["CancellationReasons"],
                self.cancellation_reasons,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_TRANSACTION_CANCELED_EXCEPTION.members["Message"]
                    )

                case 1:
                    kwargs["cancellation_reasons"] = (
                        _deserialize_cancellation_reason_list(
                            de,
                            _SCHEMA_TRANSACTION_CANCELED_EXCEPTION.members[
                                "CancellationReasons"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_TRANSACTION_CANCELED_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class BatchGetItemInput:
    """Represents the input of a `BatchGetItem` operation."""

    request_items: "dict[str, KeysAndAttributes] | None" = None
    """
    A map of one or more table names or table ARNs and, for each table, a
    map that describes one or more items to retrieve from that table. Each
    table name or ARN can be used only once per `BatchGetItem` request.

    Each element in the map of items to retrieve consists of the following:

    - `ConsistentRead` - If `true`, a strongly consistent read is used; if
      `false` (the default), an eventually consistent read is used.

    - `ExpressionAttributeNames` - One or more substitution tokens for
      attribute names in the `ProjectionExpression` parameter. The following
      are some use cases for using `ExpressionAttributeNames`:

      - To access an attribute whose name conflicts with a DynamoDB reserved
        word.

      - To create a placeholder for repeating occurrences of an attribute
        name in an expression.

      - To prevent special characters in an attribute name from being
        misinterpreted in an expression.

      Use the **#** character in an expression to dereference an attribute
      name. For example, consider the following attribute name:

      - `Percentile`

      The name of this attribute conflicts with a reserved word, so it
      cannot be used directly in an expression. (For the complete list of
      reserved words, see [Reserved
      Words](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
      in the *Amazon DynamoDB Developer Guide*). To work around this, you
      could specify the following for `ExpressionAttributeNames`:

      - `{"#P":"Percentile"}`

      You could then use this substitution in an expression, as in this
      example:

      - `#P = :val`

      Note:
        Tokens that begin with the **:** character are *expression attribute
        values*, which are placeholders for the actual value at runtime.

      For more information about expression attribute names, see [Accessing
      Item
      Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
      in the *Amazon DynamoDB Developer Guide*.

    - `Keys` - An array of primary key attribute values that define specific
      items in the table. For each primary key, you must provide *all* of
      the key attributes. For example, with a simple primary key, you only
      need to provide the partition key value. For a composite key, you must
      provide *both* the partition key value and the sort key value.

    - `ProjectionExpression` - A string that identifies one or more
      attributes to retrieve from the table. These attributes can include
      scalars, sets, or elements of a JSON document. The attributes in the
      expression must be separated by commas.

      If no attribute names are specified, then all attributes are returned.
      If any of the requested attributes are not found, they do not appear
      in the result.

      For more information, see [Accessing Item
      Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
      in the *Amazon DynamoDB Developer Guide*.

    - `AttributesToGet` - This is a legacy parameter. Use
      `ProjectionExpression` instead. For more information, see
      [AttributesToGet](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.AttributesToGet.html)
      in the *Amazon DynamoDB Developer Guide*.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_GET_ITEM_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.request_items is not None:
            _serialize_batch_get_request_map(
                serializer,
                _SCHEMA_BATCH_GET_ITEM_INPUT.members["RequestItems"],
                self.request_items,
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_BATCH_GET_ITEM_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["request_items"] = _deserialize_batch_get_request_map(
                        de, _SCHEMA_BATCH_GET_ITEM_INPUT.members["RequestItems"]
                    )

                case 1:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_BATCH_GET_ITEM_INPUT.members[
                                "ReturnConsumedCapacity"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BATCH_GET_ITEM_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ExpectedAttributeValue:
    """
    Represents a condition to be compared with an attribute value. This
    condition can be used with `DeleteItem`, `PutItem`, or `UpdateItem`
    operations; if the comparison evaluates to true, the operation succeeds;
    if not, the operation fails. You can use `ExpectedAttributeValue` in one
    of two different ways:

    - Use `AttributeValueList` to specify one or more values to compare
      against an attribute. Use `ComparisonOperator` to specify how you want
      to perform the comparison. If the comparison evaluates to true, then
      the conditional operation succeeds.

    - Use `Value` to specify a value that DynamoDB will compare against an
      attribute. If the values match, then `ExpectedAttributeValue`
      evaluates to true and the conditional operation succeeds. Optionally,
      you can also set `Exists` to false, indicating that you *do not*
      expect to find the attribute value in the table. In this case, the
      conditional operation succeeds only if the comparison evaluates to
      false.

    `Value` and `Exists` are incompatible with `AttributeValueList` and
    `ComparisonOperator`. Note that if you use both sets of parameters at
    once, DynamoDB will return a `ValidationException` exception.
    """

    value: "AttributeValue | None" = None
    """
    Represents the data for the expected attribute.

    Each attribute value is described as a name-value pair. The name is the
    data type, and the value is the data itself.

    For more information, see [Data
    Types](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.NamingRulesDataTypes.html#HowItWorks.DataTypes)
    in the *Amazon DynamoDB Developer Guide*.
    """

    exists: bool | None = None
    """
    Causes DynamoDB to evaluate the value before attempting a conditional
    operation:

    - If `Exists` is `true`, DynamoDB will check to see if that attribute
      value already exists in the table. If it is found, then the operation
      succeeds. If it is not found, the operation fails with a
      `ConditionCheckFailedException`.

    - If `Exists` is `false`, DynamoDB assumes that the attribute value does
      not exist in the table. If in fact the value does not exist, then the
      assumption is valid and the operation succeeds. If the value is found,
      despite the assumption that it does not exist, the operation fails
      with a `ConditionCheckFailedException`.

    The default setting for `Exists` is `true`. If you supply a `Value` all
    by itself, DynamoDB assumes the attribute exists: You don't have to set
    `Exists` to `true`, because it is implied.

    DynamoDB returns a `ValidationException` if:

    - `Exists` is `true` but there is no `Value` to check. (You expect a
      value to exist, but don't specify what that value is.)

    - `Exists` is `false` but you also provide a `Value`. (You cannot expect
      an attribute to have a value, while also expecting it not to exist.)
    """

    comparison_operator: ComparisonOperator | None = None
    """
    A comparator for evaluating attributes in the `AttributeValueList`. For
    example, equals, greater than, less than, etc.

    The following comparison operators are available:

    `EQ | NE | LE | LT | GE | GT | NOT_NULL | NULL | CONTAINS | NOT_CONTAINS | BEGINS_WITH | IN | BETWEEN`

    The following are descriptions of each comparison operator.

    - `EQ` : Equal. `EQ` is supported for all data types, including lists
      and maps.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, Binary, String Set, Number Set, or Binary Set. If
      an item contains an `AttributeValue` element of a different type than
      the one provided in the request, the value does not match. For
      example, `{"S":"6"}` does not equal `{"N":"6"}`. Also, `{"N":"6"}`
      does not equal `{"NS":["6", "2", "1"]}`.

    - `NE` : Not equal. `NE` is supported for all data types, including
      lists and maps.

      `AttributeValueList` can contain only one `AttributeValue` of type
      String, Number, Binary, String Set, Number Set, or Binary Set. If an
      item contains an `AttributeValue` of a different type than the one
      provided in the request, the value does not match. For example,
      `{"S":"6"}` does not equal `{"N":"6"}`. Also, `{"N":"6"}` does not
      equal `{"NS":["6", "2", "1"]}`.

    - `LE` : Less than or equal.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, or Binary (not a set type). If an item contains
      an `AttributeValue` element of a different type than the one provided
      in the request, the value does not match. For example, `{"S":"6"}`
      does not equal `{"N":"6"}`. Also, `{"N":"6"}` does not compare to
      `{"NS":["6", "2", "1"]}`.

    - `LT` : Less than.

      `AttributeValueList` can contain only one `AttributeValue` of type
      String, Number, or Binary (not a set type). If an item contains an
      `AttributeValue` element of a different type than the one provided in
      the request, the value does not match. For example, `{"S":"6"}` does
      not equal `{"N":"6"}`. Also, `{"N":"6"}` does not compare to
      `{"NS":["6", "2", "1"]}`.

    - `GE` : Greater than or equal.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, or Binary (not a set type). If an item contains
      an `AttributeValue` element of a different type than the one provided
      in the request, the value does not match. For example, `{"S":"6"}`
      does not equal `{"N":"6"}`. Also, `{"N":"6"}` does not compare to
      `{"NS":["6", "2", "1"]}`.

    - `GT` : Greater than.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, or Binary (not a set type). If an item contains
      an `AttributeValue` element of a different type than the one provided
      in the request, the value does not match. For example, `{"S":"6"}`
      does not equal `{"N":"6"}`. Also, `{"N":"6"}` does not compare to
      `{"NS":["6", "2", "1"]}`.

    - `NOT_NULL` : The attribute exists. `NOT_NULL` is supported for all
      data types, including lists and maps.

      Note:
        This operator tests for the existence of an attribute, not its data
        type. If the data type of attribute \"`a`\" is null, and you evaluate
        it using `NOT_NULL`, the result is a Boolean `true`. This result is
        because the attribute \"`a`\" exists; its data type is not relevant to
        the `NOT_NULL` comparison operator.

    - `NULL` : The attribute does not exist. `NULL` is supported for all
      data types, including lists and maps.

      Note:
        This operator tests for the nonexistence of an attribute, not its data
        type. If the data type of attribute \"`a`\" is null, and you evaluate
        it using `NULL`, the result is a Boolean `false`. This is because the
        attribute \"`a`\" exists; its data type is not relevant to the `NULL`
        comparison operator.

    - `CONTAINS` : Checks for a subsequence, or value in a set.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, or Binary (not a set type). If the target
      attribute of the comparison is of type String, then the operator
      checks for a substring match. If the target attribute of the
      comparison is of type Binary, then the operator looks for a
      subsequence of the target that matches the input. If the target
      attribute of the comparison is a set (\"`SS`\", \"`NS`\", or
      \"`BS`\"), then the operator evaluates to true if it finds an exact
      match with any member of the set.

      CONTAINS is supported for lists: When evaluating \"`a CONTAINS b`\",
      \"`a`\" can be a list; however, \"`b`\" cannot be a set, a map, or a
      list.

    - `NOT_CONTAINS` : Checks for absence of a subsequence, or absence of a
      value in a set.

      `AttributeValueList` can contain only one `AttributeValue` element of
      type String, Number, or Binary (not a set type). If the target
      attribute of the comparison is a String, then the operator checks for
      the absence of a substring match. If the target attribute of the
      comparison is Binary, then the operator checks for the absence of a
      subsequence of the target that matches the input. If the target
      attribute of the comparison is a set (\"`SS`\", \"`NS`\", or
      \"`BS`\"), then the operator evaluates to true if it *does not* find
      an exact match with any member of the set.

      NOT_CONTAINS is supported for lists: When evaluating
      \"`a NOT CONTAINS b`\", \"`a`\" can be a list; however, \"`b`\" cannot
      be a set, a map, or a list.

    - `BEGINS_WITH` : Checks for a prefix.

      `AttributeValueList` can contain only one `AttributeValue` of type
      String or Binary (not a Number or a set type). The target attribute of
      the comparison must be of type String or Binary (not a Number or a set
      type).

    - `IN` : Checks for matching elements in a list.

      `AttributeValueList` can contain one or more `AttributeValue` elements
      of type String, Number, or Binary. These attributes are compared
      against an existing attribute of an item. If any elements of the input
      are equal to the item attribute, the expression evaluates to true.

    - `BETWEEN` : Greater than or equal to the first value, and less than or
      equal to the second value.

      `AttributeValueList` must contain two `AttributeValue` elements of the
      same type, either String, Number, or Binary (not a set type). A target
      attribute matches if the target value is greater than, or equal to,
      the first element and less than, or equal to, the second element. If
      an item contains an `AttributeValue` element of a different type than
      the one provided in the request, the value does not match. For
      example, `{"S":"6"}` does not compare to `{"N":"6"}`. Also,
      `{"N":"6"}` does not compare to `{"NS":["6", "2", "1"]}`
    """

    attribute_value_list: "list[AttributeValue] | None" = None
    """
    One or more values to evaluate against the supplied attribute. The
    number of values in the list depends on the `ComparisonOperator` being
    used.

    For type Number, value comparisons are numeric.

    String value comparisons for greater than, equals, or less than are
    based on ASCII character code values. For example, `a` is greater than
    `A`, and `a` is greater than `B`. For a list of code values, see
    <http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters>.

    For Binary, DynamoDB treats each byte of the binary data as unsigned
    when it compares binary values.

    For information on specifying data types in JSON, see [JSON Data
    Format](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DataFormat.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXPECTED_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.value is not None:
            serializer.write_struct(
                _SCHEMA_EXPECTED_ATTRIBUTE_VALUE.members["Value"], self.value
            )

        if self.exists is not None:
            serializer.write_boolean(
                _SCHEMA_EXPECTED_ATTRIBUTE_VALUE.members["Exists"], self.exists
            )

        if self.comparison_operator is not None:
            serializer.write_string(
                _SCHEMA_EXPECTED_ATTRIBUTE_VALUE.members["ComparisonOperator"],
                self.comparison_operator,
            )

        if self.attribute_value_list is not None:
            _serialize_attribute_value_list(
                serializer,
                _SCHEMA_EXPECTED_ATTRIBUTE_VALUE.members["AttributeValueList"],
                self.attribute_value_list,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["value"] = _AttributeValueDeserializer().deserialize(de)

                case 1:
                    kwargs["exists"] = de.read_boolean(
                        _SCHEMA_EXPECTED_ATTRIBUTE_VALUE.members["Exists"]
                    )

                case 2:
                    kwargs["comparison_operator"] = ComparisonOperator(
                        de.read_string(
                            _SCHEMA_EXPECTED_ATTRIBUTE_VALUE.members[
                                "ComparisonOperator"
                            ]
                        )
                    )

                case 3:
                    kwargs["attribute_value_list"] = _deserialize_attribute_value_list(
                        de,
                        _SCHEMA_EXPECTED_ATTRIBUTE_VALUE.members["AttributeValueList"],
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EXPECTED_ATTRIBUTE_VALUE, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class TransactGetItemsInput:
    """Dataclass for TransactGetItemsInput structure."""

    transact_items: "list[TransactGetItem] | None" = None
    """
    An ordered array of up to 100 `TransactGetItem` objects, each of which
    contains a `Get` structure.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    A value of `TOTAL` causes consumed capacity information to be returned,
    and a value of `NONE` prevents that information from being returned. No
    other value is valid.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSACT_GET_ITEMS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.transact_items is not None:
            _serialize_transact_get_item_list(
                serializer,
                _SCHEMA_TRANSACT_GET_ITEMS_INPUT.members["TransactItems"],
                self.transact_items,
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_TRANSACT_GET_ITEMS_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["transact_items"] = _deserialize_transact_get_item_list(
                        de, _SCHEMA_TRANSACT_GET_ITEMS_INPUT.members["TransactItems"]
                    )

                case 1:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_TRANSACT_GET_ITEMS_INPUT.members[
                                "ReturnConsumedCapacity"
                            ]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TRANSACT_GET_ITEMS_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class TransactWriteItemsOutput:
    """Dataclass for TransactWriteItemsOutput structure."""

    consumed_capacity: list[ConsumedCapacity] | None = None
    """
    The capacity units consumed by the entire `TransactWriteItems`
    operation. The values of the list are ordered according to the ordering
    of the `TransactItems` request parameter.

    If the table has vector indexes, each element also includes a
    `VectorIndexes` field with `VectorWriteRequestBytes` consumed for each
    affected vector index.
    """

    item_collection_metrics: "dict[str, list[ItemCollectionMetrics]] | None" = None
    """
    A list of tables that were processed by `TransactWriteItems` and, for
    each table, information about any item collections that were affected by
    individual `UpdateItem`, `PutItem`, or `DeleteItem` operations.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSACT_WRITE_ITEMS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.consumed_capacity is not None:
            _serialize_consumed_capacity_multiple(
                serializer,
                _SCHEMA_TRANSACT_WRITE_ITEMS_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

        if self.item_collection_metrics is not None:
            _serialize_item_collection_metrics_per_table(
                serializer,
                _SCHEMA_TRANSACT_WRITE_ITEMS_OUTPUT.members["ItemCollectionMetrics"],
                self.item_collection_metrics,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["consumed_capacity"] = (
                        _deserialize_consumed_capacity_multiple(
                            de,
                            _SCHEMA_TRANSACT_WRITE_ITEMS_OUTPUT.members[
                                "ConsumedCapacity"
                            ],
                        )
                    )

                case 1:
                    kwargs["item_collection_metrics"] = (
                        _deserialize_item_collection_metrics_per_table(
                            de,
                            _SCHEMA_TRANSACT_WRITE_ITEMS_OUTPUT.members[
                                "ItemCollectionMetrics"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_TRANSACT_WRITE_ITEMS_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ConditionCheck:
    """
    Represents a request to perform a check that an item exists or to check
    the condition of specific attributes of the item.
    """

    key: "dict[str, AttributeValue]"
    """
    The primary key of the item to be checked. Each element consists of an
    attribute name and a value for that attribute.
    """

    table_name: str
    """
    Name of the table for the check item request. You can also provide the
    Amazon Resource Name (ARN) of the table in this parameter.
    """

    condition_expression: str
    """
    A condition that must be satisfied in order for a conditional update to
    succeed. For more information, see [Condition
    expressions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.ConditionExpressions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_names: dict[str, str] | None = None
    """
    One or more substitution tokens for attribute names in an expression.
    For more information, see [Expression attribute
    names](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.ExpressionAttributeNames.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_values: "dict[str, AttributeValue] | None" = None
    """
    One or more values that can be substituted in an expression. For more
    information, see [Condition
    expressions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.ConditionExpressions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    return_values_on_condition_check_failure: (
        ReturnValuesOnConditionCheckFailure | None
    ) = None
    """
    Use `ReturnValuesOnConditionCheckFailure` to get the item attributes if
    the `ConditionCheck` condition fails. For
    `ReturnValuesOnConditionCheckFailure`, the valid values are: NONE and
    ALL_OLD.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CONDITION_CHECK, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_key(serializer, _SCHEMA_CONDITION_CHECK.members["Key"], self.key)
        serializer.write_string(
            _SCHEMA_CONDITION_CHECK.members["TableName"], self.table_name
        )
        serializer.write_string(
            _SCHEMA_CONDITION_CHECK.members["ConditionExpression"],
            self.condition_expression,
        )
        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_CONDITION_CHECK.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

        if self.expression_attribute_values is not None:
            _serialize_expression_attribute_value_map(
                serializer,
                _SCHEMA_CONDITION_CHECK.members["ExpressionAttributeValues"],
                self.expression_attribute_values,
            )

        if self.return_values_on_condition_check_failure is not None:
            serializer.write_string(
                _SCHEMA_CONDITION_CHECK.members["ReturnValuesOnConditionCheckFailure"],
                self.return_values_on_condition_check_failure,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["key"] = _deserialize_key(
                        de, _SCHEMA_CONDITION_CHECK.members["Key"]
                    )

                case 1:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_CONDITION_CHECK.members["TableName"]
                    )

                case 2:
                    kwargs["condition_expression"] = de.read_string(
                        _SCHEMA_CONDITION_CHECK.members["ConditionExpression"]
                    )

                case 3:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de,
                            _SCHEMA_CONDITION_CHECK.members["ExpressionAttributeNames"],
                        )
                    )

                case 4:
                    kwargs["expression_attribute_values"] = (
                        _deserialize_expression_attribute_value_map(
                            de,
                            _SCHEMA_CONDITION_CHECK.members[
                                "ExpressionAttributeValues"
                            ],
                        )
                    )

                case 5:
                    kwargs["return_values_on_condition_check_failure"] = (
                        ReturnValuesOnConditionCheckFailure(
                            de.read_string(
                                _SCHEMA_CONDITION_CHECK.members[
                                    "ReturnValuesOnConditionCheckFailure"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CONDITION_CHECK, consumer=_consumer)
        if "key" not in kwargs:
            kwargs["key"] = {}
        if "table_name" not in kwargs:
            kwargs["table_name"] = ""
        if "condition_expression" not in kwargs:
            kwargs["condition_expression"] = ""
        return kwargs


@dataclass(kw_only=True)
class Delete:
    """Represents a request to perform a `DeleteItem` operation."""

    key: "dict[str, AttributeValue]"
    """
    The primary key of the item to be deleted. Each element consists of an
    attribute name and a value for that attribute.
    """

    table_name: str
    """
    Name of the table in which the item to be deleted resides. You can also
    provide the Amazon Resource Name (ARN) of the table in this parameter.
    """

    condition_expression: str | None = None
    """
    A condition that must be satisfied in order for a conditional delete to
    succeed.
    """

    expression_attribute_names: dict[str, str] | None = None
    """One or more substitution tokens for attribute names in an expression."""

    expression_attribute_values: "dict[str, AttributeValue] | None" = None
    """One or more values that can be substituted in an expression."""

    return_values_on_condition_check_failure: (
        ReturnValuesOnConditionCheckFailure | None
    ) = None
    """
    Use `ReturnValuesOnConditionCheckFailure` to get the item attributes if
    the `Delete` condition fails. For `ReturnValuesOnConditionCheckFailure`,
    the valid values are: NONE and ALL_OLD.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_key(serializer, _SCHEMA_DELETE.members["Key"], self.key)
        serializer.write_string(_SCHEMA_DELETE.members["TableName"], self.table_name)
        if self.condition_expression is not None:
            serializer.write_string(
                _SCHEMA_DELETE.members["ConditionExpression"], self.condition_expression
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_DELETE.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

        if self.expression_attribute_values is not None:
            _serialize_expression_attribute_value_map(
                serializer,
                _SCHEMA_DELETE.members["ExpressionAttributeValues"],
                self.expression_attribute_values,
            )

        if self.return_values_on_condition_check_failure is not None:
            serializer.write_string(
                _SCHEMA_DELETE.members["ReturnValuesOnConditionCheckFailure"],
                self.return_values_on_condition_check_failure,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["key"] = _deserialize_key(de, _SCHEMA_DELETE.members["Key"])

                case 1:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DELETE.members["TableName"]
                    )

                case 2:
                    kwargs["condition_expression"] = de.read_string(
                        _SCHEMA_DELETE.members["ConditionExpression"]
                    )

                case 3:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de, _SCHEMA_DELETE.members["ExpressionAttributeNames"]
                        )
                    )

                case 4:
                    kwargs["expression_attribute_values"] = (
                        _deserialize_expression_attribute_value_map(
                            de, _SCHEMA_DELETE.members["ExpressionAttributeValues"]
                        )
                    )

                case 5:
                    kwargs["return_values_on_condition_check_failure"] = (
                        ReturnValuesOnConditionCheckFailure(
                            de.read_string(
                                _SCHEMA_DELETE.members[
                                    "ReturnValuesOnConditionCheckFailure"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE, consumer=_consumer)
        if "key" not in kwargs:
            kwargs["key"] = {}
        if "table_name" not in kwargs:
            kwargs["table_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class Put:
    """Represents a request to perform a `PutItem` operation."""

    item: "dict[str, AttributeValue]"
    """
    A map of attribute name to attribute values, representing the primary
    key of the item to be written by `PutItem`. All of the table's primary
    key attributes must be specified, and their data types must match those
    of the table's key schema. If any attributes are present in the item
    that are part of an index key schema for the table, their types must
    match the index key schema.
    """

    table_name: str
    """
    Name of the table in which to write the item. You can also provide the
    Amazon Resource Name (ARN) of the table in this parameter.
    """

    condition_expression: str | None = None
    """
    A condition that must be satisfied in order for a conditional update to
    succeed.
    """

    expression_attribute_names: dict[str, str] | None = None
    """One or more substitution tokens for attribute names in an expression."""

    expression_attribute_values: "dict[str, AttributeValue] | None" = None
    """One or more values that can be substituted in an expression."""

    return_values_on_condition_check_failure: (
        ReturnValuesOnConditionCheckFailure | None
    ) = None
    """
    Use `ReturnValuesOnConditionCheckFailure` to get the item attributes if
    the `Put` condition fails. For `ReturnValuesOnConditionCheckFailure`,
    the valid values are: NONE and ALL_OLD.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_put_item_input_attribute_map(
            serializer, _SCHEMA_PUT.members["Item"], self.item
        )
        serializer.write_string(_SCHEMA_PUT.members["TableName"], self.table_name)
        if self.condition_expression is not None:
            serializer.write_string(
                _SCHEMA_PUT.members["ConditionExpression"], self.condition_expression
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_PUT.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

        if self.expression_attribute_values is not None:
            _serialize_expression_attribute_value_map(
                serializer,
                _SCHEMA_PUT.members["ExpressionAttributeValues"],
                self.expression_attribute_values,
            )

        if self.return_values_on_condition_check_failure is not None:
            serializer.write_string(
                _SCHEMA_PUT.members["ReturnValuesOnConditionCheckFailure"],
                self.return_values_on_condition_check_failure,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["item"] = _deserialize_put_item_input_attribute_map(
                        de, _SCHEMA_PUT.members["Item"]
                    )

                case 1:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_PUT.members["TableName"]
                    )

                case 2:
                    kwargs["condition_expression"] = de.read_string(
                        _SCHEMA_PUT.members["ConditionExpression"]
                    )

                case 3:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de, _SCHEMA_PUT.members["ExpressionAttributeNames"]
                        )
                    )

                case 4:
                    kwargs["expression_attribute_values"] = (
                        _deserialize_expression_attribute_value_map(
                            de, _SCHEMA_PUT.members["ExpressionAttributeValues"]
                        )
                    )

                case 5:
                    kwargs["return_values_on_condition_check_failure"] = (
                        ReturnValuesOnConditionCheckFailure(
                            de.read_string(
                                _SCHEMA_PUT.members[
                                    "ReturnValuesOnConditionCheckFailure"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PUT, consumer=_consumer)
        if "item" not in kwargs:
            kwargs["item"] = {}
        if "table_name" not in kwargs:
            kwargs["table_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class SearchVectorsInput:
    """Dataclass for SearchVectorsInput structure."""

    table_name: str | None = None
    """
    The name or Amazon Resource Name (ARN) of the table containing the
    vector index.
    """

    index_name: str | None = None
    """
    The name of the vector index to search. The index must be in the
    `ACTIVE` state.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    expression_attribute_names: dict[str, str] | None = None
    """
    One or more substitution tokens for attribute names in an expression.
    Use the `#` character in an expression to dereference an attribute name.
    """

    expression_attribute_values: "dict[str, AttributeValue] | None" = None
    """
    One or more values that can be substituted in an expression. Use the `:`
    character in an expression to dereference an attribute value.
    """

    projection_expression: str | None = None
    """
    A string that identifies one or more attributes to retrieve from the
    index. Separate attribute names with commas. If not specified, the
    operation returns all attributes projected into the vector index.

    Only attributes projected into the vector index can be retrieved.
    """

    search_vector: "list[AttributeValue] | None" = None
    """
    The search vector to compare against the indexed vectors. Each element
    is a 32-bit IEEE-754 floating point number, provided in DynamoDB list
    format.

    The number of dimensions must match the number of dimensions configured
    for the vector index.
    """

    search_condition_expression: str | None = None
    """
    A condition expression used to filter the vector search results. The
    expression can reference attributes defined in the vector index search
    schema, including `HASH` and `INLINE_FILTER` key elements.

    Only the equality operator (`=`) is supported for `HASH` attributes.
    Comparison and range operators are supported for `INLINE_FILTER`
    attributes. Only top-level attributes from the search schema can be
    referenced.
    """

    top_k: int | None = None
    """The number of most similar results to return."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SEARCH_VECTORS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_SEARCH_VECTORS_INPUT.members["TableName"], self.table_name
            )

        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_SEARCH_VECTORS_INPUT.members["IndexName"], self.index_name
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_SEARCH_VECTORS_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_SEARCH_VECTORS_INPUT.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

        if self.expression_attribute_values is not None:
            _serialize_expression_attribute_value_map(
                serializer,
                _SCHEMA_SEARCH_VECTORS_INPUT.members["ExpressionAttributeValues"],
                self.expression_attribute_values,
            )

        if self.projection_expression is not None:
            serializer.write_string(
                _SCHEMA_SEARCH_VECTORS_INPUT.members["ProjectionExpression"],
                self.projection_expression,
            )

        if self.search_vector is not None:
            _serialize_search_vector_list(
                serializer,
                _SCHEMA_SEARCH_VECTORS_INPUT.members["SearchVector"],
                self.search_vector,
            )

        if self.search_condition_expression is not None:
            serializer.write_string(
                _SCHEMA_SEARCH_VECTORS_INPUT.members["SearchConditionExpression"],
                self.search_condition_expression,
            )

        if self.top_k is not None:
            serializer.write_integer(
                _SCHEMA_SEARCH_VECTORS_INPUT.members["TopK"], self.top_k
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_SEARCH_VECTORS_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_SEARCH_VECTORS_INPUT.members["IndexName"]
                    )

                case 2:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_SEARCH_VECTORS_INPUT.members[
                                "ReturnConsumedCapacity"
                            ]
                        )
                    )

                case 3:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de,
                            _SCHEMA_SEARCH_VECTORS_INPUT.members[
                                "ExpressionAttributeNames"
                            ],
                        )
                    )

                case 4:
                    kwargs["expression_attribute_values"] = (
                        _deserialize_expression_attribute_value_map(
                            de,
                            _SCHEMA_SEARCH_VECTORS_INPUT.members[
                                "ExpressionAttributeValues"
                            ],
                        )
                    )

                case 5:
                    kwargs["projection_expression"] = de.read_string(
                        _SCHEMA_SEARCH_VECTORS_INPUT.members["ProjectionExpression"]
                    )

                case 6:
                    kwargs["search_vector"] = _deserialize_search_vector_list(
                        de, _SCHEMA_SEARCH_VECTORS_INPUT.members["SearchVector"]
                    )

                case 7:
                    kwargs["search_condition_expression"] = de.read_string(
                        _SCHEMA_SEARCH_VECTORS_INPUT.members[
                            "SearchConditionExpression"
                        ]
                    )

                case 8:
                    kwargs["top_k"] = de.read_integer(
                        _SCHEMA_SEARCH_VECTORS_INPUT.members["TopK"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SEARCH_VECTORS_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class Update:
    """Represents a request to perform an `UpdateItem` operation."""

    key: "dict[str, AttributeValue]"
    """
    The primary key of the item to be updated. Each element consists of an
    attribute name and a value for that attribute.
    """

    update_expression: str
    """
    An expression that defines one or more attributes to be updated, the
    action to be performed on them, and new value(s) for them.
    """

    table_name: str
    """
    Name of the table for the `UpdateItem` request. You can also provide the
    Amazon Resource Name (ARN) of the table in this parameter.
    """

    condition_expression: str | None = None
    """
    A condition that must be satisfied in order for a conditional update to
    succeed.
    """

    expression_attribute_names: dict[str, str] | None = None
    """One or more substitution tokens for attribute names in an expression."""

    expression_attribute_values: "dict[str, AttributeValue] | None" = None
    """One or more values that can be substituted in an expression."""

    return_values_on_condition_check_failure: (
        ReturnValuesOnConditionCheckFailure | None
    ) = None
    """
    Use `ReturnValuesOnConditionCheckFailure` to get the item attributes if
    the `Update` condition fails. For `ReturnValuesOnConditionCheckFailure`,
    the valid values are: NONE and ALL_OLD.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_key(serializer, _SCHEMA_UPDATE.members["Key"], self.key)
        serializer.write_string(
            _SCHEMA_UPDATE.members["UpdateExpression"], self.update_expression
        )
        serializer.write_string(_SCHEMA_UPDATE.members["TableName"], self.table_name)
        if self.condition_expression is not None:
            serializer.write_string(
                _SCHEMA_UPDATE.members["ConditionExpression"], self.condition_expression
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_UPDATE.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

        if self.expression_attribute_values is not None:
            _serialize_expression_attribute_value_map(
                serializer,
                _SCHEMA_UPDATE.members["ExpressionAttributeValues"],
                self.expression_attribute_values,
            )

        if self.return_values_on_condition_check_failure is not None:
            serializer.write_string(
                _SCHEMA_UPDATE.members["ReturnValuesOnConditionCheckFailure"],
                self.return_values_on_condition_check_failure,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["key"] = _deserialize_key(de, _SCHEMA_UPDATE.members["Key"])

                case 1:
                    kwargs["update_expression"] = de.read_string(
                        _SCHEMA_UPDATE.members["UpdateExpression"]
                    )

                case 2:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_UPDATE.members["TableName"]
                    )

                case 3:
                    kwargs["condition_expression"] = de.read_string(
                        _SCHEMA_UPDATE.members["ConditionExpression"]
                    )

                case 4:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de, _SCHEMA_UPDATE.members["ExpressionAttributeNames"]
                        )
                    )

                case 5:
                    kwargs["expression_attribute_values"] = (
                        _deserialize_expression_attribute_value_map(
                            de, _SCHEMA_UPDATE.members["ExpressionAttributeValues"]
                        )
                    )

                case 6:
                    kwargs["return_values_on_condition_check_failure"] = (
                        ReturnValuesOnConditionCheckFailure(
                            de.read_string(
                                _SCHEMA_UPDATE.members[
                                    "ReturnValuesOnConditionCheckFailure"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UPDATE, consumer=_consumer)
        if "key" not in kwargs:
            kwargs["key"] = {}
        if "update_expression" not in kwargs:
            kwargs["update_expression"] = ""
        if "table_name" not in kwargs:
            kwargs["table_name"] = ""
        return kwargs


@dataclass(kw_only=True)
class BatchStatementResponse:
    """A PartiQL batch statement response.."""

    error: "BatchStatementError | None" = None
    """The error associated with a failed PartiQL batch statement."""

    table_name: str | None = None
    """The table name associated with a failed PartiQL batch statement."""

    item: "dict[str, AttributeValue] | None" = None
    """A DynamoDB item associated with a BatchStatementResponse"""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_STATEMENT_RESPONSE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.error is not None:
            serializer.write_struct(
                _SCHEMA_BATCH_STATEMENT_RESPONSE.members["Error"], self.error
            )

        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_BATCH_STATEMENT_RESPONSE.members["TableName"], self.table_name
            )

        if self.item is not None:
            _serialize_attribute_map(
                serializer, _SCHEMA_BATCH_STATEMENT_RESPONSE.members["Item"], self.item
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["error"] = BatchStatementError.deserialize(de)

                case 1:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_BATCH_STATEMENT_RESPONSE.members["TableName"]
                    )

                case 2:
                    kwargs["item"] = _deserialize_attribute_map(
                        de, _SCHEMA_BATCH_STATEMENT_RESPONSE.members["Item"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BATCH_STATEMENT_RESPONSE, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DeleteItemOutput:
    """Represents the output of a `DeleteItem` operation."""

    attributes: "dict[str, AttributeValue] | None" = None
    """
    A map of attribute names to `AttributeValue` objects, representing the
    item as it appeared before the `DeleteItem` operation. This map appears
    in the response only if `ReturnValues` was specified as `ALL_OLD` in the
    request.
    """

    consumed_capacity: ConsumedCapacity | None = None
    """
    The capacity units consumed by the `DeleteItem` operation. The data
    returned includes the total provisioned throughput consumed, along with
    statistics for the table and any indexes involved in the operation.
    `ConsumedCapacity` is only returned if the `ReturnConsumedCapacity`
    parameter was specified. For more information, see [Provisioned capacity
    mode](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/provisioned-capacity-mode.html)
    in the *Amazon DynamoDB Developer Guide*.

    If the table has vector indexes, the response includes a `VectorIndexes`
    field with `VectorWriteRequestBytes` consumed for each affected vector
    index.
    """

    item_collection_metrics: "ItemCollectionMetrics | None" = None
    """
    Information about item collections, if any, that were affected by the
    `DeleteItem` operation. `ItemCollectionMetrics` is only returned if the
    `ReturnItemCollectionMetrics` parameter was specified. If the table does
    not have any local secondary indexes, this information is not returned
    in the response.

    Each `ItemCollectionMetrics` element consists of:

    - `ItemCollectionKey` - The partition key value of the item collection.
      This is the same as the partition key value of the item itself.

    - `SizeEstimateRangeGB` - An estimate of item collection size, in
      gigabytes. This value is a two-element array containing a lower bound
      and an upper bound for the estimate. The estimate includes the size of
      all the items in the table, plus the size of all attributes projected
      into all of the local secondary indexes on that table. Use this
      estimate to measure whether a local secondary index is approaching its
      size limit.

      The estimate is subject to change over time; therefore, do not rely on
      the precision or accuracy of the estimate.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_ITEM_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.attributes is not None:
            _serialize_attribute_map(
                serializer,
                _SCHEMA_DELETE_ITEM_OUTPUT.members["Attributes"],
                self.attributes,
            )

        if self.consumed_capacity is not None:
            serializer.write_struct(
                _SCHEMA_DELETE_ITEM_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

        if self.item_collection_metrics is not None:
            serializer.write_struct(
                _SCHEMA_DELETE_ITEM_OUTPUT.members["ItemCollectionMetrics"],
                self.item_collection_metrics,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attributes"] = _deserialize_attribute_map(
                        de, _SCHEMA_DELETE_ITEM_OUTPUT.members["Attributes"]
                    )

                case 1:
                    kwargs["consumed_capacity"] = ConsumedCapacity.deserialize(de)

                case 2:
                    kwargs["item_collection_metrics"] = (
                        ItemCollectionMetrics.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_ITEM_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ExecuteStatementOutput:
    """Dataclass for ExecuteStatementOutput structure."""

    items: "list[dict[str, AttributeValue]] | None" = None
    """
    If a read operation was used, this property will contain the result of
    the read operation; a map of attribute names and their values. For the
    write operations this value will be empty.
    """

    next_token: str | None = None
    """
    If the response of a read request exceeds the response payload limit
    DynamoDB will set this value in the response. If set, you can use that
    this value in the subsequent request to get the remaining results.
    """

    consumed_capacity: ConsumedCapacity | None = None
    """
    The capacity units consumed by an operation. The data returned includes
    the total provisioned throughput consumed, along with statistics for the
    table and any indexes involved in the operation. `ConsumedCapacity` is
    only returned if the request asked for it. For more information, see
    [Provisioned capacity
    mode](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/provisioned-capacity-mode.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    last_evaluated_key: "dict[str, AttributeValue] | None" = None
    """
    The primary key of the item where the operation stopped, inclusive of
    the previous result set. Use this value to start a new operation,
    excluding this value in the new request. If `LastEvaluatedKey` is empty,
    then the \"last page\" of results has been processed and there is no
    more data to be retrieved. If `LastEvaluatedKey` is not empty, it does
    not necessarily mean that there is more data in the result set. The only
    way to know when you have reached the end of the result set is when
    `LastEvaluatedKey` is empty.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EXECUTE_STATEMENT_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.items is not None:
            _serialize_item_list(
                serializer,
                _SCHEMA_EXECUTE_STATEMENT_OUTPUT.members["Items"],
                self.items,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_EXECUTE_STATEMENT_OUTPUT.members["NextToken"], self.next_token
            )

        if self.consumed_capacity is not None:
            serializer.write_struct(
                _SCHEMA_EXECUTE_STATEMENT_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

        if self.last_evaluated_key is not None:
            _serialize_key(
                serializer,
                _SCHEMA_EXECUTE_STATEMENT_OUTPUT.members["LastEvaluatedKey"],
                self.last_evaluated_key,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["items"] = _deserialize_item_list(
                        de, _SCHEMA_EXECUTE_STATEMENT_OUTPUT.members["Items"]
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_EXECUTE_STATEMENT_OUTPUT.members["NextToken"]
                    )

                case 2:
                    kwargs["consumed_capacity"] = ConsumedCapacity.deserialize(de)

                case 3:
                    kwargs["last_evaluated_key"] = _deserialize_key(
                        de, _SCHEMA_EXECUTE_STATEMENT_OUTPUT.members["LastEvaluatedKey"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EXECUTE_STATEMENT_OUTPUT, consumer=_consumer)
        return kwargs


def _serialize_expected_attribute_map(
    serializer: ShapeSerializer,
    schema: Schema,
    value: dict[str, ExpectedAttributeValue],
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_expected_attribute_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, ExpectedAttributeValue]:
    result: dict[str, ExpectedAttributeValue] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = ExpectedAttributeValue.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


GET_ITEM = APIOperation(
    input=GetItemInput,
    output=GetItemOutput,
    schema=_SCHEMA_GET_ITEM,
    input_schema=_SCHEMA_GET_ITEM_INPUT,
    output_schema=_SCHEMA_GET_ITEM_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class PutItemOutput:
    """Represents the output of a `PutItem` operation."""

    attributes: "dict[str, AttributeValue] | None" = None
    """
    The attribute values as they appeared before the `PutItem` operation,
    but only if `ReturnValues` is specified as `ALL_OLD` in the request.
    Each element consists of an attribute name and an attribute value.
    """

    consumed_capacity: ConsumedCapacity | None = None
    """
    The capacity units consumed by the `PutItem` operation. The data
    returned includes the total provisioned throughput consumed, along with
    statistics for the table and any indexes involved in the operation.
    `ConsumedCapacity` is only returned if the `ReturnConsumedCapacity`
    parameter was specified. For more information, see [Capacity unity
    consumption for write
    operations](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/read-write-operations.html#write-operation-consumption)
    in the *Amazon DynamoDB Developer Guide*.

    If the table has vector indexes, the response includes a `VectorIndexes`
    field with `VectorWriteRequestBytes` consumed for each affected vector
    index.
    """

    item_collection_metrics: "ItemCollectionMetrics | None" = None
    """
    Information about item collections, if any, that were affected by the
    `PutItem` operation. `ItemCollectionMetrics` is only returned if the
    `ReturnItemCollectionMetrics` parameter was specified. If the table does
    not have any local secondary indexes, this information is not returned
    in the response.

    Each `ItemCollectionMetrics` element consists of:

    - `ItemCollectionKey` - The partition key value of the item collection.
      This is the same as the partition key value of the item itself.

    - `SizeEstimateRangeGB` - An estimate of item collection size, in
      gigabytes. This value is a two-element array containing a lower bound
      and an upper bound for the estimate. The estimate includes the size of
      all the items in the table, plus the size of all attributes projected
      into all of the local secondary indexes on that table. Use this
      estimate to measure whether a local secondary index is approaching its
      size limit.

      The estimate is subject to change over time; therefore, do not rely on
      the precision or accuracy of the estimate.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PUT_ITEM_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.attributes is not None:
            _serialize_attribute_map(
                serializer,
                _SCHEMA_PUT_ITEM_OUTPUT.members["Attributes"],
                self.attributes,
            )

        if self.consumed_capacity is not None:
            serializer.write_struct(
                _SCHEMA_PUT_ITEM_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

        if self.item_collection_metrics is not None:
            serializer.write_struct(
                _SCHEMA_PUT_ITEM_OUTPUT.members["ItemCollectionMetrics"],
                self.item_collection_metrics,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attributes"] = _deserialize_attribute_map(
                        de, _SCHEMA_PUT_ITEM_OUTPUT.members["Attributes"]
                    )

                case 1:
                    kwargs["consumed_capacity"] = ConsumedCapacity.deserialize(de)

                case 2:
                    kwargs["item_collection_metrics"] = (
                        ItemCollectionMetrics.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PUT_ITEM_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class QueryOutput:
    """Represents the output of a `Query` operation."""

    items: "list[dict[str, AttributeValue]] | None" = None
    """
    An array of item attributes that match the query criteria. Each element
    in this array consists of an attribute name and the value for that
    attribute.
    """

    count: int = 0
    """
    The number of items in the response.

    If you used a `QueryFilter` in the request, then `Count` is the number
    of items returned after the filter was applied, and `ScannedCount` is
    the number of matching items before the filter was applied.

    If you did not use a filter in the request, then `Count` and
    `ScannedCount` are the same.
    """

    scanned_count: int = 0
    """
    The number of items evaluated, before any `QueryFilter` is applied. A
    high `ScannedCount` value with few, or no, `Count` results indicates an
    inefficient `Query` operation. For more information, see [Count and
    ScannedCount](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Scan.html#Scan.Count)
    in the *Amazon DynamoDB Developer Guide*.

    If you did not use a filter in the request, then `ScannedCount` is the
    same as `Count`.
    """

    last_evaluated_key: "dict[str, AttributeValue] | None" = None
    """
    The primary key of the item where the operation stopped, inclusive of
    the previous result set. Use this value to start a new operation,
    excluding this value in the new request.

    If `LastEvaluatedKey` is empty, then the \"last page\" of results has
    been processed and there is no more data to be retrieved.

    If `LastEvaluatedKey` is not empty, it does not necessarily mean that
    there is more data in the result set. The only way to know when you have
    reached the end of the result set is when `LastEvaluatedKey` is empty.
    """

    consumed_capacity: ConsumedCapacity | None = None
    """
    The capacity units consumed by the `Query` operation. The data returned
    includes the total provisioned throughput consumed, along with
    statistics for the table and any indexes involved in the operation.
    `ConsumedCapacity` is only returned if the `ReturnConsumedCapacity`
    parameter was specified. For more information, see [Capacity unit
    consumption for read
    operations](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/read-write-operations.html#read-operation-consumption)
    in the *Amazon DynamoDB Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_QUERY_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.items is not None:
            _serialize_item_list(
                serializer, _SCHEMA_QUERY_OUTPUT.members["Items"], self.items
            )

        serializer.write_integer(_SCHEMA_QUERY_OUTPUT.members["Count"], self.count)
        serializer.write_integer(
            _SCHEMA_QUERY_OUTPUT.members["ScannedCount"], self.scanned_count
        )
        if self.last_evaluated_key is not None:
            _serialize_key(
                serializer,
                _SCHEMA_QUERY_OUTPUT.members["LastEvaluatedKey"],
                self.last_evaluated_key,
            )

        if self.consumed_capacity is not None:
            serializer.write_struct(
                _SCHEMA_QUERY_OUTPUT.members["ConsumedCapacity"], self.consumed_capacity
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["items"] = _deserialize_item_list(
                        de, _SCHEMA_QUERY_OUTPUT.members["Items"]
                    )

                case 1:
                    kwargs["count"] = de.read_integer(
                        _SCHEMA_QUERY_OUTPUT.members["Count"]
                    )

                case 2:
                    kwargs["scanned_count"] = de.read_integer(
                        _SCHEMA_QUERY_OUTPUT.members["ScannedCount"]
                    )

                case 3:
                    kwargs["last_evaluated_key"] = _deserialize_key(
                        de, _SCHEMA_QUERY_OUTPUT.members["LastEvaluatedKey"]
                    )

                case 4:
                    kwargs["consumed_capacity"] = ConsumedCapacity.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_QUERY_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ScanOutput:
    """Represents the output of a `Scan` operation."""

    items: "list[dict[str, AttributeValue]] | None" = None
    """
    An array of item attributes that match the scan criteria. Each element
    in this array consists of an attribute name and the value for that
    attribute.
    """

    count: int = 0
    """
    The number of items in the response.

    If you set `ScanFilter` in the request, then `Count` is the number of
    items returned after the filter was applied, and `ScannedCount` is the
    number of matching items before the filter was applied.

    If you did not use a filter in the request, then `Count` is the same as
    `ScannedCount`.
    """

    scanned_count: int = 0
    """
    The number of items evaluated, before any `ScanFilter` is applied. A
    high `ScannedCount` value with few, or no, `Count` results indicates an
    inefficient `Scan` operation. For more information, see [Count and
    ScannedCount](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/QueryAndScan.html#Count)
    in the *Amazon DynamoDB Developer Guide*.

    If you did not use a filter in the request, then `ScannedCount` is the
    same as `Count`.
    """

    last_evaluated_key: "dict[str, AttributeValue] | None" = None
    """
    The primary key of the item where the operation stopped, inclusive of
    the previous result set. Use this value to start a new operation,
    excluding this value in the new request.

    If `LastEvaluatedKey` is empty, then the \"last page\" of results has
    been processed and there is no more data to be retrieved.

    If `LastEvaluatedKey` is not empty, it does not necessarily mean that
    there is more data in the result set. The only way to know when you have
    reached the end of the result set is when `LastEvaluatedKey` is empty.
    """

    consumed_capacity: ConsumedCapacity | None = None
    """
    The capacity units consumed by the `Scan` operation. The data returned
    includes the total provisioned throughput consumed, along with
    statistics for the table and any indexes involved in the operation.
    `ConsumedCapacity` is only returned if the `ReturnConsumedCapacity`
    parameter was specified. For more information, see [Capacity unit
    consumption for read
    operations](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/read-write-operations.html#read-operation-consumption)
    in the *Amazon DynamoDB Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SCAN_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.items is not None:
            _serialize_item_list(
                serializer, _SCHEMA_SCAN_OUTPUT.members["Items"], self.items
            )

        serializer.write_integer(_SCHEMA_SCAN_OUTPUT.members["Count"], self.count)
        serializer.write_integer(
            _SCHEMA_SCAN_OUTPUT.members["ScannedCount"], self.scanned_count
        )
        if self.last_evaluated_key is not None:
            _serialize_key(
                serializer,
                _SCHEMA_SCAN_OUTPUT.members["LastEvaluatedKey"],
                self.last_evaluated_key,
            )

        if self.consumed_capacity is not None:
            serializer.write_struct(
                _SCHEMA_SCAN_OUTPUT.members["ConsumedCapacity"], self.consumed_capacity
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["items"] = _deserialize_item_list(
                        de, _SCHEMA_SCAN_OUTPUT.members["Items"]
                    )

                case 1:
                    kwargs["count"] = de.read_integer(
                        _SCHEMA_SCAN_OUTPUT.members["Count"]
                    )

                case 2:
                    kwargs["scanned_count"] = de.read_integer(
                        _SCHEMA_SCAN_OUTPUT.members["ScannedCount"]
                    )

                case 3:
                    kwargs["last_evaluated_key"] = _deserialize_key(
                        de, _SCHEMA_SCAN_OUTPUT.members["LastEvaluatedKey"]
                    )

                case 4:
                    kwargs["consumed_capacity"] = ConsumedCapacity.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SCAN_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class UpdateItemOutput:
    """Represents the output of an `UpdateItem` operation."""

    attributes: "dict[str, AttributeValue] | None" = None
    """
    A map of attribute values as they appear before or after the
    `UpdateItem` operation, as determined by the `ReturnValues` parameter.

    The `Attributes` map is only present if the update was successful and
    `ReturnValues` was specified as something other than `NONE` in the
    request. Each element represents one attribute.
    """

    consumed_capacity: ConsumedCapacity | None = None
    """
    The capacity units consumed by the `UpdateItem` operation. The data
    returned includes the total provisioned throughput consumed, along with
    statistics for the table and any indexes involved in the operation.
    `ConsumedCapacity` is only returned if the `ReturnConsumedCapacity`
    parameter was specified. For more information, see [Capacity unity
    consumption for write
    operations](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/read-write-operations.html#write-operation-consumption)
    in the *Amazon DynamoDB Developer Guide*.

    If the table has vector indexes, the response includes a `VectorIndexes`
    field with `VectorWriteRequestBytes` consumed for each affected vector
    index.
    """

    item_collection_metrics: "ItemCollectionMetrics | None" = None
    """
    Information about item collections, if any, that were affected by the
    `UpdateItem` operation. `ItemCollectionMetrics` is only returned if the
    `ReturnItemCollectionMetrics` parameter was specified. If the table does
    not have any local secondary indexes, this information is not returned
    in the response.

    Each `ItemCollectionMetrics` element consists of:

    - `ItemCollectionKey` - The partition key value of the item collection.
      This is the same as the partition key value of the item itself.

    - `SizeEstimateRangeGB` - An estimate of item collection size, in
      gigabytes. This value is a two-element array containing a lower bound
      and an upper bound for the estimate. The estimate includes the size of
      all the items in the table, plus the size of all attributes projected
      into all of the local secondary indexes on that table. Use this
      estimate to measure whether a local secondary index is approaching its
      size limit.

      The estimate is subject to change over time; therefore, do not rely on
      the precision or accuracy of the estimate.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_ITEM_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.attributes is not None:
            _serialize_attribute_map(
                serializer,
                _SCHEMA_UPDATE_ITEM_OUTPUT.members["Attributes"],
                self.attributes,
            )

        if self.consumed_capacity is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_ITEM_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

        if self.item_collection_metrics is not None:
            serializer.write_struct(
                _SCHEMA_UPDATE_ITEM_OUTPUT.members["ItemCollectionMetrics"],
                self.item_collection_metrics,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attributes"] = _deserialize_attribute_map(
                        de, _SCHEMA_UPDATE_ITEM_OUTPUT.members["Attributes"]
                    )

                case 1:
                    kwargs["consumed_capacity"] = ConsumedCapacity.deserialize(de)

                case 2:
                    kwargs["item_collection_metrics"] = (
                        ItemCollectionMetrics.deserialize(de)
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UPDATE_ITEM_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class WriteRequest:
    """
    Represents an operation to perform - either `DeleteItem` or `PutItem`.
    You can only request one of these operations, not both, in a single
    `WriteRequest`. If you do need to perform both of these operations, you
    need to provide two separate `WriteRequest` objects.
    """

    put_request: "PutRequest | None" = None
    """A request to perform a `PutItem` operation."""

    delete_request: "DeleteRequest | None" = None
    """A request to perform a `DeleteItem` operation."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_WRITE_REQUEST, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.put_request is not None:
            serializer.write_struct(
                _SCHEMA_WRITE_REQUEST.members["PutRequest"], self.put_request
            )

        if self.delete_request is not None:
            serializer.write_struct(
                _SCHEMA_WRITE_REQUEST.members["DeleteRequest"], self.delete_request
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["put_request"] = PutRequest.deserialize(de)

                case 1:
                    kwargs["delete_request"] = DeleteRequest.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_WRITE_REQUEST, consumer=_consumer)
        return kwargs


def _serialize_parti_ql_batch_response(
    serializer: ShapeSerializer, schema: Schema, value: list[BatchStatementResponse]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_parti_ql_batch_response(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[BatchStatementResponse]:
    result: list[BatchStatementResponse] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(BatchStatementResponse.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_write_requests(
    serializer: ShapeSerializer, schema: Schema, value: list[WriteRequest]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_write_requests(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[WriteRequest]:
    result: list[WriteRequest] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(WriteRequest.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class BatchExecuteStatementOutput:
    """Dataclass for BatchExecuteStatementOutput structure."""

    responses: "list[BatchStatementResponse] | None" = None
    """
    The response to each PartiQL statement in the batch. The values of the
    list are ordered according to the ordering of the request statements.
    """

    consumed_capacity: list[ConsumedCapacity] | None = None
    """
    The capacity units consumed by the entire operation. The values of the
    list are ordered according to the ordering of the statements.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_EXECUTE_STATEMENT_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.responses is not None:
            _serialize_parti_ql_batch_response(
                serializer,
                _SCHEMA_BATCH_EXECUTE_STATEMENT_OUTPUT.members["Responses"],
                self.responses,
            )

        if self.consumed_capacity is not None:
            _serialize_consumed_capacity_multiple(
                serializer,
                _SCHEMA_BATCH_EXECUTE_STATEMENT_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["responses"] = _deserialize_parti_ql_batch_response(
                        de, _SCHEMA_BATCH_EXECUTE_STATEMENT_OUTPUT.members["Responses"]
                    )

                case 1:
                    kwargs["consumed_capacity"] = (
                        _deserialize_consumed_capacity_multiple(
                            de,
                            _SCHEMA_BATCH_EXECUTE_STATEMENT_OUTPUT.members[
                                "ConsumedCapacity"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_BATCH_EXECUTE_STATEMENT_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class BatchGetItemOutput:
    """Represents the output of a `BatchGetItem` operation."""

    responses: "dict[str, list[dict[str, AttributeValue]]] | None" = None
    """
    A map of table name or table ARN to a list of items. Each object in
    `Responses` consists of a table name or ARN, along with a map of
    attribute data consisting of the data type and attribute value.
    """

    unprocessed_keys: "dict[str, KeysAndAttributes] | None" = None
    """
    A map of tables and their respective keys that were not processed with
    the current response. The `UnprocessedKeys` value is in the same form as
    `RequestItems`, so the value can be provided directly to a subsequent
    `BatchGetItem` operation. For more information, see `RequestItems` in
    the Request Parameters section.

    Each element consists of:

    - `Keys` - An array of primary key attribute values that define specific
      items in the table.

    - `ProjectionExpression` - One or more attributes to be retrieved from
      the table or index. By default, all attributes are returned. If a
      requested attribute is not found, it does not appear in the result.

    - `ConsistentRead` - The consistency of a read operation. If set to
      `true`, then a strongly consistent read is used; otherwise, an
      eventually consistent read is used.

    If there are no unprocessed keys remaining, the response contains an
    empty `UnprocessedKeys` map.
    """

    consumed_capacity: list[ConsumedCapacity] | None = None
    """
    The read capacity units consumed by the entire `BatchGetItem` operation.

    Each element consists of:

    - `TableName` - The table that consumed the provisioned throughput.

    - `CapacityUnits` - The total number of capacity units consumed.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_GET_ITEM_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.responses is not None:
            _serialize_batch_get_response_map(
                serializer,
                _SCHEMA_BATCH_GET_ITEM_OUTPUT.members["Responses"],
                self.responses,
            )

        if self.unprocessed_keys is not None:
            _serialize_batch_get_request_map(
                serializer,
                _SCHEMA_BATCH_GET_ITEM_OUTPUT.members["UnprocessedKeys"],
                self.unprocessed_keys,
            )

        if self.consumed_capacity is not None:
            _serialize_consumed_capacity_multiple(
                serializer,
                _SCHEMA_BATCH_GET_ITEM_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["responses"] = _deserialize_batch_get_response_map(
                        de, _SCHEMA_BATCH_GET_ITEM_OUTPUT.members["Responses"]
                    )

                case 1:
                    kwargs["unprocessed_keys"] = _deserialize_batch_get_request_map(
                        de, _SCHEMA_BATCH_GET_ITEM_OUTPUT.members["UnprocessedKeys"]
                    )

                case 2:
                    kwargs["consumed_capacity"] = (
                        _deserialize_consumed_capacity_multiple(
                            de,
                            _SCHEMA_BATCH_GET_ITEM_OUTPUT.members["ConsumedCapacity"],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BATCH_GET_ITEM_OUTPUT, consumer=_consumer)
        return kwargs


def _serialize_batch_write_item_request_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, list[WriteRequest]]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: _serialize_write_requests(vs, value_schema, v))


def _deserialize_batch_write_item_request_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, list[WriteRequest]]:
    result: dict[str, list[WriteRequest]] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = _deserialize_write_requests(d, value_schema)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ScanInput:
    """Represents the input of a `Scan` operation."""

    table_name: str | None = None
    """
    The name of the table containing the requested items or if you provide
    `IndexName`, the name of the table to which that index belongs.

    You can also provide the Amazon Resource Name (ARN) of the table in this
    parameter.
    """

    index_name: str | None = None
    """
    The name of a secondary index to scan. This index can be any local
    secondary index or global secondary index. Note that if you use the
    `IndexName` parameter, you must also provide `TableName`.
    """

    attributes_to_get: list[str] | None = None
    """
    This is a legacy parameter. Use `ProjectionExpression` instead. For more
    information, see
    [AttributesToGet](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.AttributesToGet.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    limit: int | None = None
    """
    The maximum number of items to evaluate (not necessarily the number of
    matching items). If DynamoDB processes the number of items up to the
    limit while processing the results, it stops the operation and returns
    the matching values up to that point, and a key in `LastEvaluatedKey` to
    apply in a subsequent operation, so that you can pick up where you left
    off. Also, if the processed dataset size exceeds 1 MB before DynamoDB
    reaches this limit, it stops the operation and returns the matching
    values up to the limit, and a key in `LastEvaluatedKey` to apply in a
    subsequent operation to continue the operation. For more information,
    see [Working with
    Queries](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/QueryAndScan.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    select: Select | None = None
    """
    The attributes to be returned in the result. You can retrieve all item
    attributes, specific item attributes, the count of matching items, or in
    the case of an index, some or all of the attributes projected into the
    index.

    - `ALL_ATTRIBUTES` - Returns all of the item attributes from the
      specified table or index. If you query a local secondary index, then
      for each matching item in the index, DynamoDB fetches the entire item
      from the parent table. If the index is configured to project all item
      attributes, then all of the data can be obtained from the local
      secondary index, and no fetching is required.

    - `ALL_PROJECTED_ATTRIBUTES` - Allowed only when querying an index.
      Retrieves all attributes that have been projected into the index. If
      the index is configured to project all attributes, this return value
      is equivalent to specifying `ALL_ATTRIBUTES`.

    - `COUNT` - Returns the number of matching items, rather than the
      matching items themselves. Note that this uses the same quantity of
      read capacity units as getting the items, and is subject to the same
      item size calculations.

    - `SPECIFIC_ATTRIBUTES` - Returns only the attributes listed in
      `ProjectionExpression`. This return value is equivalent to specifying
      `ProjectionExpression` without specifying any value for `Select`.

      If you query or scan a local secondary index and request only
      attributes that are projected into that index, the operation reads
      only the index and not the table. If any of the requested attributes
      are not projected into the local secondary index, DynamoDB fetches
      each of these attributes from the parent table. This extra fetching
      incurs additional throughput cost and latency.

      If you query or scan a global secondary index, you can only request
      attributes that are projected into the index. Global secondary index
      queries cannot fetch attributes from the parent table.

    If neither `Select` nor `ProjectionExpression` are specified, DynamoDB
    defaults to `ALL_ATTRIBUTES` when accessing a table, and
    `ALL_PROJECTED_ATTRIBUTES` when accessing an index. You cannot use both
    `Select` and `ProjectionExpression` together in a single request, unless
    the value for `Select` is `SPECIFIC_ATTRIBUTES`. (This usage is
    equivalent to specifying `ProjectionExpression` without any value for
    `Select`.)

    Note:
        If you use the `ProjectionExpression` parameter, then the value for
        `Select` can only be `SPECIFIC_ATTRIBUTES`. Any other value for `Select`
        will return an error.
    """

    scan_filter: "dict[str, Condition] | None" = None
    """
    This is a legacy parameter. Use `FilterExpression` instead. For more
    information, see
    [ScanFilter](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.ScanFilter.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    conditional_operator: ConditionalOperator | None = None
    """
    This is a legacy parameter. Use `FilterExpression` instead. For more
    information, see
    [ConditionalOperator](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.ConditionalOperator.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    exclusive_start_key: "dict[str, AttributeValue] | None" = None
    """
    The primary key of the first item that this operation will evaluate. Use
    the value that was returned for `LastEvaluatedKey` in the previous
    operation.

    The data type for `ExclusiveStartKey` must be String, Number or Binary.
    No set data types are allowed.

    In a parallel scan, a `Scan` request that includes `ExclusiveStartKey`
    must specify the same segment whose previous `Scan` returned the
    corresponding value of `LastEvaluatedKey`.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    total_segments: int | None = None
    """
    For a parallel `Scan` request, `TotalSegments` represents the total
    number of segments into which the `Scan` operation will be divided. The
    value of `TotalSegments` corresponds to the number of application
    workers that will perform the parallel scan. For example, if you want to
    use four application threads to scan a table or an index, specify a
    `TotalSegments` value of 4.

    The value for `TotalSegments` must be greater than or equal to 1, and
    less than or equal to 1000000. If you specify a `TotalSegments` value of
    1, the `Scan` operation will be sequential rather than parallel.

    If you specify `TotalSegments`, you must also specify `Segment`.
    """

    segment: int | None = None
    """
    For a parallel `Scan` request, `Segment` identifies an individual
    segment to be scanned by an application worker.

    Segment IDs are zero-based, so the first segment is always 0. For
    example, if you want to use four application threads to scan a table or
    an index, then the first thread specifies a `Segment` value of 0, the
    second thread specifies 1, and so on.

    The value of `LastEvaluatedKey` returned from a parallel `Scan` request
    must be used as `ExclusiveStartKey` with the same segment ID in a
    subsequent `Scan` operation.

    The value for `Segment` must be greater than or equal to 0, and less
    than the value provided for `TotalSegments`.

    If you provide `Segment`, you must also provide `TotalSegments`.
    """

    projection_expression: str | None = None
    """
    A string that identifies one or more attributes to retrieve from the
    specified table or index. These attributes can include scalars, sets, or
    elements of a JSON document. The attributes in the expression must be
    separated by commas.

    If no attribute names are specified, then all attributes will be
    returned. If any of the requested attributes are not found, they will
    not appear in the result.

    For more information, see [Specifying Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    filter_expression: str | None = None
    """
    A string that contains conditions that DynamoDB applies after the `Scan`
    operation, but before the data is returned to you. Items that do not
    satisfy the `FilterExpression` criteria are not returned.

    Note:
        A `FilterExpression` is applied after the items have already been read;
        the process of filtering does not consume any additional read capacity
        units.

    For more information, see [Filter
    Expressions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Scan.html#Scan.FilterExpression)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_names: dict[str, str] | None = None
    """
    One or more substitution tokens for attribute names in an expression.
    The following are some use cases for using `ExpressionAttributeNames`:

    - To access an attribute whose name conflicts with a DynamoDB reserved
      word.

    - To create a placeholder for repeating occurrences of an attribute name
      in an expression.

    - To prevent special characters in an attribute name from being
      misinterpreted in an expression.

    Use the **#** character in an expression to dereference an attribute
    name. For example, consider the following attribute name:

    - `Percentile`

    The name of this attribute conflicts with a reserved word, so it cannot
    be used directly in an expression. (For the complete list of reserved
    words, see [Reserved
    Words](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
    in the *Amazon DynamoDB Developer Guide*). To work around this, you
    could specify the following for `ExpressionAttributeNames`:

    - `{"#P":"Percentile"}`

    You could then use this substitution in an expression, as in this
    example:

    - `#P = :val`

    Note:
        Tokens that begin with the **:** character are *expression attribute
        values*, which are placeholders for the actual value at runtime.

    For more information on expression attribute names, see [Specifying Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_values: "dict[str, AttributeValue] | None" = None
    """
    One or more values that can be substituted in an expression.

    Use the **:** (colon) character in an expression to dereference an
    attribute value. For example, suppose that you wanted to check whether
    the value of the `ProductStatus` attribute was one of the following:

    `Available | Backordered | Discontinued`

    You would first need to specify `ExpressionAttributeValues` as follows:

    `{ ":avail":{"S":"Available"}, ":back":{"S":"Backordered"}, ":disc":{"S":"Discontinued"} }`

    You could then use these values in an expression, such as this:

    `ProductStatus IN (:avail, :back, :disc)`

    For more information on expression attribute values, see [Condition
    Expressions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.SpecifyingConditions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    consistent_read: bool | None = None
    """
    A Boolean value that determines the read consistency model during the
    scan:

    - If `ConsistentRead` is `false`, then the data returned from `Scan`
      might not contain the results from other recently completed write
      operations (`PutItem`, `UpdateItem`, or `DeleteItem`).

    - If `ConsistentRead` is `true`, then all of the write operations that
      completed before the `Scan` began are guaranteed to be contained in
      the `Scan` response.

    The default setting for `ConsistentRead` is `false`.

    The `ConsistentRead` parameter is not supported on global secondary
    indexes. If you scan a global secondary index with `ConsistentRead` set
    to true, you will receive a `ValidationException`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SCAN_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_SCAN_INPUT.members["TableName"], self.table_name
            )

        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_SCAN_INPUT.members["IndexName"], self.index_name
            )

        if self.attributes_to_get is not None:
            _serialize_attribute_name_list(
                serializer,
                _SCHEMA_SCAN_INPUT.members["AttributesToGet"],
                self.attributes_to_get,
            )

        if self.limit is not None:
            serializer.write_integer(_SCHEMA_SCAN_INPUT.members["Limit"], self.limit)

        if self.select is not None:
            serializer.write_string(_SCHEMA_SCAN_INPUT.members["Select"], self.select)

        if self.scan_filter is not None:
            _serialize_filter_condition_map(
                serializer, _SCHEMA_SCAN_INPUT.members["ScanFilter"], self.scan_filter
            )

        if self.conditional_operator is not None:
            serializer.write_string(
                _SCHEMA_SCAN_INPUT.members["ConditionalOperator"],
                self.conditional_operator,
            )

        if self.exclusive_start_key is not None:
            _serialize_key(
                serializer,
                _SCHEMA_SCAN_INPUT.members["ExclusiveStartKey"],
                self.exclusive_start_key,
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_SCAN_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

        if self.total_segments is not None:
            serializer.write_integer(
                _SCHEMA_SCAN_INPUT.members["TotalSegments"], self.total_segments
            )

        if self.segment is not None:
            serializer.write_integer(
                _SCHEMA_SCAN_INPUT.members["Segment"], self.segment
            )

        if self.projection_expression is not None:
            serializer.write_string(
                _SCHEMA_SCAN_INPUT.members["ProjectionExpression"],
                self.projection_expression,
            )

        if self.filter_expression is not None:
            serializer.write_string(
                _SCHEMA_SCAN_INPUT.members["FilterExpression"], self.filter_expression
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_SCAN_INPUT.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

        if self.expression_attribute_values is not None:
            _serialize_expression_attribute_value_map(
                serializer,
                _SCHEMA_SCAN_INPUT.members["ExpressionAttributeValues"],
                self.expression_attribute_values,
            )

        if self.consistent_read is not None:
            serializer.write_boolean(
                _SCHEMA_SCAN_INPUT.members["ConsistentRead"], self.consistent_read
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_SCAN_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_SCAN_INPUT.members["IndexName"]
                    )

                case 2:
                    kwargs["attributes_to_get"] = _deserialize_attribute_name_list(
                        de, _SCHEMA_SCAN_INPUT.members["AttributesToGet"]
                    )

                case 3:
                    kwargs["limit"] = de.read_integer(
                        _SCHEMA_SCAN_INPUT.members["Limit"]
                    )

                case 4:
                    kwargs["select"] = Select(
                        de.read_string(_SCHEMA_SCAN_INPUT.members["Select"])
                    )

                case 5:
                    kwargs["scan_filter"] = _deserialize_filter_condition_map(
                        de, _SCHEMA_SCAN_INPUT.members["ScanFilter"]
                    )

                case 6:
                    kwargs["conditional_operator"] = ConditionalOperator(
                        de.read_string(
                            _SCHEMA_SCAN_INPUT.members["ConditionalOperator"]
                        )
                    )

                case 7:
                    kwargs["exclusive_start_key"] = _deserialize_key(
                        de, _SCHEMA_SCAN_INPUT.members["ExclusiveStartKey"]
                    )

                case 8:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_SCAN_INPUT.members["ReturnConsumedCapacity"]
                        )
                    )

                case 9:
                    kwargs["total_segments"] = de.read_integer(
                        _SCHEMA_SCAN_INPUT.members["TotalSegments"]
                    )

                case 10:
                    kwargs["segment"] = de.read_integer(
                        _SCHEMA_SCAN_INPUT.members["Segment"]
                    )

                case 11:
                    kwargs["projection_expression"] = de.read_string(
                        _SCHEMA_SCAN_INPUT.members["ProjectionExpression"]
                    )

                case 12:
                    kwargs["filter_expression"] = de.read_string(
                        _SCHEMA_SCAN_INPUT.members["FilterExpression"]
                    )

                case 13:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de, _SCHEMA_SCAN_INPUT.members["ExpressionAttributeNames"]
                        )
                    )

                case 14:
                    kwargs["expression_attribute_values"] = (
                        _deserialize_expression_attribute_value_map(
                            de, _SCHEMA_SCAN_INPUT.members["ExpressionAttributeValues"]
                        )
                    )

                case 15:
                    kwargs["consistent_read"] = de.read_boolean(
                        _SCHEMA_SCAN_INPUT.members["ConsistentRead"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SCAN_INPUT, consumer=_consumer)
        return kwargs


SEARCH_VECTORS = APIOperation(
    input=SearchVectorsInput,
    output=SearchVectorsOutput,
    schema=_SCHEMA_SEARCH_VECTORS,
    input_schema=_SCHEMA_SEARCH_VECTORS_INPUT,
    output_schema=_SCHEMA_SEARCH_VECTORS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class BatchWriteItemInput:
    """Represents the input of a `BatchWriteItem` operation."""

    request_items: "dict[str, list[WriteRequest]] | None" = None
    """
    A map of one or more table names or table ARNs and, for each table, a
    list of operations to be performed (`DeleteRequest` or `PutRequest`).
    Each element in the map consists of the following:

    - `DeleteRequest` - Perform a `DeleteItem` operation on the specified
      item. The item to be deleted is identified by a `Key` subelement:

      - `Key` - A map of primary key attribute values that uniquely identify
        the item. Each entry in this map consists of an attribute name and
        an attribute value. For each primary key, you must provide *all* of
        the key attributes. For example, with a simple primary key, you only
        need to provide a value for the partition key. For a composite
        primary key, you must provide values for *both* the partition key
        and the sort key.

    - `PutRequest` - Perform a `PutItem` operation on the specified item.
      The item to be put is identified by an `Item` subelement:

      - `Item` - A map of attributes and their values. Each entry in this
        map consists of an attribute name and an attribute value. Attribute
        values must not be null; string and binary type attributes must have
        lengths greater than zero; and set type attributes must not be
        empty. Requests that contain empty values are rejected with a
        `ValidationException` exception.

        If you specify any attributes that are part of an index key, then
        the data types for those attributes must match those of the schema
        in the table's attribute definition.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    return_item_collection_metrics: ReturnItemCollectionMetrics | None = None
    """
    Determines whether item collection metrics are returned. If set to
    `SIZE`, the response includes statistics about item collections, if any,
    that were modified during the operation are returned in the response. If
    set to `NONE` (the default), no statistics are returned.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_WRITE_ITEM_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.request_items is not None:
            _serialize_batch_write_item_request_map(
                serializer,
                _SCHEMA_BATCH_WRITE_ITEM_INPUT.members["RequestItems"],
                self.request_items,
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_BATCH_WRITE_ITEM_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

        if self.return_item_collection_metrics is not None:
            serializer.write_string(
                _SCHEMA_BATCH_WRITE_ITEM_INPUT.members["ReturnItemCollectionMetrics"],
                self.return_item_collection_metrics,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["request_items"] = _deserialize_batch_write_item_request_map(
                        de, _SCHEMA_BATCH_WRITE_ITEM_INPUT.members["RequestItems"]
                    )

                case 1:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_BATCH_WRITE_ITEM_INPUT.members[
                                "ReturnConsumedCapacity"
                            ]
                        )
                    )

                case 2:
                    kwargs["return_item_collection_metrics"] = (
                        ReturnItemCollectionMetrics(
                            de.read_string(
                                _SCHEMA_BATCH_WRITE_ITEM_INPUT.members[
                                    "ReturnItemCollectionMetrics"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BATCH_WRITE_ITEM_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DeleteItemInput:
    """Represents the input of a `DeleteItem` operation."""

    table_name: str | None = None
    """
    The name of the table from which to delete the item. You can also
    provide the Amazon Resource Name (ARN) of the table in this parameter.
    """

    key: "dict[str, AttributeValue] | None" = None
    """
    A map of attribute names to `AttributeValue` objects, representing the
    primary key of the item to delete.

    For the primary key, you must provide all of the key attributes. For
    example, with a simple primary key, you only need to provide a value for
    the partition key. For a composite primary key, you must provide values
    for both the partition key and the sort key.
    """

    expected: "dict[str, ExpectedAttributeValue] | None" = None
    """
    This is a legacy parameter. Use `ConditionExpression` instead. For more
    information, see
    [Expected](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.Expected.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    conditional_operator: ConditionalOperator | None = None
    """
    This is a legacy parameter. Use `ConditionExpression` instead. For more
    information, see
    [ConditionalOperator](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.ConditionalOperator.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    return_values: ReturnValue | None = None
    """
    Use `ReturnValues` if you want to get the item attributes as they
    appeared before they were deleted. For `DeleteItem`, the valid values
    are:

    - `NONE` - If `ReturnValues` is not specified, or if its value is
      `NONE`, then nothing is returned. (This setting is the default for
      `ReturnValues`.)

    - `ALL_OLD` - The content of the old item is returned.

    There is no additional cost associated with requesting a return value
    aside from the small network and processing overhead of receiving a
    larger response. No read capacity units are consumed.

    Note:
        The `ReturnValues` parameter is used by several DynamoDB operations;
        however, `DeleteItem` does not recognize any values other than `NONE` or
        `ALL_OLD`.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    return_item_collection_metrics: ReturnItemCollectionMetrics | None = None
    """
    Determines whether item collection metrics are returned. If set to
    `SIZE`, the response includes statistics about item collections, if any,
    that were modified during the operation are returned in the response. If
    set to `NONE` (the default), no statistics are returned.
    """

    condition_expression: str | None = None
    """
    A condition that must be satisfied in order for a conditional
    `DeleteItem` to succeed.

    An expression can contain any of the following:

    - Functions:
      `attribute_exists | attribute_not_exists | attribute_type | contains | begins_with | size`

      These function names are case-sensitive.

    - Comparison operators: `= | <> | < | > | <= | >= | BETWEEN | IN `

    - Logical operators: `AND | OR | NOT`

    For more information about condition expressions, see [Condition
    Expressions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.SpecifyingConditions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_names: dict[str, str] | None = None
    """
    One or more substitution tokens for attribute names in an expression.
    The following are some use cases for using `ExpressionAttributeNames`:

    - To access an attribute whose name conflicts with a DynamoDB reserved
      word.

    - To create a placeholder for repeating occurrences of an attribute name
      in an expression.

    - To prevent special characters in an attribute name from being
      misinterpreted in an expression.

    Use the **#** character in an expression to dereference an attribute
    name. For example, consider the following attribute name:

    - `Percentile`

    The name of this attribute conflicts with a reserved word, so it cannot
    be used directly in an expression. (For the complete list of reserved
    words, see [Reserved
    Words](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
    in the *Amazon DynamoDB Developer Guide*). To work around this, you
    could specify the following for `ExpressionAttributeNames`:

    - `{"#P":"Percentile"}`

    You could then use this substitution in an expression, as in this
    example:

    - `#P = :val`

    Note:
        Tokens that begin with the **:** character are *expression attribute
        values*, which are placeholders for the actual value at runtime.

    For more information on expression attribute names, see [Specifying Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_values: "dict[str, AttributeValue] | None" = None
    """
    One or more values that can be substituted in an expression.

    Use the **:** (colon) character in an expression to dereference an
    attribute value. For example, suppose that you wanted to check whether
    the value of the *ProductStatus* attribute was one of the following:

    `Available | Backordered | Discontinued`

    You would first need to specify `ExpressionAttributeValues` as follows:

    `{ ":avail":{"S":"Available"}, ":back":{"S":"Backordered"}, ":disc":{"S":"Discontinued"} }`

    You could then use these values in an expression, such as this:

    `ProductStatus IN (:avail, :back, :disc)`

    For more information on expression attribute values, see [Condition
    Expressions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.SpecifyingConditions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    return_values_on_condition_check_failure: (
        ReturnValuesOnConditionCheckFailure | None
    ) = None
    """
    An optional parameter that returns the item attributes for a
    `DeleteItem` operation that failed a condition check.

    There is no additional cost associated with requesting a return value
    aside from the small network and processing overhead of receiving a
    larger response. No read capacity units are consumed.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_ITEM_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_DELETE_ITEM_INPUT.members["TableName"], self.table_name
            )

        if self.key is not None:
            _serialize_key(
                serializer, _SCHEMA_DELETE_ITEM_INPUT.members["Key"], self.key
            )

        if self.expected is not None:
            _serialize_expected_attribute_map(
                serializer, _SCHEMA_DELETE_ITEM_INPUT.members["Expected"], self.expected
            )

        if self.conditional_operator is not None:
            serializer.write_string(
                _SCHEMA_DELETE_ITEM_INPUT.members["ConditionalOperator"],
                self.conditional_operator,
            )

        if self.return_values is not None:
            serializer.write_string(
                _SCHEMA_DELETE_ITEM_INPUT.members["ReturnValues"], self.return_values
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_DELETE_ITEM_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

        if self.return_item_collection_metrics is not None:
            serializer.write_string(
                _SCHEMA_DELETE_ITEM_INPUT.members["ReturnItemCollectionMetrics"],
                self.return_item_collection_metrics,
            )

        if self.condition_expression is not None:
            serializer.write_string(
                _SCHEMA_DELETE_ITEM_INPUT.members["ConditionExpression"],
                self.condition_expression,
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_DELETE_ITEM_INPUT.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

        if self.expression_attribute_values is not None:
            _serialize_expression_attribute_value_map(
                serializer,
                _SCHEMA_DELETE_ITEM_INPUT.members["ExpressionAttributeValues"],
                self.expression_attribute_values,
            )

        if self.return_values_on_condition_check_failure is not None:
            serializer.write_string(
                _SCHEMA_DELETE_ITEM_INPUT.members[
                    "ReturnValuesOnConditionCheckFailure"
                ],
                self.return_values_on_condition_check_failure,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_DELETE_ITEM_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["key"] = _deserialize_key(
                        de, _SCHEMA_DELETE_ITEM_INPUT.members["Key"]
                    )

                case 2:
                    kwargs["expected"] = _deserialize_expected_attribute_map(
                        de, _SCHEMA_DELETE_ITEM_INPUT.members["Expected"]
                    )

                case 3:
                    kwargs["conditional_operator"] = ConditionalOperator(
                        de.read_string(
                            _SCHEMA_DELETE_ITEM_INPUT.members["ConditionalOperator"]
                        )
                    )

                case 4:
                    kwargs["return_values"] = ReturnValue(
                        de.read_string(
                            _SCHEMA_DELETE_ITEM_INPUT.members["ReturnValues"]
                        )
                    )

                case 5:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_DELETE_ITEM_INPUT.members["ReturnConsumedCapacity"]
                        )
                    )

                case 6:
                    kwargs["return_item_collection_metrics"] = (
                        ReturnItemCollectionMetrics(
                            de.read_string(
                                _SCHEMA_DELETE_ITEM_INPUT.members[
                                    "ReturnItemCollectionMetrics"
                                ]
                            )
                        )
                    )

                case 7:
                    kwargs["condition_expression"] = de.read_string(
                        _SCHEMA_DELETE_ITEM_INPUT.members["ConditionExpression"]
                    )

                case 8:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de,
                            _SCHEMA_DELETE_ITEM_INPUT.members[
                                "ExpressionAttributeNames"
                            ],
                        )
                    )

                case 9:
                    kwargs["expression_attribute_values"] = (
                        _deserialize_expression_attribute_value_map(
                            de,
                            _SCHEMA_DELETE_ITEM_INPUT.members[
                                "ExpressionAttributeValues"
                            ],
                        )
                    )

                case 10:
                    kwargs["return_values_on_condition_check_failure"] = (
                        ReturnValuesOnConditionCheckFailure(
                            de.read_string(
                                _SCHEMA_DELETE_ITEM_INPUT.members[
                                    "ReturnValuesOnConditionCheckFailure"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_ITEM_INPUT, consumer=_consumer)
        return kwargs


EXECUTE_STATEMENT = APIOperation(
    input=ExecuteStatementInput,
    output=ExecuteStatementOutput,
    schema=_SCHEMA_EXECUTE_STATEMENT,
    input_schema=_SCHEMA_EXECUTE_STATEMENT_INPUT,
    output_schema=_SCHEMA_EXECUTE_STATEMENT_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#ConditionalCheckFailedException"
            ): ConditionalCheckFailedException,
            ShapeID(
                "com.amazonaws.dynamodb#DuplicateItemException"
            ): DuplicateItemException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#ItemCollectionSizeLimitExceededException"
            ): ItemCollectionSizeLimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
            ShapeID(
                "com.amazonaws.dynamodb#TransactionConflictException"
            ): TransactionConflictException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION,
        _SCHEMA_DUPLICATE_ITEM_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_TRANSACTION_CONFLICT_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class PutItemInput:
    """Represents the input of a `PutItem` operation."""

    table_name: str | None = None
    """
    The name of the table to contain the item. You can also provide the
    Amazon Resource Name (ARN) of the table in this parameter.
    """

    item: "dict[str, AttributeValue] | None" = None
    """
    A map of attribute name/value pairs, one for each attribute. Only the
    primary key attributes are required; you can optionally provide other
    attribute name-value pairs for the item.

    You must provide all of the attributes for the primary key. For example,
    with a simple primary key, you only need to provide a value for the
    partition key. For a composite primary key, you must provide both values
    for both the partition key and the sort key.

    If you specify any attributes that are part of an index key, then the
    data types for those attributes must match those of the schema in the
    table's attribute definition.

    If the table has vector indexes, the following validations apply to
    write operations. A violation of any of these constraints results in a
    `ValidationException`:

    - The vector attribute must be a list of numbers with dimensions
      matching the index configuration.

    - Vector values must fit in 32-bit IEEE-754 floating point format (f32).

    - Partition key and inline filter attributes defined in the search
      schema must have data types matching the index schema definition.

    Empty String and Binary attribute values are allowed. Attribute values
    of type String and Binary must have a length greater than zero if the
    attribute is used as a key attribute for a table or index.

    For more information about primary keys, see [Primary
    Key](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.CoreComponents.html#HowItWorks.CoreComponents.PrimaryKey)
    in the *Amazon DynamoDB Developer Guide*.

    Each element in the `Item` map is an `AttributeValue` object.
    """

    expected: "dict[str, ExpectedAttributeValue] | None" = None
    """
    This is a legacy parameter. Use `ConditionExpression` instead. For more
    information, see
    [Expected](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.Expected.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    return_values: ReturnValue | None = None
    """
    Use `ReturnValues` if you want to get the item attributes as they
    appeared before they were updated with the `PutItem` request. For
    `PutItem`, the valid values are:

    - `NONE` - If `ReturnValues` is not specified, or if its value is
      `NONE`, then nothing is returned. (This setting is the default for
      `ReturnValues`.)

    - `ALL_OLD` - If `PutItem` overwrote an attribute name-value pair, then
      the content of the old item is returned.

    The values returned are strongly consistent.

    There is no additional cost associated with requesting a return value
    aside from the small network and processing overhead of receiving a
    larger response. No read capacity units are consumed.

    Note:
        The `ReturnValues` parameter is used by several DynamoDB operations;
        however, `PutItem` does not recognize any values other than `NONE` or
        `ALL_OLD`.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    return_item_collection_metrics: ReturnItemCollectionMetrics | None = None
    """
    Determines whether item collection metrics are returned. If set to
    `SIZE`, the response includes statistics about item collections, if any,
    that were modified during the operation are returned in the response. If
    set to `NONE` (the default), no statistics are returned.
    """

    conditional_operator: ConditionalOperator | None = None
    """
    This is a legacy parameter. Use `ConditionExpression` instead. For more
    information, see
    [ConditionalOperator](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.ConditionalOperator.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    condition_expression: str | None = None
    """
    A condition that must be satisfied in order for a conditional `PutItem`
    operation to succeed.

    An expression can contain any of the following:

    - Functions:
      `attribute_exists | attribute_not_exists | attribute_type | contains | begins_with | size`

      These function names are case-sensitive.

    - Comparison operators: `= | <> | < | > | <= | >= | BETWEEN | IN `

    - Logical operators: `AND | OR | NOT`

    For more information on condition expressions, see [Condition
    Expressions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.SpecifyingConditions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_names: dict[str, str] | None = None
    """
    One or more substitution tokens for attribute names in an expression.
    The following are some use cases for using `ExpressionAttributeNames`:

    - To access an attribute whose name conflicts with a DynamoDB reserved
      word.

    - To create a placeholder for repeating occurrences of an attribute name
      in an expression.

    - To prevent special characters in an attribute name from being
      misinterpreted in an expression.

    Use the **#** character in an expression to dereference an attribute
    name. For example, consider the following attribute name:

    - `Percentile`

    The name of this attribute conflicts with a reserved word, so it cannot
    be used directly in an expression. (For the complete list of reserved
    words, see [Reserved
    Words](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
    in the *Amazon DynamoDB Developer Guide*). To work around this, you
    could specify the following for `ExpressionAttributeNames`:

    - `{"#P":"Percentile"}`

    You could then use this substitution in an expression, as in this
    example:

    - `#P = :val`

    Note:
        Tokens that begin with the **:** character are *expression attribute
        values*, which are placeholders for the actual value at runtime.

    For more information on expression attribute names, see [Specifying Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_values: "dict[str, AttributeValue] | None" = None
    """
    One or more values that can be substituted in an expression.

    Use the **:** (colon) character in an expression to dereference an
    attribute value. For example, suppose that you wanted to check whether
    the value of the *ProductStatus* attribute was one of the following:

    `Available | Backordered | Discontinued`

    You would first need to specify `ExpressionAttributeValues` as follows:

    `{ ":avail":{"S":"Available"}, ":back":{"S":"Backordered"}, ":disc":{"S":"Discontinued"} }`

    You could then use these values in an expression, such as this:

    `ProductStatus IN (:avail, :back, :disc)`

    For more information on expression attribute values, see [Condition
    Expressions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.SpecifyingConditions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    return_values_on_condition_check_failure: (
        ReturnValuesOnConditionCheckFailure | None
    ) = None
    """
    An optional parameter that returns the item attributes for a `PutItem`
    operation that failed a condition check.

    There is no additional cost associated with requesting a return value
    aside from the small network and processing overhead of receiving a
    larger response. No read capacity units are consumed.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PUT_ITEM_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_PUT_ITEM_INPUT.members["TableName"], self.table_name
            )

        if self.item is not None:
            _serialize_put_item_input_attribute_map(
                serializer, _SCHEMA_PUT_ITEM_INPUT.members["Item"], self.item
            )

        if self.expected is not None:
            _serialize_expected_attribute_map(
                serializer, _SCHEMA_PUT_ITEM_INPUT.members["Expected"], self.expected
            )

        if self.return_values is not None:
            serializer.write_string(
                _SCHEMA_PUT_ITEM_INPUT.members["ReturnValues"], self.return_values
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_PUT_ITEM_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

        if self.return_item_collection_metrics is not None:
            serializer.write_string(
                _SCHEMA_PUT_ITEM_INPUT.members["ReturnItemCollectionMetrics"],
                self.return_item_collection_metrics,
            )

        if self.conditional_operator is not None:
            serializer.write_string(
                _SCHEMA_PUT_ITEM_INPUT.members["ConditionalOperator"],
                self.conditional_operator,
            )

        if self.condition_expression is not None:
            serializer.write_string(
                _SCHEMA_PUT_ITEM_INPUT.members["ConditionExpression"],
                self.condition_expression,
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_PUT_ITEM_INPUT.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

        if self.expression_attribute_values is not None:
            _serialize_expression_attribute_value_map(
                serializer,
                _SCHEMA_PUT_ITEM_INPUT.members["ExpressionAttributeValues"],
                self.expression_attribute_values,
            )

        if self.return_values_on_condition_check_failure is not None:
            serializer.write_string(
                _SCHEMA_PUT_ITEM_INPUT.members["ReturnValuesOnConditionCheckFailure"],
                self.return_values_on_condition_check_failure,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_PUT_ITEM_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["item"] = _deserialize_put_item_input_attribute_map(
                        de, _SCHEMA_PUT_ITEM_INPUT.members["Item"]
                    )

                case 2:
                    kwargs["expected"] = _deserialize_expected_attribute_map(
                        de, _SCHEMA_PUT_ITEM_INPUT.members["Expected"]
                    )

                case 3:
                    kwargs["return_values"] = ReturnValue(
                        de.read_string(_SCHEMA_PUT_ITEM_INPUT.members["ReturnValues"])
                    )

                case 4:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_PUT_ITEM_INPUT.members["ReturnConsumedCapacity"]
                        )
                    )

                case 5:
                    kwargs["return_item_collection_metrics"] = (
                        ReturnItemCollectionMetrics(
                            de.read_string(
                                _SCHEMA_PUT_ITEM_INPUT.members[
                                    "ReturnItemCollectionMetrics"
                                ]
                            )
                        )
                    )

                case 6:
                    kwargs["conditional_operator"] = ConditionalOperator(
                        de.read_string(
                            _SCHEMA_PUT_ITEM_INPUT.members["ConditionalOperator"]
                        )
                    )

                case 7:
                    kwargs["condition_expression"] = de.read_string(
                        _SCHEMA_PUT_ITEM_INPUT.members["ConditionExpression"]
                    )

                case 8:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de,
                            _SCHEMA_PUT_ITEM_INPUT.members["ExpressionAttributeNames"],
                        )
                    )

                case 9:
                    kwargs["expression_attribute_values"] = (
                        _deserialize_expression_attribute_value_map(
                            de,
                            _SCHEMA_PUT_ITEM_INPUT.members["ExpressionAttributeValues"],
                        )
                    )

                case 10:
                    kwargs["return_values_on_condition_check_failure"] = (
                        ReturnValuesOnConditionCheckFailure(
                            de.read_string(
                                _SCHEMA_PUT_ITEM_INPUT.members[
                                    "ReturnValuesOnConditionCheckFailure"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PUT_ITEM_INPUT, consumer=_consumer)
        return kwargs


EXECUTE_TRANSACTION = APIOperation(
    input=ExecuteTransactionInput,
    output=ExecuteTransactionOutput,
    schema=_SCHEMA_EXECUTE_TRANSACTION,
    input_schema=_SCHEMA_EXECUTE_TRANSACTION_INPUT,
    output_schema=_SCHEMA_EXECUTE_TRANSACTION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#IdempotentParameterMismatchException"
            ): IdempotentParameterMismatchException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
            ShapeID(
                "com.amazonaws.dynamodb#TransactionCanceledException"
            ): TransactionCanceledException,
            ShapeID(
                "com.amazonaws.dynamodb#TransactionInProgressException"
            ): TransactionInProgressException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_IDEMPOTENT_PARAMETER_MISMATCH_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_TRANSACTION_CANCELED_EXCEPTION,
        _SCHEMA_TRANSACTION_IN_PROGRESS_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class QueryInput:
    """Represents the input of a `Query` operation."""

    table_name: str | None = None
    """
    The name of the table containing the requested items. You can also
    provide the Amazon Resource Name (ARN) of the table in this parameter.
    """

    index_name: str | None = None
    """
    The name of an index to query. This index can be any local secondary
    index or global secondary index on the table. Note that if you use the
    `IndexName` parameter, you must also provide `TableName.`
    """

    select: Select | None = None
    """
    The attributes to be returned in the result. You can retrieve all item
    attributes, specific item attributes, the count of matching items, or in
    the case of an index, some or all of the attributes projected into the
    index.

    - `ALL_ATTRIBUTES` - Returns all of the item attributes from the
      specified table or index. If you query a local secondary index, then
      for each matching item in the index, DynamoDB fetches the entire item
      from the parent table. If the index is configured to project all item
      attributes, then all of the data can be obtained from the local
      secondary index, and no fetching is required.

    - `ALL_PROJECTED_ATTRIBUTES` - Allowed only when querying an index.
      Retrieves all attributes that have been projected into the index. If
      the index is configured to project all attributes, this return value
      is equivalent to specifying `ALL_ATTRIBUTES`.

    - `COUNT` - Returns the number of matching items, rather than the
      matching items themselves. Note that this uses the same quantity of
      read capacity units as getting the items, and is subject to the same
      item size calculations.

    - `SPECIFIC_ATTRIBUTES` - Returns only the attributes listed in
      `ProjectionExpression`. This return value is equivalent to specifying
      `ProjectionExpression` without specifying any value for `Select`.

      If you query or scan a local secondary index and request only
      attributes that are projected into that index, the operation will read
      only the index and not the table. If any of the requested attributes
      are not projected into the local secondary index, DynamoDB fetches
      each of these attributes from the parent table. This extra fetching
      incurs additional throughput cost and latency.

      If you query or scan a global secondary index, you can only request
      attributes that are projected into the index. Global secondary index
      queries cannot fetch attributes from the parent table.

    If neither `Select` nor `ProjectionExpression` are specified, DynamoDB
    defaults to `ALL_ATTRIBUTES` when accessing a table, and
    `ALL_PROJECTED_ATTRIBUTES` when accessing an index. You cannot use both
    `Select` and `ProjectionExpression` together in a single request, unless
    the value for `Select` is `SPECIFIC_ATTRIBUTES`. (This usage is
    equivalent to specifying `ProjectionExpression` without any value for
    `Select`.)

    Note:
        If you use the `ProjectionExpression` parameter, then the value for
        `Select` can only be `SPECIFIC_ATTRIBUTES`. Any other value for `Select`
        will return an error.
    """

    attributes_to_get: list[str] | None = None
    """
    This is a legacy parameter. Use `ProjectionExpression` instead. For more
    information, see
    [AttributesToGet](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.AttributesToGet.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    limit: int | None = None
    """
    The maximum number of items to evaluate (not necessarily the number of
    matching items). If DynamoDB processes the number of items up to the
    limit while processing the results, it stops the operation and returns
    the matching values up to that point, and a key in `LastEvaluatedKey` to
    apply in a subsequent operation, so that you can pick up where you left
    off. Also, if the processed dataset size exceeds 1 MB before DynamoDB
    reaches this limit, it stops the operation and returns the matching
    values up to the limit, and a key in `LastEvaluatedKey` to apply in a
    subsequent operation to continue the operation. For more information,
    see [Query and
    Scan](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/QueryAndScan.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    consistent_read: bool | None = None
    """
    Determines the read consistency model: If set to `true`, then the
    operation uses strongly consistent reads; otherwise, the operation uses
    eventually consistent reads.

    Strongly consistent reads are not supported on global secondary indexes.
    If you query a global secondary index with `ConsistentRead` set to
    `true`, you will receive a `ValidationException`.
    """

    key_conditions: "dict[str, Condition] | None" = None
    """
    This is a legacy parameter. Use `KeyConditionExpression` instead. For
    more information, see
    [KeyConditions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.KeyConditions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    query_filter: "dict[str, Condition] | None" = None
    """
    This is a legacy parameter. Use `FilterExpression` instead. For more
    information, see
    [QueryFilter](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.QueryFilter.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    conditional_operator: ConditionalOperator | None = None
    """
    This is a legacy parameter. Use `FilterExpression` instead. For more
    information, see
    [ConditionalOperator](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.ConditionalOperator.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    scan_index_forward: bool | None = None
    """
    Specifies the order for index traversal: If `true` (default), the
    traversal is performed in ascending order; if `false`, the traversal is
    performed in descending order.

    Items with the same partition key value are stored in sorted order by
    sort key. If the sort key data type is Number, the results are stored in
    numeric order. For type String, the results are stored in order of UTF-8
    bytes. For type Binary, DynamoDB treats each byte of the binary data as
    unsigned.

    If `ScanIndexForward` is `true`, DynamoDB returns the results in the
    order in which they are stored (by sort key value). This is the default
    behavior. If `ScanIndexForward` is `false`, DynamoDB reads the results
    in reverse order by sort key value, and then returns the results to the
    client.
    """

    exclusive_start_key: "dict[str, AttributeValue] | None" = None
    """
    The primary key of the first item that this operation will evaluate. Use
    the value that was returned for `LastEvaluatedKey` in the previous
    operation.

    The data type for `ExclusiveStartKey` must be String, Number, or Binary.
    No set data types are allowed.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    projection_expression: str | None = None
    """
    A string that identifies one or more attributes to retrieve from the
    table. These attributes can include scalars, sets, or elements of a JSON
    document. The attributes in the expression must be separated by commas.

    If no attribute names are specified, then all attributes will be
    returned. If any of the requested attributes are not found, they will
    not appear in the result.

    For more information, see [Accessing Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    filter_expression: str | None = None
    """
    A string that contains conditions that DynamoDB applies after the
    `Query` operation, but before the data is returned to you. Items that do
    not satisfy the `FilterExpression` criteria are not returned.

    A `FilterExpression` does not allow key attributes. You cannot define a
    filter expression based on a partition key or a sort key.

    Note:
        A `FilterExpression` is applied after the items have already been read;
        the process of filtering does not consume any additional read capacity
        units.

    For more information, see [Filter
    Expressions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Query.FilterExpression.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    key_condition_expression: str | None = None
    """
    The condition that specifies the key values for items to be retrieved by
    the `Query` action.

    The condition must perform an equality test on a single partition key
    value.

    The condition can optionally perform one of several comparison tests on
    a single sort key value. This allows `Query` to retrieve one item with a
    given partition key value and sort key value, or several items that have
    the same partition key value but different sort key values.

    The partition key equality test is required, and must be specified in
    the following format:

    `partitionKeyName` *=* `:partitionkeyval`

    If you also want to provide a condition for the sort key, it must be
    combined using `AND` with the condition for the sort key. Following is
    an example, using the **=** comparison operator for the sort key:

    `partitionKeyName` `=` `:partitionkeyval` `AND` `sortKeyName` `=`
    `:sortkeyval`

    Valid comparisons for the sort key condition are as follows:

    - `sortKeyName` `=` `:sortkeyval` - true if the sort key value is equal
      to `:sortkeyval`.

    - `sortKeyName` `<` `:sortkeyval` - true if the sort key value is less
      than `:sortkeyval`.

    - `sortKeyName` `<=` `:sortkeyval` - true if the sort key value is less
      than or equal to `:sortkeyval`.

    - `sortKeyName` `>` `:sortkeyval` - true if the sort key value is
      greater than `:sortkeyval`.

    - `sortKeyName` `>= ` `:sortkeyval` - true if the sort key value is
      greater than or equal to `:sortkeyval`.

    - `sortKeyName` `BETWEEN` `:sortkeyval1` `AND` `:sortkeyval2` - true if
      the sort key value is greater than or equal to `:sortkeyval1`, and
      less than or equal to `:sortkeyval2`.

    - `begins_with (` `sortKeyName`, `:sortkeyval` `)` - true if the sort
      key value begins with a particular operand. (You cannot use this
      function with a sort key that is of type Number.) Note that the
      function name `begins_with` is case-sensitive.

    Use the `ExpressionAttributeValues` parameter to replace tokens such as
    `:partitionval` and `:sortval` with actual values at runtime.

    You can optionally use the `ExpressionAttributeNames` parameter to
    replace the names of the partition key and sort key with placeholder
    tokens. This option might be necessary if an attribute name conflicts
    with a DynamoDB reserved word. For example, the following
    `KeyConditionExpression` parameter causes an error because *Size* is a
    reserved word:

    - `Size = :myval`

    To work around this, define a placeholder (such a `#S`) to represent the
    attribute name *Size*. `KeyConditionExpression` then is as follows:

    - `#S = :myval`

    For a list of reserved words, see [Reserved
    Words](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
    in the *Amazon DynamoDB Developer Guide*.

    For more information on `ExpressionAttributeNames` and
    `ExpressionAttributeValues`, see [Using Placeholders for Attribute Names
    and
    Values](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ExpressionPlaceholders.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_names: dict[str, str] | None = None
    """
    One or more substitution tokens for attribute names in an expression.
    The following are some use cases for using `ExpressionAttributeNames`:

    - To access an attribute whose name conflicts with a DynamoDB reserved
      word.

    - To create a placeholder for repeating occurrences of an attribute name
      in an expression.

    - To prevent special characters in an attribute name from being
      misinterpreted in an expression.

    Use the **#** character in an expression to dereference an attribute
    name. For example, consider the following attribute name:

    - `Percentile`

    The name of this attribute conflicts with a reserved word, so it cannot
    be used directly in an expression. (For the complete list of reserved
    words, see [Reserved
    Words](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
    in the *Amazon DynamoDB Developer Guide*). To work around this, you
    could specify the following for `ExpressionAttributeNames`:

    - `{"#P":"Percentile"}`

    You could then use this substitution in an expression, as in this
    example:

    - `#P = :val`

    Note:
        Tokens that begin with the **:** character are *expression attribute
        values*, which are placeholders for the actual value at runtime.

    For more information on expression attribute names, see [Specifying Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_values: "dict[str, AttributeValue] | None" = None
    """
    One or more values that can be substituted in an expression.

    Use the **:** (colon) character in an expression to dereference an
    attribute value. For example, suppose that you wanted to check whether
    the value of the *ProductStatus* attribute was one of the following:

    `Available | Backordered | Discontinued`

    You would first need to specify `ExpressionAttributeValues` as follows:

    `{ ":avail":{"S":"Available"}, ":back":{"S":"Backordered"}, ":disc":{"S":"Discontinued"} }`

    You could then use these values in an expression, such as this:

    `ProductStatus IN (:avail, :back, :disc)`

    For more information on expression attribute values, see [Specifying
    Conditions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.SpecifyingConditions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_QUERY_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_QUERY_INPUT.members["TableName"], self.table_name
            )

        if self.index_name is not None:
            serializer.write_string(
                _SCHEMA_QUERY_INPUT.members["IndexName"], self.index_name
            )

        if self.select is not None:
            serializer.write_string(_SCHEMA_QUERY_INPUT.members["Select"], self.select)

        if self.attributes_to_get is not None:
            _serialize_attribute_name_list(
                serializer,
                _SCHEMA_QUERY_INPUT.members["AttributesToGet"],
                self.attributes_to_get,
            )

        if self.limit is not None:
            serializer.write_integer(_SCHEMA_QUERY_INPUT.members["Limit"], self.limit)

        if self.consistent_read is not None:
            serializer.write_boolean(
                _SCHEMA_QUERY_INPUT.members["ConsistentRead"], self.consistent_read
            )

        if self.key_conditions is not None:
            _serialize_key_conditions(
                serializer,
                _SCHEMA_QUERY_INPUT.members["KeyConditions"],
                self.key_conditions,
            )

        if self.query_filter is not None:
            _serialize_filter_condition_map(
                serializer,
                _SCHEMA_QUERY_INPUT.members["QueryFilter"],
                self.query_filter,
            )

        if self.conditional_operator is not None:
            serializer.write_string(
                _SCHEMA_QUERY_INPUT.members["ConditionalOperator"],
                self.conditional_operator,
            )

        if self.scan_index_forward is not None:
            serializer.write_boolean(
                _SCHEMA_QUERY_INPUT.members["ScanIndexForward"], self.scan_index_forward
            )

        if self.exclusive_start_key is not None:
            _serialize_key(
                serializer,
                _SCHEMA_QUERY_INPUT.members["ExclusiveStartKey"],
                self.exclusive_start_key,
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_QUERY_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

        if self.projection_expression is not None:
            serializer.write_string(
                _SCHEMA_QUERY_INPUT.members["ProjectionExpression"],
                self.projection_expression,
            )

        if self.filter_expression is not None:
            serializer.write_string(
                _SCHEMA_QUERY_INPUT.members["FilterExpression"], self.filter_expression
            )

        if self.key_condition_expression is not None:
            serializer.write_string(
                _SCHEMA_QUERY_INPUT.members["KeyConditionExpression"],
                self.key_condition_expression,
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_QUERY_INPUT.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

        if self.expression_attribute_values is not None:
            _serialize_expression_attribute_value_map(
                serializer,
                _SCHEMA_QUERY_INPUT.members["ExpressionAttributeValues"],
                self.expression_attribute_values,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_QUERY_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["index_name"] = de.read_string(
                        _SCHEMA_QUERY_INPUT.members["IndexName"]
                    )

                case 2:
                    kwargs["select"] = Select(
                        de.read_string(_SCHEMA_QUERY_INPUT.members["Select"])
                    )

                case 3:
                    kwargs["attributes_to_get"] = _deserialize_attribute_name_list(
                        de, _SCHEMA_QUERY_INPUT.members["AttributesToGet"]
                    )

                case 4:
                    kwargs["limit"] = de.read_integer(
                        _SCHEMA_QUERY_INPUT.members["Limit"]
                    )

                case 5:
                    kwargs["consistent_read"] = de.read_boolean(
                        _SCHEMA_QUERY_INPUT.members["ConsistentRead"]
                    )

                case 6:
                    kwargs["key_conditions"] = _deserialize_key_conditions(
                        de, _SCHEMA_QUERY_INPUT.members["KeyConditions"]
                    )

                case 7:
                    kwargs["query_filter"] = _deserialize_filter_condition_map(
                        de, _SCHEMA_QUERY_INPUT.members["QueryFilter"]
                    )

                case 8:
                    kwargs["conditional_operator"] = ConditionalOperator(
                        de.read_string(
                            _SCHEMA_QUERY_INPUT.members["ConditionalOperator"]
                        )
                    )

                case 9:
                    kwargs["scan_index_forward"] = de.read_boolean(
                        _SCHEMA_QUERY_INPUT.members["ScanIndexForward"]
                    )

                case 10:
                    kwargs["exclusive_start_key"] = _deserialize_key(
                        de, _SCHEMA_QUERY_INPUT.members["ExclusiveStartKey"]
                    )

                case 11:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_QUERY_INPUT.members["ReturnConsumedCapacity"]
                        )
                    )

                case 12:
                    kwargs["projection_expression"] = de.read_string(
                        _SCHEMA_QUERY_INPUT.members["ProjectionExpression"]
                    )

                case 13:
                    kwargs["filter_expression"] = de.read_string(
                        _SCHEMA_QUERY_INPUT.members["FilterExpression"]
                    )

                case 14:
                    kwargs["key_condition_expression"] = de.read_string(
                        _SCHEMA_QUERY_INPUT.members["KeyConditionExpression"]
                    )

                case 15:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de, _SCHEMA_QUERY_INPUT.members["ExpressionAttributeNames"]
                        )
                    )

                case 16:
                    kwargs["expression_attribute_values"] = (
                        _deserialize_expression_attribute_value_map(
                            de, _SCHEMA_QUERY_INPUT.members["ExpressionAttributeValues"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_QUERY_INPUT, consumer=_consumer)
        return kwargs


BATCH_EXECUTE_STATEMENT = APIOperation(
    input=BatchExecuteStatementInput,
    output=BatchExecuteStatementOutput,
    schema=_SCHEMA_BATCH_EXECUTE_STATEMENT,
    input_schema=_SCHEMA_BATCH_EXECUTE_STATEMENT_INPUT,
    output_schema=_SCHEMA_BATCH_EXECUTE_STATEMENT_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_THROTTLING_EXCEPTION,
    ],
)

TRANSACT_GET_ITEMS = APIOperation(
    input=TransactGetItemsInput,
    output=TransactGetItemsOutput,
    schema=_SCHEMA_TRANSACT_GET_ITEMS,
    input_schema=_SCHEMA_TRANSACT_GET_ITEMS_INPUT,
    output_schema=_SCHEMA_TRANSACT_GET_ITEMS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
            ShapeID(
                "com.amazonaws.dynamodb#TransactionCanceledException"
            ): TransactionCanceledException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_TRANSACTION_CANCELED_EXCEPTION,
    ],
)

BATCH_GET_ITEM = APIOperation(
    input=BatchGetItemInput,
    output=BatchGetItemOutput,
    schema=_SCHEMA_BATCH_GET_ITEM,
    input_schema=_SCHEMA_BATCH_GET_ITEM_INPUT,
    output_schema=_SCHEMA_BATCH_GET_ITEM_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class BatchWriteItemOutput:
    """Represents the output of a `BatchWriteItem` operation."""

    unprocessed_items: "dict[str, list[WriteRequest]] | None" = None
    """
    A map of tables and requests against those tables that were not
    processed. The `UnprocessedItems` value is in the same form as
    `RequestItems`, so you can provide this value directly to a subsequent
    `BatchWriteItem` operation. For more information, see `RequestItems` in
    the Request Parameters section.

    Each `UnprocessedItems` entry consists of a table name or table ARN and,
    for that table, a list of operations to perform (`DeleteRequest` or
    `PutRequest`).

    - `DeleteRequest` - Perform a `DeleteItem` operation on the specified
      item. The item to be deleted is identified by a `Key` subelement:

      - `Key` - A map of primary key attribute values that uniquely identify
        the item. Each entry in this map consists of an attribute name and
        an attribute value.

    - `PutRequest` - Perform a `PutItem` operation on the specified item.
      The item to be put is identified by an `Item` subelement:

      - `Item` - A map of attributes and their values. Each entry in this
        map consists of an attribute name and an attribute value. Attribute
        values must not be null; string and binary type attributes must have
        lengths greater than zero; and set type attributes must not be
        empty. Requests that contain empty values will be rejected with a
        `ValidationException` exception.

        If you specify any attributes that are part of an index key, then
        the data types for those attributes must match those of the schema
        in the table's attribute definition.

    If there are no unprocessed items remaining, the response contains an
    empty `UnprocessedItems` map.
    """

    item_collection_metrics: "dict[str, list[ItemCollectionMetrics]] | None" = None
    """
    A list of tables that were processed by `BatchWriteItem` and, for each
    table, information about any item collections that were affected by
    individual `DeleteItem` or `PutItem` operations.

    Each entry consists of the following subelements:

    - `ItemCollectionKey` - The partition key value of the item collection.
      This is the same as the partition key value of the item.

    - `SizeEstimateRangeGB` - An estimate of item collection size, expressed
      in GB. This is a two-element array containing a lower bound and an
      upper bound for the estimate. The estimate includes the size of all
      the items in the table, plus the size of all attributes projected into
      all of the local secondary indexes on the table. Use this estimate to
      measure whether a local secondary index is approaching its size limit.

      The estimate is subject to change over time; therefore, do not rely on
      the precision or accuracy of the estimate.
    """

    consumed_capacity: list[ConsumedCapacity] | None = None
    """
    The capacity units consumed by the entire `BatchWriteItem` operation.

    Each element consists of:

    - `TableName` - The table that consumed the provisioned throughput.

    - `CapacityUnits` - The total number of capacity units consumed.

    If the table has vector indexes, each element also includes a
    `VectorIndexes` field with `VectorWriteRequestBytes` consumed for each
    affected vector index.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_WRITE_ITEM_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.unprocessed_items is not None:
            _serialize_batch_write_item_request_map(
                serializer,
                _SCHEMA_BATCH_WRITE_ITEM_OUTPUT.members["UnprocessedItems"],
                self.unprocessed_items,
            )

        if self.item_collection_metrics is not None:
            _serialize_item_collection_metrics_per_table(
                serializer,
                _SCHEMA_BATCH_WRITE_ITEM_OUTPUT.members["ItemCollectionMetrics"],
                self.item_collection_metrics,
            )

        if self.consumed_capacity is not None:
            _serialize_consumed_capacity_multiple(
                serializer,
                _SCHEMA_BATCH_WRITE_ITEM_OUTPUT.members["ConsumedCapacity"],
                self.consumed_capacity,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["unprocessed_items"] = (
                        _deserialize_batch_write_item_request_map(
                            de,
                            _SCHEMA_BATCH_WRITE_ITEM_OUTPUT.members["UnprocessedItems"],
                        )
                    )

                case 1:
                    kwargs["item_collection_metrics"] = (
                        _deserialize_item_collection_metrics_per_table(
                            de,
                            _SCHEMA_BATCH_WRITE_ITEM_OUTPUT.members[
                                "ItemCollectionMetrics"
                            ],
                        )
                    )

                case 2:
                    kwargs["consumed_capacity"] = (
                        _deserialize_consumed_capacity_multiple(
                            de,
                            _SCHEMA_BATCH_WRITE_ITEM_OUTPUT.members["ConsumedCapacity"],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BATCH_WRITE_ITEM_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class UpdateItemInput:
    """Represents the input of an `UpdateItem` operation."""

    table_name: str | None = None
    """
    The name of the table containing the item to update. You can also
    provide the Amazon Resource Name (ARN) of the table in this parameter.
    """

    key: "dict[str, AttributeValue] | None" = None
    """
    The primary key of the item to be updated. Each element consists of an
    attribute name and a value for that attribute.

    For the primary key, you must provide all of the attributes. For
    example, with a simple primary key, you only need to provide a value for
    the partition key. For a composite primary key, you must provide values
    for both the partition key and the sort key.
    """

    attribute_updates: "dict[str, AttributeValueUpdate] | None" = None
    """
    This is a legacy parameter. Use `UpdateExpression` instead. For more
    information, see
    [AttributeUpdates](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.AttributeUpdates.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expected: "dict[str, ExpectedAttributeValue] | None" = None
    """
    This is a legacy parameter. Use `ConditionExpression` instead. For more
    information, see
    [Expected](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.Expected.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    conditional_operator: ConditionalOperator | None = None
    """
    This is a legacy parameter. Use `ConditionExpression` instead. For more
    information, see
    [ConditionalOperator](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/LegacyConditionalParameters.ConditionalOperator.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    return_values: ReturnValue | None = None
    """
    Use `ReturnValues` if you want to get the item attributes as they appear
    before or after they are successfully updated. For `UpdateItem`, the
    valid values are:

    - `NONE` - If `ReturnValues` is not specified, or if its value is
      `NONE`, then nothing is returned. (This setting is the default for
      `ReturnValues`.)

    - `ALL_OLD` - Returns all of the attributes of the item, as they
      appeared before the UpdateItem operation.

    - `UPDATED_OLD` - Returns only the updated attributes, as they appeared
      before the UpdateItem operation.

    - `ALL_NEW` - Returns all of the attributes of the item, as they appear
      after the UpdateItem operation.

    - `UPDATED_NEW` - Returns only the updated attributes, as they appear
      after the UpdateItem operation.

    There is no additional cost associated with requesting a return value
    aside from the small network and processing overhead of receiving a
    larger response. No read capacity units are consumed.

    The values returned are strongly consistent.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    return_item_collection_metrics: ReturnItemCollectionMetrics | None = None
    """
    Determines whether item collection metrics are returned. If set to
    `SIZE`, the response includes statistics about item collections, if any,
    that were modified during the operation are returned in the response. If
    set to `NONE` (the default), no statistics are returned.
    """

    update_expression: str | None = None
    """
    An expression that defines one or more attributes to be updated, the
    action to be performed on them, and new values for them.

    The following action values are available for `UpdateExpression`.

    - `SET` - Adds one or more attributes and values to an item. If any of
      these attributes already exist, they are replaced by the new values.
      You can also use `SET` to add or subtract from an attribute that is of
      type Number. For example: `SET myNum = myNum + :val`

      `SET` supports the following functions:

      - `if_not_exists (path, operand)` - if the item does not contain an
        attribute at the specified path, then `if_not_exists` evaluates to
        operand; otherwise, it evaluates to path. You can use this function
        to avoid overwriting an attribute that may already be present in the
        item.

      - `list_append (operand, operand)` - evaluates to a list with a new
        element added to it. You can append the new element to the start or
        the end of the list by reversing the order of the operands.

      These function names are case-sensitive.

    - `REMOVE` - Removes one or more attributes from an item.

    - `ADD` - Adds the specified value to the item, if the attribute does
      not already exist. If the attribute does exist, then the behavior of
      `ADD` depends on the data type of the attribute:

      - If the existing attribute is a number, and if `Value` is also a
        number, then `Value` is mathematically added to the existing
        attribute. If `Value` is a negative number, then it is subtracted
        from the existing attribute.

        Note:
        If you use `ADD` to increment or decrement a number value for an
        item that doesn't exist before the update, DynamoDB uses `0` as the
        initial value.

        Similarly, if you use `ADD` for an existing item to increment or
        decrement an attribute value that doesn't exist before the update,
        DynamoDB uses `0` as the initial value. For example, suppose that
        the item you want to update doesn't have an attribute named
        `itemcount`, but you decide to `ADD` the number `3` to this
        attribute anyway. DynamoDB will create the `itemcount` attribute,
        set its initial value to `0`, and finally add `3` to it. The result
        will be a new `itemcount` attribute in the item, with a value of
        `3`.

      - If the existing data type is a set and if `Value` is also a set,
        then `Value` is added to the existing set. For example, if the
        attribute value is the set `[1,2]`, and the `ADD` action specified
        `[3]`, then the final attribute value is `[1,2,3]`. An error occurs
        if an `ADD` action is specified for a set attribute and the
        attribute type specified does not match the existing set type.

        Both sets must have the same primitive data type. For example, if
        the existing data type is a set of strings, the `Value` must also be
        a set of strings.

      Warning:
        The `ADD` action only supports Number and set data types.

    - `DELETE` - Deletes an element from a set.

      If a set of values is specified, then those values are subtracted from
      the old set. For example, if the attribute value was the set `[a,b,c]`
      and the `DELETE` action specifies `[a,c]`, then the final attribute
      value is `[b]`. Specifying an empty set is an error.

      Warning:
        The `DELETE` action only supports set data types.

    You can have many actions in a single expression, such as the following:
    `SET a=:value1, b=:value2 DELETE :value3, :value4, :value5`

    For more information on update expressions, see [Modifying Items and
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.Modifying.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    condition_expression: str | None = None
    """
    A condition that must be satisfied in order for a conditional update to
    succeed.

    An expression can contain any of the following:

    - Functions:
      `attribute_exists | attribute_not_exists | attribute_type | contains | begins_with | size`

      These function names are case-sensitive.

    - Comparison operators: `= | <> | < | > | <= | >= | BETWEEN | IN `

    - Logical operators: `AND | OR | NOT`

    For more information about condition expressions, see [Specifying
    Conditions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.SpecifyingConditions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_names: dict[str, str] | None = None
    """
    One or more substitution tokens for attribute names in an expression.
    The following are some use cases for using `ExpressionAttributeNames`:

    - To access an attribute whose name conflicts with a DynamoDB reserved
      word.

    - To create a placeholder for repeating occurrences of an attribute name
      in an expression.

    - To prevent special characters in an attribute name from being
      misinterpreted in an expression.

    Use the **#** character in an expression to dereference an attribute
    name. For example, consider the following attribute name:

    - `Percentile`

    The name of this attribute conflicts with a reserved word, so it cannot
    be used directly in an expression. (For the complete list of reserved
    words, see [Reserved
    Words](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/ReservedWords.html)
    in the *Amazon DynamoDB Developer Guide*.) To work around this, you
    could specify the following for `ExpressionAttributeNames`:

    - `{"#P":"Percentile"}`

    You could then use this substitution in an expression, as in this
    example:

    - `#P = :val`

    Note:
        Tokens that begin with the **:** character are *expression attribute
        values*, which are placeholders for the actual value at runtime.

    For more information about expression attribute names, see [Specifying
    Item
    Attributes](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.AccessingItemAttributes.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    expression_attribute_values: "dict[str, AttributeValue] | None" = None
    """
    One or more values that can be substituted in an expression.

    Use the **:** (colon) character in an expression to dereference an
    attribute value. For example, suppose that you wanted to check whether
    the value of the `ProductStatus` attribute was one of the following:

    `Available | Backordered | Discontinued`

    You would first need to specify `ExpressionAttributeValues` as follows:

    `{ ":avail":{"S":"Available"}, ":back":{"S":"Backordered"}, ":disc":{"S":"Discontinued"} }`

    You could then use these values in an expression, such as this:

    `ProductStatus IN (:avail, :back, :disc)`

    For more information on expression attribute values, see [Condition
    Expressions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.SpecifyingConditions.html)
    in the *Amazon DynamoDB Developer Guide*.
    """

    return_values_on_condition_check_failure: (
        ReturnValuesOnConditionCheckFailure | None
    ) = None
    """
    An optional parameter that returns the item attributes for an
    `UpdateItem` operation that failed a condition check.

    There is no additional cost associated with requesting a return value
    aside from the small network and processing overhead of receiving a
    larger response. No read capacity units are consumed.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UPDATE_ITEM_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.table_name is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_ITEM_INPUT.members["TableName"], self.table_name
            )

        if self.key is not None:
            _serialize_key(
                serializer, _SCHEMA_UPDATE_ITEM_INPUT.members["Key"], self.key
            )

        if self.attribute_updates is not None:
            _serialize_attribute_updates(
                serializer,
                _SCHEMA_UPDATE_ITEM_INPUT.members["AttributeUpdates"],
                self.attribute_updates,
            )

        if self.expected is not None:
            _serialize_expected_attribute_map(
                serializer, _SCHEMA_UPDATE_ITEM_INPUT.members["Expected"], self.expected
            )

        if self.conditional_operator is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_ITEM_INPUT.members["ConditionalOperator"],
                self.conditional_operator,
            )

        if self.return_values is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_ITEM_INPUT.members["ReturnValues"], self.return_values
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_ITEM_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

        if self.return_item_collection_metrics is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_ITEM_INPUT.members["ReturnItemCollectionMetrics"],
                self.return_item_collection_metrics,
            )

        if self.update_expression is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_ITEM_INPUT.members["UpdateExpression"],
                self.update_expression,
            )

        if self.condition_expression is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_ITEM_INPUT.members["ConditionExpression"],
                self.condition_expression,
            )

        if self.expression_attribute_names is not None:
            _serialize_expression_attribute_name_map(
                serializer,
                _SCHEMA_UPDATE_ITEM_INPUT.members["ExpressionAttributeNames"],
                self.expression_attribute_names,
            )

        if self.expression_attribute_values is not None:
            _serialize_expression_attribute_value_map(
                serializer,
                _SCHEMA_UPDATE_ITEM_INPUT.members["ExpressionAttributeValues"],
                self.expression_attribute_values,
            )

        if self.return_values_on_condition_check_failure is not None:
            serializer.write_string(
                _SCHEMA_UPDATE_ITEM_INPUT.members[
                    "ReturnValuesOnConditionCheckFailure"
                ],
                self.return_values_on_condition_check_failure,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["table_name"] = de.read_string(
                        _SCHEMA_UPDATE_ITEM_INPUT.members["TableName"]
                    )

                case 1:
                    kwargs["key"] = _deserialize_key(
                        de, _SCHEMA_UPDATE_ITEM_INPUT.members["Key"]
                    )

                case 2:
                    kwargs["attribute_updates"] = _deserialize_attribute_updates(
                        de, _SCHEMA_UPDATE_ITEM_INPUT.members["AttributeUpdates"]
                    )

                case 3:
                    kwargs["expected"] = _deserialize_expected_attribute_map(
                        de, _SCHEMA_UPDATE_ITEM_INPUT.members["Expected"]
                    )

                case 4:
                    kwargs["conditional_operator"] = ConditionalOperator(
                        de.read_string(
                            _SCHEMA_UPDATE_ITEM_INPUT.members["ConditionalOperator"]
                        )
                    )

                case 5:
                    kwargs["return_values"] = ReturnValue(
                        de.read_string(
                            _SCHEMA_UPDATE_ITEM_INPUT.members["ReturnValues"]
                        )
                    )

                case 6:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_UPDATE_ITEM_INPUT.members["ReturnConsumedCapacity"]
                        )
                    )

                case 7:
                    kwargs["return_item_collection_metrics"] = (
                        ReturnItemCollectionMetrics(
                            de.read_string(
                                _SCHEMA_UPDATE_ITEM_INPUT.members[
                                    "ReturnItemCollectionMetrics"
                                ]
                            )
                        )
                    )

                case 8:
                    kwargs["update_expression"] = de.read_string(
                        _SCHEMA_UPDATE_ITEM_INPUT.members["UpdateExpression"]
                    )

                case 9:
                    kwargs["condition_expression"] = de.read_string(
                        _SCHEMA_UPDATE_ITEM_INPUT.members["ConditionExpression"]
                    )

                case 10:
                    kwargs["expression_attribute_names"] = (
                        _deserialize_expression_attribute_name_map(
                            de,
                            _SCHEMA_UPDATE_ITEM_INPUT.members[
                                "ExpressionAttributeNames"
                            ],
                        )
                    )

                case 11:
                    kwargs["expression_attribute_values"] = (
                        _deserialize_expression_attribute_value_map(
                            de,
                            _SCHEMA_UPDATE_ITEM_INPUT.members[
                                "ExpressionAttributeValues"
                            ],
                        )
                    )

                case 12:
                    kwargs["return_values_on_condition_check_failure"] = (
                        ReturnValuesOnConditionCheckFailure(
                            de.read_string(
                                _SCHEMA_UPDATE_ITEM_INPUT.members[
                                    "ReturnValuesOnConditionCheckFailure"
                                ]
                            )
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UPDATE_ITEM_INPUT, consumer=_consumer)
        return kwargs


SCAN = APIOperation(
    input=ScanInput,
    output=ScanOutput,
    schema=_SCHEMA_SCAN,
    input_schema=_SCHEMA_SCAN_INPUT,
    output_schema=_SCHEMA_SCAN_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
    ],
)

QUERY = APIOperation(
    input=QueryInput,
    output=QueryOutput,
    schema=_SCHEMA_QUERY,
    input_schema=_SCHEMA_QUERY_INPUT,
    output_schema=_SCHEMA_QUERY_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
    ],
)

DELETE_ITEM = APIOperation(
    input=DeleteItemInput,
    output=DeleteItemOutput,
    schema=_SCHEMA_DELETE_ITEM,
    input_schema=_SCHEMA_DELETE_ITEM_INPUT,
    output_schema=_SCHEMA_DELETE_ITEM_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#ConditionalCheckFailedException"
            ): ConditionalCheckFailedException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ItemCollectionSizeLimitExceededException"
            ): ItemCollectionSizeLimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ReplicatedWriteConflictException"
            ): ReplicatedWriteConflictException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
            ShapeID(
                "com.amazonaws.dynamodb#TransactionConflictException"
            ): TransactionConflictException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REPLICATED_WRITE_CONFLICT_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_TRANSACTION_CONFLICT_EXCEPTION,
    ],
)

PUT_ITEM = APIOperation(
    input=PutItemInput,
    output=PutItemOutput,
    schema=_SCHEMA_PUT_ITEM,
    input_schema=_SCHEMA_PUT_ITEM_INPUT,
    output_schema=_SCHEMA_PUT_ITEM_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#ConditionalCheckFailedException"
            ): ConditionalCheckFailedException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ItemCollectionSizeLimitExceededException"
            ): ItemCollectionSizeLimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ReplicatedWriteConflictException"
            ): ReplicatedWriteConflictException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
            ShapeID(
                "com.amazonaws.dynamodb#TransactionConflictException"
            ): TransactionConflictException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REPLICATED_WRITE_CONFLICT_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_TRANSACTION_CONFLICT_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class TransactWriteItem:
    """
    A list of requests that can perform update, put, delete, or check
    operations on multiple items in one or more tables atomically.
    """

    condition_check: "ConditionCheck | None" = None
    """A request to perform a check item operation."""

    put: "Put | None" = None
    """A request to perform a `PutItem` operation."""

    delete: "Delete | None" = None
    """A request to perform a `DeleteItem` operation."""

    update: "Update | None" = None
    """A request to perform an `UpdateItem` operation."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSACT_WRITE_ITEM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.condition_check is not None:
            serializer.write_struct(
                _SCHEMA_TRANSACT_WRITE_ITEM.members["ConditionCheck"],
                self.condition_check,
            )

        if self.put is not None:
            serializer.write_struct(
                _SCHEMA_TRANSACT_WRITE_ITEM.members["Put"], self.put
            )

        if self.delete is not None:
            serializer.write_struct(
                _SCHEMA_TRANSACT_WRITE_ITEM.members["Delete"], self.delete
            )

        if self.update is not None:
            serializer.write_struct(
                _SCHEMA_TRANSACT_WRITE_ITEM.members["Update"], self.update
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["condition_check"] = ConditionCheck.deserialize(de)

                case 1:
                    kwargs["put"] = Put.deserialize(de)

                case 2:
                    kwargs["delete"] = Delete.deserialize(de)

                case 3:
                    kwargs["update"] = Update.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TRANSACT_WRITE_ITEM, consumer=_consumer)
        return kwargs


UPDATE_ITEM = APIOperation(
    input=UpdateItemInput,
    output=UpdateItemOutput,
    schema=_SCHEMA_UPDATE_ITEM,
    input_schema=_SCHEMA_UPDATE_ITEM_INPUT,
    output_schema=_SCHEMA_UPDATE_ITEM_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#ConditionalCheckFailedException"
            ): ConditionalCheckFailedException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ItemCollectionSizeLimitExceededException"
            ): ItemCollectionSizeLimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ReplicatedWriteConflictException"
            ): ReplicatedWriteConflictException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
            ShapeID(
                "com.amazonaws.dynamodb#TransactionConflictException"
            ): TransactionConflictException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_CONDITIONAL_CHECK_FAILED_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REPLICATED_WRITE_CONFLICT_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_TRANSACTION_CONFLICT_EXCEPTION,
    ],
)

BATCH_WRITE_ITEM = APIOperation(
    input=BatchWriteItemInput,
    output=BatchWriteItemOutput,
    schema=_SCHEMA_BATCH_WRITE_ITEM,
    input_schema=_SCHEMA_BATCH_WRITE_ITEM_INPUT,
    output_schema=_SCHEMA_BATCH_WRITE_ITEM_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ItemCollectionSizeLimitExceededException"
            ): ItemCollectionSizeLimitExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#ReplicatedWriteConflictException"
            ): ReplicatedWriteConflictException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_ITEM_COLLECTION_SIZE_LIMIT_EXCEEDED_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REPLICATED_WRITE_CONFLICT_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
    ],
)


def _serialize_transact_write_item_list(
    serializer: ShapeSerializer, schema: Schema, value: list[TransactWriteItem]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_transact_write_item_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[TransactWriteItem]:
    result: list[TransactWriteItem] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(TransactWriteItem.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class TransactWriteItemsInput:
    """Dataclass for TransactWriteItemsInput structure."""

    transact_items: "list[TransactWriteItem] | None" = None
    """
    An ordered array of up to 100 `TransactWriteItem` objects, each of which
    contains a `ConditionCheck`, `Put`, `Update`, or `Delete` object. These
    can operate on items in different tables, but the tables must reside in
    the same Amazon Web Services account and Region, and no two of them can
    operate on the same item.
    """

    return_consumed_capacity: ReturnConsumedCapacity | None = None
    """
    Determines the level of detail about either provisioned or on-demand
    throughput consumption that is returned in the response:

    - `INDEXES` - The response includes the aggregate `ConsumedCapacity` for
      the operation, together with `ConsumedCapacity` for each table and
      secondary index that was accessed.

      Note that some operations, such as `GetItem` and `BatchGetItem`, do
      not access any indexes at all. In these cases, specifying `INDEXES`
      will only return `ConsumedCapacity` information for table(s).

    - `TOTAL` - The response includes only the aggregate `ConsumedCapacity`
      for the operation.

    - `NONE` - No `ConsumedCapacity` details are included in the response.
    """

    return_item_collection_metrics: ReturnItemCollectionMetrics | None = None
    """
    Determines whether item collection metrics are returned. If set to
    `SIZE`, the response includes statistics about item collections (if
    any), that were modified during the operation and are returned in the
    response. If set to `NONE` (the default), no statistics are returned.
    """

    client_request_token: str | None = None
    """
    Providing a `ClientRequestToken` makes the call to `TransactWriteItems`
    idempotent, meaning that multiple identical calls have the same effect
    as one single call.

    Although multiple identical calls using the same client request token
    produce the same result on the server (no side effects), the responses
    to the calls might not be the same. If the `ReturnConsumedCapacity`
    parameter is set, then the initial `TransactWriteItems` call returns the
    amount of write capacity units consumed in making the changes.
    Subsequent `TransactWriteItems` calls with the same client token return
    the number of read capacity units consumed in reading the item.

    A client request token is valid for 10 minutes after the first request
    that uses it is completed. After 10 minutes, any request with the same
    client token is treated as a new request. Do not resubmit the same
    request with the same client token for more than 10 minutes, or the
    result might not be idempotent.

    If you submit a request with the same client token but a change in other
    parameters within the 10-minute idempotency window, DynamoDB returns an
    `IdempotentParameterMismatch` exception.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSACT_WRITE_ITEMS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.transact_items is not None:
            _serialize_transact_write_item_list(
                serializer,
                _SCHEMA_TRANSACT_WRITE_ITEMS_INPUT.members["TransactItems"],
                self.transact_items,
            )

        if self.return_consumed_capacity is not None:
            serializer.write_string(
                _SCHEMA_TRANSACT_WRITE_ITEMS_INPUT.members["ReturnConsumedCapacity"],
                self.return_consumed_capacity,
            )

        if self.return_item_collection_metrics is not None:
            serializer.write_string(
                _SCHEMA_TRANSACT_WRITE_ITEMS_INPUT.members[
                    "ReturnItemCollectionMetrics"
                ],
                self.return_item_collection_metrics,
            )

        if self.client_request_token is not None:
            serializer.write_string(
                _SCHEMA_TRANSACT_WRITE_ITEMS_INPUT.members["ClientRequestToken"],
                self.client_request_token,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["transact_items"] = _deserialize_transact_write_item_list(
                        de, _SCHEMA_TRANSACT_WRITE_ITEMS_INPUT.members["TransactItems"]
                    )

                case 1:
                    kwargs["return_consumed_capacity"] = ReturnConsumedCapacity(
                        de.read_string(
                            _SCHEMA_TRANSACT_WRITE_ITEMS_INPUT.members[
                                "ReturnConsumedCapacity"
                            ]
                        )
                    )

                case 2:
                    kwargs["return_item_collection_metrics"] = (
                        ReturnItemCollectionMetrics(
                            de.read_string(
                                _SCHEMA_TRANSACT_WRITE_ITEMS_INPUT.members[
                                    "ReturnItemCollectionMetrics"
                                ]
                            )
                        )
                    )

                case 3:
                    kwargs["client_request_token"] = de.read_string(
                        _SCHEMA_TRANSACT_WRITE_ITEMS_INPUT.members["ClientRequestToken"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TRANSACT_WRITE_ITEMS_INPUT, consumer=_consumer)
        return kwargs


TRANSACT_WRITE_ITEMS = APIOperation(
    input=TransactWriteItemsInput,
    output=TransactWriteItemsOutput,
    schema=_SCHEMA_TRANSACT_WRITE_ITEMS,
    input_schema=_SCHEMA_TRANSACT_WRITE_ITEMS_INPUT,
    output_schema=_SCHEMA_TRANSACT_WRITE_ITEMS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.dynamodb#IdempotentParameterMismatchException"
            ): IdempotentParameterMismatchException,
            ShapeID("com.amazonaws.dynamodb#InternalServerError"): InternalServerError,
            ShapeID(
                "com.amazonaws.dynamodb#InvalidEndpointException"
            ): InvalidEndpointException,
            ShapeID(
                "com.amazonaws.dynamodb#ProvisionedThroughputExceededException"
            ): ProvisionedThroughputExceededException,
            ShapeID(
                "com.amazonaws.dynamodb#RequestLimitExceeded"
            ): RequestLimitExceeded,
            ShapeID(
                "com.amazonaws.dynamodb#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.dynamodb#ThrottlingException"): ThrottlingException,
            ShapeID(
                "com.amazonaws.dynamodb#TransactionCanceledException"
            ): TransactionCanceledException,
            ShapeID(
                "com.amazonaws.dynamodb#TransactionInProgressException"
            ): TransactionInProgressException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_IDEMPOTENT_PARAMETER_MISMATCH_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_ERROR,
        _SCHEMA_INVALID_ENDPOINT_EXCEPTION,
        _SCHEMA_PROVISIONED_THROUGHPUT_EXCEEDED_EXCEPTION,
        _SCHEMA_REQUEST_LIMIT_EXCEEDED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_TRANSACTION_CANCELED_EXCEPTION,
        _SCHEMA_TRANSACTION_IN_PROGRESS_EXCEPTION,
    ],
)

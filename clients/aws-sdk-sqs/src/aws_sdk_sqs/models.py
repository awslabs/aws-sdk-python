# Code generated by smithy-python-codegen DO NOT EDIT.

from dataclasses import dataclass
from enum import StrEnum
import logging
from typing import Any, Literal, Self

from smithy_core.deserializers import ShapeDeserializer
from smithy_core.documents import TypeRegistry
from smithy_core.exceptions import ModeledError
from smithy_core.schemas import APIOperation, Schema
from smithy_core.serializers import ShapeSerializer
from smithy_core.shapes import ShapeID
from smithy_core.types import UnknownEnumMixin

from ._private.schemas import (
    ADD_PERMISSION as _SCHEMA_ADD_PERMISSION,
    ADD_PERMISSION_INPUT as _SCHEMA_ADD_PERMISSION_INPUT,
    ADD_PERMISSION_OUTPUT as _SCHEMA_ADD_PERMISSION_OUTPUT,
    BATCH_ENTRY_IDS_NOT_DISTINCT as _SCHEMA_BATCH_ENTRY_IDS_NOT_DISTINCT,
    BATCH_REQUEST_TOO_LONG as _SCHEMA_BATCH_REQUEST_TOO_LONG,
    BATCH_RESULT_ERROR_ENTRY as _SCHEMA_BATCH_RESULT_ERROR_ENTRY,
    CANCEL_MESSAGE_MOVE_TASK as _SCHEMA_CANCEL_MESSAGE_MOVE_TASK,
    CANCEL_MESSAGE_MOVE_TASK_INPUT as _SCHEMA_CANCEL_MESSAGE_MOVE_TASK_INPUT,
    CANCEL_MESSAGE_MOVE_TASK_OUTPUT as _SCHEMA_CANCEL_MESSAGE_MOVE_TASK_OUTPUT,
    CHANGE_MESSAGE_VISIBILITY as _SCHEMA_CHANGE_MESSAGE_VISIBILITY,
    CHANGE_MESSAGE_VISIBILITY_BATCH as _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH,
    CHANGE_MESSAGE_VISIBILITY_BATCH_INPUT as _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_INPUT,
    CHANGE_MESSAGE_VISIBILITY_BATCH_OUTPUT as _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_OUTPUT,
    CHANGE_MESSAGE_VISIBILITY_BATCH_REQUEST_ENTRY as _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_REQUEST_ENTRY,
    CHANGE_MESSAGE_VISIBILITY_BATCH_RESULT_ENTRY as _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_RESULT_ENTRY,
    CHANGE_MESSAGE_VISIBILITY_INPUT as _SCHEMA_CHANGE_MESSAGE_VISIBILITY_INPUT,
    CHANGE_MESSAGE_VISIBILITY_OUTPUT as _SCHEMA_CHANGE_MESSAGE_VISIBILITY_OUTPUT,
    CREATE_QUEUE as _SCHEMA_CREATE_QUEUE,
    CREATE_QUEUE_INPUT as _SCHEMA_CREATE_QUEUE_INPUT,
    CREATE_QUEUE_OUTPUT as _SCHEMA_CREATE_QUEUE_OUTPUT,
    DELETE_MESSAGE as _SCHEMA_DELETE_MESSAGE,
    DELETE_MESSAGE_BATCH as _SCHEMA_DELETE_MESSAGE_BATCH,
    DELETE_MESSAGE_BATCH_INPUT as _SCHEMA_DELETE_MESSAGE_BATCH_INPUT,
    DELETE_MESSAGE_BATCH_OUTPUT as _SCHEMA_DELETE_MESSAGE_BATCH_OUTPUT,
    DELETE_MESSAGE_BATCH_REQUEST_ENTRY as _SCHEMA_DELETE_MESSAGE_BATCH_REQUEST_ENTRY,
    DELETE_MESSAGE_BATCH_RESULT_ENTRY as _SCHEMA_DELETE_MESSAGE_BATCH_RESULT_ENTRY,
    DELETE_MESSAGE_INPUT as _SCHEMA_DELETE_MESSAGE_INPUT,
    DELETE_MESSAGE_OUTPUT as _SCHEMA_DELETE_MESSAGE_OUTPUT,
    DELETE_QUEUE as _SCHEMA_DELETE_QUEUE,
    DELETE_QUEUE_INPUT as _SCHEMA_DELETE_QUEUE_INPUT,
    DELETE_QUEUE_OUTPUT as _SCHEMA_DELETE_QUEUE_OUTPUT,
    EMPTY_BATCH_REQUEST as _SCHEMA_EMPTY_BATCH_REQUEST,
    GET_QUEUE_ATTRIBUTES as _SCHEMA_GET_QUEUE_ATTRIBUTES,
    GET_QUEUE_ATTRIBUTES_INPUT as _SCHEMA_GET_QUEUE_ATTRIBUTES_INPUT,
    GET_QUEUE_ATTRIBUTES_OUTPUT as _SCHEMA_GET_QUEUE_ATTRIBUTES_OUTPUT,
    GET_QUEUE_URL as _SCHEMA_GET_QUEUE_URL,
    GET_QUEUE_URL_INPUT as _SCHEMA_GET_QUEUE_URL_INPUT,
    GET_QUEUE_URL_OUTPUT as _SCHEMA_GET_QUEUE_URL_OUTPUT,
    INVALID_ADDRESS as _SCHEMA_INVALID_ADDRESS,
    INVALID_ATTRIBUTE_NAME as _SCHEMA_INVALID_ATTRIBUTE_NAME,
    INVALID_ATTRIBUTE_VALUE as _SCHEMA_INVALID_ATTRIBUTE_VALUE,
    INVALID_BATCH_ENTRY_ID as _SCHEMA_INVALID_BATCH_ENTRY_ID,
    INVALID_ID_FORMAT as _SCHEMA_INVALID_ID_FORMAT,
    INVALID_MESSAGE_CONTENTS as _SCHEMA_INVALID_MESSAGE_CONTENTS,
    INVALID_SECURITY as _SCHEMA_INVALID_SECURITY,
    KMS_ACCESS_DENIED as _SCHEMA_KMS_ACCESS_DENIED,
    KMS_DISABLED as _SCHEMA_KMS_DISABLED,
    KMS_INVALID_KEY_USAGE as _SCHEMA_KMS_INVALID_KEY_USAGE,
    KMS_INVALID_STATE as _SCHEMA_KMS_INVALID_STATE,
    KMS_NOT_FOUND as _SCHEMA_KMS_NOT_FOUND,
    KMS_OPT_IN_REQUIRED as _SCHEMA_KMS_OPT_IN_REQUIRED,
    KMS_THROTTLED as _SCHEMA_KMS_THROTTLED,
    LIST_DEAD_LETTER_SOURCE_QUEUES as _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES,
    LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT as _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT,
    LIST_DEAD_LETTER_SOURCE_QUEUES_OUTPUT as _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_OUTPUT,
    LIST_MESSAGE_MOVE_TASKS as _SCHEMA_LIST_MESSAGE_MOVE_TASKS,
    LIST_MESSAGE_MOVE_TASKS_INPUT as _SCHEMA_LIST_MESSAGE_MOVE_TASKS_INPUT,
    LIST_MESSAGE_MOVE_TASKS_OUTPUT as _SCHEMA_LIST_MESSAGE_MOVE_TASKS_OUTPUT,
    LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY as _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY,
    LIST_QUEUES as _SCHEMA_LIST_QUEUES,
    LIST_QUEUES_INPUT as _SCHEMA_LIST_QUEUES_INPUT,
    LIST_QUEUES_OUTPUT as _SCHEMA_LIST_QUEUES_OUTPUT,
    LIST_QUEUE_TAGS as _SCHEMA_LIST_QUEUE_TAGS,
    LIST_QUEUE_TAGS_INPUT as _SCHEMA_LIST_QUEUE_TAGS_INPUT,
    LIST_QUEUE_TAGS_OUTPUT as _SCHEMA_LIST_QUEUE_TAGS_OUTPUT,
    MESSAGE as _SCHEMA_MESSAGE,
    MESSAGE_ATTRIBUTE_VALUE as _SCHEMA_MESSAGE_ATTRIBUTE_VALUE,
    MESSAGE_NOT_INFLIGHT as _SCHEMA_MESSAGE_NOT_INFLIGHT,
    MESSAGE_SYSTEM_ATTRIBUTE_VALUE as _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE,
    OVER_LIMIT as _SCHEMA_OVER_LIMIT,
    PURGE_QUEUE as _SCHEMA_PURGE_QUEUE,
    PURGE_QUEUE_INPUT as _SCHEMA_PURGE_QUEUE_INPUT,
    PURGE_QUEUE_IN_PROGRESS as _SCHEMA_PURGE_QUEUE_IN_PROGRESS,
    PURGE_QUEUE_OUTPUT as _SCHEMA_PURGE_QUEUE_OUTPUT,
    QUEUE_DELETED_RECENTLY as _SCHEMA_QUEUE_DELETED_RECENTLY,
    QUEUE_DOES_NOT_EXIST as _SCHEMA_QUEUE_DOES_NOT_EXIST,
    QUEUE_NAME_EXISTS as _SCHEMA_QUEUE_NAME_EXISTS,
    RECEIPT_HANDLE_IS_INVALID as _SCHEMA_RECEIPT_HANDLE_IS_INVALID,
    RECEIVE_MESSAGE as _SCHEMA_RECEIVE_MESSAGE,
    RECEIVE_MESSAGE_INPUT as _SCHEMA_RECEIVE_MESSAGE_INPUT,
    RECEIVE_MESSAGE_OUTPUT as _SCHEMA_RECEIVE_MESSAGE_OUTPUT,
    REMOVE_PERMISSION as _SCHEMA_REMOVE_PERMISSION,
    REMOVE_PERMISSION_INPUT as _SCHEMA_REMOVE_PERMISSION_INPUT,
    REMOVE_PERMISSION_OUTPUT as _SCHEMA_REMOVE_PERMISSION_OUTPUT,
    REQUEST_THROTTLED as _SCHEMA_REQUEST_THROTTLED,
    RESOURCE_NOT_FOUND_EXCEPTION as _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    SEND_MESSAGE as _SCHEMA_SEND_MESSAGE,
    SEND_MESSAGE_BATCH as _SCHEMA_SEND_MESSAGE_BATCH,
    SEND_MESSAGE_BATCH_INPUT as _SCHEMA_SEND_MESSAGE_BATCH_INPUT,
    SEND_MESSAGE_BATCH_OUTPUT as _SCHEMA_SEND_MESSAGE_BATCH_OUTPUT,
    SEND_MESSAGE_BATCH_REQUEST_ENTRY as _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY,
    SEND_MESSAGE_BATCH_RESULT_ENTRY as _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY,
    SEND_MESSAGE_INPUT as _SCHEMA_SEND_MESSAGE_INPUT,
    SEND_MESSAGE_OUTPUT as _SCHEMA_SEND_MESSAGE_OUTPUT,
    SET_QUEUE_ATTRIBUTES as _SCHEMA_SET_QUEUE_ATTRIBUTES,
    SET_QUEUE_ATTRIBUTES_INPUT as _SCHEMA_SET_QUEUE_ATTRIBUTES_INPUT,
    SET_QUEUE_ATTRIBUTES_OUTPUT as _SCHEMA_SET_QUEUE_ATTRIBUTES_OUTPUT,
    START_MESSAGE_MOVE_TASK as _SCHEMA_START_MESSAGE_MOVE_TASK,
    START_MESSAGE_MOVE_TASK_INPUT as _SCHEMA_START_MESSAGE_MOVE_TASK_INPUT,
    START_MESSAGE_MOVE_TASK_OUTPUT as _SCHEMA_START_MESSAGE_MOVE_TASK_OUTPUT,
    TAG_QUEUE as _SCHEMA_TAG_QUEUE,
    TAG_QUEUE_INPUT as _SCHEMA_TAG_QUEUE_INPUT,
    TAG_QUEUE_OUTPUT as _SCHEMA_TAG_QUEUE_OUTPUT,
    TOO_MANY_ENTRIES_IN_BATCH_REQUEST as _SCHEMA_TOO_MANY_ENTRIES_IN_BATCH_REQUEST,
    UNSUPPORTED_OPERATION as _SCHEMA_UNSUPPORTED_OPERATION,
    UNTAG_QUEUE as _SCHEMA_UNTAG_QUEUE,
    UNTAG_QUEUE_INPUT as _SCHEMA_UNTAG_QUEUE_INPUT,
    UNTAG_QUEUE_OUTPUT as _SCHEMA_UNTAG_QUEUE_OUTPUT,
)


logger = logging.getLogger(__name__)


class ServiceError(ModeledError):
    """
    Base error for all errors in the service.

    Some exceptions do not extend from this class, including
    synthetic, implicit, and shared exception types.
    """


def _serialize_action_name_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_action_name_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_aws_account_id_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_aws_account_id_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class AddPermissionInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue to which permissions are added.

    Queue URLs and names are case-sensitive.
    """

    label: str | None = None
    """
    The unique identification of the permission you're setting (for
    example, `AliceSendMessage`). Maximum 80 characters. Allowed characters
    include alphanumeric characters, hyphens (`-`), and underscores (`_`).
    """

    aws_account_ids: list[str] | None = None
    """
    The Amazon Web Services account numbers of the
    [principals](https://docs.aws.amazon.com/general/latest/gr/glos-chap.html#P)
    who are to receive permission. For information about locating the Amazon
    Web Services account identification, see [Your Amazon Web Services
    Identifiers](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-making-api-requests.html#sqs-api-request-authentication)
    in the *Amazon SQS Developer Guide*.
    """

    actions: list[str] | None = None
    """
    The action the client wants to allow for the specified principal. Valid
    values: the name of any action or `*`.

    For more information about these actions, see [Overview of Managing
    Access Permissions to Your Amazon Simple Queue Service
    Resource](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-overview-of-managing-access.html)
    in the *Amazon SQS Developer Guide*.

    Specifying `SendMessage`, `DeleteMessage`, or `ChangeMessageVisibility`
    for `ActionName.n` also grants permissions for the corresponding batch
    versions of those actions: `SendMessageBatch`, `DeleteMessageBatch`, and
    `ChangeMessageVisibilityBatch`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ADD_PERMISSION_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_ADD_PERMISSION_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.label is not None:
            serializer.write_string(
                _SCHEMA_ADD_PERMISSION_INPUT.members["Label"], self.label
            )

        if self.aws_account_ids is not None:
            _serialize_aws_account_id_list(
                serializer,
                _SCHEMA_ADD_PERMISSION_INPUT.members["AWSAccountIds"],
                self.aws_account_ids,
            )

        if self.actions is not None:
            _serialize_action_name_list(
                serializer,
                _SCHEMA_ADD_PERMISSION_INPUT.members["Actions"],
                self.actions,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_ADD_PERMISSION_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["label"] = de.read_string(
                        _SCHEMA_ADD_PERMISSION_INPUT.members["Label"]
                    )

                case 2:
                    kwargs["aws_account_ids"] = _deserialize_aws_account_id_list(
                        de, _SCHEMA_ADD_PERMISSION_INPUT.members["AWSAccountIds"]
                    )

                case 3:
                    kwargs["actions"] = _deserialize_action_name_list(
                        de, _SCHEMA_ADD_PERMISSION_INPUT.members["Actions"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ADD_PERMISSION_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class AddPermissionOutput:
    """Dataclass for AddPermissionOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ADD_PERMISSION_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ADD_PERMISSION_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class InvalidAddress(ServiceError):
    """The specified ID is invalid."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_ADDRESS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_ADDRESS.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_ADDRESS.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INVALID_ADDRESS, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class InvalidSecurity(ServiceError):
    """The request was not made over HTTPS or did not use SigV4 for signing."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_SECURITY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_SECURITY.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_SECURITY.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INVALID_SECURITY, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class OverLimit(ServiceError):
    """
    The specified action violates a limit. For example, `ReceiveMessage`
    returns this error if the maximum number of in flight messages is
    reached and `AddPermission` returns this error if the maximum number of
    permissions for the queue is reached.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_OVER_LIMIT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(_SCHEMA_OVER_LIMIT.members["message"], self.message)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_OVER_LIMIT.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_OVER_LIMIT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class QueueDoesNotExist(ServiceError):
    """
    Ensure that the `QueueUrl` is correct and that the queue has not been
    deleted.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_QUEUE_DOES_NOT_EXIST, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_QUEUE_DOES_NOT_EXIST.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_QUEUE_DOES_NOT_EXIST.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_QUEUE_DOES_NOT_EXIST, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class RequestThrottled(ServiceError):
    """
    The request was denied due to request throttling.

    - Exceeds the permitted request rate for the queue or for the recipient
      of the request.

    - Ensure that the request rate is within the Amazon SQS limits for
      sending messages. For more information, see [Amazon SQS
      quotas](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-quotas.html#quotas-requests)
      in the *Amazon SQS Developer Guide*.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REQUEST_THROTTLED, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_REQUEST_THROTTLED.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_REQUEST_THROTTLED.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_REQUEST_THROTTLED, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class UnsupportedOperation(ServiceError):
    """Error code 400. Unsupported operation."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UNSUPPORTED_OPERATION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_UNSUPPORTED_OPERATION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_UNSUPPORTED_OPERATION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UNSUPPORTED_OPERATION, consumer=_consumer)
        return kwargs


ADD_PERMISSION = APIOperation(
    input=AddPermissionInput,
    output=AddPermissionOutput,
    schema=_SCHEMA_ADD_PERMISSION,
    input_schema=_SCHEMA_ADD_PERMISSION_INPUT,
    output_schema=_SCHEMA_ADD_PERMISSION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#OverLimit"): OverLimit,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_OVER_LIMIT,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class CancelMessageMoveTaskInput:
    """Dataclass for CancelMessageMoveTaskInput structure."""

    task_handle: str | None = None
    """An identifier associated with a message movement task."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CANCEL_MESSAGE_MOVE_TASK_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.task_handle is not None:
            serializer.write_string(
                _SCHEMA_CANCEL_MESSAGE_MOVE_TASK_INPUT.members["TaskHandle"],
                self.task_handle,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["task_handle"] = de.read_string(
                        _SCHEMA_CANCEL_MESSAGE_MOVE_TASK_INPUT.members["TaskHandle"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CANCEL_MESSAGE_MOVE_TASK_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class CancelMessageMoveTaskOutput:
    """Dataclass for CancelMessageMoveTaskOutput structure."""

    approximate_number_of_messages_moved: int = 0
    """
    The approximate number of messages already moved to the destination
    queue.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CANCEL_MESSAGE_MOVE_TASK_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_long(
            _SCHEMA_CANCEL_MESSAGE_MOVE_TASK_OUTPUT.members[
                "ApproximateNumberOfMessagesMoved"
            ],
            self.approximate_number_of_messages_moved,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["approximate_number_of_messages_moved"] = de.read_long(
                        _SCHEMA_CANCEL_MESSAGE_MOVE_TASK_OUTPUT.members[
                            "ApproximateNumberOfMessagesMoved"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CANCEL_MESSAGE_MOVE_TASK_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ResourceNotFoundException(ServiceError):
    """One or more specified resources don't exist."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION, consumer=_consumer
        )
        return kwargs


CANCEL_MESSAGE_MOVE_TASK = APIOperation(
    input=CancelMessageMoveTaskInput,
    output=CancelMessageMoveTaskOutput,
    schema=_SCHEMA_CANCEL_MESSAGE_MOVE_TASK,
    input_schema=_SCHEMA_CANCEL_MESSAGE_MOVE_TASK_INPUT,
    output_schema=_SCHEMA_CANCEL_MESSAGE_MOVE_TASK_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID(
                "com.amazonaws.sqs#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class ChangeMessageVisibilityInput:
    """Dataclass for ChangeMessageVisibilityInput structure."""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue whose message's visibility is changed.

    Queue URLs and names are case-sensitive.
    """

    receipt_handle: str | None = None
    """
    The receipt handle associated with the message, whose visibility timeout
    is changed. This parameter is returned by the ` ``ReceiveMessage`` `
    action.
    """

    visibility_timeout: int | None = None
    """
    The new value for the message's visibility timeout (in seconds). Values
    range: `0` to `43200`. Maximum: 12 hours.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CHANGE_MESSAGE_VISIBILITY_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_CHANGE_MESSAGE_VISIBILITY_INPUT.members["QueueUrl"],
                self.queue_url,
            )

        if self.receipt_handle is not None:
            serializer.write_string(
                _SCHEMA_CHANGE_MESSAGE_VISIBILITY_INPUT.members["ReceiptHandle"],
                self.receipt_handle,
            )

        if self.visibility_timeout is not None:
            serializer.write_integer(
                _SCHEMA_CHANGE_MESSAGE_VISIBILITY_INPUT.members["VisibilityTimeout"],
                self.visibility_timeout,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_CHANGE_MESSAGE_VISIBILITY_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["receipt_handle"] = de.read_string(
                        _SCHEMA_CHANGE_MESSAGE_VISIBILITY_INPUT.members["ReceiptHandle"]
                    )

                case 2:
                    kwargs["visibility_timeout"] = de.read_integer(
                        _SCHEMA_CHANGE_MESSAGE_VISIBILITY_INPUT.members[
                            "VisibilityTimeout"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ChangeMessageVisibilityOutput:
    """Dataclass for ChangeMessageVisibilityOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CHANGE_MESSAGE_VISIBILITY_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_OUTPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class MessageNotInflight(ServiceError):
    """The specified message isn't in flight."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_MESSAGE_NOT_INFLIGHT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_MESSAGE_NOT_INFLIGHT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ReceiptHandleIsInvalid(ServiceError):
    """The specified receipt handle isn't valid."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RECEIPT_HANDLE_IS_INVALID, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_RECEIPT_HANDLE_IS_INVALID.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_RECEIPT_HANDLE_IS_INVALID.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RECEIPT_HANDLE_IS_INVALID, consumer=_consumer)
        return kwargs


CHANGE_MESSAGE_VISIBILITY = APIOperation(
    input=ChangeMessageVisibilityInput,
    output=ChangeMessageVisibilityOutput,
    schema=_SCHEMA_CHANGE_MESSAGE_VISIBILITY,
    input_schema=_SCHEMA_CHANGE_MESSAGE_VISIBILITY_INPUT,
    output_schema=_SCHEMA_CHANGE_MESSAGE_VISIBILITY_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#MessageNotInflight"): MessageNotInflight,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#ReceiptHandleIsInvalid"): ReceiptHandleIsInvalid,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_MESSAGE_NOT_INFLIGHT,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_RECEIPT_HANDLE_IS_INVALID,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class BatchEntryIdsNotDistinct(ServiceError):
    """Two or more batch entries in the request have the same `Id`."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_ENTRY_IDS_NOT_DISTINCT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_BATCH_ENTRY_IDS_NOT_DISTINCT.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_BATCH_ENTRY_IDS_NOT_DISTINCT.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_BATCH_ENTRY_IDS_NOT_DISTINCT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ChangeMessageVisibilityBatchRequestEntry:
    """
    Encloses a receipt handle and an entry ID for each message in
    ` ``ChangeMessageVisibilityBatch``.`
    """

    id: str
    """
    An identifier for this particular receipt handle used to communicate the
    result.

    Note:
        The `Id`s of a batch request need to be unique within a request.

        This identifier can have up to 80 characters. The following characters
        are accepted: alphanumeric characters, hyphens(-), and underscores (_).
    """

    receipt_handle: str
    """A receipt handle."""

    visibility_timeout: int | None = None
    """The new value (in seconds) for the message's visibility timeout."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_REQUEST_ENTRY, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_REQUEST_ENTRY.members["Id"], self.id
        )
        serializer.write_string(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_REQUEST_ENTRY.members[
                "ReceiptHandle"
            ],
            self.receipt_handle,
        )
        if self.visibility_timeout is not None:
            serializer.write_integer(
                _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_REQUEST_ENTRY.members[
                    "VisibilityTimeout"
                ],
                self.visibility_timeout,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["id"] = de.read_string(
                        _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_REQUEST_ENTRY.members[
                            "Id"
                        ]
                    )

                case 1:
                    kwargs["receipt_handle"] = de.read_string(
                        _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_REQUEST_ENTRY.members[
                            "ReceiptHandle"
                        ]
                    )

                case 2:
                    kwargs["visibility_timeout"] = de.read_integer(
                        _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_REQUEST_ENTRY.members[
                            "VisibilityTimeout"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_REQUEST_ENTRY, consumer=_consumer
        )
        if "id" not in kwargs:
            kwargs["id"] = ""
        if "receipt_handle" not in kwargs:
            kwargs["receipt_handle"] = ""
        return kwargs


def _serialize_change_message_visibility_batch_request_entry_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[ChangeMessageVisibilityBatchRequestEntry],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_change_message_visibility_batch_request_entry_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ChangeMessageVisibilityBatchRequestEntry]:
    result: list[ChangeMessageVisibilityBatchRequestEntry] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ChangeMessageVisibilityBatchRequestEntry.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ChangeMessageVisibilityBatchInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue whose messages' visibility is changed.

    Queue URLs and names are case-sensitive.
    """

    entries: list[ChangeMessageVisibilityBatchRequestEntry] | None = None
    """
    Lists the receipt handles of the messages for which the visibility
    timeout must be changed.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_INPUT.members["QueueUrl"],
                self.queue_url,
            )

        if self.entries is not None:
            _serialize_change_message_visibility_batch_request_entry_list(
                serializer,
                _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_INPUT.members["Entries"],
                self.entries,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_INPUT.members[
                            "QueueUrl"
                        ]
                    )

                case 1:
                    kwargs["entries"] = (
                        _deserialize_change_message_visibility_batch_request_entry_list(
                            de,
                            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_INPUT.members[
                                "Entries"
                            ],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class BatchResultErrorEntry:
    """
    Gives a detailed description of the result of an action on each entry in
    the request.
    """

    id: str
    """The `Id` of an entry in a batch request."""

    code: str
    """An error code representing why the action failed on this entry."""

    sender_fault: bool = False
    """
    Specifies whether the error happened due to the caller of the batch API
    action.
    """

    message: str | None = None
    """A message explaining why the action failed on this entry."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_RESULT_ERROR_ENTRY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_BATCH_RESULT_ERROR_ENTRY.members["Id"], self.id)
        serializer.write_boolean(
            _SCHEMA_BATCH_RESULT_ERROR_ENTRY.members["SenderFault"], self.sender_fault
        )
        serializer.write_string(
            _SCHEMA_BATCH_RESULT_ERROR_ENTRY.members["Code"], self.code
        )
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_BATCH_RESULT_ERROR_ENTRY.members["Message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["id"] = de.read_string(
                        _SCHEMA_BATCH_RESULT_ERROR_ENTRY.members["Id"]
                    )

                case 1:
                    kwargs["sender_fault"] = de.read_boolean(
                        _SCHEMA_BATCH_RESULT_ERROR_ENTRY.members["SenderFault"]
                    )

                case 2:
                    kwargs["code"] = de.read_string(
                        _SCHEMA_BATCH_RESULT_ERROR_ENTRY.members["Code"]
                    )

                case 3:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_BATCH_RESULT_ERROR_ENTRY.members["Message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BATCH_RESULT_ERROR_ENTRY, consumer=_consumer)
        if "id" not in kwargs:
            kwargs["id"] = ""
        if "code" not in kwargs:
            kwargs["code"] = ""
        return kwargs


def _serialize_batch_result_error_entry_list(
    serializer: ShapeSerializer, schema: Schema, value: list[BatchResultErrorEntry]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_batch_result_error_entry_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[BatchResultErrorEntry]:
    result: list[BatchResultErrorEntry] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(BatchResultErrorEntry.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ChangeMessageVisibilityBatchResultEntry:
    """Encloses the `Id` of an entry in ` ``ChangeMessageVisibilityBatch``.`"""

    id: str
    """
    Represents a message whose visibility timeout has been changed
    successfully.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_RESULT_ENTRY, self
        )

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_RESULT_ENTRY.members["Id"], self.id
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["id"] = de.read_string(
                        _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_RESULT_ENTRY.members[
                            "Id"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_RESULT_ENTRY, consumer=_consumer
        )
        if "id" not in kwargs:
            kwargs["id"] = ""
        return kwargs


def _serialize_change_message_visibility_batch_result_entry_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[ChangeMessageVisibilityBatchResultEntry],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_change_message_visibility_batch_result_entry_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ChangeMessageVisibilityBatchResultEntry]:
    result: list[ChangeMessageVisibilityBatchResultEntry] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ChangeMessageVisibilityBatchResultEntry.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ChangeMessageVisibilityBatchOutput:
    """
    For each message in the batch, the response contains a
    ` ``ChangeMessageVisibilityBatchResultEntry`` ` tag if the message
    succeeds or a ` ``BatchResultErrorEntry`` ` tag if the message fails.
    """

    successful: list[ChangeMessageVisibilityBatchResultEntry]
    """A list of ` ``ChangeMessageVisibilityBatchResultEntry`` ` items."""

    failed: list[BatchResultErrorEntry]
    """A list of ` ``BatchResultErrorEntry`` ` items."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_change_message_visibility_batch_result_entry_list(
            serializer,
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_OUTPUT.members["Successful"],
            self.successful,
        )
        _serialize_batch_result_error_entry_list(
            serializer,
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_OUTPUT.members["Failed"],
            self.failed,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["successful"] = (
                        _deserialize_change_message_visibility_batch_result_entry_list(
                            de,
                            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_OUTPUT.members[
                                "Successful"
                            ],
                        )
                    )

                case 1:
                    kwargs["failed"] = _deserialize_batch_result_error_entry_list(
                        de,
                        _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_OUTPUT.members[
                            "Failed"
                        ],
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_OUTPUT, consumer=_consumer
        )
        if "successful" not in kwargs:
            kwargs["successful"] = []
        if "failed" not in kwargs:
            kwargs["failed"] = []
        return kwargs


@dataclass(kw_only=True)
class EmptyBatchRequest(ServiceError):
    """The batch request doesn't contain any entries."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_EMPTY_BATCH_REQUEST, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_EMPTY_BATCH_REQUEST.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_EMPTY_BATCH_REQUEST.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_EMPTY_BATCH_REQUEST, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class InvalidBatchEntryId(ServiceError):
    """
    The `Id` of a batch entry in a batch request doesn't abide by the
    specification.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_BATCH_ENTRY_ID, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_BATCH_ENTRY_ID.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_BATCH_ENTRY_ID.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INVALID_BATCH_ENTRY_ID, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class TooManyEntriesInBatchRequest(ServiceError):
    """
    The batch request contains more entries than permissible. For Amazon
    SQS, the maximum number of entries you can include in a single
    [SendMessageBatch](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/APIReference/API_SendMessageBatch.html),
    [DeleteMessageBatch](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/APIReference/API_DeleteMessageBatch.html),
    or
    [ChangeMessageVisibilityBatch](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/APIReference/API_ChangeMessageVisibilityBatch.html)
    request is 10.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TOO_MANY_ENTRIES_IN_BATCH_REQUEST, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_TOO_MANY_ENTRIES_IN_BATCH_REQUEST.members["message"],
                self.message,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_TOO_MANY_ENTRIES_IN_BATCH_REQUEST.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_TOO_MANY_ENTRIES_IN_BATCH_REQUEST, consumer=_consumer
        )
        return kwargs


CHANGE_MESSAGE_VISIBILITY_BATCH = APIOperation(
    input=ChangeMessageVisibilityBatchInput,
    output=ChangeMessageVisibilityBatchOutput,
    schema=_SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH,
    input_schema=_SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_INPUT,
    output_schema=_SCHEMA_CHANGE_MESSAGE_VISIBILITY_BATCH_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.sqs#BatchEntryIdsNotDistinct"
            ): BatchEntryIdsNotDistinct,
            ShapeID("com.amazonaws.sqs#EmptyBatchRequest"): EmptyBatchRequest,
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidBatchEntryId"): InvalidBatchEntryId,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID(
                "com.amazonaws.sqs#TooManyEntriesInBatchRequest"
            ): TooManyEntriesInBatchRequest,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_BATCH_ENTRY_IDS_NOT_DISTINCT,
        _SCHEMA_EMPTY_BATCH_REQUEST,
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_BATCH_ENTRY_ID,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_TOO_MANY_ENTRIES_IN_BATCH_REQUEST,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


class QueueAttributeName(UnknownEnumMixin, StrEnum):
    ALL = "All"
    POLICY = "Policy"
    VISIBILITY_TIMEOUT = "VisibilityTimeout"
    MAXIMUM_MESSAGE_SIZE = "MaximumMessageSize"
    MESSAGE_RETENTION_PERIOD = "MessageRetentionPeriod"
    APPROXIMATE_NUMBER_OF_MESSAGES = "ApproximateNumberOfMessages"
    APPROXIMATE_NUMBER_OF_MESSAGES_NOT_VISIBLE = "ApproximateNumberOfMessagesNotVisible"
    CREATED_TIMESTAMP = "CreatedTimestamp"
    LAST_MODIFIED_TIMESTAMP = "LastModifiedTimestamp"
    QUEUE_ARN = "QueueArn"
    APPROXIMATE_NUMBER_OF_MESSAGES_DELAYED = "ApproximateNumberOfMessagesDelayed"
    DELAY_SECONDS = "DelaySeconds"
    RECEIVE_MESSAGE_WAIT_TIME_SECONDS = "ReceiveMessageWaitTimeSeconds"
    REDRIVE_POLICY = "RedrivePolicy"
    FIFO_QUEUE = "FifoQueue"
    CONTENT_BASED_DEDUPLICATION = "ContentBasedDeduplication"
    KMS_MASTER_KEY_ID = "KmsMasterKeyId"
    KMS_DATA_KEY_REUSE_PERIOD_SECONDS = "KmsDataKeyReusePeriodSeconds"
    DEDUPLICATION_SCOPE = "DeduplicationScope"
    FIFO_THROUGHPUT_LIMIT = "FifoThroughputLimit"
    REDRIVE_ALLOW_POLICY = "RedriveAllowPolicy"
    SQS_MANAGED_SSE_ENABLED = "SqsManagedSseEnabled"


def _serialize_queue_attribute_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, str]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_string(value_schema, v))


def _deserialize_queue_attribute_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, str]:
    result: dict[str, str] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = d.read_string(value_schema)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_tag_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, str]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_string(value_schema, v))


def _deserialize_tag_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, str]:
    result: dict[str, str] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = d.read_string(value_schema)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class CreateQueueInput:
    """"""

    queue_name: str | None = None
    """
    The name of the new queue. The following limits apply to this name:

    - A queue name can have up to 80 characters.

    - Valid values: alphanumeric characters, hyphens (`-`), and underscores
      (`_`).

    - A FIFO queue name must end with the `.fifo` suffix.

    Queue URLs and names are case-sensitive.
    """

    attributes: dict[str, str] | None = None
    """
    A map of attributes with their corresponding values.

    The following lists the names, descriptions, and values of the special
    request parameters that the `CreateQueue` action uses:

    - `DelaySeconds` -- The length of time, in seconds, for which the
      delivery of all messages in the queue is delayed. Valid values: An
      integer from 0 to 900 seconds (15 minutes). Default: 0.

    - `MaximumMessageSize` -- The limit of how many bytes a message can
      contain before Amazon SQS rejects it. Valid values: An integer from
      1,024 bytes (1 KiB) to 1,048,576 bytes (1 MiB). Default: 1,048,576
      bytes (1 MiB).

    - `MessageRetentionPeriod` -- The length of time, in seconds, for which
      Amazon SQS retains a message. Valid values: An integer from 60 seconds
      (1 minute) to 1,209,600 seconds (14 days). Default: 345,600 (4 days).
      When you change a queue's attributes, the change can take up to 60
      seconds for most of the attributes to propagate throughout the Amazon
      SQS system. Changes made to the `MessageRetentionPeriod` attribute can
      take up to 15 minutes and will impact existing messages in the queue
      potentially causing them to be expired and deleted if the
      `MessageRetentionPeriod` is reduced below the age of existing
      messages.

    - `Policy` -- The queue's policy. A valid Amazon Web Services policy.
      For more information about policy structure, see [Overview of Amazon
      Web Services IAM
      Policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/PoliciesOverview.html)
      in the *IAM User Guide*.

    - `ReceiveMessageWaitTimeSeconds` -- The length of time, in seconds, for
      which a ` ``ReceiveMessage`` ` action waits for a message to arrive.
      Valid values: An integer from 0 to 20 (seconds). Default: 0.

    - `VisibilityTimeout` -- The visibility timeout for the queue, in
      seconds. Valid values: An integer from 0 to 43,200 (12 hours).
      Default: 30. For more information about the visibility timeout, see
      [Visibility
      Timeout](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html)
      in the *Amazon SQS Developer Guide*.

    The following attributes apply only to [dead-letter
    queues:](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-dead-letter-queues.html)

    - `RedrivePolicy` -- The string that includes the parameters for the
      dead-letter queue functionality of the source queue as a JSON object.
      The parameters are as follows:

      - `deadLetterTargetArn` -- The Amazon Resource Name (ARN) of the
        dead-letter queue to which Amazon SQS moves messages after the value
        of `maxReceiveCount` is exceeded.

      - `maxReceiveCount` -- The number of times a message is delivered to
        the source queue before being moved to the dead-letter queue.
        Default: 10. When the `ReceiveCount` for a message exceeds the
        `maxReceiveCount` for a queue, Amazon SQS moves the message to the
        dead-letter-queue.

    - `RedriveAllowPolicy` -- The string that includes the parameters for
      the permissions for the dead-letter queue redrive permission and which
      source queues can specify dead-letter queues as a JSON object. The
      parameters are as follows:

      - `redrivePermission` -- The permission type that defines which source
        queues can specify the current queue as the dead-letter queue. Valid
        values are:

        - `allowAll` -- (Default) Any source queues in this Amazon Web
          Services account in the same Region can specify this queue as the
          dead-letter queue.

        - `denyAll` -- No source queues can specify this queue as the
          dead-letter queue.

        - `byQueue` -- Only queues specified by the `sourceQueueArns`
          parameter can specify this queue as the dead-letter queue.

      - `sourceQueueArns` -- The Amazon Resource Names (ARN)s of the source
        queues that can specify this queue as the dead-letter queue and
        redrive messages. You can specify this parameter only when the
        `redrivePermission` parameter is set to `byQueue`. You can specify
        up to 10 source queue ARNs. To allow more than 10 source queues to
        specify dead-letter queues, set the `redrivePermission` parameter to
        `allowAll`.

    Note:
        The dead-letter queue of a FIFO queue must also be a FIFO queue.
        Similarly, the dead-letter queue of a standard queue must also be a
        standard queue.

    The following attributes apply only to
    [server-side-encryption](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-server-side-encryption.html):

    - `KmsMasterKeyId` -- The ID of an Amazon Web Services managed customer
      master key (CMK) for Amazon SQS or a custom CMK. For more information,
      see [Key
      Terms](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-server-side-encryption.html#sqs-sse-key-terms).
      While the alias of the Amazon Web Services managed CMK for Amazon SQS
      is always `alias/aws/sqs`, the alias of a custom CMK can, for example,
      be `alias/`*`MyAlias`*` `. For more examples, see
      [KeyId](https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters)
      in the *Key Management Service API Reference*.

    - `KmsDataKeyReusePeriodSeconds` -- The length of time, in seconds, for
      which Amazon SQS can reuse a [data
      key](https://docs.aws.amazon.com/kms/latest/developerguide/concepts.html#data-keys)
      to encrypt or decrypt messages before calling KMS again. An integer
      representing seconds, between 60 seconds (1 minute) and 86,400 seconds
      (24 hours). Default: 300 (5 minutes). A shorter time period provides
      better security but results in more calls to KMS which might incur
      charges after Free Tier. For more information, see [How Does the Data
      Key Reuse Period
      Work?](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-server-side-encryption.html#sqs-how-does-the-data-key-reuse-period-work)

    - `SqsManagedSseEnabled` -- Enables server-side queue encryption using
      SQS owned encryption keys. Only one server-side encryption option is
      supported per queue (for example,
      [SSE-KMS](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-configure-sse-existing-queue.html)
      or
      [SSE-SQS](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-configure-sqs-sse-queue.html)).

    The following attributes apply only to [FIFO (first-in-first-out)
    queues](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues.html):

    - `FifoQueue` -- Designates a queue as FIFO. Valid values are `true` and
      `false`. If you don't specify the `FifoQueue` attribute, Amazon SQS
      creates a standard queue. You can provide this attribute only during
      queue creation. You can't change it for an existing queue. When you
      set this attribute, you must also provide the `MessageGroupId` for
      your messages explicitly.

      For more information, see [FIFO queue
      logic](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-understanding-logic.html)
      in the *Amazon SQS Developer Guide*.

    - `ContentBasedDeduplication` -- Enables content-based deduplication.
      Valid values are `true` and `false`. For more information, see
      [Exactly-once
      processing](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-exactly-once-processing.html)
      in the *Amazon SQS Developer Guide*. Note the following:

      - Every message must have a unique `MessageDeduplicationId`.

        - You may provide a `MessageDeduplicationId` explicitly.

        - If you aren't able to provide a `MessageDeduplicationId` and you
          enable `ContentBasedDeduplication` for your queue, Amazon SQS uses
          a SHA-256 hash to generate the `MessageDeduplicationId` using the
          body of the message (but not the attributes of the message).

        - If you don't provide a `MessageDeduplicationId` and the queue
          doesn't have `ContentBasedDeduplication` set, the action fails
          with an error.

        - If the queue has `ContentBasedDeduplication` set, your
          `MessageDeduplicationId` overrides the generated one.

      - When `ContentBasedDeduplication` is in effect, messages with
        identical content sent within the deduplication interval are treated
        as duplicates and only one copy of the message is delivered.

      - If you send one message with `ContentBasedDeduplication` enabled and
        then another message with a `MessageDeduplicationId` that is the
        same as the one generated for the first `MessageDeduplicationId`,
        the two messages are treated as duplicates and only one copy of the
        message is delivered.

    The following attributes apply only to [high throughput for FIFO
    queues](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/high-throughput-fifo.html):

    - `DeduplicationScope` -- Specifies whether message deduplication occurs
      at the message group or queue level. Valid values are `messageGroup`
      and `queue`.

    - `FifoThroughputLimit` -- Specifies whether the FIFO queue throughput
      quota applies to the entire queue or per message group. Valid values
      are `perQueue` and `perMessageGroupId`. The `perMessageGroupId` value
      is allowed only when the value for `DeduplicationScope` is
      `messageGroup`.

    To enable high throughput for FIFO queues, do the following:

    - Set `DeduplicationScope` to `messageGroup`.

    - Set `FifoThroughputLimit` to `perMessageGroupId`.

    If you set these attributes to anything other than the values shown for
    enabling high throughput, normal throughput is in effect and
    deduplication occurs as specified.

    For information on throughput quotas, see [Quotas related to
    messages](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/quotas-messages.html)
    in the *Amazon SQS Developer Guide*.
    """

    tags: dict[str, str] | None = None
    """
    Add cost allocation tags to the specified Amazon SQS queue. For an
    overview, see [Tagging Your Amazon SQS
    Queues](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-queue-tags.html)
    in the *Amazon SQS Developer Guide*.

    When you use queue tags, keep the following guidelines in mind:

    - Adding more than 50 tags to a queue isn't recommended.

    - Tags don't have any semantic meaning. Amazon SQS interprets tags as
      character strings.

    - Tags are case-sensitive.

    - A new tag with a key identical to that of an existing tag overwrites
      the existing tag.

    For a full list of tag restrictions, see [Quotas related to
    queues](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-limits.html#limits-queues)
    in the *Amazon SQS Developer Guide*.

    Note:
        To be able to tag a queue on creation, you must have the
        `sqs:CreateQueue` and `sqs:TagQueue` permissions.

        Cross-account permissions don't apply to this action. For more
        information, see [Grant cross-account permissions to a role and a
        username](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-customer-managed-policy-examples.html#grant-cross-account-permissions-to-role-and-user-name)
        in the *Amazon SQS Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_QUEUE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_name is not None:
            serializer.write_string(
                _SCHEMA_CREATE_QUEUE_INPUT.members["QueueName"], self.queue_name
            )

        if self.attributes is not None:
            _serialize_queue_attribute_map(
                serializer,
                _SCHEMA_CREATE_QUEUE_INPUT.members["Attributes"],
                self.attributes,
            )

        if self.tags is not None:
            _serialize_tag_map(
                serializer, _SCHEMA_CREATE_QUEUE_INPUT.members["tags"], self.tags
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_name"] = de.read_string(
                        _SCHEMA_CREATE_QUEUE_INPUT.members["QueueName"]
                    )

                case 1:
                    kwargs["attributes"] = _deserialize_queue_attribute_map(
                        de, _SCHEMA_CREATE_QUEUE_INPUT.members["Attributes"]
                    )

                case 2:
                    kwargs["tags"] = _deserialize_tag_map(
                        de, _SCHEMA_CREATE_QUEUE_INPUT.members["tags"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREATE_QUEUE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class CreateQueueOutput:
    """Returns the `QueueUrl` attribute of the created queue."""

    queue_url: str | None = None
    """The URL of the created Amazon SQS queue."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CREATE_QUEUE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_CREATE_QUEUE_OUTPUT.members["QueueUrl"], self.queue_url
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_CREATE_QUEUE_OUTPUT.members["QueueUrl"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CREATE_QUEUE_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class InvalidAttributeName(ServiceError):
    """The specified attribute doesn't exist."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_ATTRIBUTE_NAME, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_ATTRIBUTE_NAME.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_ATTRIBUTE_NAME.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INVALID_ATTRIBUTE_NAME, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class InvalidAttributeValue(ServiceError):
    """A queue attribute value is invalid."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_ATTRIBUTE_VALUE.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_ATTRIBUTE_VALUE.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INVALID_ATTRIBUTE_VALUE, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class QueueDeletedRecently(ServiceError):
    """
    You must wait 60 seconds after deleting a queue before you can create
    another queue with the same name.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_QUEUE_DELETED_RECENTLY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_QUEUE_DELETED_RECENTLY.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_QUEUE_DELETED_RECENTLY.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_QUEUE_DELETED_RECENTLY, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class QueueNameExists(ServiceError):
    """
    A queue with this name already exists. Amazon SQS returns this error
    only if the request includes attributes whose values differ from those
    of the existing queue.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_QUEUE_NAME_EXISTS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_QUEUE_NAME_EXISTS.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_QUEUE_NAME_EXISTS.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_QUEUE_NAME_EXISTS, consumer=_consumer)
        return kwargs


CREATE_QUEUE = APIOperation(
    input=CreateQueueInput,
    output=CreateQueueOutput,
    schema=_SCHEMA_CREATE_QUEUE,
    input_schema=_SCHEMA_CREATE_QUEUE_INPUT,
    output_schema=_SCHEMA_CREATE_QUEUE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidAttributeName"): InvalidAttributeName,
            ShapeID("com.amazonaws.sqs#InvalidAttributeValue"): InvalidAttributeValue,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDeletedRecently"): QueueDeletedRecently,
            ShapeID("com.amazonaws.sqs#QueueNameExists"): QueueNameExists,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_ATTRIBUTE_NAME,
        _SCHEMA_INVALID_ATTRIBUTE_VALUE,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DELETED_RECENTLY,
        _SCHEMA_QUEUE_NAME_EXISTS,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class DeleteMessageInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue from which messages are deleted.

    Queue URLs and names are case-sensitive.
    """

    receipt_handle: str | None = None
    """The receipt handle associated with the message to delete."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_MESSAGE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_DELETE_MESSAGE_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.receipt_handle is not None:
            serializer.write_string(
                _SCHEMA_DELETE_MESSAGE_INPUT.members["ReceiptHandle"],
                self.receipt_handle,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_DELETE_MESSAGE_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["receipt_handle"] = de.read_string(
                        _SCHEMA_DELETE_MESSAGE_INPUT.members["ReceiptHandle"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_MESSAGE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DeleteMessageOutput:
    """Dataclass for DeleteMessageOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_MESSAGE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_MESSAGE_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class InvalidIdFormat(ServiceError):
    """The specified receipt handle isn't valid for the current version."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_ID_FORMAT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INVALID_ID_FORMAT, consumer=_consumer)
        return kwargs


DELETE_MESSAGE = APIOperation(
    input=DeleteMessageInput,
    output=DeleteMessageOutput,
    schema=_SCHEMA_DELETE_MESSAGE,
    input_schema=_SCHEMA_DELETE_MESSAGE_INPUT,
    output_schema=_SCHEMA_DELETE_MESSAGE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidIdFormat"): InvalidIdFormat,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#ReceiptHandleIsInvalid"): ReceiptHandleIsInvalid,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_ID_FORMAT,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_RECEIPT_HANDLE_IS_INVALID,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class DeleteMessageBatchRequestEntry:
    """Encloses a receipt handle and an identifier for it."""

    id: str
    """
    The identifier for this particular receipt handle. This is used to
    communicate the result.

    Note:
        The `Id`s of a batch request need to be unique within a request.

        This identifier can have up to 80 characters. The following characters
        are accepted: alphanumeric characters, hyphens(-), and underscores (_).
    """

    receipt_handle: str
    """A receipt handle."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_MESSAGE_BATCH_REQUEST_ENTRY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_DELETE_MESSAGE_BATCH_REQUEST_ENTRY.members["Id"], self.id
        )
        serializer.write_string(
            _SCHEMA_DELETE_MESSAGE_BATCH_REQUEST_ENTRY.members["ReceiptHandle"],
            self.receipt_handle,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["id"] = de.read_string(
                        _SCHEMA_DELETE_MESSAGE_BATCH_REQUEST_ENTRY.members["Id"]
                    )

                case 1:
                    kwargs["receipt_handle"] = de.read_string(
                        _SCHEMA_DELETE_MESSAGE_BATCH_REQUEST_ENTRY.members[
                            "ReceiptHandle"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DELETE_MESSAGE_BATCH_REQUEST_ENTRY, consumer=_consumer
        )
        if "id" not in kwargs:
            kwargs["id"] = ""
        if "receipt_handle" not in kwargs:
            kwargs["receipt_handle"] = ""
        return kwargs


def _serialize_delete_message_batch_request_entry_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[DeleteMessageBatchRequestEntry],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_delete_message_batch_request_entry_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[DeleteMessageBatchRequestEntry]:
    result: list[DeleteMessageBatchRequestEntry] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(DeleteMessageBatchRequestEntry.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class DeleteMessageBatchInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue from which messages are deleted.

    Queue URLs and names are case-sensitive.
    """

    entries: list[DeleteMessageBatchRequestEntry] | None = None
    """Lists the receipt handles for the messages to be deleted."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_MESSAGE_BATCH_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_DELETE_MESSAGE_BATCH_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.entries is not None:
            _serialize_delete_message_batch_request_entry_list(
                serializer,
                _SCHEMA_DELETE_MESSAGE_BATCH_INPUT.members["Entries"],
                self.entries,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_DELETE_MESSAGE_BATCH_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["entries"] = (
                        _deserialize_delete_message_batch_request_entry_list(
                            de, _SCHEMA_DELETE_MESSAGE_BATCH_INPUT.members["Entries"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_MESSAGE_BATCH_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DeleteMessageBatchResultEntry:
    """Encloses the `Id` of an entry in ` ``DeleteMessageBatch``.`"""

    id: str
    """Represents a successfully deleted message."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_MESSAGE_BATCH_RESULT_ENTRY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_DELETE_MESSAGE_BATCH_RESULT_ENTRY.members["Id"], self.id
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["id"] = de.read_string(
                        _SCHEMA_DELETE_MESSAGE_BATCH_RESULT_ENTRY.members["Id"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DELETE_MESSAGE_BATCH_RESULT_ENTRY, consumer=_consumer
        )
        if "id" not in kwargs:
            kwargs["id"] = ""
        return kwargs


def _serialize_delete_message_batch_result_entry_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[DeleteMessageBatchResultEntry],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_delete_message_batch_result_entry_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[DeleteMessageBatchResultEntry]:
    result: list[DeleteMessageBatchResultEntry] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(DeleteMessageBatchResultEntry.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class DeleteMessageBatchOutput:
    """
    For each message in the batch, the response contains a
    ` ``DeleteMessageBatchResultEntry`` ` tag if the message is deleted or a
    ` ``BatchResultErrorEntry`` ` tag if the message can't be deleted.
    """

    successful: list[DeleteMessageBatchResultEntry]
    """A list of ` ``DeleteMessageBatchResultEntry`` ` items."""

    failed: list[BatchResultErrorEntry]
    """A list of ` ``BatchResultErrorEntry`` ` items."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_MESSAGE_BATCH_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_delete_message_batch_result_entry_list(
            serializer,
            _SCHEMA_DELETE_MESSAGE_BATCH_OUTPUT.members["Successful"],
            self.successful,
        )
        _serialize_batch_result_error_entry_list(
            serializer,
            _SCHEMA_DELETE_MESSAGE_BATCH_OUTPUT.members["Failed"],
            self.failed,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["successful"] = (
                        _deserialize_delete_message_batch_result_entry_list(
                            de,
                            _SCHEMA_DELETE_MESSAGE_BATCH_OUTPUT.members["Successful"],
                        )
                    )

                case 1:
                    kwargs["failed"] = _deserialize_batch_result_error_entry_list(
                        de, _SCHEMA_DELETE_MESSAGE_BATCH_OUTPUT.members["Failed"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DELETE_MESSAGE_BATCH_OUTPUT, consumer=_consumer
        )
        if "successful" not in kwargs:
            kwargs["successful"] = []
        if "failed" not in kwargs:
            kwargs["failed"] = []
        return kwargs


DELETE_MESSAGE_BATCH = APIOperation(
    input=DeleteMessageBatchInput,
    output=DeleteMessageBatchOutput,
    schema=_SCHEMA_DELETE_MESSAGE_BATCH,
    input_schema=_SCHEMA_DELETE_MESSAGE_BATCH_INPUT,
    output_schema=_SCHEMA_DELETE_MESSAGE_BATCH_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.sqs#BatchEntryIdsNotDistinct"
            ): BatchEntryIdsNotDistinct,
            ShapeID("com.amazonaws.sqs#EmptyBatchRequest"): EmptyBatchRequest,
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidBatchEntryId"): InvalidBatchEntryId,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID(
                "com.amazonaws.sqs#TooManyEntriesInBatchRequest"
            ): TooManyEntriesInBatchRequest,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_BATCH_ENTRY_IDS_NOT_DISTINCT,
        _SCHEMA_EMPTY_BATCH_REQUEST,
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_BATCH_ENTRY_ID,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_TOO_MANY_ENTRIES_IN_BATCH_REQUEST,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class DeleteQueueInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue to delete.

    Queue URLs and names are case-sensitive.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_QUEUE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_DELETE_QUEUE_INPUT.members["QueueUrl"], self.queue_url
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_DELETE_QUEUE_INPUT.members["QueueUrl"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_QUEUE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DeleteQueueOutput:
    """Dataclass for DeleteQueueOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_QUEUE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_QUEUE_OUTPUT, consumer=_consumer)
        return kwargs


DELETE_QUEUE = APIOperation(
    input=DeleteQueueInput,
    output=DeleteQueueOutput,
    schema=_SCHEMA_DELETE_QUEUE,
    input_schema=_SCHEMA_DELETE_QUEUE_INPUT,
    output_schema=_SCHEMA_DELETE_QUEUE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


def _serialize_attribute_name_list(
    serializer: ShapeSerializer, schema: Schema, value: list[QueueAttributeName]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_attribute_name_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[QueueAttributeName]:
    result: list[QueueAttributeName] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(QueueAttributeName(d.read_string(member_schema)))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class GetQueueAttributesInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue whose attribute information is
    retrieved.

    Queue URLs and names are case-sensitive.
    """

    attribute_names: list[QueueAttributeName] | None = None
    """
    A list of attributes for which to retrieve information.

    The `AttributeNames` parameter is optional, but if you don't specify
    values for this parameter, the request returns empty results.

    Note:
        In the future, new attributes might be added. If you write code that
        calls this action, we recommend that you structure your code so that it
        can handle new attributes gracefully.

    The following attributes are supported:

    Warning:
        The `ApproximateNumberOfMessagesDelayed`,
        `ApproximateNumberOfMessagesNotVisible`, and
        `ApproximateNumberOfMessages` metrics may not achieve consistency until
        at least 1 minute after the producers stop sending messages. This period
        is required for the queue metadata to reach eventual consistency.

    - `All` -- Returns all values.

    - `ApproximateNumberOfMessages` -- Returns the approximate number of
      messages available for retrieval from the queue.

    - `ApproximateNumberOfMessagesDelayed` -- Returns the approximate number
      of messages in the queue that are delayed and not available for
      reading immediately. This can happen when the queue is configured as a
      delay queue or when a message has been sent with a delay parameter.

    - `ApproximateNumberOfMessagesNotVisible` -- Returns the approximate
      number of messages that are in flight. Messages are considered to be
      *in flight* if they have been sent to a client but have not yet been
      deleted or have not yet reached the end of their visibility window.

    - `CreatedTimestamp` -- Returns the time when the queue was created in
      seconds ([epoch time](http://en.wikipedia.org/wiki/Unix_time)).

    - `DelaySeconds` -- Returns the default delay on the queue in seconds.

    - `LastModifiedTimestamp` -- Returns the time when the queue was last
      changed in seconds ([epoch
      time](http://en.wikipedia.org/wiki/Unix_time)).

    - `MaximumMessageSize` -- Returns the limit of how many bytes a message
      can contain before Amazon SQS rejects it.

    - `MessageRetentionPeriod` -- Returns the length of time, in seconds,
      for which Amazon SQS retains a message. When you change a queue's
      attributes, the change can take up to 60 seconds for most of the
      attributes to propagate throughout the Amazon SQS system. Changes made
      to the `MessageRetentionPeriod` attribute can take up to 15 minutes
      and will impact existing messages in the queue potentially causing
      them to be expired and deleted if the `MessageRetentionPeriod` is
      reduced below the age of existing messages.

    - `Policy` -- Returns the policy of the queue.

    - `QueueArn` -- Returns the Amazon resource name (ARN) of the queue.

    - `ReceiveMessageWaitTimeSeconds` -- Returns the length of time, in
      seconds, for which the `ReceiveMessage` action waits for a message to
      arrive.

    - `VisibilityTimeout` -- Returns the visibility timeout for the queue.
      For more information about the visibility timeout, see [Visibility
      Timeout](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html)
      in the *Amazon SQS Developer Guide*.

    The following attributes apply only to [dead-letter
    queues:](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-dead-letter-queues.html)

    - `RedrivePolicy` -- The string that includes the parameters for the
      dead-letter queue functionality of the source queue as a JSON object.
      The parameters are as follows:

      - `deadLetterTargetArn` -- The Amazon Resource Name (ARN) of the
        dead-letter queue to which Amazon SQS moves messages after the value
        of `maxReceiveCount` is exceeded.

      - `maxReceiveCount` -- The number of times a message is delivered to
        the source queue before being moved to the dead-letter queue.
        Default: 10. When the `ReceiveCount` for a message exceeds the
        `maxReceiveCount` for a queue, Amazon SQS moves the message to the
        dead-letter-queue.

    - `RedriveAllowPolicy` -- The string that includes the parameters for
      the permissions for the dead-letter queue redrive permission and which
      source queues can specify dead-letter queues as a JSON object. The
      parameters are as follows:

      - `redrivePermission` -- The permission type that defines which source
        queues can specify the current queue as the dead-letter queue. Valid
        values are:

        - `allowAll` -- (Default) Any source queues in this Amazon Web
          Services account in the same Region can specify this queue as the
          dead-letter queue.

        - `denyAll` -- No source queues can specify this queue as the
          dead-letter queue.

        - `byQueue` -- Only queues specified by the `sourceQueueArns`
          parameter can specify this queue as the dead-letter queue.

      - `sourceQueueArns` -- The Amazon Resource Names (ARN)s of the source
        queues that can specify this queue as the dead-letter queue and
        redrive messages. You can specify this parameter only when the
        `redrivePermission` parameter is set to `byQueue`. You can specify
        up to 10 source queue ARNs. To allow more than 10 source queues to
        specify dead-letter queues, set the `redrivePermission` parameter to
        `allowAll`.

    Note:
        The dead-letter queue of a FIFO queue must also be a FIFO queue.
        Similarly, the dead-letter queue of a standard queue must also be a
        standard queue.

    The following attributes apply only to
    [server-side-encryption](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-server-side-encryption.html):

    - `KmsMasterKeyId` -- Returns the ID of an Amazon Web Services managed
      customer master key (CMK) for Amazon SQS or a custom CMK. For more
      information, see [Key
      Terms](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-server-side-encryption.html#sqs-sse-key-terms).

    - `KmsDataKeyReusePeriodSeconds` -- Returns the length of time, in
      seconds, for which Amazon SQS can reuse a data key to encrypt or
      decrypt messages before calling KMS again. For more information, see
      [How Does the Data Key Reuse Period
      Work?](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-server-side-encryption.html#sqs-how-does-the-data-key-reuse-period-work).

    - `SqsManagedSseEnabled` -- Returns information about whether the queue
      is using SSE-SQS encryption using SQS owned encryption keys. Only one
      server-side encryption option is supported per queue (for example,
      [SSE-KMS](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-configure-sse-existing-queue.html)
      or
      [SSE-SQS](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-configure-sqs-sse-queue.html)).

    The following attributes apply only to [FIFO (first-in-first-out)
    queues](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues.html):

    - `FifoQueue` -- Returns information about whether the queue is FIFO.
      For more information, see [FIFO queue
      logic](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-understanding-logic.html)
      in the *Amazon SQS Developer Guide*.

      Note:
        To determine whether a queue is
        [FIFO](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues.html),
        you can check whether `QueueName` ends with the `.fifo` suffix.

    - `ContentBasedDeduplication` -- Returns whether content-based
      deduplication is enabled for the queue. For more information, see
      [Exactly-once
      processing](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-exactly-once-processing.html)
      in the *Amazon SQS Developer Guide*.

    The following attributes apply only to [high throughput for FIFO
    queues](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/high-throughput-fifo.html):

    - `DeduplicationScope` -- Specifies whether message deduplication occurs
      at the message group or queue level. Valid values are `messageGroup`
      and `queue`.

    - `FifoThroughputLimit` -- Specifies whether the FIFO queue throughput
      quota applies to the entire queue or per message group. Valid values
      are `perQueue` and `perMessageGroupId`. The `perMessageGroupId` value
      is allowed only when the value for `DeduplicationScope` is
      `messageGroup`.

    To enable high throughput for FIFO queues, do the following:

    - Set `DeduplicationScope` to `messageGroup`.

    - Set `FifoThroughputLimit` to `perMessageGroupId`.

    If you set these attributes to anything other than the values shown for
    enabling high throughput, normal throughput is in effect and
    deduplication occurs as specified.

    For information on throughput quotas, see [Quotas related to
    messages](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/quotas-messages.html)
    in the *Amazon SQS Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_QUEUE_ATTRIBUTES_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_GET_QUEUE_ATTRIBUTES_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.attribute_names is not None:
            _serialize_attribute_name_list(
                serializer,
                _SCHEMA_GET_QUEUE_ATTRIBUTES_INPUT.members["AttributeNames"],
                self.attribute_names,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_GET_QUEUE_ATTRIBUTES_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["attribute_names"] = _deserialize_attribute_name_list(
                        de, _SCHEMA_GET_QUEUE_ATTRIBUTES_INPUT.members["AttributeNames"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_QUEUE_ATTRIBUTES_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GetQueueAttributesOutput:
    """A list of returned queue attributes."""

    attributes: dict[str, str] | None = None
    """A map of attributes to their respective values."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_QUEUE_ATTRIBUTES_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.attributes is not None:
            _serialize_queue_attribute_map(
                serializer,
                _SCHEMA_GET_QUEUE_ATTRIBUTES_OUTPUT.members["Attributes"],
                self.attributes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["attributes"] = _deserialize_queue_attribute_map(
                        de, _SCHEMA_GET_QUEUE_ATTRIBUTES_OUTPUT.members["Attributes"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_GET_QUEUE_ATTRIBUTES_OUTPUT, consumer=_consumer
        )
        return kwargs


GET_QUEUE_ATTRIBUTES = APIOperation(
    input=GetQueueAttributesInput,
    output=GetQueueAttributesOutput,
    schema=_SCHEMA_GET_QUEUE_ATTRIBUTES,
    input_schema=_SCHEMA_GET_QUEUE_ATTRIBUTES_INPUT,
    output_schema=_SCHEMA_GET_QUEUE_ATTRIBUTES_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidAttributeName"): InvalidAttributeName,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_ATTRIBUTE_NAME,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class GetQueueUrlInput:
    """
    Retrieves the URL of an existing queue based on its name and,
    optionally, the Amazon Web Services account ID.
    """

    queue_name: str | None = None
    """
    (Required) The name of the queue for which you want to fetch the URL.
    The name can be up to 80 characters long and can include alphanumeric
    characters, hyphens (-), and underscores (_). Queue URLs and names are
    case-sensitive.
    """

    queue_owner_aws_account_id: str | None = None
    """
    (Optional) The Amazon Web Services account ID of the account that
    created the queue. This is only required when you are attempting to
    access a queue owned by another Amazon Web Services account.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_QUEUE_URL_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_name is not None:
            serializer.write_string(
                _SCHEMA_GET_QUEUE_URL_INPUT.members["QueueName"], self.queue_name
            )

        if self.queue_owner_aws_account_id is not None:
            serializer.write_string(
                _SCHEMA_GET_QUEUE_URL_INPUT.members["QueueOwnerAWSAccountId"],
                self.queue_owner_aws_account_id,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_name"] = de.read_string(
                        _SCHEMA_GET_QUEUE_URL_INPUT.members["QueueName"]
                    )

                case 1:
                    kwargs["queue_owner_aws_account_id"] = de.read_string(
                        _SCHEMA_GET_QUEUE_URL_INPUT.members["QueueOwnerAWSAccountId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_QUEUE_URL_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GetQueueUrlOutput:
    """
    For more information, see [Interpreting
    Responses](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-api-responses.html)
    in the *Amazon SQS Developer Guide*.
    """

    queue_url: str | None = None
    """The URL of the queue."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_QUEUE_URL_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_GET_QUEUE_URL_OUTPUT.members["QueueUrl"], self.queue_url
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_GET_QUEUE_URL_OUTPUT.members["QueueUrl"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_QUEUE_URL_OUTPUT, consumer=_consumer)
        return kwargs


GET_QUEUE_URL = APIOperation(
    input=GetQueueUrlInput,
    output=GetQueueUrlOutput,
    schema=_SCHEMA_GET_QUEUE_URL,
    input_schema=_SCHEMA_GET_QUEUE_URL_INPUT,
    output_schema=_SCHEMA_GET_QUEUE_URL_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class ListDeadLetterSourceQueuesInput:
    """"""

    queue_url: str | None = None
    """
    The URL of a dead-letter queue.

    Queue URLs and names are case-sensitive.
    """

    next_token: str | None = None
    """Pagination token to request the next set of results."""

    max_results: int | None = None
    """
    Maximum number of results to include in the response. Value range is 1
    to 1000. You must set `MaxResults` to receive a value for `NextToken` in
    the response.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT.members["QueueUrl"],
                self.queue_url,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT.members["NextToken"],
                self.next_token,
            )

        if self.max_results is not None:
            serializer.write_integer(
                _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT.members["MaxResults"],
                self.max_results,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT.members[
                            "NextToken"
                        ]
                    )

                case 2:
                    kwargs["max_results"] = de.read_integer(
                        _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT.members[
                            "MaxResults"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT, consumer=_consumer
        )
        return kwargs


def _serialize_queue_url_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_queue_url_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ListDeadLetterSourceQueuesOutput:
    """A list of your dead letter source queues."""

    queue_urls: list[str]
    """
    A list of source queue URLs that have the `RedrivePolicy` queue
    attribute configured with a dead-letter queue.
    """

    next_token: str | None = None
    """
    Pagination token to include in the next request. Token value is `null`
    if there are no additional results to request, or if you did not set
    `MaxResults` in the request.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_queue_url_list(
            serializer,
            _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_OUTPUT.members["queueUrls"],
            self.queue_urls,
        )
        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_OUTPUT.members["NextToken"],
                self.next_token,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_urls"] = _deserialize_queue_url_list(
                        de,
                        _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_OUTPUT.members[
                            "queueUrls"
                        ],
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_OUTPUT.members[
                            "NextToken"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_OUTPUT, consumer=_consumer
        )
        if "queue_urls" not in kwargs:
            kwargs["queue_urls"] = []
        return kwargs


LIST_DEAD_LETTER_SOURCE_QUEUES = APIOperation(
    input=ListDeadLetterSourceQueuesInput,
    output=ListDeadLetterSourceQueuesOutput,
    schema=_SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES,
    input_schema=_SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_INPUT,
    output_schema=_SCHEMA_LIST_DEAD_LETTER_SOURCE_QUEUES_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class ListMessageMoveTasksInput:
    """Dataclass for ListMessageMoveTasksInput structure."""

    source_arn: str | None = None
    """The ARN of the queue whose message movement tasks are to be listed."""

    max_results: int | None = None
    """
    The maximum number of results to include in the response. The default is
    1, which provides the most recent message movement task. The upper limit
    is 10.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_MESSAGE_MOVE_TASKS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.source_arn is not None:
            serializer.write_string(
                _SCHEMA_LIST_MESSAGE_MOVE_TASKS_INPUT.members["SourceArn"],
                self.source_arn,
            )

        if self.max_results is not None:
            serializer.write_integer(
                _SCHEMA_LIST_MESSAGE_MOVE_TASKS_INPUT.members["MaxResults"],
                self.max_results,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["source_arn"] = de.read_string(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_INPUT.members["SourceArn"]
                    )

                case 1:
                    kwargs["max_results"] = de.read_integer(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_INPUT.members["MaxResults"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_LIST_MESSAGE_MOVE_TASKS_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ListMessageMoveTasksResultEntry:
    """Contains the details of a message movement task."""

    task_handle: str | None = None
    """
    An identifier associated with a message movement task. When this field
    is returned in the response of the `ListMessageMoveTasks` action, it is
    only populated for tasks that are in RUNNING status.
    """

    status: str | None = None
    """
    The status of the message movement task. Possible values are: RUNNING,
    COMPLETED, CANCELLING, CANCELLED, and FAILED.
    """

    source_arn: str | None = None
    """
    The ARN of the queue that contains the messages to be moved to another
    queue.
    """

    destination_arn: str | None = None
    """
    The ARN of the destination queue if it has been specified in the
    `StartMessageMoveTask` request. If a `DestinationArn` has not been
    specified in the `StartMessageMoveTask` request, this field value will
    be NULL.
    """

    max_number_of_messages_per_second: int | None = None
    """
    The number of messages to be moved per second (the message movement
    rate), if it has been specified in the `StartMessageMoveTask` request.
    If a `MaxNumberOfMessagesPerSecond` has not been specified in the
    `StartMessageMoveTask` request, this field value will be NULL.
    """

    approximate_number_of_messages_moved: int = 0
    """
    The approximate number of messages already moved to the destination
    queue.
    """

    approximate_number_of_messages_to_move: int | None = None
    """
    The number of messages to be moved from the source queue. This number is
    obtained at the time of starting the message movement task and is only
    included after the message movement task is selected to start.
    """

    failure_reason: str | None = None
    """The task failure reason (only included if the task status is FAILED)."""

    started_timestamp: int = 0
    """The timestamp of starting the message movement task."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.task_handle is not None:
            serializer.write_string(
                _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members["TaskHandle"],
                self.task_handle,
            )

        if self.status is not None:
            serializer.write_string(
                _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members["Status"],
                self.status,
            )

        if self.source_arn is not None:
            serializer.write_string(
                _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members["SourceArn"],
                self.source_arn,
            )

        if self.destination_arn is not None:
            serializer.write_string(
                _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members["DestinationArn"],
                self.destination_arn,
            )

        if self.max_number_of_messages_per_second is not None:
            serializer.write_integer(
                _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                    "MaxNumberOfMessagesPerSecond"
                ],
                self.max_number_of_messages_per_second,
            )

        serializer.write_long(
            _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                "ApproximateNumberOfMessagesMoved"
            ],
            self.approximate_number_of_messages_moved,
        )
        if self.approximate_number_of_messages_to_move is not None:
            serializer.write_long(
                _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                    "ApproximateNumberOfMessagesToMove"
                ],
                self.approximate_number_of_messages_to_move,
            )

        if self.failure_reason is not None:
            serializer.write_string(
                _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members["FailureReason"],
                self.failure_reason,
            )

        serializer.write_long(
            _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members["StartedTimestamp"],
            self.started_timestamp,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["task_handle"] = de.read_string(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                            "TaskHandle"
                        ]
                    )

                case 1:
                    kwargs["status"] = de.read_string(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members["Status"]
                    )

                case 2:
                    kwargs["source_arn"] = de.read_string(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                            "SourceArn"
                        ]
                    )

                case 3:
                    kwargs["destination_arn"] = de.read_string(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                            "DestinationArn"
                        ]
                    )

                case 4:
                    kwargs["max_number_of_messages_per_second"] = de.read_integer(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                            "MaxNumberOfMessagesPerSecond"
                        ]
                    )

                case 5:
                    kwargs["approximate_number_of_messages_moved"] = de.read_long(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                            "ApproximateNumberOfMessagesMoved"
                        ]
                    )

                case 6:
                    kwargs["approximate_number_of_messages_to_move"] = de.read_long(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                            "ApproximateNumberOfMessagesToMove"
                        ]
                    )

                case 7:
                    kwargs["failure_reason"] = de.read_string(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                            "FailureReason"
                        ]
                    )

                case 8:
                    kwargs["started_timestamp"] = de.read_long(
                        _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY.members[
                            "StartedTimestamp"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_LIST_MESSAGE_MOVE_TASKS_RESULT_ENTRY, consumer=_consumer
        )
        return kwargs


def _serialize_list_message_move_tasks_result_entry_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[ListMessageMoveTasksResultEntry],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_list_message_move_tasks_result_entry_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ListMessageMoveTasksResultEntry]:
    result: list[ListMessageMoveTasksResultEntry] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ListMessageMoveTasksResultEntry.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ListMessageMoveTasksOutput:
    """Dataclass for ListMessageMoveTasksOutput structure."""

    results: list[ListMessageMoveTasksResultEntry] | None = None
    """A list of message movement tasks and their attributes."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_MESSAGE_MOVE_TASKS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.results is not None:
            _serialize_list_message_move_tasks_result_entry_list(
                serializer,
                _SCHEMA_LIST_MESSAGE_MOVE_TASKS_OUTPUT.members["Results"],
                self.results,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["results"] = (
                        _deserialize_list_message_move_tasks_result_entry_list(
                            de,
                            _SCHEMA_LIST_MESSAGE_MOVE_TASKS_OUTPUT.members["Results"],
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_LIST_MESSAGE_MOVE_TASKS_OUTPUT, consumer=_consumer
        )
        return kwargs


LIST_MESSAGE_MOVE_TASKS = APIOperation(
    input=ListMessageMoveTasksInput,
    output=ListMessageMoveTasksOutput,
    schema=_SCHEMA_LIST_MESSAGE_MOVE_TASKS,
    input_schema=_SCHEMA_LIST_MESSAGE_MOVE_TASKS_INPUT,
    output_schema=_SCHEMA_LIST_MESSAGE_MOVE_TASKS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID(
                "com.amazonaws.sqs#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class ListQueuesInput:
    """"""

    queue_name_prefix: str | None = None
    """
    A string to use for filtering the list results. Only those queues whose
    name begins with the specified string are returned.

    Queue URLs and names are case-sensitive.
    """

    next_token: str | None = None
    """Pagination token to request the next set of results."""

    max_results: int | None = None
    """
    Maximum number of results to include in the response. Value range is 1
    to 1000. You must set `MaxResults` to receive a value for `NextToken` in
    the response.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_QUEUES_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_name_prefix is not None:
            serializer.write_string(
                _SCHEMA_LIST_QUEUES_INPUT.members["QueueNamePrefix"],
                self.queue_name_prefix,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_QUEUES_INPUT.members["NextToken"], self.next_token
            )

        if self.max_results is not None:
            serializer.write_integer(
                _SCHEMA_LIST_QUEUES_INPUT.members["MaxResults"], self.max_results
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_name_prefix"] = de.read_string(
                        _SCHEMA_LIST_QUEUES_INPUT.members["QueueNamePrefix"]
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_QUEUES_INPUT.members["NextToken"]
                    )

                case 2:
                    kwargs["max_results"] = de.read_integer(
                        _SCHEMA_LIST_QUEUES_INPUT.members["MaxResults"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_QUEUES_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ListQueuesOutput:
    """A list of your queues."""

    queue_urls: list[str] | None = None
    """
    A list of queue URLs, up to 1,000 entries, or the value of `MaxResults`
    that you sent in the request.
    """

    next_token: str | None = None
    """
    Pagination token to include in the next request. Token value is `null`
    if there are no additional results to request, or if you did not set
    `MaxResults` in the request.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_QUEUES_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_urls is not None:
            _serialize_queue_url_list(
                serializer,
                _SCHEMA_LIST_QUEUES_OUTPUT.members["QueueUrls"],
                self.queue_urls,
            )

        if self.next_token is not None:
            serializer.write_string(
                _SCHEMA_LIST_QUEUES_OUTPUT.members["NextToken"], self.next_token
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_urls"] = _deserialize_queue_url_list(
                        de, _SCHEMA_LIST_QUEUES_OUTPUT.members["QueueUrls"]
                    )

                case 1:
                    kwargs["next_token"] = de.read_string(
                        _SCHEMA_LIST_QUEUES_OUTPUT.members["NextToken"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_QUEUES_OUTPUT, consumer=_consumer)
        return kwargs


LIST_QUEUES = APIOperation(
    input=ListQueuesInput,
    output=ListQueuesOutput,
    schema=_SCHEMA_LIST_QUEUES,
    input_schema=_SCHEMA_LIST_QUEUES_INPUT,
    output_schema=_SCHEMA_LIST_QUEUES_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class ListQueueTagsInput:
    """Dataclass for ListQueueTagsInput structure."""

    queue_url: str | None = None
    """The URL of the queue."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_QUEUE_TAGS_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_LIST_QUEUE_TAGS_INPUT.members["QueueUrl"], self.queue_url
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_LIST_QUEUE_TAGS_INPUT.members["QueueUrl"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_QUEUE_TAGS_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ListQueueTagsOutput:
    """Dataclass for ListQueueTagsOutput structure."""

    tags: dict[str, str] | None = None
    """The list of all tags added to the specified queue."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_LIST_QUEUE_TAGS_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.tags is not None:
            _serialize_tag_map(
                serializer, _SCHEMA_LIST_QUEUE_TAGS_OUTPUT.members["Tags"], self.tags
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["tags"] = _deserialize_tag_map(
                        de, _SCHEMA_LIST_QUEUE_TAGS_OUTPUT.members["Tags"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_LIST_QUEUE_TAGS_OUTPUT, consumer=_consumer)
        return kwargs


LIST_QUEUE_TAGS = APIOperation(
    input=ListQueueTagsInput,
    output=ListQueueTagsOutput,
    schema=_SCHEMA_LIST_QUEUE_TAGS,
    input_schema=_SCHEMA_LIST_QUEUE_TAGS_INPUT,
    output_schema=_SCHEMA_LIST_QUEUE_TAGS_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class PurgeQueueInProgress(ServiceError):
    """
    Indicates that the specified queue previously received a `PurgeQueue`
    request within the last 60 seconds (the time it can take to delete the
    messages in the queue).
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PURGE_QUEUE_IN_PROGRESS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_PURGE_QUEUE_IN_PROGRESS.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_PURGE_QUEUE_IN_PROGRESS.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PURGE_QUEUE_IN_PROGRESS, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class PurgeQueueInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the queue from which the `PurgeQueue` action deletes
    messages.

    Queue URLs and names are case-sensitive.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PURGE_QUEUE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_PURGE_QUEUE_INPUT.members["QueueUrl"], self.queue_url
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_PURGE_QUEUE_INPUT.members["QueueUrl"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PURGE_QUEUE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class PurgeQueueOutput:
    """Dataclass for PurgeQueueOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PURGE_QUEUE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PURGE_QUEUE_OUTPUT, consumer=_consumer)
        return kwargs


PURGE_QUEUE = APIOperation(
    input=PurgeQueueInput,
    output=PurgeQueueOutput,
    schema=_SCHEMA_PURGE_QUEUE,
    input_schema=_SCHEMA_PURGE_QUEUE_INPUT,
    output_schema=_SCHEMA_PURGE_QUEUE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#PurgeQueueInProgress"): PurgeQueueInProgress,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_PURGE_QUEUE_IN_PROGRESS,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class KmsAccessDenied(ServiceError):
    """The caller doesn't have the required KMS access."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_KMS_ACCESS_DENIED, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_KMS_ACCESS_DENIED.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_KMS_ACCESS_DENIED.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_KMS_ACCESS_DENIED, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class KmsDisabled(ServiceError):
    """The request was denied due to request throttling."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_KMS_DISABLED, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_KMS_DISABLED.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_KMS_DISABLED.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_KMS_DISABLED, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class KmsInvalidKeyUsage(ServiceError):
    """
    The request was rejected for one of the following reasons:

    - The KeyUsage value of the KMS key is incompatible with the API
      operation.

    - The encryption algorithm or signing algorithm specified for the
      operation is incompatible with the type of key material in the KMS key
      (KeySpec).
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_KMS_INVALID_KEY_USAGE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_KMS_INVALID_KEY_USAGE.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_KMS_INVALID_KEY_USAGE.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_KMS_INVALID_KEY_USAGE, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class KmsInvalidState(ServiceError):
    """
    The request was rejected because the state of the specified resource is
    not valid for this request.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_KMS_INVALID_STATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_KMS_INVALID_STATE.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_KMS_INVALID_STATE.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_KMS_INVALID_STATE, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class KmsNotFound(ServiceError):
    """
    The request was rejected because the specified entity or resource could
    not be found.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_KMS_NOT_FOUND, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_KMS_NOT_FOUND.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_KMS_NOT_FOUND.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_KMS_NOT_FOUND, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class KmsOptInRequired(ServiceError):
    """
    The request was rejected because the specified key policy isn't
    syntactically or semantically correct.
    """

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_KMS_OPT_IN_REQUIRED, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_KMS_OPT_IN_REQUIRED.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_KMS_OPT_IN_REQUIRED.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_KMS_OPT_IN_REQUIRED, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class KmsThrottled(ServiceError):
    """Amazon Web Services KMS throttles requests for the following conditions."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_KMS_THROTTLED, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_KMS_THROTTLED.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_KMS_THROTTLED.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_KMS_THROTTLED, consumer=_consumer)
        return kwargs


def _serialize_message_attribute_name_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_message_attribute_name_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


class MessageSystemAttributeName(UnknownEnumMixin, StrEnum):
    ALL = "All"
    SENDER_ID = "SenderId"
    SENT_TIMESTAMP = "SentTimestamp"
    APPROXIMATE_RECEIVE_COUNT = "ApproximateReceiveCount"
    APPROXIMATE_FIRST_RECEIVE_TIMESTAMP = "ApproximateFirstReceiveTimestamp"
    SEQUENCE_NUMBER = "SequenceNumber"
    MESSAGE_DEDUPLICATION_ID = "MessageDeduplicationId"
    MESSAGE_GROUP_ID = "MessageGroupId"
    AWS_TRACE_HEADER = "AWSTraceHeader"
    DEAD_LETTER_QUEUE_SOURCE_ARN = "DeadLetterQueueSourceArn"


def _serialize_message_system_attribute_list(
    serializer: ShapeSerializer, schema: Schema, value: list[MessageSystemAttributeName]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_message_system_attribute_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[MessageSystemAttributeName]:
    result: list[MessageSystemAttributeName] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(MessageSystemAttributeName(d.read_string(member_schema)))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ReceiveMessageInput:
    """Retrieves one or more messages from a specified queue."""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue from which messages are received.

    Queue URLs and names are case-sensitive.
    """

    attribute_names: list[QueueAttributeName] | None = None
    """
    Warning:
        This parameter has been discontinued but will be supported for backward
        compatibility. To provide attribute names, you are encouraged to use
        `MessageSystemAttributeNames`.

    A list of attributes that need to be returned along with each message.
    These attributes include:

    - `All` -- Returns all values.

    - `ApproximateFirstReceiveTimestamp` -- Returns the time the message was
      first received from the queue ([epoch
      time](http://en.wikipedia.org/wiki/Unix_time) in milliseconds).

    - `ApproximateReceiveCount` -- Returns the number of times a message has
      been received across all queues but not deleted.

    - `AWSTraceHeader` -- Returns the X-Ray trace header string.

    - `SenderId`

      - For a user, returns the user ID, for example
        `ABCDEFGHI1JKLMNOPQ23R`.

      - For an IAM role, returns the IAM role ID, for example
        `ABCDE1F2GH3I4JK5LMNOP:i-a123b456`.

    - `SentTimestamp` -- Returns the time the message was sent to the queue
      ([epoch time](http://en.wikipedia.org/wiki/Unix_time) in
      milliseconds).

    - `SqsManagedSseEnabled` -- Enables server-side queue encryption using
      SQS owned encryption keys. Only one server-side encryption option is
      supported per queue (for example,
      [SSE-KMS](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-configure-sse-existing-queue.html)
      or
      [SSE-SQS](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-configure-sqs-sse-queue.html)).

    - `MessageDeduplicationId` -- Returns the value provided by the producer
      that calls the ` ``SendMessage`` ` action.

    - `MessageGroupId` -- Returns the value provided by the producer that
      calls the ` ``SendMessage`` ` action.

    - `SequenceNumber` -- Returns the value provided by Amazon SQS.
    """

    message_system_attribute_names: list[MessageSystemAttributeName] | None = None
    """
    A list of attributes that need to be returned along with each message.
    These attributes include:

    - `All` -- Returns all values.

    - `ApproximateFirstReceiveTimestamp` -- Returns the time the message was
      first received from the queue ([epoch
      time](http://en.wikipedia.org/wiki/Unix_time) in milliseconds).

    - `ApproximateReceiveCount` -- Returns the number of times a message has
      been received across all queues but not deleted.

    - `AWSTraceHeader` -- Returns the X-Ray trace header string.

    - `SenderId`

      - For a user, returns the user ID, for example
        `ABCDEFGHI1JKLMNOPQ23R`.

      - For an IAM role, returns the IAM role ID, for example
        `ABCDE1F2GH3I4JK5LMNOP:i-a123b456`.

    - `SentTimestamp` -- Returns the time the message was sent to the queue
      ([epoch time](http://en.wikipedia.org/wiki/Unix_time) in
      milliseconds).

    - `SqsManagedSseEnabled` -- Enables server-side queue encryption using
      SQS owned encryption keys. Only one server-side encryption option is
      supported per queue (for example,
      [SSE-KMS](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-configure-sse-existing-queue.html)
      or
      [SSE-SQS](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-configure-sqs-sse-queue.html)).

    - `MessageDeduplicationId` -- Returns the value provided by the producer
      that calls the ` ``SendMessage`` ` action.

    - `MessageGroupId` -- Returns the value provided by the producer that
      calls the ` ``SendMessage`` ` action.

    - `SequenceNumber` -- Returns the value provided by Amazon SQS.
    """

    message_attribute_names: list[str] | None = None
    """
    The name of the message attribute, where *N* is the index.

    - The name can contain alphanumeric characters and the underscore (`_`),
      hyphen (`-`), and period (`.`).

    - The name is case-sensitive and must be unique among all attribute
      names for the message.

    - The name must not start with AWS-reserved prefixes such as `AWS.` or
      `Amazon.` (or any casing variants).

    - The name must not start or end with a period (`.`), and it should not
      have periods in succession (`..`).

    - The name can be up to 256 characters long.

    When using `ReceiveMessage`, you can send a list of attribute names to
    receive, or you can return all of the attributes by specifying `All` or
    `.*` in your request. You can also use all message attributes starting
    with a prefix, for example `bar.*`.
    """

    max_number_of_messages: int | None = None
    """
    The maximum number of messages to return. Amazon SQS never returns more
    messages than this value (however, fewer messages might be returned).
    Valid values: 1 to 10. Default: 1.
    """

    visibility_timeout: int | None = None
    """
    The duration (in seconds) that the received messages are hidden from
    subsequent retrieve requests after being retrieved by a `ReceiveMessage`
    request. If not specified, the default visibility timeout for the queue
    is used, which is 30 seconds.

    Understanding `VisibilityTimeout`:

    - When a message is received from a queue, it becomes temporarily
      invisible to other consumers for the duration of the visibility
      timeout. This prevents multiple consumers from processing the same
      message simultaneously. If the message is not deleted or its
      visibility timeout is not extended before the timeout expires, it
      becomes visible again and can be retrieved by other consumers.

    - Setting an appropriate visibility timeout is crucial. If it's too
      short, the message might become visible again before processing is
      complete, leading to duplicate processing. If it's too long, it
      delays the reprocessing of messages if the initial processing fails.

    - You can adjust the visibility timeout using the `--visibility-timeout`
      parameter in the `receive-message` command to match the processing
      time required by your application.

    - A message that isn't deleted or a message whose visibility isn't
      extended before the visibility timeout expires counts as a failed
      receive. Depending on the configuration of the queue, the message
      might be sent to the dead-letter queue.

    For more information, see [Visibility
    Timeout](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html)
    in the *Amazon SQS Developer Guide*.
    """

    wait_time_seconds: int | None = None
    """
    The duration (in seconds) for which the call waits for a message to
    arrive in the queue before returning. If a message is available, the
    call returns sooner than `WaitTimeSeconds`. If no messages are available
    and the wait time expires, the call does not return a message list. If
    you are using the Java SDK, it returns a `ReceiveMessageResponse`
    object, which has a empty list instead of a Null object.

    Warning:
        To avoid HTTP errors, ensure that the HTTP response timeout for
        `ReceiveMessage` requests is longer than the `WaitTimeSeconds`
        parameter. For example, with the Java SDK, you can set HTTP transport
        settings using the
        [NettyNioAsyncHttpClient](https://sdk.amazonaws.com/java/api/latest/software/amazon/awssdk/http/nio/netty/NettyNioAsyncHttpClient.html)
        for asynchronous clients, or the
        [ApacheHttpClient](https://sdk.amazonaws.com/java/api/latest/software/amazon/awssdk/http/apache/ApacheHttpClient.html)
        for synchronous clients.
    """

    receive_request_attempt_id: str | None = None
    """
    This parameter applies only to FIFO (first-in-first-out) queues.

    The token used for deduplication of `ReceiveMessage` calls. If a
    networking issue occurs after a `ReceiveMessage` action, and instead of
    a response you receive a generic error, it is possible to retry the same
    action with an identical `ReceiveRequestAttemptId` to retrieve the same
    set of messages, even if their visibility timeout has not yet expired.

    - You can use `ReceiveRequestAttemptId` only for 5 minutes after a
      `ReceiveMessage` action.

    - When you set `FifoQueue`, a caller of the `ReceiveMessage` action can
      provide a `ReceiveRequestAttemptId` explicitly.

    - It is possible to retry the `ReceiveMessage` action with the same
      `ReceiveRequestAttemptId` if none of the messages have been modified
      (deleted or had their visibility changes).

    - During a visibility timeout, subsequent calls with the same
      `ReceiveRequestAttemptId` return the same messages and receipt
      handles. If a retry occurs within the deduplication interval, it
      resets the visibility timeout. For more information, see [Visibility
      Timeout](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html)
      in the *Amazon SQS Developer Guide*.

      Warning:
        If a caller of the `ReceiveMessage` action still processes messages
        when the visibility timeout expires and messages become visible,
        another worker consuming from the same queue can receive the same
        messages and therefore process duplicates. Also, if a consumer whose
        message processing time is longer than the visibility timeout tries to
        delete the processed messages, the action fails with an error.

        To mitigate this effect, ensure that your application observes a safe
        threshold before the visibility timeout expires and extend the
        visibility timeout as necessary.

    - While messages with a particular `MessageGroupId` are invisible, no
      more messages belonging to the same `MessageGroupId` are returned
      until the visibility timeout expires. You can still receive messages
      with another `MessageGroupId` from your FIFO queue as long as they are
      visible.

    - If a caller of `ReceiveMessage` can't track the
      `ReceiveRequestAttemptId`, no retries work until the original
      visibility timeout expires. As a result, delays might occur but the
      messages in the queue remain in a strict order.

    The maximum length of `ReceiveRequestAttemptId` is 128 characters.
    `ReceiveRequestAttemptId` can contain alphanumeric characters (`a-z`,
    `A-Z`, `0-9`) and punctuation (`` !"#$%&'()*+,-./:;<=>?@[]^_`{|}~ ``).

    For best practices of using `ReceiveRequestAttemptId`, see [Using the
    ReceiveRequestAttemptId Request
    Parameter](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/using-receiverequestattemptid-request-parameter.html)
    in the *Amazon SQS Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RECEIVE_MESSAGE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_RECEIVE_MESSAGE_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.attribute_names is not None:
            _serialize_attribute_name_list(
                serializer,
                _SCHEMA_RECEIVE_MESSAGE_INPUT.members["AttributeNames"],
                self.attribute_names,
            )

        if self.message_system_attribute_names is not None:
            _serialize_message_system_attribute_list(
                serializer,
                _SCHEMA_RECEIVE_MESSAGE_INPUT.members["MessageSystemAttributeNames"],
                self.message_system_attribute_names,
            )

        if self.message_attribute_names is not None:
            _serialize_message_attribute_name_list(
                serializer,
                _SCHEMA_RECEIVE_MESSAGE_INPUT.members["MessageAttributeNames"],
                self.message_attribute_names,
            )

        if self.max_number_of_messages is not None:
            serializer.write_integer(
                _SCHEMA_RECEIVE_MESSAGE_INPUT.members["MaxNumberOfMessages"],
                self.max_number_of_messages,
            )

        if self.visibility_timeout is not None:
            serializer.write_integer(
                _SCHEMA_RECEIVE_MESSAGE_INPUT.members["VisibilityTimeout"],
                self.visibility_timeout,
            )

        if self.wait_time_seconds is not None:
            serializer.write_integer(
                _SCHEMA_RECEIVE_MESSAGE_INPUT.members["WaitTimeSeconds"],
                self.wait_time_seconds,
            )

        if self.receive_request_attempt_id is not None:
            serializer.write_string(
                _SCHEMA_RECEIVE_MESSAGE_INPUT.members["ReceiveRequestAttemptId"],
                self.receive_request_attempt_id,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_RECEIVE_MESSAGE_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["attribute_names"] = _deserialize_attribute_name_list(
                        de, _SCHEMA_RECEIVE_MESSAGE_INPUT.members["AttributeNames"]
                    )

                case 2:
                    kwargs["message_system_attribute_names"] = (
                        _deserialize_message_system_attribute_list(
                            de,
                            _SCHEMA_RECEIVE_MESSAGE_INPUT.members[
                                "MessageSystemAttributeNames"
                            ],
                        )
                    )

                case 3:
                    kwargs["message_attribute_names"] = (
                        _deserialize_message_attribute_name_list(
                            de,
                            _SCHEMA_RECEIVE_MESSAGE_INPUT.members[
                                "MessageAttributeNames"
                            ],
                        )
                    )

                case 4:
                    kwargs["max_number_of_messages"] = de.read_integer(
                        _SCHEMA_RECEIVE_MESSAGE_INPUT.members["MaxNumberOfMessages"]
                    )

                case 5:
                    kwargs["visibility_timeout"] = de.read_integer(
                        _SCHEMA_RECEIVE_MESSAGE_INPUT.members["VisibilityTimeout"]
                    )

                case 6:
                    kwargs["wait_time_seconds"] = de.read_integer(
                        _SCHEMA_RECEIVE_MESSAGE_INPUT.members["WaitTimeSeconds"]
                    )

                case 7:
                    kwargs["receive_request_attempt_id"] = de.read_string(
                        _SCHEMA_RECEIVE_MESSAGE_INPUT.members["ReceiveRequestAttemptId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RECEIVE_MESSAGE_INPUT, consumer=_consumer)
        return kwargs


def _serialize_message_system_attribute_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, str]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_string(value_schema, v))


def _deserialize_message_system_attribute_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, str]:
    result: dict[str, str] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = d.read_string(value_schema)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_binary_list(
    serializer: ShapeSerializer, schema: Schema, value: list[bytes]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_blob(member_schema, e)


def _deserialize_binary_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[bytes]:
    result: list[bytes] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_blob(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_string_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_string_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class MessageAttributeValue:
    """
    The user-specified message attribute value. For string data types, the
    `Value` attribute has the same restrictions on the content as the
    message body. For more information, see ` ``SendMessage``.`

    `Name`, `type`, `value` and the message body must not be empty or null.
    All parts of the message attribute, including `Name`, `Type`, and
    `Value`, are part of the message size restriction (1 MiB or 1,048,576
    bytes).
    """

    data_type: str
    """
    Amazon SQS supports the following logical data types: `String`,
    `Number`, and `Binary`. For the `Number` data type, you must use
    `StringValue`.

    You can also append custom labels. For more information, see [Amazon SQS
    Message
    Attributes](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-message-metadata.html#sqs-message-attributes)
    in the *Amazon SQS Developer Guide*.
    """

    string_value: str | None = None
    """
    Strings are Unicode with UTF-8 binary encoding. For a list of code
    values, see [ASCII Printable
    Characters](http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters).
    """

    binary_value: bytes | None = None
    """
    Binary type attributes can store any binary data, such as compressed
    data, encrypted data, or images.
    """

    string_list_values: list[str] | None = None
    """Not implemented. Reserved for future use."""

    binary_list_values: list[bytes] | None = None
    """Not implemented. Reserved for future use."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_MESSAGE_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.string_value is not None:
            serializer.write_string(
                _SCHEMA_MESSAGE_ATTRIBUTE_VALUE.members["StringValue"],
                self.string_value,
            )

        if self.binary_value is not None:
            serializer.write_blob(
                _SCHEMA_MESSAGE_ATTRIBUTE_VALUE.members["BinaryValue"],
                self.binary_value,
            )

        if self.string_list_values is not None:
            _serialize_string_list(
                serializer,
                _SCHEMA_MESSAGE_ATTRIBUTE_VALUE.members["StringListValues"],
                self.string_list_values,
            )

        if self.binary_list_values is not None:
            _serialize_binary_list(
                serializer,
                _SCHEMA_MESSAGE_ATTRIBUTE_VALUE.members["BinaryListValues"],
                self.binary_list_values,
            )

        serializer.write_string(
            _SCHEMA_MESSAGE_ATTRIBUTE_VALUE.members["DataType"], self.data_type
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["string_value"] = de.read_string(
                        _SCHEMA_MESSAGE_ATTRIBUTE_VALUE.members["StringValue"]
                    )

                case 1:
                    kwargs["binary_value"] = de.read_blob(
                        _SCHEMA_MESSAGE_ATTRIBUTE_VALUE.members["BinaryValue"]
                    )

                case 2:
                    kwargs["string_list_values"] = _deserialize_string_list(
                        de, _SCHEMA_MESSAGE_ATTRIBUTE_VALUE.members["StringListValues"]
                    )

                case 3:
                    kwargs["binary_list_values"] = _deserialize_binary_list(
                        de, _SCHEMA_MESSAGE_ATTRIBUTE_VALUE.members["BinaryListValues"]
                    )

                case 4:
                    kwargs["data_type"] = de.read_string(
                        _SCHEMA_MESSAGE_ATTRIBUTE_VALUE.members["DataType"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_MESSAGE_ATTRIBUTE_VALUE, consumer=_consumer)
        if "data_type" not in kwargs:
            kwargs["data_type"] = ""
        return kwargs


def _serialize_message_body_attribute_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, MessageAttributeValue]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_message_body_attribute_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, MessageAttributeValue]:
    result: dict[str, MessageAttributeValue] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = MessageAttributeValue.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class Message:
    """An Amazon SQS message."""

    message_id: str | None = None
    """
    A unique identifier for the message. A `MessageId`is considered unique
    across all Amazon Web Services accounts for an extended period of time.
    """

    receipt_handle: str | None = None
    """
    An identifier associated with the act of receiving the message. A new
    receipt handle is returned every time you receive a message. When
    deleting a message, you provide the last received receipt handle to
    delete the message.
    """

    md5_of_body: str | None = None
    """An MD5 digest of the non-URL-encoded message body string."""

    body: str | None = None
    """The message's contents (not URL-encoded)."""

    attributes: dict[str, str] | None = None
    """
    A map of the attributes requested in ` ``ReceiveMessage`` ` to their
    respective values. Supported attributes:

    - `ApproximateReceiveCount`

    - `ApproximateFirstReceiveTimestamp`

    - `MessageDeduplicationId`

    - `MessageGroupId`

    - `SenderId`

    - `SentTimestamp`

    - `SequenceNumber`

    `ApproximateFirstReceiveTimestamp` and `SentTimestamp` are each returned
    as an integer representing the [epoch
    time](http://en.wikipedia.org/wiki/Unix_time) in milliseconds.
    """

    md5_of_message_attributes: str | None = None
    """
    An MD5 digest of the non-URL-encoded message attribute string. You can
    use this attribute to verify that Amazon SQS received the message
    correctly. Amazon SQS URL-decodes the message before creating the MD5
    digest. For information about MD5, see
    [RFC1321](https://www.ietf.org/rfc/rfc1321.txt).
    """

    message_attributes: dict[str, MessageAttributeValue] | None = None
    """
    Each message attribute consists of a `Name`, `Type`, and `Value`. For
    more information, see [Amazon SQS message
    attributes](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-message-metadata.html#sqs-message-attributes)
    in the *Amazon SQS Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_MESSAGE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message_id is not None:
            serializer.write_string(
                _SCHEMA_MESSAGE.members["MessageId"], self.message_id
            )

        if self.receipt_handle is not None:
            serializer.write_string(
                _SCHEMA_MESSAGE.members["ReceiptHandle"], self.receipt_handle
            )

        if self.md5_of_body is not None:
            serializer.write_string(
                _SCHEMA_MESSAGE.members["MD5OfBody"], self.md5_of_body
            )

        if self.body is not None:
            serializer.write_string(_SCHEMA_MESSAGE.members["Body"], self.body)

        if self.attributes is not None:
            _serialize_message_system_attribute_map(
                serializer, _SCHEMA_MESSAGE.members["Attributes"], self.attributes
            )

        if self.md5_of_message_attributes is not None:
            serializer.write_string(
                _SCHEMA_MESSAGE.members["MD5OfMessageAttributes"],
                self.md5_of_message_attributes,
            )

        if self.message_attributes is not None:
            _serialize_message_body_attribute_map(
                serializer,
                _SCHEMA_MESSAGE.members["MessageAttributes"],
                self.message_attributes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message_id"] = de.read_string(
                        _SCHEMA_MESSAGE.members["MessageId"]
                    )

                case 1:
                    kwargs["receipt_handle"] = de.read_string(
                        _SCHEMA_MESSAGE.members["ReceiptHandle"]
                    )

                case 2:
                    kwargs["md5_of_body"] = de.read_string(
                        _SCHEMA_MESSAGE.members["MD5OfBody"]
                    )

                case 3:
                    kwargs["body"] = de.read_string(_SCHEMA_MESSAGE.members["Body"])

                case 4:
                    kwargs["attributes"] = _deserialize_message_system_attribute_map(
                        de, _SCHEMA_MESSAGE.members["Attributes"]
                    )

                case 5:
                    kwargs["md5_of_message_attributes"] = de.read_string(
                        _SCHEMA_MESSAGE.members["MD5OfMessageAttributes"]
                    )

                case 6:
                    kwargs["message_attributes"] = (
                        _deserialize_message_body_attribute_map(
                            de, _SCHEMA_MESSAGE.members["MessageAttributes"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_MESSAGE, consumer=_consumer)
        return kwargs


def _serialize_message_list(
    serializer: ShapeSerializer, schema: Schema, value: list[Message]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_message_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[Message]:
    result: list[Message] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(Message.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ReceiveMessageOutput:
    """A list of received messages."""

    messages: list[Message] | None = None
    """A list of messages."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RECEIVE_MESSAGE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.messages is not None:
            _serialize_message_list(
                serializer,
                _SCHEMA_RECEIVE_MESSAGE_OUTPUT.members["Messages"],
                self.messages,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["messages"] = _deserialize_message_list(
                        de, _SCHEMA_RECEIVE_MESSAGE_OUTPUT.members["Messages"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RECEIVE_MESSAGE_OUTPUT, consumer=_consumer)
        return kwargs


RECEIVE_MESSAGE = APIOperation(
    input=ReceiveMessageInput,
    output=ReceiveMessageOutput,
    schema=_SCHEMA_RECEIVE_MESSAGE,
    input_schema=_SCHEMA_RECEIVE_MESSAGE_INPUT,
    output_schema=_SCHEMA_RECEIVE_MESSAGE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#KmsAccessDenied"): KmsAccessDenied,
            ShapeID("com.amazonaws.sqs#KmsDisabled"): KmsDisabled,
            ShapeID("com.amazonaws.sqs#KmsInvalidKeyUsage"): KmsInvalidKeyUsage,
            ShapeID("com.amazonaws.sqs#KmsInvalidState"): KmsInvalidState,
            ShapeID("com.amazonaws.sqs#KmsNotFound"): KmsNotFound,
            ShapeID("com.amazonaws.sqs#KmsOptInRequired"): KmsOptInRequired,
            ShapeID("com.amazonaws.sqs#KmsThrottled"): KmsThrottled,
            ShapeID("com.amazonaws.sqs#OverLimit"): OverLimit,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_KMS_ACCESS_DENIED,
        _SCHEMA_KMS_DISABLED,
        _SCHEMA_KMS_INVALID_KEY_USAGE,
        _SCHEMA_KMS_INVALID_STATE,
        _SCHEMA_KMS_NOT_FOUND,
        _SCHEMA_KMS_OPT_IN_REQUIRED,
        _SCHEMA_KMS_THROTTLED,
        _SCHEMA_OVER_LIMIT,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class RemovePermissionInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue from which permissions are removed.

    Queue URLs and names are case-sensitive.
    """

    label: str | None = None
    """
    The identification of the permission to remove. This is the label added
    using the ` ``AddPermission`` ` action.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REMOVE_PERMISSION_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_REMOVE_PERMISSION_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.label is not None:
            serializer.write_string(
                _SCHEMA_REMOVE_PERMISSION_INPUT.members["Label"], self.label
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_REMOVE_PERMISSION_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["label"] = de.read_string(
                        _SCHEMA_REMOVE_PERMISSION_INPUT.members["Label"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_REMOVE_PERMISSION_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class RemovePermissionOutput:
    """Dataclass for RemovePermissionOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_REMOVE_PERMISSION_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_REMOVE_PERMISSION_OUTPUT, consumer=_consumer)
        return kwargs


REMOVE_PERMISSION = APIOperation(
    input=RemovePermissionInput,
    output=RemovePermissionOutput,
    schema=_SCHEMA_REMOVE_PERMISSION,
    input_schema=_SCHEMA_REMOVE_PERMISSION_INPUT,
    output_schema=_SCHEMA_REMOVE_PERMISSION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class InvalidMessageContents(ServiceError):
    """The message contains characters outside the allowed set."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INVALID_MESSAGE_CONTENTS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_INVALID_MESSAGE_CONTENTS.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INVALID_MESSAGE_CONTENTS.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INVALID_MESSAGE_CONTENTS, consumer=_consumer)
        return kwargs


class MessageSystemAttributeNameForSends(UnknownEnumMixin, StrEnum):
    AWS_TRACE_HEADER = "AWSTraceHeader"


@dataclass(kw_only=True)
class MessageSystemAttributeValue:
    """
    The user-specified message system attribute value. For string data
    types, the `Value` attribute has the same restrictions on the content as
    the message body. For more information, see ` ``SendMessage``.`

    `Name`, `type`, `value` and the message body must not be empty or null.
    """

    data_type: str
    """
    Amazon SQS supports the following logical data types: `String`,
    `Number`, and `Binary`. For the `Number` data type, you must use
    `StringValue`.

    You can also append custom labels. For more information, see [Amazon SQS
    Message
    Attributes](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-message-metadata.html#sqs-message-attributes)
    in the *Amazon SQS Developer Guide*.
    """

    string_value: str | None = None
    """
    Strings are Unicode with UTF-8 binary encoding. For a list of code
    values, see [ASCII Printable
    Characters](http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters).
    """

    binary_value: bytes | None = None
    """
    Binary type attributes can store any binary data, such as compressed
    data, encrypted data, or images.
    """

    string_list_values: list[str] | None = None
    """Not implemented. Reserved for future use."""

    binary_list_values: list[bytes] | None = None
    """Not implemented. Reserved for future use."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.string_value is not None:
            serializer.write_string(
                _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE.members["StringValue"],
                self.string_value,
            )

        if self.binary_value is not None:
            serializer.write_blob(
                _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE.members["BinaryValue"],
                self.binary_value,
            )

        if self.string_list_values is not None:
            _serialize_string_list(
                serializer,
                _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE.members["StringListValues"],
                self.string_list_values,
            )

        if self.binary_list_values is not None:
            _serialize_binary_list(
                serializer,
                _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE.members["BinaryListValues"],
                self.binary_list_values,
            )

        serializer.write_string(
            _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE.members["DataType"], self.data_type
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["string_value"] = de.read_string(
                        _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE.members["StringValue"]
                    )

                case 1:
                    kwargs["binary_value"] = de.read_blob(
                        _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE.members["BinaryValue"]
                    )

                case 2:
                    kwargs["string_list_values"] = _deserialize_string_list(
                        de,
                        _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE.members[
                            "StringListValues"
                        ],
                    )

                case 3:
                    kwargs["binary_list_values"] = _deserialize_binary_list(
                        de,
                        _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE.members[
                            "BinaryListValues"
                        ],
                    )

                case 4:
                    kwargs["data_type"] = de.read_string(
                        _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE.members["DataType"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_MESSAGE_SYSTEM_ATTRIBUTE_VALUE, consumer=_consumer
        )
        if "data_type" not in kwargs:
            kwargs["data_type"] = ""
        return kwargs


def _serialize_message_body_system_attribute_map(
    serializer: ShapeSerializer,
    schema: Schema,
    value: dict[str, MessageSystemAttributeValue],
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_message_body_system_attribute_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, MessageSystemAttributeValue]:
    result: dict[str, MessageSystemAttributeValue] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = MessageSystemAttributeValue.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class SendMessageInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue to which a message is sent.

    Queue URLs and names are case-sensitive.
    """

    message_body: str | None = None
    """
    The message to send. The minimum size is one character. The maximum size
    is 1 MiB or 1,048,576 bytes

    Warning:
        A message can include only XML, JSON, and unformatted text. The
        following Unicode characters are allowed. For more information, see the
        [W3C specification for
        characters](http://www.w3.org/TR/REC-xml/#charsets).

        `#x9` | `#xA` | `#xD` | `#x20` to `#xD7FF` | `#xE000` to `#xFFFD` |
        `#x10000` to `#x10FFFF`

        If a message contains characters outside the allowed set, Amazon SQS
        rejects the message and returns an InvalidMessageContents error. Ensure
        that your message body includes only valid characters to avoid this
        exception.
    """

    delay_seconds: int | None = None
    """
    The length of time, in seconds, for which to delay a specific message.
    Valid values: 0 to 900. Maximum: 15 minutes. Messages with a positive
    `DelaySeconds` value become available for processing after the delay
    period is finished. If you don't specify a value, the default value for
    the queue applies.

    Note:
        When you set `FifoQueue`, you can't set `DelaySeconds` per message. You
        can set this parameter only on a queue level.
    """

    message_attributes: dict[str, MessageAttributeValue] | None = None
    """
    Each message attribute consists of a `Name`, `Type`, and `Value`. For
    more information, see [Amazon SQS message
    attributes](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-message-metadata.html#sqs-message-attributes)
    in the *Amazon SQS Developer Guide*.
    """

    message_system_attributes: dict[str, MessageSystemAttributeValue] | None = None
    """
    The message system attribute to send. Each message system attribute
    consists of a `Name`, `Type`, and `Value`.

    Warning:
        - Currently, the only supported message system attribute is
        `AWSTraceHeader`. Its type must be `String` and its value must be a
        correctly formatted X-Ray trace header string.

        - The size of a message system attribute doesn't count towards the
        total size of a message.
    """

    message_deduplication_id: str | None = None
    """
    This parameter applies only to FIFO (first-in-first-out) queues.

    The token used for deduplication of sent messages. If a message with a
    particular `MessageDeduplicationId` is sent successfully, any messages
    sent with the same `MessageDeduplicationId` are accepted successfully
    but aren't delivered during the 5-minute deduplication interval. For
    more information, see [Exactly-once
    processing](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-exactly-once-processing.html)
    in the *Amazon SQS Developer Guide*.

    - Every message must have a unique `MessageDeduplicationId`,

      - You may provide a `MessageDeduplicationId` explicitly.

      - If you aren't able to provide a `MessageDeduplicationId` and you
        enable `ContentBasedDeduplication` for your queue, Amazon SQS uses a
        SHA-256 hash to generate the `MessageDeduplicationId` using the body
        of the message (but not the attributes of the message).

      - If you don't provide a `MessageDeduplicationId` and the queue
        doesn't have `ContentBasedDeduplication` set, the action fails with
        an error.

      - If the queue has `ContentBasedDeduplication` set, your
        `MessageDeduplicationId` overrides the generated one.

    - When `ContentBasedDeduplication` is in effect, messages with identical
      content sent within the deduplication interval are treated as
      duplicates and only one copy of the message is delivered.

    - If you send one message with `ContentBasedDeduplication` enabled and
      then another message with a `MessageDeduplicationId` that is the same
      as the one generated for the first `MessageDeduplicationId`, the two
      messages are treated as duplicates and only one copy of the message is
      delivered.

    Note:
        The `MessageDeduplicationId` is available to the consumer of the message
        (this can be useful for troubleshooting delivery issues).

        If a message is sent successfully but the acknowledgement is lost and
        the message is resent with the same `MessageDeduplicationId` after the
        deduplication interval, Amazon SQS can't detect duplicate messages.

        Amazon SQS continues to keep track of the message deduplication ID even
        after the message is received and deleted.

    The maximum length of `MessageDeduplicationId` is 128 characters.
    `MessageDeduplicationId` can contain alphanumeric characters (`a-z`,
    `A-Z`, `0-9`) and punctuation (`` !"#$%&'()*+,-./:;<=>?@[]^_`{|}~ ``).

    For best practices of using `MessageDeduplicationId`, see [Using the
    MessageDeduplicationId
    Property](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/using-messagededuplicationid-property.html)
    in the *Amazon SQS Developer Guide*.
    """

    message_group_id: str | None = None
    """
    `MessageGroupId` is an attribute used in Amazon SQS FIFO
    (First-In-First-Out) and standard queues. In FIFO queues,
    `MessageGroupId` organizes messages into distinct groups. Messages
    within the same message group are always processed one at a time, in
    strict order, ensuring that no two messages from the same group are
    processed simultaneously. In standard queues, using `MessageGroupId`
    enables fair queues. It is used to identify the tenant a message belongs
    to, helping maintain consistent message dwell time across all tenants
    during noisy neighbor events. Unlike FIFO queues, messages with the same
    `MessageGroupId` can be processed in parallel, maintaining the high
    throughput of standard queues.

    - **FIFO queues:** `MessageGroupId` acts as the tag that specifies that
      a message belongs to a specific message group. Messages that belong to
      the same message group are processed in a FIFO manner (however,
      messages in different message groups might be processed out of order).
      To interleave multiple ordered streams within a single queue, use
      `MessageGroupId` values (for example, session data for multiple
      users). In this scenario, multiple consumers can process the queue,
      but the session data of each user is processed in a FIFO fashion.

      If you do not provide a `MessageGroupId` when sending a message to a
      FIFO queue, the action fails.

      `ReceiveMessage` might return messages with multiple `MessageGroupId`
      values. For each `MessageGroupId`, the messages are sorted by time
      sent.

    - **Standard queues:**Use `MessageGroupId` in standard queues to enable
      fair queues. The `MessageGroupId` identifies the tenant a message
      belongs to. A tenant can be any entity that shares a queue with
      others, such as your customer, a client application, or a request
      type. When one tenant sends a disproportionately large volume of
      messages or has messages that require longer processing time, fair
      queues ensure other tenants' messages maintain low dwell time. This
      preserves quality of service for all tenants while maintaining the
      scalability and throughput of standard queues. We recommend that you
      include a `MessageGroupId` in all messages when using fair queues.

    The length of `MessageGroupId` is 128 characters. Valid values:
    alphanumeric characters and punctuation
    `` (!"#$%&'()*+,-./:;<=>?@[]^_`{|}~) ``.

    For best practices of using `MessageGroupId`, see [Using the
    MessageGroupId
    Property](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/using-messagegroupid-property.html)
    in the *Amazon SQS Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SEND_MESSAGE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.message_body is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_INPUT.members["MessageBody"], self.message_body
            )

        if self.delay_seconds is not None:
            serializer.write_integer(
                _SCHEMA_SEND_MESSAGE_INPUT.members["DelaySeconds"], self.delay_seconds
            )

        if self.message_attributes is not None:
            _serialize_message_body_attribute_map(
                serializer,
                _SCHEMA_SEND_MESSAGE_INPUT.members["MessageAttributes"],
                self.message_attributes,
            )

        if self.message_system_attributes is not None:
            _serialize_message_body_system_attribute_map(
                serializer,
                _SCHEMA_SEND_MESSAGE_INPUT.members["MessageSystemAttributes"],
                self.message_system_attributes,
            )

        if self.message_deduplication_id is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_INPUT.members["MessageDeduplicationId"],
                self.message_deduplication_id,
            )

        if self.message_group_id is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_INPUT.members["MessageGroupId"],
                self.message_group_id,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["message_body"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_INPUT.members["MessageBody"]
                    )

                case 2:
                    kwargs["delay_seconds"] = de.read_integer(
                        _SCHEMA_SEND_MESSAGE_INPUT.members["DelaySeconds"]
                    )

                case 3:
                    kwargs["message_attributes"] = (
                        _deserialize_message_body_attribute_map(
                            de, _SCHEMA_SEND_MESSAGE_INPUT.members["MessageAttributes"]
                        )
                    )

                case 4:
                    kwargs["message_system_attributes"] = (
                        _deserialize_message_body_system_attribute_map(
                            de,
                            _SCHEMA_SEND_MESSAGE_INPUT.members[
                                "MessageSystemAttributes"
                            ],
                        )
                    )

                case 5:
                    kwargs["message_deduplication_id"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_INPUT.members["MessageDeduplicationId"]
                    )

                case 6:
                    kwargs["message_group_id"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_INPUT.members["MessageGroupId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SEND_MESSAGE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class SendMessageOutput:
    """The `MD5OfMessageBody` and `MessageId` elements."""

    md5_of_message_body: str | None = None
    """
    An MD5 digest of the non-URL-encoded message body string. You can use
    this attribute to verify that Amazon SQS received the message correctly.
    Amazon SQS URL-decodes the message before creating the MD5 digest. For
    information about MD5, see
    [RFC1321](https://www.ietf.org/rfc/rfc1321.txt).
    """

    md5_of_message_attributes: str | None = None
    """
    An MD5 digest of the non-URL-encoded message attribute string. You can
    use this attribute to verify that Amazon SQS received the message
    correctly. Amazon SQS URL-decodes the message before creating the MD5
    digest. For information about MD5, see
    [RFC1321](https://www.ietf.org/rfc/rfc1321.txt).
    """

    md5_of_message_system_attributes: str | None = None
    """
    An MD5 digest of the non-URL-encoded message system attribute string.
    You can use this attribute to verify that Amazon SQS received the
    message correctly. Amazon SQS URL-decodes the message before creating
    the MD5 digest.
    """

    message_id: str | None = None
    """
    An attribute containing the `MessageId` of the message sent to the
    queue. For more information, see [Queue and Message
    Identifiers](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-queue-message-identifiers.html)
    in the *Amazon SQS Developer Guide*.
    """

    sequence_number: str | None = None
    """
    This parameter applies only to FIFO (first-in-first-out) queues.

    The large, non-consecutive number that Amazon SQS assigns to each
    message.

    The length of `SequenceNumber` is 128 bits. `SequenceNumber` continues
    to increase for a particular `MessageGroupId`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SEND_MESSAGE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.md5_of_message_body is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_OUTPUT.members["MD5OfMessageBody"],
                self.md5_of_message_body,
            )

        if self.md5_of_message_attributes is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_OUTPUT.members["MD5OfMessageAttributes"],
                self.md5_of_message_attributes,
            )

        if self.md5_of_message_system_attributes is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_OUTPUT.members["MD5OfMessageSystemAttributes"],
                self.md5_of_message_system_attributes,
            )

        if self.message_id is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_OUTPUT.members["MessageId"], self.message_id
            )

        if self.sequence_number is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_OUTPUT.members["SequenceNumber"],
                self.sequence_number,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["md5_of_message_body"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_OUTPUT.members["MD5OfMessageBody"]
                    )

                case 1:
                    kwargs["md5_of_message_attributes"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_OUTPUT.members["MD5OfMessageAttributes"]
                    )

                case 2:
                    kwargs["md5_of_message_system_attributes"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_OUTPUT.members[
                            "MD5OfMessageSystemAttributes"
                        ]
                    )

                case 3:
                    kwargs["message_id"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_OUTPUT.members["MessageId"]
                    )

                case 4:
                    kwargs["sequence_number"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_OUTPUT.members["SequenceNumber"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SEND_MESSAGE_OUTPUT, consumer=_consumer)
        return kwargs


SEND_MESSAGE = APIOperation(
    input=SendMessageInput,
    output=SendMessageOutput,
    schema=_SCHEMA_SEND_MESSAGE,
    input_schema=_SCHEMA_SEND_MESSAGE_INPUT,
    output_schema=_SCHEMA_SEND_MESSAGE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidMessageContents"): InvalidMessageContents,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#KmsAccessDenied"): KmsAccessDenied,
            ShapeID("com.amazonaws.sqs#KmsDisabled"): KmsDisabled,
            ShapeID("com.amazonaws.sqs#KmsInvalidKeyUsage"): KmsInvalidKeyUsage,
            ShapeID("com.amazonaws.sqs#KmsInvalidState"): KmsInvalidState,
            ShapeID("com.amazonaws.sqs#KmsNotFound"): KmsNotFound,
            ShapeID("com.amazonaws.sqs#KmsOptInRequired"): KmsOptInRequired,
            ShapeID("com.amazonaws.sqs#KmsThrottled"): KmsThrottled,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_MESSAGE_CONTENTS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_KMS_ACCESS_DENIED,
        _SCHEMA_KMS_DISABLED,
        _SCHEMA_KMS_INVALID_KEY_USAGE,
        _SCHEMA_KMS_INVALID_STATE,
        _SCHEMA_KMS_NOT_FOUND,
        _SCHEMA_KMS_OPT_IN_REQUIRED,
        _SCHEMA_KMS_THROTTLED,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class BatchRequestTooLong(ServiceError):
    """The length of all the messages put together is more than the limit."""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BATCH_REQUEST_TOO_LONG, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.message is not None:
            serializer.write_string(
                _SCHEMA_BATCH_REQUEST_TOO_LONG.members["message"], self.message
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_BATCH_REQUEST_TOO_LONG.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BATCH_REQUEST_TOO_LONG, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class SendMessageBatchRequestEntry:
    """Contains the details of a single Amazon SQS message along with an `Id`."""

    id: str
    """
    An identifier for a message in this batch used to communicate the
    result.

    Note:
        The `Id`s of a batch request need to be unique within a request.

        This identifier can have up to 80 characters. The following characters
        are accepted: alphanumeric characters, hyphens(-), and underscores (_).
    """

    message_body: str
    """The body of the message."""

    delay_seconds: int | None = None
    """
    The length of time, in seconds, for which a specific message is delayed.
    Valid values: 0 to 900. Maximum: 15 minutes. Messages with a positive
    `DelaySeconds` value become available for processing after the delay
    period is finished. If you don't specify a value, the default value for
    the queue is applied.

    Note:
        When you set `FifoQueue`, you can't set `DelaySeconds` per message. You
        can set this parameter only on a queue level.
    """

    message_attributes: dict[str, MessageAttributeValue] | None = None
    """
    Each message attribute consists of a `Name`, `Type`, and `Value`. For
    more information, see [Amazon SQS message
    attributes](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-message-metadata.html#sqs-message-attributes)
    in the *Amazon SQS Developer Guide*.
    """

    message_system_attributes: dict[str, MessageSystemAttributeValue] | None = None
    """
    The message system attribute to send Each message system attribute
    consists of a `Name`, `Type`, and `Value`.

    Warning:
        - Currently, the only supported message system attribute is
        `AWSTraceHeader`. Its type must be `String` and its value must be a
        correctly formatted X-Ray trace header string.

        - The size of a message system attribute doesn't count towards the
        total size of a message.
    """

    message_deduplication_id: str | None = None
    """
    This parameter applies only to FIFO (first-in-first-out) queues.

    The token used for deduplication of messages within a 5-minute minimum
    deduplication interval. If a message with a particular
    `MessageDeduplicationId` is sent successfully, subsequent messages with
    the same `MessageDeduplicationId` are accepted successfully but aren't
    delivered. For more information, see [Exactly-once
    processing](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-exactly-once-processing.html)
    in the *Amazon SQS Developer Guide*.

    - Every message must have a unique `MessageDeduplicationId`,

      - You may provide a `MessageDeduplicationId` explicitly.

      - If you aren't able to provide a `MessageDeduplicationId` and you
        enable `ContentBasedDeduplication` for your queue, Amazon SQS uses a
        SHA-256 hash to generate the `MessageDeduplicationId` using the body
        of the message (but not the attributes of the message).

      - If you don't provide a `MessageDeduplicationId` and the queue
        doesn't have `ContentBasedDeduplication` set, the action fails with
        an error.

      - If the queue has `ContentBasedDeduplication` set, your
        `MessageDeduplicationId` overrides the generated one.

    - When `ContentBasedDeduplication` is in effect, messages with identical
      content sent within the deduplication interval are treated as
      duplicates and only one copy of the message is delivered.

    - If you send one message with `ContentBasedDeduplication` enabled and
      then another message with a `MessageDeduplicationId` that is the same
      as the one generated for the first `MessageDeduplicationId`, the two
      messages are treated as duplicates and only one copy of the message is
      delivered.

    Note:
        The `MessageDeduplicationId` is available to the consumer of the message
        (this can be useful for troubleshooting delivery issues).

        If a message is sent successfully but the acknowledgement is lost and
        the message is resent with the same `MessageDeduplicationId` after the
        deduplication interval, Amazon SQS can't detect duplicate messages.

        Amazon SQS continues to keep track of the message deduplication ID even
        after the message is received and deleted.

    The length of `MessageDeduplicationId` is 128 characters.
    `MessageDeduplicationId` can contain alphanumeric characters (`a-z`,
    `A-Z`, `0-9`) and punctuation (`` !"#$%&'()*+,-./:;<=>?@[]^_`{|}~ ``).

    For best practices of using `MessageDeduplicationId`, see [Using the
    MessageDeduplicationId
    Property](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/using-messagededuplicationid-property.html)
    in the *Amazon SQS Developer Guide*.
    """

    message_group_id: str | None = None
    """
    `MessageGroupId` is an attribute used in Amazon SQS FIFO
    (First-In-First-Out) and standard queues. In FIFO queues,
    `MessageGroupId` organizes messages into distinct groups. Messages
    within the same message group are always processed one at a time, in
    strict order, ensuring that no two messages from the same group are
    processed simultaneously. In standard queues, using `MessageGroupId`
    enables fair queues. It is used to identify the tenant a message belongs
    to, helping maintain consistent message dwell time across all tenants
    during noisy neighbor events. Unlike FIFO queues, messages with the same
    `MessageGroupId` can be processed in parallel, maintaining the high
    throughput of standard queues.

    - **FIFO queues:** `MessageGroupId` acts as the tag that specifies that
      a message belongs to a specific message group. Messages that belong to
      the same message group are processed in a FIFO manner (however,
      messages in different message groups might be processed out of order).
      To interleave multiple ordered streams within a single queue, use
      `MessageGroupId` values (for example, session data for multiple
      users). In this scenario, multiple consumers can process the queue,
      but the session data of each user is processed in a FIFO fashion.

      If you do not provide a `MessageGroupId` when sending a message to a
      FIFO queue, the action fails.

      `ReceiveMessage` might return messages with multiple `MessageGroupId`
      values. For each `MessageGroupId`, the messages are sorted by time
      sent.

    - **Standard queues:**Use `MessageGroupId` in standard queues to enable
      fair queues. The `MessageGroupId` identifies the tenant a message
      belongs to. A tenant can be any entity that shares a queue with
      others, such as your customer, a client application, or a request
      type. When one tenant sends a disproportionately large volume of
      messages or has messages that require longer processing time, fair
      queues ensure other tenants' messages maintain low dwell time. This
      preserves quality of service for all tenants while maintaining the
      scalability and throughput of standard queues. We recommend that you
      include a `MessageGroupId` in all messages when using fair queues.

    The length of `MessageGroupId` is 128 characters. Valid values:
    alphanumeric characters and punctuation
    `` (!"#$%&'()*+,-./:;<=>?@[]^_`{|}~) ``.

    For best practices of using `MessageGroupId`, see [Using the
    MessageGroupId
    Property](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/using-messagegroupid-property.html)
    in the *Amazon SQS Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members["Id"], self.id
        )
        serializer.write_string(
            _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members["MessageBody"],
            self.message_body,
        )
        if self.delay_seconds is not None:
            serializer.write_integer(
                _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members["DelaySeconds"],
                self.delay_seconds,
            )

        if self.message_attributes is not None:
            _serialize_message_body_attribute_map(
                serializer,
                _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members["MessageAttributes"],
                self.message_attributes,
            )

        if self.message_system_attributes is not None:
            _serialize_message_body_system_attribute_map(
                serializer,
                _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members[
                    "MessageSystemAttributes"
                ],
                self.message_system_attributes,
            )

        if self.message_deduplication_id is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members[
                    "MessageDeduplicationId"
                ],
                self.message_deduplication_id,
            )

        if self.message_group_id is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members["MessageGroupId"],
                self.message_group_id,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["id"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members["Id"]
                    )

                case 1:
                    kwargs["message_body"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members["MessageBody"]
                    )

                case 2:
                    kwargs["delay_seconds"] = de.read_integer(
                        _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members["DelaySeconds"]
                    )

                case 3:
                    kwargs["message_attributes"] = (
                        _deserialize_message_body_attribute_map(
                            de,
                            _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members[
                                "MessageAttributes"
                            ],
                        )
                    )

                case 4:
                    kwargs["message_system_attributes"] = (
                        _deserialize_message_body_system_attribute_map(
                            de,
                            _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members[
                                "MessageSystemAttributes"
                            ],
                        )
                    )

                case 5:
                    kwargs["message_deduplication_id"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members[
                            "MessageDeduplicationId"
                        ]
                    )

                case 6:
                    kwargs["message_group_id"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY.members[
                            "MessageGroupId"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_SEND_MESSAGE_BATCH_REQUEST_ENTRY, consumer=_consumer
        )
        if "id" not in kwargs:
            kwargs["id"] = ""
        if "message_body" not in kwargs:
            kwargs["message_body"] = ""
        return kwargs


def _serialize_send_message_batch_request_entry_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[SendMessageBatchRequestEntry],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_send_message_batch_request_entry_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[SendMessageBatchRequestEntry]:
    result: list[SendMessageBatchRequestEntry] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(SendMessageBatchRequestEntry.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class SendMessageBatchInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue to which batched messages are sent.

    Queue URLs and names are case-sensitive.
    """

    entries: list[SendMessageBatchRequestEntry] | None = None
    """A list of ` ``SendMessageBatchRequestEntry`` ` items."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SEND_MESSAGE_BATCH_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_BATCH_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.entries is not None:
            _serialize_send_message_batch_request_entry_list(
                serializer,
                _SCHEMA_SEND_MESSAGE_BATCH_INPUT.members["Entries"],
                self.entries,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["entries"] = (
                        _deserialize_send_message_batch_request_entry_list(
                            de, _SCHEMA_SEND_MESSAGE_BATCH_INPUT.members["Entries"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SEND_MESSAGE_BATCH_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class SendMessageBatchResultEntry:
    """
    Encloses a `MessageId` for a successfully-enqueued message in a
    ` ``SendMessageBatch``.`
    """

    id: str
    """An identifier for the message in this batch."""

    message_id: str
    """An identifier for the message."""

    md5_of_message_body: str
    """
    An MD5 digest of the non-URL-encoded message body string. You can use
    this attribute to verify that Amazon SQS received the message correctly.
    Amazon SQS URL-decodes the message before creating the MD5 digest. For
    information about MD5, see
    [RFC1321](https://www.ietf.org/rfc/rfc1321.txt).
    """

    md5_of_message_attributes: str | None = None
    """
    An MD5 digest of the non-URL-encoded message attribute string. You can
    use this attribute to verify that Amazon SQS received the message
    correctly. Amazon SQS URL-decodes the message before creating the MD5
    digest. For information about MD5, see
    [RFC1321](https://www.ietf.org/rfc/rfc1321.txt).
    """

    md5_of_message_system_attributes: str | None = None
    """
    An MD5 digest of the non-URL-encoded message system attribute string.
    You can use this attribute to verify that Amazon SQS received the
    message correctly. Amazon SQS URL-decodes the message before creating
    the MD5 digest. For information about MD5, see
    [RFC1321](https://www.ietf.org/rfc/rfc1321.txt).
    """

    sequence_number: str | None = None
    """
    This parameter applies only to FIFO (first-in-first-out) queues.

    The large, non-consecutive number that Amazon SQS assigns to each
    message.

    The length of `SequenceNumber` is 128 bits. As `SequenceNumber`
    continues to increase for a particular `MessageGroupId`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members["Id"], self.id
        )
        serializer.write_string(
            _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members["MessageId"],
            self.message_id,
        )
        serializer.write_string(
            _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members["MD5OfMessageBody"],
            self.md5_of_message_body,
        )
        if self.md5_of_message_attributes is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members[
                    "MD5OfMessageAttributes"
                ],
                self.md5_of_message_attributes,
            )

        if self.md5_of_message_system_attributes is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members[
                    "MD5OfMessageSystemAttributes"
                ],
                self.md5_of_message_system_attributes,
            )

        if self.sequence_number is not None:
            serializer.write_string(
                _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members["SequenceNumber"],
                self.sequence_number,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["id"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members["Id"]
                    )

                case 1:
                    kwargs["message_id"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members["MessageId"]
                    )

                case 2:
                    kwargs["md5_of_message_body"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members[
                            "MD5OfMessageBody"
                        ]
                    )

                case 3:
                    kwargs["md5_of_message_attributes"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members[
                            "MD5OfMessageAttributes"
                        ]
                    )

                case 4:
                    kwargs["md5_of_message_system_attributes"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members[
                            "MD5OfMessageSystemAttributes"
                        ]
                    )

                case 5:
                    kwargs["sequence_number"] = de.read_string(
                        _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY.members[
                            "SequenceNumber"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_SEND_MESSAGE_BATCH_RESULT_ENTRY, consumer=_consumer
        )
        if "id" not in kwargs:
            kwargs["id"] = ""
        if "message_id" not in kwargs:
            kwargs["message_id"] = ""
        if "md5_of_message_body" not in kwargs:
            kwargs["md5_of_message_body"] = ""
        return kwargs


def _serialize_send_message_batch_result_entry_list(
    serializer: ShapeSerializer,
    schema: Schema,
    value: list[SendMessageBatchResultEntry],
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_send_message_batch_result_entry_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[SendMessageBatchResultEntry]:
    result: list[SendMessageBatchResultEntry] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(SendMessageBatchResultEntry.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class SendMessageBatchOutput:
    """
    For each message in the batch, the response contains a
    ` ``SendMessageBatchResultEntry`` ` tag if the message succeeds or a
    ` ``BatchResultErrorEntry`` ` tag if the message fails.
    """

    successful: list[SendMessageBatchResultEntry]
    """A list of ` ``SendMessageBatchResultEntry`` ` items."""

    failed: list[BatchResultErrorEntry]
    """
    A list of ` ``BatchResultErrorEntry`` ` items with error details about
    each message that can't be enqueued.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SEND_MESSAGE_BATCH_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        _serialize_send_message_batch_result_entry_list(
            serializer,
            _SCHEMA_SEND_MESSAGE_BATCH_OUTPUT.members["Successful"],
            self.successful,
        )
        _serialize_batch_result_error_entry_list(
            serializer, _SCHEMA_SEND_MESSAGE_BATCH_OUTPUT.members["Failed"], self.failed
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["successful"] = (
                        _deserialize_send_message_batch_result_entry_list(
                            de, _SCHEMA_SEND_MESSAGE_BATCH_OUTPUT.members["Successful"]
                        )
                    )

                case 1:
                    kwargs["failed"] = _deserialize_batch_result_error_entry_list(
                        de, _SCHEMA_SEND_MESSAGE_BATCH_OUTPUT.members["Failed"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SEND_MESSAGE_BATCH_OUTPUT, consumer=_consumer)
        if "successful" not in kwargs:
            kwargs["successful"] = []
        if "failed" not in kwargs:
            kwargs["failed"] = []
        return kwargs


SEND_MESSAGE_BATCH = APIOperation(
    input=SendMessageBatchInput,
    output=SendMessageBatchOutput,
    schema=_SCHEMA_SEND_MESSAGE_BATCH,
    input_schema=_SCHEMA_SEND_MESSAGE_BATCH_INPUT,
    output_schema=_SCHEMA_SEND_MESSAGE_BATCH_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.sqs#BatchEntryIdsNotDistinct"
            ): BatchEntryIdsNotDistinct,
            ShapeID("com.amazonaws.sqs#BatchRequestTooLong"): BatchRequestTooLong,
            ShapeID("com.amazonaws.sqs#EmptyBatchRequest"): EmptyBatchRequest,
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidBatchEntryId"): InvalidBatchEntryId,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#KmsAccessDenied"): KmsAccessDenied,
            ShapeID("com.amazonaws.sqs#KmsDisabled"): KmsDisabled,
            ShapeID("com.amazonaws.sqs#KmsInvalidKeyUsage"): KmsInvalidKeyUsage,
            ShapeID("com.amazonaws.sqs#KmsInvalidState"): KmsInvalidState,
            ShapeID("com.amazonaws.sqs#KmsNotFound"): KmsNotFound,
            ShapeID("com.amazonaws.sqs#KmsOptInRequired"): KmsOptInRequired,
            ShapeID("com.amazonaws.sqs#KmsThrottled"): KmsThrottled,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID(
                "com.amazonaws.sqs#TooManyEntriesInBatchRequest"
            ): TooManyEntriesInBatchRequest,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_BATCH_ENTRY_IDS_NOT_DISTINCT,
        _SCHEMA_BATCH_REQUEST_TOO_LONG,
        _SCHEMA_EMPTY_BATCH_REQUEST,
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_BATCH_ENTRY_ID,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_KMS_ACCESS_DENIED,
        _SCHEMA_KMS_DISABLED,
        _SCHEMA_KMS_INVALID_KEY_USAGE,
        _SCHEMA_KMS_INVALID_STATE,
        _SCHEMA_KMS_NOT_FOUND,
        _SCHEMA_KMS_OPT_IN_REQUIRED,
        _SCHEMA_KMS_THROTTLED,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_TOO_MANY_ENTRIES_IN_BATCH_REQUEST,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class SetQueueAttributesInput:
    """"""

    queue_url: str | None = None
    """
    The URL of the Amazon SQS queue whose attributes are set.

    Queue URLs and names are case-sensitive.
    """

    attributes: dict[str, str] | None = None
    """
    A map of attributes to set.

    The following lists the names, descriptions, and values of the special
    request parameters that the `SetQueueAttributes` action uses:

    - `DelaySeconds` -- The length of time, in seconds, for which the
      delivery of all messages in the queue is delayed. Valid values: An
      integer from 0 to 900 (15 minutes). Default: 0.

    - `MaximumMessageSize` -- The limit of how many bytes a message can
      contain before Amazon SQS rejects it. Valid values: An integer from
      1,024 bytes (1 KiB) up to 1,048,576 bytes (1 MiB). Default: 1,048,576
      bytes (1 MiB).

    - `MessageRetentionPeriod` -- The length of time, in seconds, for which
      Amazon SQS retains a message. Valid values: An integer representing
      seconds, from 60 (1 minute) to 1,209,600 (14 days). Default: 345,600
      (4 days). When you change a queue's attributes, the change can take
      up to 60 seconds for most of the attributes to propagate throughout
      the Amazon SQS system. Changes made to the `MessageRetentionPeriod`
      attribute can take up to 15 minutes and will impact existing messages
      in the queue potentially causing them to be expired and deleted if the
      `MessageRetentionPeriod` is reduced below the age of existing
      messages.

    - `Policy` -- The queue's policy. A valid Amazon Web Services policy.
      For more information about policy structure, see [Overview of Amazon
      Web Services IAM
      Policies](https://docs.aws.amazon.com/IAM/latest/UserGuide/PoliciesOverview.html)
      in the *Identity and Access Management User Guide*.

    - `ReceiveMessageWaitTimeSeconds` -- The length of time, in seconds, for
      which a ` ``ReceiveMessage`` ` action waits for a message to arrive.
      Valid values: An integer from 0 to 20 (seconds). Default: 0.

    - `VisibilityTimeout` -- The visibility timeout for the queue, in
      seconds. Valid values: An integer from 0 to 43,200 (12 hours).
      Default: 30. For more information about the visibility timeout, see
      [Visibility
      Timeout](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html)
      in the *Amazon SQS Developer Guide*.

    The following attributes apply only to [dead-letter
    queues:](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-dead-letter-queues.html)

    - `RedrivePolicy` -- The string that includes the parameters for the
      dead-letter queue functionality of the source queue as a JSON object.
      The parameters are as follows:

      - `deadLetterTargetArn` -- The Amazon Resource Name (ARN) of the
        dead-letter queue to which Amazon SQS moves messages after the value
        of `maxReceiveCount` is exceeded.

      - `maxReceiveCount` -- The number of times a message is delivered to
        the source queue before being moved to the dead-letter queue.
        Default: 10. When the `ReceiveCount` for a message exceeds the
        `maxReceiveCount` for a queue, Amazon SQS moves the message to the
        dead-letter-queue.

    - `RedriveAllowPolicy` -- The string that includes the parameters for
      the permissions for the dead-letter queue redrive permission and which
      source queues can specify dead-letter queues as a JSON object. The
      parameters are as follows:

      - `redrivePermission` -- The permission type that defines which source
        queues can specify the current queue as the dead-letter queue. Valid
        values are:

        - `allowAll` -- (Default) Any source queues in this Amazon Web
          Services account in the same Region can specify this queue as the
          dead-letter queue.

        - `denyAll` -- No source queues can specify this queue as the
          dead-letter queue.

        - `byQueue` -- Only queues specified by the `sourceQueueArns`
          parameter can specify this queue as the dead-letter queue.

      - `sourceQueueArns` -- The Amazon Resource Names (ARN)s of the source
        queues that can specify this queue as the dead-letter queue and
        redrive messages. You can specify this parameter only when the
        `redrivePermission` parameter is set to `byQueue`. You can specify
        up to 10 source queue ARNs. To allow more than 10 source queues to
        specify dead-letter queues, set the `redrivePermission` parameter to
        `allowAll`.

    Note:
        The dead-letter queue of a FIFO queue must also be a FIFO queue.
        Similarly, the dead-letter queue of a standard queue must also be a
        standard queue.

    The following attributes apply only to
    [server-side-encryption](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-server-side-encryption.html):

    - `KmsMasterKeyId` -- The ID of an Amazon Web Services managed customer
      master key (CMK) for Amazon SQS or a custom CMK. For more information,
      see [Key
      Terms](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-server-side-encryption.html#sqs-sse-key-terms).
      While the alias of the AWS-managed CMK for Amazon SQS is always
      `alias/aws/sqs`, the alias of a custom CMK can, for example, be
      `alias/`*`MyAlias`*` `. For more examples, see
      [KeyId](https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters)
      in the *Key Management Service API Reference*.

    - `KmsDataKeyReusePeriodSeconds` -- The length of time, in seconds, for
      which Amazon SQS can reuse a [data
      key](https://docs.aws.amazon.com/kms/latest/developerguide/concepts.html#data-keys)
      to encrypt or decrypt messages before calling KMS again. An integer
      representing seconds, between 60 seconds (1 minute) and 86,400 seconds
      (24 hours). Default: 300 (5 minutes). A shorter time period provides
      better security but results in more calls to KMS which might incur
      charges after Free Tier. For more information, see [How Does the Data
      Key Reuse Period
      Work?](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-server-side-encryption.html#sqs-how-does-the-data-key-reuse-period-work).

    - `SqsManagedSseEnabled` -- Enables server-side queue encryption using
      SQS owned encryption keys. Only one server-side encryption option is
      supported per queue (for example,
      [SSE-KMS](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-configure-sse-existing-queue.html)
      or
      [SSE-SQS](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-configure-sqs-sse-queue.html)).

    The following attribute applies only to [FIFO (first-in-first-out)
    queues](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues.html):

    - `ContentBasedDeduplication` -- Enables content-based deduplication.
      For more information, see [Exactly-once
      processing](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-exactly-once-processing.html)
      in the *Amazon SQS Developer Guide*. Note the following:

      - Every message must have a unique `MessageDeduplicationId`.

        - You may provide a `MessageDeduplicationId` explicitly.

        - If you aren't able to provide a `MessageDeduplicationId` and you
          enable `ContentBasedDeduplication` for your queue, Amazon SQS uses
          a SHA-256 hash to generate the `MessageDeduplicationId` using the
          body of the message (but not the attributes of the message).

        - If you don't provide a `MessageDeduplicationId` and the queue
          doesn't have `ContentBasedDeduplication` set, the action fails
          with an error.

        - If the queue has `ContentBasedDeduplication` set, your
          `MessageDeduplicationId` overrides the generated one.

      - When `ContentBasedDeduplication` is in effect, messages with
        identical content sent within the deduplication interval are treated
        as duplicates and only one copy of the message is delivered.

      - If you send one message with `ContentBasedDeduplication` enabled and
        then another message with a `MessageDeduplicationId` that is the
        same as the one generated for the first `MessageDeduplicationId`,
        the two messages are treated as duplicates and only one copy of the
        message is delivered.

    The following attributes apply only to [high throughput for FIFO
    queues](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/high-throughput-fifo.html):

    - `DeduplicationScope` -- Specifies whether message deduplication occurs
      at the message group or queue level. Valid values are `messageGroup`
      and `queue`.

    - `FifoThroughputLimit` -- Specifies whether the FIFO queue throughput
      quota applies to the entire queue or per message group. Valid values
      are `perQueue` and `perMessageGroupId`. The `perMessageGroupId` value
      is allowed only when the value for `DeduplicationScope` is
      `messageGroup`.

    To enable high throughput for FIFO queues, do the following:

    - Set `DeduplicationScope` to `messageGroup`.

    - Set `FifoThroughputLimit` to `perMessageGroupId`.

    If you set these attributes to anything other than the values shown for
    enabling high throughput, normal throughput is in effect and
    deduplication occurs as specified.

    For information on throughput quotas, see [Quotas related to
    messages](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/quotas-messages.html)
    in the *Amazon SQS Developer Guide*.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SET_QUEUE_ATTRIBUTES_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_SET_QUEUE_ATTRIBUTES_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.attributes is not None:
            _serialize_queue_attribute_map(
                serializer,
                _SCHEMA_SET_QUEUE_ATTRIBUTES_INPUT.members["Attributes"],
                self.attributes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_SET_QUEUE_ATTRIBUTES_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["attributes"] = _deserialize_queue_attribute_map(
                        de, _SCHEMA_SET_QUEUE_ATTRIBUTES_INPUT.members["Attributes"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SET_QUEUE_ATTRIBUTES_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class SetQueueAttributesOutput:
    """Dataclass for SetQueueAttributesOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SET_QUEUE_ATTRIBUTES_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_SET_QUEUE_ATTRIBUTES_OUTPUT, consumer=_consumer
        )
        return kwargs


SET_QUEUE_ATTRIBUTES = APIOperation(
    input=SetQueueAttributesInput,
    output=SetQueueAttributesOutput,
    schema=_SCHEMA_SET_QUEUE_ATTRIBUTES,
    input_schema=_SCHEMA_SET_QUEUE_ATTRIBUTES_INPUT,
    output_schema=_SCHEMA_SET_QUEUE_ATTRIBUTES_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidAttributeName"): InvalidAttributeName,
            ShapeID("com.amazonaws.sqs#InvalidAttributeValue"): InvalidAttributeValue,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#OverLimit"): OverLimit,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_ATTRIBUTE_NAME,
        _SCHEMA_INVALID_ATTRIBUTE_VALUE,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_OVER_LIMIT,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class StartMessageMoveTaskInput:
    """Dataclass for StartMessageMoveTaskInput structure."""

    source_arn: str | None = None
    """
    The ARN of the queue that contains the messages to be moved to another
    queue. Currently, only ARNs of dead-letter queues (DLQs) whose sources
    are other Amazon SQS queues are accepted. DLQs whose sources are non-SQS
    queues, such as Lambda or Amazon SNS topics, are not currently
    supported.
    """

    destination_arn: str | None = None
    """
    The ARN of the queue that receives the moved messages. You can use this
    field to specify the destination queue where you would like to redrive
    messages. If this field is left blank, the messages will be redriven
    back to their respective original source queues.
    """

    max_number_of_messages_per_second: int | None = None
    """
    The number of messages to be moved per second (the message movement
    rate). You can use this field to define a fixed message movement rate.
    The maximum value for messages per second is 500. If this field is left
    blank, the system will optimize the rate based on the queue message
    backlog size, which may vary throughout the duration of the message
    movement task.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_MESSAGE_MOVE_TASK_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.source_arn is not None:
            serializer.write_string(
                _SCHEMA_START_MESSAGE_MOVE_TASK_INPUT.members["SourceArn"],
                self.source_arn,
            )

        if self.destination_arn is not None:
            serializer.write_string(
                _SCHEMA_START_MESSAGE_MOVE_TASK_INPUT.members["DestinationArn"],
                self.destination_arn,
            )

        if self.max_number_of_messages_per_second is not None:
            serializer.write_integer(
                _SCHEMA_START_MESSAGE_MOVE_TASK_INPUT.members[
                    "MaxNumberOfMessagesPerSecond"
                ],
                self.max_number_of_messages_per_second,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["source_arn"] = de.read_string(
                        _SCHEMA_START_MESSAGE_MOVE_TASK_INPUT.members["SourceArn"]
                    )

                case 1:
                    kwargs["destination_arn"] = de.read_string(
                        _SCHEMA_START_MESSAGE_MOVE_TASK_INPUT.members["DestinationArn"]
                    )

                case 2:
                    kwargs["max_number_of_messages_per_second"] = de.read_integer(
                        _SCHEMA_START_MESSAGE_MOVE_TASK_INPUT.members[
                            "MaxNumberOfMessagesPerSecond"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_START_MESSAGE_MOVE_TASK_INPUT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class StartMessageMoveTaskOutput:
    """Dataclass for StartMessageMoveTaskOutput structure."""

    task_handle: str | None = None
    """
    An identifier associated with a message movement task. You can use this
    identifier to cancel a specified message movement task using the
    `CancelMessageMoveTask` action.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_MESSAGE_MOVE_TASK_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.task_handle is not None:
            serializer.write_string(
                _SCHEMA_START_MESSAGE_MOVE_TASK_OUTPUT.members["TaskHandle"],
                self.task_handle,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["task_handle"] = de.read_string(
                        _SCHEMA_START_MESSAGE_MOVE_TASK_OUTPUT.members["TaskHandle"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_START_MESSAGE_MOVE_TASK_OUTPUT, consumer=_consumer
        )
        return kwargs


START_MESSAGE_MOVE_TASK = APIOperation(
    input=StartMessageMoveTaskInput,
    output=StartMessageMoveTaskOutput,
    schema=_SCHEMA_START_MESSAGE_MOVE_TASK,
    input_schema=_SCHEMA_START_MESSAGE_MOVE_TASK_INPUT,
    output_schema=_SCHEMA_START_MESSAGE_MOVE_TASK_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID(
                "com.amazonaws.sqs#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


@dataclass(kw_only=True)
class TagQueueInput:
    """Dataclass for TagQueueInput structure."""

    queue_url: str | None = None
    """The URL of the queue."""

    tags: dict[str, str] | None = None
    """The list of tags to be added to the specified queue."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TAG_QUEUE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_TAG_QUEUE_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.tags is not None:
            _serialize_tag_map(
                serializer, _SCHEMA_TAG_QUEUE_INPUT.members["Tags"], self.tags
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_TAG_QUEUE_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["tags"] = _deserialize_tag_map(
                        de, _SCHEMA_TAG_QUEUE_INPUT.members["Tags"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TAG_QUEUE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class TagQueueOutput:
    """Dataclass for TagQueueOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TAG_QUEUE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TAG_QUEUE_OUTPUT, consumer=_consumer)
        return kwargs


TAG_QUEUE = APIOperation(
    input=TagQueueInput,
    output=TagQueueOutput,
    schema=_SCHEMA_TAG_QUEUE,
    input_schema=_SCHEMA_TAG_QUEUE_INPUT,
    output_schema=_SCHEMA_TAG_QUEUE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)


def _serialize_tag_key_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_tag_key_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class UntagQueueInput:
    """Dataclass for UntagQueueInput structure."""

    queue_url: str | None = None
    """The URL of the queue."""

    tag_keys: list[str] | None = None
    """The list of tags to be removed from the specified queue."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UNTAG_QUEUE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.queue_url is not None:
            serializer.write_string(
                _SCHEMA_UNTAG_QUEUE_INPUT.members["QueueUrl"], self.queue_url
            )

        if self.tag_keys is not None:
            _serialize_tag_key_list(
                serializer, _SCHEMA_UNTAG_QUEUE_INPUT.members["TagKeys"], self.tag_keys
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["queue_url"] = de.read_string(
                        _SCHEMA_UNTAG_QUEUE_INPUT.members["QueueUrl"]
                    )

                case 1:
                    kwargs["tag_keys"] = _deserialize_tag_key_list(
                        de, _SCHEMA_UNTAG_QUEUE_INPUT.members["TagKeys"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UNTAG_QUEUE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class UntagQueueOutput:
    """Dataclass for UntagQueueOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_UNTAG_QUEUE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_UNTAG_QUEUE_OUTPUT, consumer=_consumer)
        return kwargs


UNTAG_QUEUE = APIOperation(
    input=UntagQueueInput,
    output=UntagQueueOutput,
    schema=_SCHEMA_UNTAG_QUEUE,
    input_schema=_SCHEMA_UNTAG_QUEUE_INPUT,
    output_schema=_SCHEMA_UNTAG_QUEUE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID("com.amazonaws.sqs#InvalidAddress"): InvalidAddress,
            ShapeID("com.amazonaws.sqs#InvalidSecurity"): InvalidSecurity,
            ShapeID("com.amazonaws.sqs#QueueDoesNotExist"): QueueDoesNotExist,
            ShapeID("com.amazonaws.sqs#RequestThrottled"): RequestThrottled,
            ShapeID("com.amazonaws.sqs#UnsupportedOperation"): UnsupportedOperation,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_INVALID_ADDRESS,
        _SCHEMA_INVALID_SECURITY,
        _SCHEMA_QUEUE_DOES_NOT_EXIST,
        _SCHEMA_REQUEST_THROTTLED,
        _SCHEMA_UNSUPPORTED_OPERATION,
    ],
)

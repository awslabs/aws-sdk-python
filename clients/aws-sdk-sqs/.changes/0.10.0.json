{
  "changes": [
    {
      "type": "feature",
      "description": "Initial client release with support for current Amazon Simple Queue Service operations."
    },
    {
      "type": "dependency",
      "description": "Bump `smithy-aws-core` from `~=0.9.0` to `~=0.10.0`."
    }
  ]
}
